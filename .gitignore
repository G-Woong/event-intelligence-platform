# Claude Code / local orchestration
.claude/*
!.claude/agents/
!.claude/agents/**
!.claude/skills/
!.claude/skills/**
!.claude/hooks/
!.claude/hooks/**
# settings.json stays gitignored (local), but track the no-secret TEMPLATE so a
# fresh clone can re-register the harness hooks (R-HarnessReproducibility).
!.claude/settings.example.json
CLAUDE.md
.claude/settings.local.json
.claude/cache/
# MCP server config (local). Canonical template lives in
# WEB_INTELLIGENCE_HARNESS_EVOLUTION.md §4 — gitignored to block accidental secret commit (sec audit WARN-1).
.mcp.json
# Playwright MCP transient snapshots/output
.playwright-mcp/

# Codex / local sub-agent
.codex/
AGENTS.md
.codex/config.toml
.codex/cache/
.codex/logs/

# Local backups
.codex_premerge_backup/

# Secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.Python
.venv/
venv/
env/
.mypy_cache/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/

# Node / frontend
node_modules/
dist/
build/
.next/
out/

# Editor / OS
.idea/
.vscode/
!.vscode/settings.json
.DS_Store
Thumbs.db
*.swp

# Logs / runtime
*.log
logs/
*.pid

# ingestion pipeline outputs (keep .gitkeep only)
ingestion/outputs/
ingestion/logs/runs/
ingestion/logs/attempts/
ingestion/logs/errors/
!ingestion/outputs/**/.gitkeep
!ingestion/logs/**/.gitkeep

# reviewer batch label intake (ADR#67/#68) — real reviewer labels carry reviewer_id/rationale PII; never commit
outputs/reviewer_batch/

# sanitized live-run snapshots (ADR#84) — reproducibility artifacts under outputs/; never commit (status/path only)
outputs/live_snapshots/

# GCP service account keys (never commit)
gcp-service-account-key.json
*service-account*.json
*-key.json

# Backups
*.bak

# Turn-closeout harness runtime (config.json is tracked; these are per-machine state)
.harness/machine_status.json
.harness/narrative_marker.json
.harness/closeout_stamp.json
.harness/audit_required.json
.harness/dead_code_candidates.json
.harness/docs_lifecycle_audit.json
.harness/last_test_result.json
docs/_TRASH/
