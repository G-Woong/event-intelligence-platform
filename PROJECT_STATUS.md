# 📊 프로젝트 진행 현황 (PROJECT_STATUS)

> 매 턴 끝에 새로 쓰입니다. 항상 "가장 최근 1턴"만. 과거는 `git log -- PROJECT_STATUS.md`.
> 사실 원본(자동): `.harness/machine_status.json` · 완료 증거: `.harness/closeout_stamp.json` · 서술: 이 파일(에이전트).

## 🟢 한눈에 (비개발자용 3줄)
- **지금 무엇을 했나:** 지난 턴(ADR#80 — recall probe를 실 Guardian×NYT 라이브 기사쌍에 적용했더니 lift 0)을 안정 기준점으로 **커밋**(`87b14f9`)한 뒤, 그 lift 0의 원인을 분해했습니다(분석 §2·20문항). 결론: 프로젝트가 죽은 게 아니라 (a) seed가 "Supreme Court ruling" 같은 **카테고리**였고 (b) provider가 **Guardian×NYT 둘뿐**이었고 (c) **한국어 소스가 미연결**이었기 때문입니다. **결정적 발견: 소스 레지스트리에 이미 57개 소스가 와이어링돼 있습니다** — "Guardian×NYT만"은 라이브 실행의 한계였지 배선의 한계가 아니었습니다. 그래서 이번 턴은 그 세 레버(**provider breadth · named single-event seed · 한국어 소스 경로**)를 실제 acquisition frontier로 끌어내렸습니다. 라이브는 **이번 턴 미실행**(이번 지시에 라이브 명시 승인이 없어 `blocked_no_live_opt_in`·다음 라이브용 named seed=`fomc_rate_decision`만 큐잉). 자동 병합·LLM/embedding 실호출·운영 DB·키 값 출력·실제 전송·입력/라벨 날조·same-event 단정·public IU 모두 **0**.
- **이번 턴에 실제로 끝낸 것:** ADR#80 커밋(`87b14f9`·21파일) → **ADR#81**: (1) `provider_breadth_inventory` — source_registry **57 소스를 9 카테고리로 분류**(query_capable_publishable 7·feed_only_publishable 7·official_source 5·search_url_candidate 4·ko_official_news 6·community_reaction_only 9·market_signal_only 6·catalog_enrichment_only 9·unknown_quarantine 4·anchor_eligible **25**)·non-anchor 카테고리 `anchor_eligible=False` 강제(`source_role_guard_preserved`)·credential secret-safe. (2) `named_event_seed_bank` — category seed 대신 **named-single-event-shaped seed bank**(validator=구조+exact denylist·fomc/ecb는 **큐레이트 가설**·discreteness 미측정·placeholder/broad reject·broad 자가검증 5/5)·`same_event_asserted=False`. (3) `ko_source_readiness` — KO news 5종 LIVE key-free·Naver 어댑터·**KO tokenization risk**(형태소/stemming/KO alias 부재)·KO floor plan(human label이 hard blocker). (4) `r1_provider_breadth_acquisition` orchestrator + internal ops frontier(`InternalOpsProviderBreadthFrontier`·**dict==pydantic==TS 30==30==30**·`GET /api/internal/ops/r1-provider-breadth`) + community reaction contract(docs §9). **변경 표면 = 신규 backend 8(도구 4+테스트 4) + 수정 backend 3 + frontend 3 + docs 10 = 24파일**(strays 4 제외).
- **정직한 한계:** **라이브 미실행**(`blocked_no_live_opt_in`)·실 production candidate freeze 0·실 reviewer contact·실 returned labels 0(`current_r1_gap=200`)·**production_gold_count 0**. provider breadth=acquisition support≠truth·named seed=candidate generation≠same-event proof·community=reaction only(anchor 금지)·search URL candidate=fetch 전 truth 아님. embedding/LLM 실호출·자동 병합·운영 DB·public IU·실 전송·GraphRAG/KG runtime 모두 **0**. 다음 hard blocker=**승인된 bounded live run**(named seed fomc_rate_decision·1~2 seed·1d·host/rate 준수) → reviewer contact → **actual returned human labels**(코드로 못 옮김). **push 금지·커밋 지시 대기**.

## 📋 자동 수집 사실 (machine_status.json)
- **git HEAD `87b14f9`**(ADR#80·직전 턴 커밋·21파일·+667/−75) 위 **ADR#81 = 미커밋**: 신규 8(backend tool 4: `tools/provider_breadth_inventory.py`·`tools/named_event_seed_bank.py`·`tools/ko_source_readiness.py`·`tools/r1_provider_breadth_acquisition.py`; backend test 4: `tests/test_provider_breadth_inventory.py`·`tests/test_named_event_seed_bank.py`·`tests/test_ko_source_readiness.py`·`tests/test_r1_provider_breadth_acquisition.py`)·수정 16(backend 3: `schemas/internal_ops.py`·`api/internal_ops.py`·`tests/test_internal_ops_api.py`; frontend 3: `lib/api/types.ts`·`lib/ops/opsPilotExecutionView.ts`·`lib/ops/__tests__/opsPilotExecutionView.test.mjs`; docs 10: `5_REFERENCE/RAG_KG_AGENT_READINESS·INTELLIGENCE_UNIT_CONTRACT·RAG_KG_ENTITY_GATE_CONTRACT`·`2_ROADMAP/00·15·19`·`_CANONICAL/02`·`_DECISIONS/2026-06`·`_RISK/RISK_REGISTER`·`PROJECT_STATUS`) = **추적 24항목**. **전부 additive(신규 도구는 단방향 import·기존 ADR#80 26-field frontier 무변경·migration 없음·ADR#80 커밋분 frozen·재구현 0)·EVENT_SCHEMA/main.py/config/.env/.env.example/package.json/LLM_EVIDENCE_PACKET_CONTRACT/r1_discrete_event_acquisition.py 무변경.**
- 열린 RISK: **종결 0·신규 4**(R-ProviderBreadthScopeCreep[LOW]·R-SearchCandidateTruthLeakage[LOW]·R-NamedSeedSelectionBias[LOW]·R-LiveRunReproducibility[LOW]). 부분진전: R-ProviderPairNarrowness·R-KoreanR1SourceGap·R-ProductionCandidateScarcity·R-CrossSourceNearMatchGap·R-DeterministicNearMatchRecall·R-RagKgPrematureBuild. throwaway 는 `.harness/_TRASH/`·`frontend/.harness/`·`--paths/`·`--help/`(stray·커밋 제외)·`outputs/`·`ingestion/outputs/state/`(gitignore)·scratchpad(저장소 밖).

## ✅ 이번 턴에 달성한 것 (provider breadth + named single-event seed + KO source path — ADR#81)
- **① ADR#80 커밋**: 21파일 → `87b14f9`(지정 subject·secret PASS·docs_lifecycle conflicts 0·closeout SIGNATURE_MATCH 26==26·unresolved 0·push 0·NO UPSTREAM). ADR#81 안정 기준점.
- **② 원자 분석(§2·20문항·구현 전 보고)**: ADR#80 live lift 0(0.1765<0.2·newly_routed 0·**sharing_entity 0**=정규화 후 100쌍 중 0쌍이 entity 토큰 공유) 원인 분해 — (a) "Supreme Court ruling"=기관+일반행위 **category-lean**(comparison 100=단일 판결 아님·영어 alias 표 14+11 협소) + (b) Guardian×NYT 2 provider + (c) KO 미연결. **결정적: source_registry.yaml에 이미 57 소스 와이어링**(병렬 Explore 3 agent로 확인).
- **③ 옵션 결정(§3)**: **A(actual input 재확인)+B(provider breadth)+C(named seed)+E(KO path)+F(frontier·제한)+G(community contract·docs) 채택. D(bounded live)=`blocked_no_live_opt_in`**(/compact에 이번 턴 live 명시 승인 없음→§7 기본값). **H(LLM/RAG/KG runtime)+I(public IU runtime) 금지.**
- **④ B — provider breadth inventory**: `provider_breadth_inventory`가 `load_registry().all()` → 57 소스를 **9 카테고리**로 결정론 분류(curated anchor-eligibility ∩ registry type·registry에 query-capable flag 부재라 provider_readiness와 동일 근거의 curated 집합). non-anchor(search/community/market/catalog/quarantine) `anchor_eligible=False` 강제·`source_role_guard_preserved` 단언·trend signal/_dummy/미지 type=fail-closed quarantine. credential `env_status`(present/missing boolean만·값 0).
- **⑤ C — named single-event seed bank**: `named_event_seed_bank` validator는 **구조 필드 + 소규모 exact denylist + placeholder reject 만** 강제한다(의미적 discreteness 검증 아님·docstring 명시). 그 위에서 `fomc_rate_decision`·`ecb_rate_decision`(**수작업 큐레이트 가설**)이 accepted·scotus/M&A/sanction 템플릿 placeholder reject·**broad 자가검증 5/5 reject**(Supreme Court ruling/Federal Reserve/election/climate change/stock market). ⚠ **정직 한계**(adversarial): denylist는 정확 일치라 "기관명+단어 추가"로 우회 가능(예: "European Central Bank Governing Council")이고, **fomc는 연 8회 반복되는 announcement TYPE**(unique one-off named event 아님)이라 SCOTUS category와 같은 족속 — discreteness는 *단일 scheduled announcement + operator의 실제 날짜 pin*에 의존하는 **미측정 가설**이다. `provenance=code_proposed_named_shape`·`event_occurrence_verified=False`·`same_event_asserted=False`. selected_seed=**fomc_rate_decision**(outlet 수렴은 **예측이지 측정 아님**·라이브 미실행).
- **⑥ E — KO source path**: `ko_source_readiness`가 KO 소스 14종 status(news 5 LIVE_SUCCESS key-free=yna/hankyung/maekyung/zdnet_korea/etnews·Naver 어댑터 2 구현·KO official·community reaction·trend quarantine)·credential secret-safe(`probe_env_var`)·**KO tokenization risk**(`[0-9A-Za-z가-힣]+` Hangul-aware이나 형태소 분절/KO stemming/KO org alias 부재→KO-KO undercount·breadth-only until analyzer[KoNLPy/Mecab deferred]·crash-safe)·KO floor plan(`ko_floor_solved=False`·human label이 hard blocker).
- **⑦ F/G — orchestrator + frontier + community contract**: `r1_provider_breadth_acquisition`가 discrete base(ADR#80)+3 도구 합성→§4 output+§10 sanitized frontier `InternalOpsProviderBreadthFrontier`(**dict==pydantic==TS 30==30==30**·per-pair score/same_event/PII/secret 미노출)·`GET /api/internal/ops/r1-provider-breadth`(read API live 0·flag off 404·503 no path leak)·필수 copy 5개. community reaction contract(`RAG_KG_ENTITY_GATE_CONTRACT §4a` 신설·allowed/forbidden·rumor amplification guard·runtime 0).
- **⑧ live 미실행(§7)**: `live_query_executed=False`·`live_run_status=blocked_no_live_opt_in`·`next_action=ask_for_bounded_live_run_approval`·secret 0·raw body 0·실 네트워크 호출 0.
- **⑨ H/I 경계**: embedding/LLM scorer runtime·production DB·semantic merge·public IU·GraphRAG runtime·KG edge 생성 미착수(이번 턴 금지).

## 🧭 cross-batch identity 4단계 + 평가/gold gate (정직)
- ① anchor 병합(ADR#40) → ② 후보 LINK(ADR#41) → ③ shadow 판정(ADR#42·…·recall probe를 LIVE pair에 적용 ADR#80·**provider breadth + named seed + KO path ADR#81**) → ④ 실 병합(**미구현**).
- **ADR#81 위치 / 핵심**: ADR#80 live lift 0의 원인(category seed·얇은 provider·KO 미연결)을 **실 acquisition frontier 머신리로** 끌어내림 — provider breadth(57=9 카테고리)·named single-event seed bank·KO source readiness. near=reviewer 라우팅이지 merge가 아니므로 breadth 확장이 false-merge 위험을 늘리지 않음(merge=fingerprint+gold+MERGE_GATE 불변·breadth는 candidate만 추가·role guard가 eligibility 결정). **same-event 단정 0·라이브 미실행.**
- **⚠ 미해소(OPEN):** 승인된 **bounded live run**(named seed로 lift 재측정)·실 production candidate freeze(0)·실 reviewer contact·첫 returned labels·실 gold(production_gold 0)·R1 gold floor(live 200·KO 50·pos/neg 67·hard-neg 20)·MERGE_GATE 런타임·KO 캘리브레이션(KO gold 0)·운영 DB 0009 배포·RAG/KG/Entity/public IU(R1~R7)·internal ops per-user auth·UI 화면 render. "breadth ≠ truth·named seed ≠ same-event proof·community ≠ anchor·search URL candidate ≠ truth·routing recall ≠ merge·contract guard ≠ runtime·라이브 미실행=일반화 미측정" — 완전종결=OVERCLAIM.

## 🔎 source orchestration / 제품 계약
- official/news=publishable evidence(merge anchor)·community=반응(anchor 불가)·market=signal·catalog=entity·search=URL candidate(truth 아님)·unknown=fail-closed. **provider breadth inventory가 57 소스에 이 역할 가드를 코드로 강제**(non-anchor `anchor_eligible=False`·`source_role_guard_preserved`)·breadth 확장이 community/market/catalog/search를 anchor로 승격시키지 않음(R-ProviderBreadthScopeCreep 차단).
- **제품 계약(raw≠public)**: 출력·`internal_ops_provider_breadth_frontier`(sanitized) 노출 표면에 score/rationale/predicted_status/raw body/secret/raw reviewer 신원·same_event truth **0** — 출력 전 `_assert_pii_safe` 재귀 가드(정확 키 매칭: `score` 금지·`max_live_recall_probe_score` 허용)·API `response_model=InternalOpsProviderBreadthFrontier`(sanitized·30 field)·frontend `FORBIDDEN_OPS_FIELDS` 재귀 가드. credential은 present/missing boolean만(값 0). production_gold_count 0·gate **exact passthrough**·`merge_allowed=False`·embedding/LLM 실호출 0·DB write 0·`actual_sending_performed=False`·live 미실행. Agent는 provider breadth·named seed selection·KO readiness·community reaction attachment를 *계획* 가능하나 breadth를 truth로·named seed를 same-event proof로·community를 anchor로 사용 불가·merge·검출 단정·secret 생성·PII 출력·실 전송 불가.

## ⚠️ 이번 턴 종결/갱신 RISK
- **신규 4(실 present blocker만·테스트 차단)**: **R-ProviderBreadthScopeCreep**(LOW·57 혼합 역할의 anchor 누출 압력·완화=non-anchor anchor_eligible False 강제·`source_role_guard_preserved` 테스트) · **R-SearchCandidateTruthLeakage**(LOW·SERP snippet truth 둔갑·완화=`search_url_candidate` 분류·fetch-전 truth 아님) · **R-NamedSeedSelectionBias**(LOW·named seed cherry-pick (i) 부풀림·완화=provenance/validator/다 seed) · **R-LiveRunReproducibility**(LOW·live 결과 self-attested·repo 영속 부재·완화=한계 명문화·sanitized snapshot 방향만).
- **부분진전(종결 0)**: **R-ProviderPairNarrowness**(breadth inventory 완성·alternative provider 실 live 미테스트·**종결 금지**) · **R-KoreanR1SourceGap**(KO path/adapter status 연결·KO gold 0·**종결 금지**) · **R-ProductionCandidateScarcity**(named seed/breadth 생성 경로 확장·freeze 0·**종결 금지**) · **R-CrossSourceNearMatchGap**/**R-DeterministicNearMatchRecall**/**R-RagKgPrematureBuild**(**종결 금지**).

## ❌ 달성하지 못한 것 & 왜 (이월)
- **실 live run / 실 (i)/(ii) 분리 / 실 production candidate freeze / 실 gold**: 이번 턴 live 미실행(`blocked_no_live_opt_in`·승인 없음)이라 named-seed 가설 미측정·publishable near-match pair 0이라 freeze 0. 해소=**승인된 bounded live run**(named seed fomc_rate_decision·1d·host/rate 준수) → entity-공유 routing band pair 측정 → reviewer 후보.
- **adjudicator runtime / RAG·KG·Entity·public IU**: contract guard(docs)일 뿐 runtime 미구현(embedding/LLM 실호출·실 병합·운영 DB·GraphRAG·public IU·KG edge 이번 턴 No-Go·옵션 H/I 금지).
- **UI 화면 render**: frontier view/types/test(데이터층)는 배선됐으나 read API가 `live_query=False` 고정이라 internal ops UI는 `blocked_no_live_opt_in`(비-live)만 표시·page.tsx 화면 render는 후순위.

## 🚧 닫을 수 없는 문제 (BLOCKED/UNKNOWN)
- BLOCKED: 실 named-seed lift = **승인된 bounded live run**(R-ProviderPairNarrowness·R-NamedSeedSelectionBias). 실 production candidate = near-match publishable pair 확보(현재 0·R-ProductionCandidateScarcity). 실 gold = 실 후보→reviewer recruitment→실제 contact→첫 returned label. 실 병합 = gold-calibrated adjudicator(R3)+MERGE_GATE(R2). KO floor = KO 후보→한국어 reviewer→KO gold ≥50(R-KoreanR1SourceGap·R-KOAnalyzerDependency).
- UNKNOWN: **named single-event seed(fomc_rate_decision)가 Guardian×NYT+breadth에서 lift를 낼지**(미측정·라이브 미실행). **provider breadth(GDELT/RSS/KO news) 추가가 cross-source near-match 수율을 올릴지**(미테스트). **KO-aware tokenizer 없이 KO-KO near-match가 측정 가능한지**(R-KOAnalyzerDependency). 실 reviewer 회수율/SLA(plan·machinery만).

## 👉 다음 할 일
1. **[커밋 대기]** 본 턴 ADR#81: 신규 8·수정 16(backend 3·frontend 3·docs 10) 미커밋. 커밋 여부 지시 대기. push 금지. (`--paths/`·`--help/` stray 정리 허가도 함께 받으면 처리.)
2. **[다음 단계·frontier]** (a) **승인된 bounded live run**(named seed fomc_rate_decision·1~2 seed·1d·publishable provider[guardian+nyt+key-free gdelt/federal_register]·host/rate 준수·raw body 0·secret 0)으로 named-seed 가설 실측. (b) provider breadth를 comparison pool에 실제 wiring(key-free anchor + KO news). (c) live-derived production candidate freeze → (d) **reviewer recruitment + actual contact + 첫 returned labels**(R1) → (e) gold denominator(live 200·KO 50·**R1**) → (f) MERGE_GATE(precision≥0.98·**R2**) → (g) embedding scorer opt-in(**R3**) → (h) entity/KG/GraphRAG(**R4~R6**) → (i) Agent/public IU(**R7**). 별도 축: internal ops UI 화면 render·per-user auth.
3. **[권장·bounded]** live acquisition 실험은 **bounded·opt-in·rate governed·named single-event seed**일 때만(무단 대량 호출 0·no-bypass·source role guard 유지).

## 📁 근거 (이번 턴 핵심)
- 커밋: `87b14f9`(ADR#80·21파일·+667/−75·push 0·NO UPSTREAM). ADR#81 변경: 신규 8(backend tool 4+test 4)·수정 16(backend 3·frontend 3·docs 10). **ADR#80 커밋분 frozen·재구현 0·EVENT_SCHEMA/main.py/config/.env/.env.example/package.json/LLM_EVIDENCE_PACKET_CONTRACT/r1_discrete_event_acquisition.py 무변경·전부 additive(신규 도구 단방향 import).**
- **provider breadth inventory(실 registry 57)**: query_capable_publishable 7·feed_only_publishable 7·official_source 5·search_url_candidate 4·ko_official_news 6·community_reaction_only 9·market_signal_only 6·catalog_enrichment_only 9·unknown_quarantine 4·**anchor_eligible 25**·`source_role_guard_preserved=True`. named seed bank: named 2(fomc/ecb)·broad reject 5/5·selected=`fomc_rate_decision`. KO: path_ready·ko_official_news 6·tokenization_risk_recorded·`ko_floor_solved=False`. live: `blocked_no_live_opt_in`·comparison 0·gold 0·gap 200.
- 검증(정직): 신규 backend 테스트 **provider_breadth_inventory 17**(+2 guard drift/exception 교차검증)** + named_event_seed_bank 10 + ko_source_readiness 11 + r1_provider_breadth_acquisition 9 + internal_ops_api +8 = 55**·ADR#79/#80 회귀 무변경(재실행 208 통과)·**backend 전체 1605 passed / 101 skipped / 0 failed**(992.85s 측정 1603 + 가드 테스트 2·재실행 확인)·ingestion 무변경·frontend **tsc 0**·**lint 0**(`next lint`)·node:test **58 passed**(ops view +9: 49→58)·ruff 실게이트(E/F/I/B/W−E501) **0**·secret scan **PASS**(21 files·credential present/missing boolean만·값 미열람)·docs_lifecycle conflicts **0**. dict==pydantic==TS **30==30==30**.
- 감사(closeout subagents): architecture/pipeline/actual_input_gate/provider_breadth/named_single_event_seed/korean_source_path/community_reaction_boundary/source_role_guard/acquisition_frontier/live_recall_probe/internal_ops_boundary/synthetic_vs_production_separation/secret_boundary/no_merge/no_llm_embedding/rag_kg_gate/code/test/evidence/risk_closure/adversarial review. cardinal(synthetic 성공을 live 성공으로 둔갑 금지·SCOTUS category 실패를 named single-event 실패로 일반화 금지·Guardian×NYT lift 0을 전체 provider 실패로 일반화 금지·recall score를 same_event/merge로 사용 금지·named seed를 same-event proof로 사용 금지·community/market/catalog/search를 anchor로 사용 금지·search result를 fetched evidence 없이 truth로 사용 금지·MERGE_GATE 전 merge 금지·LLM/embedding 실호출 금지·raw body 저장 금지·`.env` 값 출력 금지·score/rationale/predicted_status/same_event reviewer/public 노출 금지·production_gold 임의 증가 금지·public IU 생성 금지·push 금지) 차단 확인.
- 문서: ADR#81(`_DECISIONS`)·RISK(`_RISK`·종결 0·신규 4·부분진전 6)·ADR#81 블록(`2_ROADMAP/15` descending)·ADR#81 마커(`2_ROADMAP/00`)·ADR#81 §19(`2_ROADMAP/19` ascending)·§6b-R1f ADR#81 sub-bullet(`RAG_KG_AGENT_READINESS`)·ADR#81 bullet(`IU_CONTRACT`)·community reaction contract §4a(`RAG_KG_ENTITY_GATE_CONTRACT`)·`_CANONICAL/02`(ADR#41~#81). **3_SOURCE_STATUS 부재(ABSENT)·EVENT_SCHEMA/LLM_EVIDENCE_PACKET_CONTRACT 무변경**(ADR#81 DB-free·runtime 0).

---
_as_of: 2026-06-28 · ADR#81 provider breadth + named single-event seed + KO source path — ADR#80 커밋(`87b14f9`) 후, live lift 0의 원인을 분해(category seed·얇은 provider·KO 미연결)하고 세 레버를 **실 acquisition frontier 머신리로** 끌어내림(신규 3 도구+1 orchestrator·전부 additive): `provider_breadth_inventory`(57=9 카테고리·anchor_eligible 25·source_role_guard_preserved·credential secret-safe)·`named_event_seed_bank`(category→named single-event·fomc/ecb accepted·broad 5/5 reject·same_event_asserted=False)·`ko_source_readiness`(KO news 5 LIVE key-free·Naver 어댑터·KO tokenization risk·floor solved=False)·`r1_provider_breadth_acquisition`(orchestrator·§4 output+§10 frontier `InternalOpsProviderBreadthFrontier` dict==pydantic==TS 30==30==30·GET /api/internal/ops/r1-provider-breadth). **라이브 미실행**(이번 턴 승인 없음→`blocked_no_live_opt_in`·selected seed=fomc_rate_decision queued). **측정**: 신규 backend 테스트 55(breadth 17+seed 10+ko 11+orchestrator 9+API 8)·backend 전체 1605p/101skip/0fail·frontend tsc0/lint0/test58(ops +9)·ruff 0·secret PASS·docs_lifecycle 0·dict==pydantic==TS 30. **정직 경계**: provider breadth ≠ truth·named seed ≠ same-event proof·community ≠ anchor·search URL candidate ≠ truth·라이브 미실행=일반화 미측정·production_gold 0·freeze 0·gate exact passthrough·adjudicator runtime/public IU/merge/운영 DB/embedding·LLM 실호출/실 전송 0·완전종결=OVERCLAIM. 다음 hard blocker=승인된 bounded live run(named seed) → 실 reviewer contact → actual returned labels(R1). **신규 R-ProviderBreadthScopeCreep/R-SearchCandidateTruthLeakage/R-NamedSeedSelectionBias/R-LiveRunReproducibility(LOW)**·종결 0. ADR#80 커밋 `87b14f9` 위 ADR#81 미커밋(신규 8·수정 16)·커밋 지시 대기·push 안 함._
