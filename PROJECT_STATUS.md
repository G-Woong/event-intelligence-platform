# 📊 프로젝트 진행 현황 (PROJECT_STATUS)

> 매 턴 끝에 새로 쓰입니다. 항상 "가장 최근 1턴"만. 과거는 `git log -- PROJECT_STATUS.md`.
> 사실 원본(자동): `.harness/machine_status.json` · 완료 증거: `.harness/closeout_stamp.json` · 서술: 이 파일(에이전트).

## 🟢 한눈에 (비개발자용 3줄)
- **지금 무엇을 했나:** 지난 턴(ADR#74 — R1 라벨 수집 운영 *계획*)을 안정 기준점으로 **커밋**(`4de4fc0`)한 뒤, 그 계획을 **실제 reviewer 에게 넘길 수 있는 "첫 동결 pilot batch"**로 만들었습니다(ADR#75). 신규 `r1_reviewer_pilot_batch`가 실 입력을 재확인하고(여전히 0), 검토할 후보쌍 5개를 **동결**(변경 불가·고유 지문[signature] 포함)하며, 운영자가 그대로 손으로 실행할 수 있는 **배포 패키지**(지시문·라벨 양식·파일명·검증 명령·금지사항)를 만들고, 운영 화면·API 에 "보낼 준비 됨" 상태를 읽기 전용으로 보여줍니다. **단, 이 5개 쌍은 합성(synthetic) 견본**이라 production 후보로 둔갑시키지 않습니다(라벨이 와도 production gold 로 올라가지 않음). 자동 병합·LLM/embedding 실호출·운영 DB·키 값 출력·실제 전송·입력/라벨 날조는 모두 **0**입니다.
- **이번 턴에 실제로 끝낸 것:** ADR#74 커밋(`4de4fc0`·19파일) → **ADR#75**: 신규 `r1_reviewer_pilot_batch.py`(`run_actual_input_gate` 단일 호출 re-check + 순수 builder freeze·deterministic signature·5-state launch_status·operator launch checklist·합성 provenance 정직·**template dataset_source=synthetic 태깅**)+`reviewer_batch_launch.py`(`build_intake_plan` +intake_dir override·additive)+`schemas/internal_ops.py`(+`InternalOpsR1PilotBatchStatus`)+`api/internal_ops.py`(+`GET /api/internal/ops/r1-pilot-batch`·이중 게이트·read-only)+frontend(launch-readiness 패널·node:test +6). 신규 backend **44 테스트**(r1 pilot batch 36+API 8)·frontend **+6**. **ADR#74 커밋분(r1_gold_acquisition_plan) 미변경**. **adversarial PROCEED-WITH-FIXES**(HIGH-1 template synthetic 태깅·MEDIUM-1 dry-run hard-stop)·**code-review LGTM-WITH-NITS**(triple-consistency 22==22==22).
- **정직한 한계:** 이번 진전은 **첫 R1 reviewer pilot batch 동결 + operator launch package + launch readiness**이지 **실 reviewer 입력이 아닙니다**. **입력 디렉터리 부재→`no_actual_input`·`r1_status=blocked_no_labels`·실 reviewer contact·실 returned labels·실 gold 여전히 0**(production_gold_count 0·gate exact passthrough·frozen batch 가 미증가), calibration_ready/merge_gate_ready False. **frozen pilot batch 는 합성 captured fixture**(실 source 아님)·`pilot_batch_is_production_candidate=False`·**frozen 5<<target 200=pilot_n**·라벨 회수해도 dataset_source=synthetic→production gold 미승격(machinery 강제). frozen batch 는 **reviewer worklist 동결이지 event truth 가 아닙니다**. 실제 email/slack/webhook 전송·embedding/LLM 실호출·자동 병합·운영 DB·public IU 모두 **0**. internal ops 는 **workflow state(public truth 아님)**. 다음 hard blocker=**실 production candidate(live overlap)→실 reviewer contact→actual returned labels**. **push 금지·커밋 지시 대기**.

## 📋 자동 수집 사실 (machine_status.json)
- **git HEAD `4de4fc0`**(ADR#74, 본 턴 첫 커밋·19파일·+1070/−45) 위 **ADR#75 = 미커밋**: 신규 2(backend code 1: `tools/r1_reviewer_pilot_batch.py` + backend test 1: `tests/test_r1_reviewer_pilot_batch.py`) + 수정 17(backend 4: `tools/reviewer_batch_launch.py`[+intake_dir override]·`schemas/internal_ops.py`·`api/internal_ops.py`·`tests/test_internal_ops_api.py` + frontend 4: `lib/api/types.ts`·`lib/ops/opsPilotExecutionView.ts`·`app/internal/ops-pilot/page.tsx`·`lib/ops/__tests__/opsPilotExecutionView.test.mjs` + docs 8: `2_ROADMAP/00·15·19`·`_RISK/RISK_REGISTER`·`5_REFERENCE/RAG_KG_AGENT_READINESS·INTELLIGENCE_UNIT_CONTRACT`·`_CANONICAL/02`·`_DECISIONS/2026-06` + `PROJECT_STATUS`) = **추적 19항목**. **migration 없음·ADR#74 커밋분 frozen(도구는 gate 호출+순수 builder 재사용만)·단방향 import·재구현 0·EVENT_SCHEMA(DB-free라 무변경)·main.py/config/.env.example/package.json 무변경.**
- 열린 RISK: **종결 0·신규 1**(R-BatchFreezeAsTruth[LOW]). 부분진전: R-GoldAcquisitionPlanOnly(#74→#75)·R-ReviewerPilotExecution(#74→#75)·R-IdentityHumanLabeling·R-ReviewerAgreement·R-GoldSamplingBias·R-IdentityEvalDataset. throwaway 는 `.harness/_TRASH/`·`frontend/.harness/`·`--paths/`·`--help/`(stray·**이번 턴 docs_lifecycle --help 오호출로 생성**·커밋 제외)·`outputs/reviewer_batch/`(gitignore).

## ✅ 이번 턴에 달성한 것 (R1 first reviewer pilot batch freeze + operator launch handoff + internal ops launch readiness — ADR#75)
- **① ADR#74 커밋**: 19파일 → `4de4fc0`(제공 메시지 그대로·secret PASS 9643 files·docs_lifecycle conflicts 0·push 0·NO UPSTREAM).
- **② 원자 분석(§2·20문항·구현 전 보고)**: reviewer candidate pair source=`build_captured_overlap_fixture`(**합성**·6 record)→`discover_overlap`(순수·오프라인)→`build_near_match_reviewer_queue`→**5 distinct pair**(10 packet row·2 reviewer/pair). 실 production 후보=live overlap(credentials·이번 턴 No-Go). **핵심 정직성 결정**: 합성 fixture 를 production 후보로 둔갑 0(freeze=machinery 증명·dataset_source=synthetic→production gold 미승격). 다음 hard blocker=실 production candidate→실 reviewer contact→actual returned labels. docs/3_SOURCE_STATUS\* 부재 재확인·EVENT_SCHEMA 무변경·`r1_reviewer_pilot_batch` 미존재→신규.
- **③ A — actual returned labels re-check**: `run_actual_input_gate` 단일 호출(재호출 0)→`no_actual_input`·`external_input_required=True`·production_gold_count 0·`db_write/llm/embedding=False`. 입력 날조 0.
- **④ B — R1 first pilot batch freeze**: `r1_reviewer_pilot_batch.py` — 결정적·오프라인 후보 worklist 를 순수 builder 로 동결(**frozen 5 pair**·`_frozen_pair_list` allowlist 파생·dedupe·정렬·reviewer-facing safe[pair_id/source_role/title/canonical_url/observed_at/language·score/rationale/predicted_status/same_event/raw body/PII 구조적 부재])·`_batch_signature` deterministic sha256(정렬 pair 정체성+config·PII/score 제외)·`target_pair_count=200`→**frozen 5<<target=pilot_n**·`candidate_provenance=synthetic_fixture`·`pilot_batch_is_production_candidate=False`·전체 출력 재귀 PII 가드.
- **⑤ C — operator launch handoff package**: `build_operator_launch_checklist`(수동 배포/회수/검증 단계+manual-only contact 지시+PII/secret 금지 reminder+no-go 경고·**전송 0·label/contact evidence/roster 생성 0**)·handoff bundle 재사용·expected label files·placement guide·validation command(`build_intake_plan` +intake_dir override→게이트 스캔 경로 단일 수렴).
- **⑥ D — internal ops launch-readiness(제한적)**: `_launch_status`(5-state: blocked_no_candidates/ready_for_manual_launch[현재]/awaiting_manual_launch/awaiting_returned_labels/labels_present)·`schemas/internal_ops.py`(+`InternalOpsR1PilotBatchStatus`)+`api/internal_ops.py`(+`GET /api/internal/ops/r1-pilot-batch`·이중 게이트·read-only·503 sanitize·기존 무변경)+frontend(`types.ts`+1·`opsPilotExecutionView.ts`+`toR1BatchDisplayRows`/`r1BatchWarnings`/`OPS_R1_BATCH_COPY`·`page.tsx`+launch-readiness 패널[provenance/pilot_n/expected files/launch_status/R1 gap/R2~R7 No-Go·"Frozen batch is a reviewer worklist, not truth"/"Manual launch required"/"Returned labels are still missing"/"Production gold remains 0 until human labels are imported"/"R2~R7 remain No-Go" copy]·warnings dedupe·node:test +6).
- **⑦ launch 상태(실측)**: actual input 부재→`r1_status=blocked_no_labels`·`launch_status=ready_for_manual_launch`·frozen 5(합성)·production_gold_count 0·current_r1_gap 200·calibration_ready/merge_gate_ready False.
- **⑧ §9 Agent contract**: pilot batch 출력이 Agent 의 R1 pilot batch freeze readiness·launch status·operator launch package·candidate provenance(synthetic≠production) 계획 권한 표면화(Agent 는 *계획*만·batch freeze 를 truth 로 사용/label 조작/contact evidence·label 임의 생성/merge/public IU/anchor/secret 출력/실제 전송 불가·`merge_allowed=False`·`llm_invoked=False`·`embedding_invoked=False`).
- **⑨ E/F 경계**: embedding/LLM scorer runtime(E)·production DB activation/semantic merge/public IU(F) 미착수.

## 🧭 cross-batch identity 4단계 + 평가/gold gate (정직)
- ① anchor 병합(ADR#40) → ② 후보 LINK(ADR#41) → ③ shadow 판정(ADR#42·운영 배선 ADR#48~#53·smoke ADR#54~#56·overlap ADR#57·acquisition ADR#58·near-match 큐 ADR#59·targeted ADR#60·provider readiness ADR#61·adapter ADR#62·live query ADR#63·cross-source ADR#64·scorer ADR#65·label ops ADR#66·batch launch ADR#67·intake ADR#68·follow-up ADR#69·pilot handoff ADR#70·pilot execution ledger ADR#71·actual input gate + internal ops bridge ADR#72·internal ops auth/deploy preflight + R1~R7 readiness matrix ADR#73·R1 gold acquisition operating plan + R1 gap + source storage strategy ADR#74·**R1 first reviewer pilot batch freeze + operator launch handoff + internal ops launch readiness ADR#75**) → ④ 실 병합(**미구현**).
- **ADR#75 위치**: 단계 ③ shadow 위 reviewer 사람-루프에서 **R1 plan(ADR#74)을 실제 reviewer 에게 넘길 첫 동결 pilot batch + operator launch package + launch readiness 로 물질화**. R1 plan 의 "무엇을 모아야 하는가"를 "동결된 worklist·deterministic signature·operator 가 손으로 실행할 패키지"로 전환. **실 reviewer 입력이 아님**(production_gold_count 0·r1_status blocked_no_labels·frozen=합성 fixture·pilot_batch_is_production_candidate=False·gate exact passthrough·merge_gate_ready False·실제 전송 0).
- **⚠ 미해소(OPEN):** 실 production candidate(live overlap·합성 탈피)·실 reviewer contact·첫 returned labels·실 production reviewer 라벨·실 gold(production_gold 0)·R1 gold floor(live 200·KO 50·pos/neg 67·hard-neg 20)·gold-calibrated embedding/LLM **실 병합 판정기**(No-Go)·MERGE_GATE 런타임·한국어 캘리브레이션·운영 DB 0009 배포·source storage runtime·RAG/KG/Entity/public IU(R1~R7)·internal ops per-user auth. "pilot batch freeze ≠ 실 라벨·frozen worklist ≠ event truth·합성 fixture ≠ production 후보·internal ops read-only ≠ public IU" — 완전종결=OVERCLAIM.

## 🔎 source orchestration / 제품 계약
- official/news=publishable evidence(merge anchor)·community=반응 evidence(anchor 불가)·market=signal·catalog=entity·search=URL·unknown=fail-closed. pilot batch/actual input gate/internal ops 는 publishable×publishable 후보 큐만 다룸(community/market/catalog anchor 차단·상속). frozen pair 는 source_role 만 노출(anchor 판정 없음)·storage strategy 는 docs-only(§6b-S).
- **제품 계약(raw≠public)**: pilot batch 출력·r1_pilot_batch_contract 의 노출 표면에 score/rationale/predicted_status/raw body/secret/raw reviewer 신원·same_event truth **0** — 출력 전 `_assert_pii_safe` 재귀 가드·API 는 `response_model=InternalOpsR1PilotBatchStatus`(sanitized 화이트리스트·22필드)·frontend 는 `FORBIDDEN_OPS_FIELDS` 재귀 가드+no-go 배너. production_gold_count 0·gate **exact passthrough**(frozen batch 가 미증가)·`merge_allowed=False`·`no_public_intelligence_unit`·embedding/LLM 실호출 0·DB write 0·`actual_sending_performed=False`. internal ops dashboard 는 **4중 게이트+preflight posture**(admin-token prod fail-closed+flag-404+nav 미노출+server-env)+`flags.internal_only/no_public_truth`. **frozen batch=reviewer worklist≠event truth**(same_event 미함의)·**합성 fixture≠production 후보**(`pilot_batch_is_production_candidate=False`·dataset_source=synthetic→gold 미승격). Agent 는 R1 pilot batch freeze readiness 를 *계획* 가능하나 label 조작·merge·검출 단정·PII/secret 출력·실제 전송 불가. LLM/Agent 진입 9조건 여전히 No-Go(1·4·5·7).

## ⚠️ 이번 턴 종결/갱신 RISK
- **신규 1(실 present blocker만)**: **R-BatchFreezeAsTruth**(LOW·동결된 합성 fixture pilot batch 가 production 후보·event truth 로 오인될 위험·완화=`candidate_provenance=synthetic_fixture`·`pilot_batch_is_production_candidate=False`·dataset_source=synthetic→production gold 미승격(machinery 강제)·`same_event_truth_exposed=False`·"worklist, not truth" copy·test lock). **남발 금지**(R-FrozenBatchNoCandidates=후보 존재로 미등록·R-ManualLaunchStall=R-GoldAcquisitionPlanOnly 에 흡수).
- **부분진전(종결 0)**: **R-GoldAcquisitionPlanOnly**(#74→#75: 첫 pilot batch freeze+operator launch package+ready_for_manual_launch·**실 returned labels 0 불변→완전종결 금지**) · **R-ReviewerPilotExecution**(#74→#75: batch freeze→execution ledger 연결+returned label 적재/검증 경로 가시·**실 reviewer contact·실 returned labels 0→완전종결 금지**) · **R-IdentityHumanLabeling**(reviewer-facing worklist frozen+template/placement/validation 연결+production/synthetic 분리·**실 production reviewer label 0→완전종결 금지**) · **R-ReviewerAgreement**(pair 당 reviewer 중복+conflict/adjudication 정책·**실 합의 0**) · **R-GoldSamplingBias**(pilot_n↔R1 target gap 가시+분포 정직[합성]·**실 production sampling 0**) · **R-IdentityEvalDataset**(frozen batch→eval/gold intake 가능+R1 target gap 명시·**MERGE_GATE evidence 0**).

## ❌ 달성하지 못한 것 & 왜 (이월)
- **실 production candidate / 실 reviewer contact / 첫 returned labels / 실 gold**: pilot batch freeze+operator package 는 닫혔으나 **frozen 후보가 합성 fixture**(실 production 후보 아님·live overlap 필요·R-SourceOverlapScarcity)이고 **실 입력 파일 0**(입력 디렉터리 부재→`no_actual_input`/`blocked_no_labels`·production_gold_count 0·`external_input_required`). 실 production candidate 획득·reviewer recruitment·실제 contact evidence·첫 returned labels 선결(R-GoldAcquisitionPlanOnly·R-ReviewerPilotExecution·R-BatchFreezeAsTruth).
- **source storage runtime / RAG·KG·Entity·public IU**: §6b-S 는 **docs 전략**일 뿐 storage runtime/schema 미구현(R4~R5 gate 전). embedding/LLM 실호출·실 병합·운영 DB 이번 턴 No-Go(옵션 E/F 금지).
- **internal ops per-user auth / 실 배포 경계**: ADR#73 preflight 가 config posture 를 봉인하나 **물리 network reachability·per-user auth 는 미증명**(`deployment_proven=False`·shared admin-token only·R-InternalOpsAuthBoundary).

## 🚧 닫을 수 없는 문제 (BLOCKED/UNKNOWN)
- BLOCKED: 실 gold = **실 production candidate(live overlap·합성 탈피)·reviewer recruitment·실제 contact(evidence)·첫 returned label 파일 회수** 선결(pilot batch=freeze machinery·production_gold 0·R-BatchFreezeAsTruth·R-GoldAcquisitionPlanOnly·R-ReviewerPilotExecution). 실 병합 = gold-calibrated embedding/LLM adjudicator(R3·옵션 E)+MERGE_GATE(R2). source storage runtime = R4~R5 gate. production 백로그 = 운영 DB 0009 배포(승인)+실 fetch.
- UNKNOWN: 합성 fixture freeze machinery 가 실 production candidate 에서 동일하게 닫히는지(구조는 동일·실 데이터 0). 실 reviewer 회수율/SLA 실측(plan·템플릿·동결 batch만·실 운영 0). R1 target floor(200/50/67/67/20)가 실 calibration 에서 충분한지(표본 추정값·실 측정 0). ADR#64 deterministic 0 이 paraphrase recall 한계인지 real scarcity 인지 n=2 미구분(gold 필요).

## 👉 다음 할 일
1. **[커밋 대기]** 본 턴 ADR#75: 신규 2(r1 pilot batch code 1+test 1) + 수정 17(batch_launch+api/schema/test_api + frontend 4 + docs 8·PROJECT_STATUS) = **19파일** 미커밋. 커밋 여부 지시 대기. push 금지. (`--paths/`·`--help/` stray 정리 허가도 함께 받으면 처리.)
2. **[다음 단계]** (a) **실 production candidate 획득**(live source overlap·credentials·합성 탈피·R-SourceOverlapScarcity) → (b) **reviewer recruitment + actual contact evidence + 첫 returned labels 회수**(actual input gate 가 자동 end-to-end·R1·R-ReviewerPilotExecution·R-GoldAcquisitionPlanOnly·R-BatchFreezeAsTruth) → (c) gold denominator(live 200·KO 50·pos/neg 67·hard-neg 20·**R1**) → (d) MERGE_GATE calibration(precision≥0.98·FPR≤0.01·**R2**) → (e) **embedding scorer opt-in 실호출**(`create_embedding_client` seam·**R3**) → (f) entity/KG/GraphRAG(**R4~R6**·source storage runtime) → (g) Agent/public IU(**R7**·모든 gate 후). 별도 축: internal ops per-user auth+배포 경계(R-InternalOpsAuthBoundary).

## 📁 근거 (이번 턴 핵심)
- 커밋: `4de4fc0`(ADR#74·19파일·+1070/−45·push 0·NO UPSTREAM). ADR#75 변경: 신규 2(r1 pilot batch code + test)·수정 17(batch_launch+api/schema/test_api + frontend 4 + docs 8·PROJECT_STATUS). **ADR#74 커밋분 frozen(도구는 gate 호출+순수 builder 재사용만)·단방향 import·재구현 0·EVENT_SCHEMA 무변경·main.py/config/.env.example/package.json 무변경.**
- 검증(정직): 신규 backend **44**(test_r1_reviewer_pilot_batch 36[actual input re-check·pilot batch freeze·reviewer-facing safety·operator launch package·internal ops contract·no merge/LLM/DB·launch_status state machine·_frozen_pair_list·**adversarial HIGH-1 synthetic 태깅·MEDIUM-1 dry-run hard-stop·signature NIT 2 fix-lock**]+test_internal_ops_api +8[/r1-pilot-batch flag 404·sanitized contract·launch readiness·no-go flags·forbidden+PII 0·read-only 405·키집합 화이트리스트·prod fail-closed 503])·frontend node:test **+6**(opsPilotExecutionView r1 pilot batch view: forbidden 가드·launch readiness rows·worklist-not-truth/manual/missing/synthetic warnings·copy·synthetic≠production·re-introduce throw)·backend 비-live **1391 passed/101 skipped/0 failed**(1347+44)·**live-PG=미실행**(DB down·도구가 DB/session/ORM 무접촉=`db_write:False`→무영향)·ingestion **1353 passed**(불변·ingestion/ 미변경)·frontend tsc**0**/node:test **38**(기존 32+6)/lint **0**·secret scan **PASS**(9643 files)·docs_lifecycle conflicts **0**·ruff 실게이트(E/F/I/B/W−E501) **0**·dead-code(신규 모듈) **미플래그**. **honest 동작**: actual input 부재→`no_actual_input`/`external_input_required`·`r1_status=blocked_no_labels`·frozen 5(합성)·signature deterministic·`launch_status=ready_for_manual_launch`·`pilot_batch_is_production_candidate=False`·production_gold_count 0·current_r1_gap 200·forbidden 키 0.
- 감사(closeout subagents): architecture/pipeline/actual_input_gate/r1_pilot_batch_freeze/candidate_sufficiency/batch_signature/reviewer_launch_package/operator_handoff/returned_label_placement/label_validation_command/internal_ops_launch_readiness/r1_gold_acquisition/source_storage_strategy/community_reaction_boundary/rag_kg_entity_readiness/internal_ops_auth_boundary/ops_ui_contract/frontend_internal_ops/public_truth_boundary/reviewer_pii/secret_boundary/code/test/evidence/risk_closure/adversarial review. cardinal(pilot 실행 둔갑/batch freeze 없이 전달 보고/contact 없이 contacted 둔갑/actual labels 없이 production_gold 증가/synthetic·model→production 둔갑/single reviewer→gold/MERGE_GATE 전 merge/RAG·KG 조기 build/same_event 노출/score·rationale·predicted_status·PII 노출/실제 전송) 차단 확인. **adversarial PROCEED-WITH-FIXES**(BLOCK 0·HIGH 1+MEDIUM 1+LOW 3): HIGH-1 template `dataset_source=live_derived` 자가당착→`SOURCE_SYNTHETIC` 태깅(회수 라벨 production gold 미승격·machinery 강제)·MEDIUM-1 합성 batch 실배포 유도→dry-run hard-stop 맨 앞·LOW 3 문서화. **code-review LGTM-WITH-NITS**(triple-consistency 22==22==22·additive 안전).
- 문서: ADR#75(`_DECISIONS`)·RISK(`_RISK`·종결 0·신규 1[R-BatchFreezeAsTruth]·R-GoldAcquisitionPlanOnly/R-ReviewerPilotExecution #74→#75)·ADR#75 블록(`2_ROADMAP/15`)·(11ag)(`2_ROADMAP/00`)·pilot batch freeze §19(`2_ROADMAP/19`)·**§6b-R1b R1 pilot batch freeze**(`RAG_KG`)·pilot batch bullet(`IU_CONTRACT`)·`_CANONICAL/02`(ADR#41~#75). **EVENT_SCHEMA 무변경**(ADR#75 DB-free·최소 변경).

---
_as_of: 2026-06-27 · ADR#75 R1 first reviewer pilot batch freeze + operator launch handoff + internal ops launch readiness — ADR#74 커밋(`4de4fc0`) 후, R1 plan 을 실제 reviewer 에게 넘길 첫 동결 pilot batch 로 물질화(신규 backend code 1·test 1·기존 보강·재구현 0·`run_actual_input_gate` 단일 호출 re-check·순수 builder freeze·DB 0·전송 0·입력 날조 0·secret 값 미열람): `r1_reviewer_pilot_batch.py`(actual input re-check[no_actual_input/external_input_required/gold 0]·frozen 5 pair worklist[reviewer-facing safe·`_batch_signature` deterministic sha256]·`_launch_status` 5-state[ready_for_manual_launch 현재]·`build_operator_launch_checklist`[전송 0·생성 0]·`candidate_provenance=synthetic_fixture`·`pilot_batch_is_production_candidate=False`·R1 gap 재사용·재귀 PII 가드)·`reviewer_batch_launch.py`(`build_intake_plan` +intake_dir override·additive)·`schemas/internal_ops.py`(InternalOpsR1PilotBatchStatus)·`api/internal_ops.py`(GET /api/internal/ops/r1-pilot-batch·이중 게이트·read-only)·frontend(launch-readiness 패널·node:test +6)·`RAG_KG_AGENT_READINESS`(§6b-R1b). 옵션 **A+B+C+D(제한적) 채택·E(embedding/LLM runtime)/F(production DB·merge·public IU) 금지·신규 RISK 1**. **측정**: 신규 backend 44(r1 pilot batch 36+API 8)·frontend +6·backend 비-live 1391 passed/101 skipped/0 failed·live-PG 미실행(DB down·경로 무접촉)·ingestion 1353p·frontend tsc0/test38/lint0·secret PASS·docs_lifecycle 0·ruff 실게이트 0·dead-code 신규 미플래그·**adversarial PROCEED-WITH-FIXES(HIGH-1 template synthetic 태깅·MEDIUM-1 dry-run hard-stop)·code-review LGTM-WITH-NITS(22==22==22)**. **정직 경계**: 진전=첫 R1 pilot batch freeze+operator launch package+internal ops launch readiness·**실 reviewer 입력 아님**(production_gold_count 0·입력 디렉터리 부재→no_actual_input/blocked_no_labels·gate exact passthrough·frozen batch 가 미증가·calibration_ready/merge_gate_ready False·**frozen=합성 fixture·pilot_batch_is_production_candidate=False·frozen 5<<target 200=pilot_n·라벨 회수해도 dataset_source=synthetic→gold 미승격**·frozen worklist≠event truth·실제 email/slack/webhook 전송 0·embedding/LLM 실호출 0·운영 DB/public IU/source storage runtime/RAG·KG·Entity 0 불변·internal ops≠public truth·완전종결=OVERCLAIM)·다음 hard blocker=실 production candidate(live overlap)→실 reviewer contact→actual returned labels(R-BatchFreezeAsTruth·R-GoldAcquisitionPlanOnly·R-ReviewerPilotExecution). **신규 R-BatchFreezeAsTruth(LOW)·R-GoldAcquisitionPlanOnly(#74→#75)·R-ReviewerPilotExecution(#74→#75)·R-IdentityHumanLabeling·R-ReviewerAgreement·R-GoldSamplingBias·R-IdentityEvalDataset 부분진전**·종결 0. ADR#74 커밋 `4de4fc0` 위 ADR#75 미커밋(신규 2+수정 17=19)·커밋 지시 대기·push 안 함._
