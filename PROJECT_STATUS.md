# 📊 프로젝트 진행 현황 (PROJECT_STATUS)

> 매 턴 끝에 새로 쓰입니다. 항상 "가장 최근 1턴"만. 과거는 `git log -- PROJECT_STATUS.md`.
> 사실 원본(자동): `.harness/machine_status.json` · 완료 증거: `.harness/closeout_stamp.json` · 서술: 이 파일(에이전트).

## 🟢 한눈에 (비개발자용 3줄)
- **지금 무엇을 했나:** 지난 턴(ADR#69 — reviewer follow-up operations cockpit)을 안정 기준점으로 **커밋**(`8587d53`)한 뒤, ADR#69 가 만든 follow-up cockpit 을 **실제 reviewer 파일럿을 시작 가능한 상태**로 넘기는 **actual reviewer pilot handoff bundle + returned-label gate**(ADR#70)를 만들었습니다. 운영자가 실 reviewer 에게 **그대로 건넬 수 있는 bundle**(라벨링 지침·reviewer 별 배정 요약·label template schema·기대 파일명·검증 명령)을 **PII-safe**(가명만)하게 산출하고, 라벨이 실제로 들어오면 intake→follow-up→agreement/gold/calibration 을 **end-to-end** 로 실행하며, 아직이면 `ready_to_contact`/`awaiting_reviewer_return` 로 **정직**하게 보고합니다. 단 **실제 email/slack 전송은 0**(operator 수동), 자동 병합·LLM/embedding 실호출·운영 DB·키 값 출력도 **0**입니다.
- **이번 턴에 실제로 끝낸 것:** ADR#69 커밋(`8587d53`·11파일) → **ADR#70**: 새 모듈 `reviewer_pilot_handoff.py`(pilot handoff orchestrator·DB-free·ADR#69 `run_reviewer_followup_ops` 단일 호출 **decorate**·재구현 0) — `build_pilot_handoff_bundle`(operator 배포용·재귀 PII 가드·전송 0)+`run_reviewer_pilot_handoff`(**8-state** `pilot_status`=not_ready/ready_to_contact/awaiting_reviewer_return/partial_returned/invalid_returned/conflict_pending/calibration_pending/imported_ready·intake dir 존재로 접촉 전/회수 대기 분할·gold **exact passthrough**)+`build_correction_templates`/`build_adjudication_handoff`/`build_calibration_gap`(전송 0)+`build_ops_ui_contract`(internal ops dashboard seed·public truth 아님). **ADR#69 커밋분(followup/intake/docs) 미변경**(pilot 은 followup 을 호출만). 신규 **57 테스트**(감사 fix-lock 5 포함).
- **정직한 한계:** 이번 진전은 **pilot handoff bundle + returned-label gate substrate**이지 **실 reviewer 실행이 아닙니다**. **실 reviewer contact·첫 returned labels·실 gold 는 여전히 0**(production_gold_count 0·followup→intake exact passthrough·handoff 만으로 미증가·`pilot_executed`=False·`real_reviewers_contacted`=0), calibration_ready/merge_gate_ready False, 실제 email/slack/webhook 전송·embedding/LLM 실호출·자동 병합·운영 DB·public IU 모두 **0**. ops UI seed contract 는 internal workflow state(public truth 아님). ready_to_contact/awaiting 은 실패가 아니라 정직한 운영 상태. 다음 hard blocker=**actual reviewer contact/첫 returned labels**(R-ReviewerPilotExecution). **push 금지·커밋 지시 대기**.

## 📋 자동 수집 사실 (machine_status.json)
- **git HEAD `8587d53`**(ADR#69, 본 턴 첫 커밋·11파일·+1405/−49) 위 **ADR#70 = 미커밋**: 신규 2(`backend/app/tools/reviewer_pilot_handoff.py`[orchestrator] + `backend/tests/test_reviewer_pilot_handoff.py`[57 테스트]) + 수정 9(docs 8: `2_ROADMAP/00·15·19`·`_RISK/RISK_REGISTER`·`5_REFERENCE/RAG_KG_AGENT_READINESS·INTELLIGENCE_UNIT_CONTRACT`·`_CANONICAL/02`·`_DECISIONS/2026-06` + `PROJECT_STATUS`) = **추적 11항목**. **migration 없음·ADR#69 커밋분 frozen(pilot 은 followup 호출만)·단방향 import·재구현 0·ADR#69/#68/#67 머신 조합·EVENT_SCHEMA(DB-free라 무변경) 무변경.**
- 열린 RISK: **종결 0·신규 2**(R-ReviewerPilotExecution[MEDIUM]·R-OpsUIPrematureTruth[LOW]). 부분진전: R-ReviewerFollowupOps(#69→#70 진전·완전종결 금지)·R-IdentityHumanLabeling·R-ReviewerAgreement·R-GoldSamplingBias·R-IdentityEvalDataset. throwaway 는 `.harness/_TRASH/`·`frontend/.harness/`·`--paths/`(stray·커밋 제외)·`outputs/reviewer_batch/`(gitignore).

## ✅ 이번 턴에 달성한 것 (actual reviewer pilot handoff + returned-label gate — ADR#70)
- **① ADR#69 커밋**: 11파일 → `8587d53`(제공 메시지 그대로·secret PASS·docs_lifecycle conflicts 0·SIGNATURE MATCH·unresolved 0·push 0·NO UPSTREAM).
- **② 원자 분석(§2·20문항·구현 전 보고)**: `run_reviewer_followup_ops`(ADR#69)가 이미 status/coverage/counts/reminder/escalation/SLA·intake passthrough(gold/calibration/conflict)를 단일 출처로 반환 → pilot 은 **1회 호출 decorate**. handoff artifacts 는 기존 pure builder(`build_reviewer_instruction`/`build_label_template`/`build_assignment_manifest`/`build_intake_plan`·mkdir 안 함) → **intake dir 존재 여부가 `ready_to_contact`(미설정) vs `awaiting_reviewer_return`(설정·빈)의 정직한 신호**. 다음 hard blocker=actual reviewer contact/첫 returned labels. **docs/3_SOURCE_STATUS\* 부재**(docs/3_\*=3_ARCHIVE 뿐)·`docs/2_ROADMAP/19_WEB_INTELLIGENCE_IMPLEMENTATION_SPEC` 존재·EVENT_SCHEMA(DB-free) 무변경·`.gitignore:81 outputs/reviewer_batch/` 실 label gitignored 확인.
- **③ A — actual reviewer pilot handoff bundle**: `build_pilot_handoff_bundle`(reviewer instruction[score/rationale/predicted 구조적 False]·reviewer 별 assignment summary[pseudonym·pair_ids·expected filename]·label template schema[fill/fixed columns·allowed labels/confidences]·intake dir·validation command·due_hint·pseudonym only)+`_assert_pii_safe` **재귀 forbidden-key 가드**(전체 출력까지). 전송 0(operator 수동 배포).
- **④ B — returned-label gate**: returned label 있으면 followup→intake end-to-end(상태/gold/calibration delta)·없으면 정직 ready_to_contact/awaiting. gold/calibration **exact passthrough**(handoff 재계산 0).
- **⑤ C — pilot status report**: **8-state** `pilot_status`(not_ready/ready_to_contact/awaiting_reviewer_return/partial_returned/invalid_returned/conflict_pending/calibration_pending/imported_ready_for_merge_gate_review). followup 7-state 매핑+intake dir 존재로 접촉 전/회수 대기 분할. `reviewer_contact_required`·`intake_directory_established`(접촉 검증 아닌 경로 설정 프록시·정직 명시).
- **⑥ D — ops UI seed contract**: `build_ops_ui_contract`(`OpsReviewBatchStatus`·미래 internal ops dashboard 가 읽을 workflow state·`flags{no_merge,no_public_iu,pii_safe,no_llm,no_db_write,gold_provenance_verified}`·**provenance caveat+invalid_file_count 동반**·public truth 아님). docs/schema(`19 §19.1`) + 최소 코드 contract.
- **⑦ §7 correction/adjudication/calibration handoff**: `build_correction_templates`(invalid 파일별 reason code only·값 0)+`build_adjudication_handoff`(conflict→human lead·**자동 다수결 금지**)+`build_calibration_gap`(gold/KO/negative gap·intake passthrough). 전송 0.
- **⑧ §9 Agent contract**: `REVIEWER_PILOT_AGENT_CONTRACT`(pilot handoff readiness/returned label status/missing follow-up/correction/adjudication/calibration gap/internal ops UI 요약/next action **계획 가능**·label 조작·임의 생성·score truth·같은 사건 확정·merge·public IU·anchor·secret·reviewer raw PII·**actual 전송** **불가**·`EMBEDDING_LLM_ADJUDICATOR_INTERFACE` No-Go).
- **⑨ E/F 경계**: embedding/LLM scorer runtime(E)·production DB activation/semantic merge(F) 미착수.

## 🧭 cross-batch identity 4단계 + 평가/gold gate (정직)
- ① anchor 병합(ADR#40) → ② 후보 LINK(ADR#41) → ③ shadow 판정(ADR#42·운영 배선 ADR#48~#53·preflight/smoke ADR#54·live-db ADR#55·replay ADR#56·overlap discovery ADR#57·acquisition ADR#58·near-match 큐 ADR#59·targeted readiness ADR#60·provider readiness ADR#61·provider adapter ADR#62·live query smoke ADR#63·2nd provider+cross-source ADR#64·semantic candidate scorer ADR#65·reviewer label operations ADR#66·reviewer batch launch pack ADR#67·first production label intake ADR#68·reviewer follow-up operations ADR#69·**actual reviewer pilot handoff ADR#70**) → ④ 실 병합(**미구현**).
- **ADR#70 위치**: 단계 ⑦(검수 패킷)~⑩(MERGE_GATE) 사이의 **reviewer pilot handoff + returned-label gate substrate**. ADR#69 follow-up cockpit 을 **실 reviewer pilot 을 시작 가능한 handoff**(bundle→returned-label gate→8-state status→correction/adjudication/calibration handoff→ops UI contract)로 연결(병합·단정 0·score/rationale/predicted labeler 숨김·reviewer_id 가명·전송 0·returned 없음→ready_to_contact/awaiting 정직). **실 reviewer 실행이 아님**(production_gold_count 0·followup→intake exact passthrough·`pilot_executed`=False·merge_gate_ready False·실제 전송 0).
- **⚠ 미해소(OPEN):** 실 reviewer contact·첫 returned labels·실 production reviewer 라벨·실 gold(production_gold 0)·실 reviewer SLA 운영·reviewer 합의 실측·gold-calibrated embedding/LLM **실 병합 판정기**(No-Go)·MERGE_GATE 런타임·한국어 캘리브레이션·운영 DB 0009 배포. "pilot handoff bundle ≠ 실 pilot 실행·실 gold·실 병합" — 완전종결=OVERCLAIM.

## 🔎 source orchestration / 제품 계약
- official/news=publishable evidence(merge anchor)·community=반응 evidence(anchor 불가)·market=signal·catalog=entity·search=URL·unknown=fail-closed. pilot handoff 는 publishable×publishable 후보 큐만 다룸(community/market/catalog anchor 차단·상속).
- **제품 계약(raw≠public)**: handoff bundle·correction/adjudication·ops UI contract 의 노출 표면에 score/rationale/predicted_status/raw body/secret/raw reviewer_id **0** — bundle 은 instruction 구조 플래그(model_score_shown=False 등)에서 숨김 **파생**+`_assert_pii_safe` 재귀 가드(전체 출력), template schema 는 `REVIEWER_ALLOWED_KEYS` allowlist, correction 은 basename+reason code only(값 0), 비-가명 파일명은 `nonconforming_file_N` 마스킹(followup passthrough). production_gold_count 0·followup→intake **exact passthrough**(handoff 만으로 미증가)·`merge_allowed=False`·`no_public_intelligence_unit`·`actual_sending_performed=False`·embedding/LLM 실호출 0·DB write 0. ops UI seed contract 는 `flags.gold_provenance_verified=False`·`production_gold_provenance_verified` 동반(internal workflow state ≠ verified public truth·R-OpsUIPrematureTruth). Agent 는 pilot handoff/returned-label status/ops UI 를 *계획/요약* 가능하나 label 조작·임의 생성·merge·검출 단정·PII/secret 출력·실제 전송 불가. LLM/Agent 진입 9조건 여전히 No-Go(1·4·5·7).

## ⚠️ 이번 턴 종결/갱신 RISK
- **신규 2(실 present blocker만)**: **R-ReviewerPilotExecution**(MEDIUM·pilot handoff bundle 은 있으나 실 reviewer contact·첫 returned labels 미실행·R-ReviewerFollowupOps 추적층에서 분리된 **실행층** blocker) · **R-OpsUIPrematureTruth**(LOW·ops UI seed contract 가 public truth 로 오인될 위험·구조적 flags+provenance caveat 로 완화·실 UI 분리 강제는 구현 시 잔여). **R-ReturnedLabelQuality 는 returned labels 0이라 가설→미등록**(watch only·남발 금지).
- **부분진전(종결 0)**: **R-ReviewerFollowupOps**(#69→#70 진전: handoff bundle+8-state pilot_status+returned-label gate+correction/adjudication/calibration handoff+ops UI contract·**실 reviewer contact·실 returned labels 0 불변→완전종결 금지**) · **R-IdentityHumanLabeling**(handoff→실 label 경로·production_gold_count exact passthrough·실 label 0) · **R-ReviewerAgreement**(conflict→adjudication handoff·실 합의 0) · **R-GoldSamplingBias**(calibration_gap=gold/KO/negative gap 표면화·실 표집 0) · **R-IdentityEvalDataset**(returned label→denominator planning·ops UI denominator status·MERGE_GATE evidence 0).

## ❌ 달성하지 못한 것 & 왜 (이월)
- **실 reviewer contact / 첫 returned labels / 실 human-reviewed production gold**: handoff bundle+returned-label gate 는 닫혔으나 **실 reviewer 가 접촉되고 회수한 label·실 gold 0**(production_gold_count 0·`pilot_executed`=False·intake passthrough). reviewer 실제 contact·첫 returned labels 선결(R-ReviewerPilotExecution).
- **실제 전송 / reviewer SLA 운영**: bundle/reminder/correction/adjudication **템플릿**은 생성되나 **실제 email/slack/webhook 전송 0**(operator 수동)·실 SLA 운영 미입증.
- **embedding/LLM 실호출 / 실 병합 / 운영 DB**: 이번 턴 No-Go(옵션 E/F 금지). gold-calibrated adjudicator runtime·운영 DB 0009 배포·production 백로그 0.

## 🚧 닫을 수 없는 문제 (BLOCKED/UNKNOWN)
- BLOCKED: 실 gold = **reviewer 실제 contact·첫 returned label 파일 회수** 선결(handoff 는 bundle·production_gold 0·R-ReviewerPilotExecution). 실 병합 = gold-calibrated embedding/LLM adjudicator(옵션 E·다음)+MERGE_GATE. production 백로그 = 운영 DB 0009 배포(승인)+실 fetch.
- UNKNOWN: 실 reviewer 회수율/SLA 실측(템플릿·bundle 만 존재·실 운영 0). semantic score 의 실 병합 허용 기준(production precision·실 gold·reviewer 합의·한국어·MERGE_GATE) → R-IdentityHumanLabeling·R-ReviewerAgreement·R-IdentityEvalDataset·R-SemanticIdentityAdjudicator 선결. ADR#64 deterministic 0 이 paraphrase recall 한계인지 real scarcity 인지 여전히 n=2 미구분(gold 필요).

## 👉 다음 할 일
1. **[커밋 대기]** 본 턴 ADR#70: 신규 2(reviewer_pilot_handoff.py+test) + 수정 9(docs 8·PROJECT_STATUS) = 11파일 미커밋. 커밋 여부 지시 대기. push 금지. (`--paths/` stray 정리 허가도 함께 받으면 처리.)
2. **[다음 단계]** (a) **actual reviewer contact + 첫 returned labels 회수**(pilot handoff bundle 로 시작·returned-label gate 로 검증·R-ReviewerPilotExecution) → (b) reviewer recruitment(운영·SLA·roster) → (c) gold denominator 충족(live 200·KO 50) → (d) MERGE_GATE calibration(precision≥0.98·FPR≤0.01) → (e) **embedding scorer opt-in 실호출**(`create_embedding_client` seam·secret-safe·bounded·internal-only) → (f) embedding/LLM/KG **실 병합** adjudicator runtime → (g) (승인 하) 운영 DB 0009 배포 + scheduler → RAG/KG/Agent/IU. (internal ops dashboard 는 `OpsReviewBatchStatus` contract 위에서 착수 가능·public IU UI 는 여전히 No-Go.)

## 📁 근거 (이번 턴 핵심)
- 커밋: `8587d53`(ADR#69·11파일·+1405/−49·push 0·NO UPSTREAM). ADR#70 변경: 신규 `reviewer_pilot_handoff.py`(orchestrator)+`test_reviewer_pilot_handoff.py`(57)·docs 8·PROJECT_STATUS(수정 9). **ADR#69 커밋분 frozen(pilot 은 followup 호출만)·단방향 import·재구현 0·ADR#69/#68/#67 머신 조합·EVENT_SCHEMA 무변경.**
- 검증(정직): 신규 **57**(test_reviewer_pilot_handoff 1-57: pilot handoff readiness 1-8·returned-label gate 9-15·safety 16-24·templates 25-30·ops UI contract 31-36·no-merge/LLM/DB 37-42·contract/policy lock 43-52·감사 fix-lock 53-57)·backend 비-live **1190 passed/101 skipped/0 failed**(1133+57·full run 1185[52-test]+1189[56-test] 앵커+격리 leaf +1·reviewer_pilot_handoff 비-test importer 0 검증)·**live-PG=미실행**(DB down; 변경이 DB/session/ORM 무접촉=pilot write-free·`db_write:False`·session 인자 부재·DB 바운드 도구 미사용→무영향·baseline 91 불변)·ingestion **1353 passed**(불변)·frontend tsc**0**/node:test **12**/lint **0**(무변경·프론트 변경 0)·secret scan **PASS**·docs_lifecycle conflicts **0**·ruff 실게이트(E/F/I/B/W−E501) **0**·dead-code(신규 모듈) **미플래그**(539 모듈·repo baseline 238 advisory 불변). **honest 동작**: bundle 완성+intake dir 미존재→`ready_to_contact`·존재·빈→`awaiting_reviewer_return`·일부→`partial_returned`·forbidden/malformed→`invalid_returned`(correction)·full+conflict→`conflict_pending`(adjudication handoff)·full+무충돌+floor 미달→`calibration_pending`(calibration gap)·empty queue→`not_ready`·production_gold_count=followup→intake exact passthrough(handoff 미증가)·merge_allowed False·`actual_sending_performed`/`pilot_executed` False·raw reviewer id/email 미노출.
- 감사: adversarial-reality-critic + code-review(high effort) → cardinal(handoff cockpit 만으로 실 pilot 됐다/no-labels 성공포장/synthetic→production/model→human/PII 노출/single→gold/conflict 자동 다수결/실 label 없이 gold 증가/MERGE_GATE 전 merge/실제 전송) 전부 차단 유지. **adversarial=PROCEED-WITH-FIXES·발견·수정 2 MEDIUM+1 LOW-MEDIUM**: ① MEDIUM(ops_ui_contract 가 production_gold_count 노출하며 `production_gold_provenance_verified` caveat 누락→미검증 gold 가 검증된 truth 로 렌더 가능)→contract 에 provenance+synthetic_gold_count+`flags.gold_provenance_verified` 추가+test_53. ② MEDIUM(ready/awaiting 가 `Path.exists()` 프록시라 "dir 존재"가 "reviewer 접촉"으로 둔갑)→`_intake_dir_established` 정직 doc+미사용 `returned_count` 제거+`intake_directory_established` 필드+awaiting next_action 보정+test_56. ③ LOW-MEDIUM(재귀 PII 가드 전체 출력 미적용+"actual pilot" 명명 과대)→`_assert_pii_safe(result)` 전체 출력+`pilot_executed`/`real_reviewers_contacted`/`real_labels_returned` 명시 필드+test_54/55. **code-review=HIGH/MEDIUM 0·LOW 2**: ① LOW(malformed 파일 시 ops_ui invalid_label_count=0 인데 invalid_returned→자기모순)→`invalid_file_count` 추가+test_57. ② LOW(label_rows+intake_directory 동시 입력 시 표시 경로 불일치)=비현실 조합·followup 충실 passthrough→**수용**(코드 변경 0). 핵심 안전축(gold exact passthrough·merge_allowed False·전송 0·PII 재귀 가드·followup/intake 발산 0·`_assert_pii_safe(result)` 거짓양성 크래시 REFUTED). 결과·정정 단일 출처=closeout stamp `audit_evidence`.
- 문서: ADR#70(`_DECISIONS`)·RISK(`_RISK`·종결 0·신규 2[R-ReviewerPilotExecution·R-OpsUIPrematureTruth]·R-ReviewerFollowupOps #69→#70)·ADR#70 블록+RealSourceLoop 표(`2_ROADMAP/15`)·(11ab)(`2_ROADMAP/00`)·ops UI seed contract §19.1(`2_ROADMAP/19`)·condition 4/agent(`RAG_KG`)·pilot handoff 계약(`IU_CONTRACT`)·`_CANONICAL/02`(ADR#41~#70 + pilot 절). **EVENT_SCHEMA 무변경**(ADR#70 DB-free·event 필드/테이블 무변경·최소 변경).

---
_as_of: 2026-06-27 · ADR#70 actual reviewer pilot handoff bundle + returned-label gate — ADR#69 커밋(`8587d53`) 후, ADR#69 follow-up cockpit 을 실 reviewer pilot 을 시작 가능한 handoff 로 연결(새 파일 1·재구현 0·ADR#69 `run_reviewer_followup_ops` 단일 호출 decorate·DB 0·전송 0): `reviewer_pilot_handoff.py` — `build_pilot_handoff_bundle`(operator 배포용 bundle·재귀 PII 가드·전송 0)·`run_reviewer_pilot_handoff`(§4·8-state pilot_status[not_ready/ready_to_contact/awaiting_reviewer_return/partial_returned/invalid_returned/conflict_pending/calibration_pending/imported_ready·intake dir 존재로 접촉 전/회수 대기 분할]·returned-label gate·gold exact passthrough)·`build_correction_templates`/`build_adjudication_handoff`/`build_calibration_gap`(reason code only·human lead·자동 다수결 금지·전송 0)·`build_ops_ui_contract`(OpsReviewBatchStatus·internal dashboard·public truth 아님)·`REVIEWER_PILOT_AGENT_CONTRACT`·CLI. 옵션 **A+B+C+D 채택·E(embedding/LLM runtime)/F(production DB·merge) 금지·신규 RISK 2**. **측정**: 신규 57·backend 비-live 1190p/101s/0f·live-PG 미실행(DB down·경로 무접촉·baseline 불변)·ingestion 1353p·frontend tsc0/test12/lint0(무변경)·secret PASS·docs_lifecycle 0·ruff 실게이트 0·dead-code 신규 미플래그. **감사 PROCEED-WITH-FIXES**: adversarial 2 MEDIUM(ops_ui provenance caveat·ready/awaiting dir≠contact)+1 LOW-MEDIUM(전체출력 PII 가드·naming) 수정·code-review LOW 1(invalid_file_count) 수정+LOW 1 수용. **정직 경계**: 진전=pilot handoff bundle+returned-label gate substrate·**실 reviewer 실행 아님**(production_gold_count 0·followup→intake exact passthrough·`pilot_executed`=False·calibration_ready/merge_gate_ready False·실제 email/slack/webhook 전송 0·embedding/LLM 실호출 0·운영 DB/public IU/production 백로그 0 불변·완전종결=OVERCLAIM)·다음 hard blocker=actual reviewer contact/첫 returned labels(R-ReviewerPilotExecution)·ops UI=internal workflow state≠public truth(R-OpsUIPrematureTruth). **신규 R-ReviewerPilotExecution(MEDIUM)·R-OpsUIPrematureTruth(LOW)·R-ReviewerFollowupOps(#69→#70 진전)·R-IdentityHumanLabeling·R-ReviewerAgreement·R-GoldSamplingBias·R-IdentityEvalDataset 부분진전**·종결 0. ADR#69 커밋 `8587d53` 위 ADR#70 미커밋(신규 2+수정 9=11)·커밋 지시 대기·push 안 함._
