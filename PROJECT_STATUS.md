# 📊 프로젝트 진행 현황 (PROJECT_STATUS)

> 매 턴 끝에 새로 쓰입니다. 항상 "가장 최근 1턴"만. 과거는 `git log -- PROJECT_STATUS.md`.
> 사실 원본(자동): `.harness/machine_status.json` · 완료 증거: `.harness/closeout_stamp.json` · 서술: 이 파일(에이전트).

## 🟢 한눈에 (비개발자용 3줄)
- **지금 무엇을 했나:** 지난 턴(ADR#59) 큐를 안정 기준점으로 **커밋**한 뒤, 그 큐가 **합성 예시(fixture) 수준에서 멈춰 있던** 한계를 넘어, "**무엇을 어디서 언제 수집해야 표현만 다른 같은 사건(near-match) 후보가 모이는가**"라는 **목적 기반 수집 계획**과 큐를 **실제로 잇는 운영 경로**를 만들었습니다. 핵심: 새 기능을 짓지 않고 **이미 있는 수집·검수·gold 기계를 전부 재사용** — 검수자 투입 직전 **운영 점검표**, gold 승격 경로의 **5가지 시나리오 검증**, 그리고 "**오늘 실제로 가능한 수집 수단이 무엇인가**"를 정직하게 드러냅니다(병합·LLM·DB쓰기 **0**).
- **이번 턴에 실제로 끝낸 것:** ADR#59 커밋(`bf3ee6d`) → **ADR#60**: 새 오케스트레이터 `targeted_same_event_acquisition.py` — 수집계획(매체쌍·주제·시간창)→수집/fixture→큐→**검수 패킷+운영 점검표**·gold 승격 **시뮬레이션**(만장일치/충돌/단일/조정→gold·**production gold 0**)·"오늘 query 가능한 key-free provider 사실상 0(GDELT 429·RSS 주제검색 불가)"를 **정직 표면화**. 감사 2종(adversarial **PROCEED-WITH-FIXES**·code-review)·지적 전부 **코드로 정정**(synthetic↔live 봉인 코드 강제·host_gate 우회 회귀 수정).
- **정직한 한계:** **실제 targeted near-match 후보·실제 사람 검수자·gold 는 여전히 0**(fixture 는 운영 경로를 입증할 뿐 실 데이터 아님). embedding/LLM 판정기 미구현(No-Go)·**운영 DB 무변경·production 백로그 0·자동 병합 0·본문 미저장·LLM 0** 불변. push 금지.

## 📋 자동 수집 사실 (machine_status.json)
- **git HEAD `bf3ee6d`**(ADR#59, 본 턴 첫 커밋) 위 **ADR#60 = 미커밋**: 신규 2(`backend/app/tools/targeted_same_event_acquisition.py`[오케스트레이터] + `backend/tests/test_targeted_same_event_acquisition.py`[57 테스트]) + docs 7(`2_ROADMAP/00·15`·`_RISK/RISK_REGISTER`·`5_REFERENCE/RAG_KG_AGENT_READINESS·INTELLIGENCE_UNIT_CONTRACT`·`_CANONICAL/02`·`_DECISIONS/2026-06`) + `PROJECT_STATUS` = **추적 10항목**. **migration 없음·파이썬 source 1 신규(targeted_same_event_acquisition.py)·기존 코드 파일 수정 0(전부 재사용).**
- 열린 RISK: **total 36**(HIGH 3·MEDIUM 21·LOW 11). **신규 0·종결 0.** 부분진전 6: R-SourceOverlapScarcity·R-IdentityHumanLabeling·R-ReviewerAgreement·R-GoldSamplingBias·R-SemanticIdentityAdjudicator·R-LiveIdentityBacklog. throwaway 는 `.harness/_TRASH/`·`frontend/.harness/`(gitignored·커밋 제외).

## ✅ 이번 턴에 달성한 것 (targeted same-event acquisition → reviewer/gold operating readiness — ADR#60)
- **① ADR#59 커밋**: 12파일 → `bf3ee6d`(secret PASS·docs_lifecycle conflicts 0·SIGNATURE 검증[stale-by-2=ADR#58 커밋 경계 아티팩트 규명]·push 0).
- **② 원자 분석(§2·20문항)**: 3-agent 서브시스템 매핑(source registry/fingerprint·labeling/gold·IU/API/frontend/readiness)+핵심 파일 정독. **핵심 발견**: 파이프라인 부품(`fetch_rss/gdelt_overlap_records`·`discover_overlap`·`build_near_match_reviewer_queue`)은 **이미 존재**하나 **셋을 잇는 orchestrator·§4 output 부재**(`near_match_reviewer_queue.main` 은 captured fixture 만). 오늘 query 현실=GDELT 만 key-free topic query 가능하나 429·RSS 는 topic 불가(time/source-pair 만)·나머지 key 필요. 분석 먼저 보고.
- **③ A — targeted acquisition→queue population(핵심)**: 신규 `run_targeted_acquisition`(주입 records>governed fetch>deterministic fixture→`discover_overlap`→`build_near_match_reviewer_queue`)·`build_targeted_acquisition_report`(§4 필수 output: acquisition_run_id·source_pair_plan·topic/time_window·candidate/near/hard/fingerprint count·reviewer_packet_exportable·labeler_prediction_hidden·gold_ready·dataset_source·honesty_boundary). **재구현 0**. `build_targeted_same_event_fixture`=실 `_title_tokens`/`_jaccard`/`semantic_identity_fingerprint` 로 band 수치검증(near 2·hard 3·fingerprint 1·**synthetic_fixture 명시**).
- **④ B — reviewer operating packet readiness**: `build_reviewer_operating_checklist`(§5·실 reviewer 투입 직전 점검표 — packet_id·dataset_source·**hidden_prediction_verified/raw_body_absent_verified 구조적 계산**[하드코딩 True 아님]·reviewer_instruction·allowed_labels·conflict/agreement/adjudication/gold_promotion/merge_policy[prohibited until MERGE_GATE]).
- **⑤ C — gold calibration simulation**: `simulate_gold_calibration`(§6·**synthetic** reviewer label 로 5경로[unanimous same/different·single→insufficient·conflict→adjudication queue·human adjudication→gold]→`resolve_queue_gold` 입증·simulated_gold 3/conflict 1/insufficient 1·**production_gold_count=0**·전부 synthetic_fixture).
- **⑥ §8/§9 — acquisition linkage + agent schema**: `build_targeted_acquisition_linkage`(expected near/hard/gold yield·reviewer_load·**query_capability/provider_status/rate_limit_risk/fallback**)·`build_provider_capability_matrix`(오늘 실 targeted near-match yield 낮음 정직)·`build_targeted_agent_schema`(agent_can_plan/agent_cannot[같은 사건 확정·merge·public IU·community/market anchor 금지]·embedding No-Go).
- **⑦ D No-Go·E 금지**: embedding/LLM adjudicator 는 `EMBEDDING_LLM_ADJUDICATOR_INTERFACE` **No-Go**(호출 0)·production DB 미접촉(옵션 E 금지·운영 DB upgrade 0).
- **⑧ honesty 코드 강제 + 감사 정정**: `_SYNTHETIC_FIXTURE_MARKER`→synthetic fixture 는 real_fetch=True 여도 live_derived 도장 불가(음성 테스트 잠금). adversarial **PROCEED-WITH-FIXES**(MEDIUM-1/2→코드 강제+음성 테스트 7)·code-review(**host_gate→gdelt 미전달 ADR#57 우회 회귀 정정**·candidate_count 분리·fixture topic 라벨·near_jaccard 死파라미터 제거·fixture 대체 정직 노출·reaction 중복 제거 6건).

## 🧭 cross-batch identity 4단계 + 평가/gold gate (정직)
- ① anchor 병합(ADR#40) → ② 후보 LINK(ADR#41) → ③ shadow 판정(ADR#42·운영 배선 ADR#48~#53·preflight/smoke ADR#54·live-db ADR#55·replay ADR#56·overlap discovery ADR#57·acquisition ADR#58·near-match reviewer/gold 큐 ADR#59·**targeted acquisition 운영 readiness ADR#60**) → ④ 실 병합(**미구현**).
- **ADR#60 위치**: ③④ 사이 **detection 레이어의 운영 준비**. ADR#59 큐(captured fixture 수준)를 targeted acquisition(매체쌍·주제·시간창)+reviewer operating checklist+gold simulation 으로 연결 — 실 near-match 후보를 채울 **운영 경로**를 닫음. provider capability matrix 가 "오늘 query-capable key-free provider 사실상 0"을 정직 표면화 → 실 backlog 충원의 다음 게이트=query-capable provider.
- **⚠ 미해소(OPEN):** 실 targeted near-match 후보(GDELT 429·RSS topic 불가·fixture=경로 입증)·실 reviewer/gold(synthetic label=경로 검증·production_gold 0)·embedding/LLM 실 병합 adjudicator(No-Go)·운영 DB 0009 배포·MERGE_GATE 충족·한국어 캘리브레이션. "운영 readiness(능력) ≠ 실 gold(actuality)" — 완전종결=OVERCLAIM.

## 🔎 source orchestration / 제품 계약
- official/news=publishable evidence(merge anchor)·community=반응 evidence(anchor 불가)·market=signal·catalog=entity·search=URL·unknown=fail-closed. targeted fixture 의 community 반응 record 는 **anchor 금지**로 필터(`source_role_compatible`·both_pub 이중 가드·테스트로 잠금). raw body 미저장(title/canonical/observed/source_type 만·allowlist 구조 강제).
- **제품 계약(raw≠public)**: 큐/checklist 는 Intelligence Unit **merge safety substrate** — near-match 는 같은 사건 단정/병합/public IU 가 아니라 **reviewer 에게 던지는 질문**(predicted_status 숨김·`labeler_facing_view` bias 0). Agent 는 reviewer queue/acquisition 을 *계획* 가능하나 `no_merge_without_gate`·`no_public_intelligence_unit`·`llm_invoked=False`. LLM/Agent 진입 9조건 여전히 No-Go(1·4·5·7).

## ⚠️ 이번 턴 종결/갱신 RISK
- **부분진전(종결 0·신규 0·total 36 불변)**: **R-SourceOverlapScarcity**(targeted acquisition→queue 연결·provider capability 현실) · **R-IdentityHumanLabeling**(reviewer operating checklist·실 reviewer 직전) · **R-ReviewerAgreement**(gold path 5경로 simulation) · **R-GoldSamplingBias**(near+hard 균형·dataset_source 봉인·production_gold 0) · **R-SemanticIdentityAdjudicator**(near→reviewer/gold 운영 경로·embedding No-Go) · **R-LiveIdentityBacklog**(captured fixture→targeted acquisition 연결).
- 디렉티브 신규 RISK 후보(R-ReviewerOpsReadiness/R-NearMatchQueuePopulation/R-GoldCalibrationReadiness)는 **독립 blocker 아님**(checklist/큐/gold-sim 은 운영 준비 substrate·실 데이터 0→adversarial HIGH-1=docs 미등재만 정정 필요였고 본 턴 해소)→기존 RISK 에 포섭(RISK 남발 금지).

## ❌ 달성하지 못한 것 & 왜 (이월)
- **실 targeted near-match 후보**: untargeted RSS overlap 0(ADR#58)·GDELT topic query=429·나머지 query provider=key 필요 → **오늘 실 targeted near-match 를 산출할 key-free provider 사실상 0**. deterministic fixture 가 운영 경로를 입증할 뿐 실 후보 아님. query-capable provider 확보가 선결.
- **실 human-reviewed gold**: checklist 가 실 reviewer 직전까지 준비하나 reviewer 충원 0 → gold_ready=False. gold 경로는 synthetic label 로 **입증**(production_gold 0).
- **embedding/LLM adjudicator**: paraphrase 검출 본경로는 gold calibration+MERGE_GATE 후에만(No-Go 인터페이스만·호출 0). 운영 DB·production 백로그 0(옵션 E 금지).

## 🚧 닫을 수 없는 문제 (BLOCKED/UNKNOWN)
- BLOCKED: 실 targeted near-match = query-capable provider(GDELT 429 우회 또는 key-required provider key) 선결. 실 reviewer/gold = reviewer 충원·운영 절차 선결. production 백로그 = 운영 DB 0009 배포(승인) + 실 fetch 볼륨.
- UNKNOWN: paraphrase overlap 의 실 병합 허용 기준(production precision·실 gold·reviewer 합의·한국어 캘리브레이션·MERGE_GATE) → R-SemanticIdentityAdjudicator·R-IdentityHumanLabeling·R-ReviewerAgreement·R-IdentityEvalDataset 선결.

## 👉 다음 할 일
1. **[커밋 대기]** 본 턴 ADR#60: 신규 2(targeted_same_event_acquisition.py+test) + docs 7 + PROJECT_STATUS = 10파일 커밋 여부 지시 대기. push 금지.
2. **[다음 단계]** (a) **query-capable provider 확보**(GDELT 429 우회 또는 key-required provider key) → 실 targeted near-match 후보 → 큐 충원 → (b) reviewer 충원·합의 gold + 한국어 캘리브레이션 → (c) embedding/LLM/KG **실 병합** adjudicator(paraphrase·MERGE_GATE·`semantic_score` seam) → (d) (승인 하) 운영 DB 0009 배포 + scheduler 가동 → RAG/KG.

## 📁 근거 (이번 턴 핵심)
- 커밋: `bf3ee6d`(ADR#59). ADR#60 변경: 신규 `targeted_same_event_acquisition.py`(오케스트레이터·587줄)+`test_targeted_same_event_acquisition.py`(57)·docs 7. **기존 코드 파일 수정 0(전부 재사용·재구현 0).**
- 검증(정직): fixture readiness — candidate 5(near 2+hard 3)·fingerprint 1·packet_exportable True·labeler_prediction_hidden True·gold_ready **False**·gold_sim simulated_gold 3/conflict 1/insufficient 1/**production_gold 0**·checklist hidden/raw verify True·real RSS transport→near≥1 dataset_source live_derived(fixture 와 정직 구분). 테스트: 신규 **57**(§10 1-49+실 fetch provenance+음성 가드 7)·backend 비-live **716 passed/4 skipped/0 failed**·live-PG **94 passed(217s·단독·write-free 무영향)**·ingestion **1353 passed**·frontend tsc**0**/node:test **12**/lint **0**·secret scan **PASS(9)**·docs_lifecycle conflicts **0**·ruff 실게이트(E/F/I/B/W) **0**(I001 import 정렬 자동수정·UP045 noise=기존 ADR#59 파일도 3건·codebase Optional 스타일). 운영 DB 무변경.
- 감사: adversarial-reality-critic **PROCEED-WITH-FIXES**(production_gold=0 봉인·synthetic≠live 봉인·community anchor 차단·near/hard/fp 산술 실측·verify 계산값 전부 VALID·HIGH-1[docs 미등재]→본 턴 해소·MEDIUM-1/2[synthetic+real_fetch seam·음성 케이스 미테스트]→`_SYNTHETIC_FIXTURE_MARKER` 코드 강제+음성 테스트 7) / code-review(10 angle: **host_gate→gdelt 미전달 ADR#57 우회 회귀**[3 finder 공통]·candidate_count 이름 분리[discovery_candidate_count]·fixture topic 라벨 일치·near_jaccard 死파라미터 제거·fixture 대체 정직 노출·reaction 중복 제거 6건 정정·CLAUDE.md 위반 0).
- 문서: ADR#60(`_DECISIONS`)·R-SourceOverlapScarcity/R-IdentityHumanLabeling/R-ReviewerAgreement/R-GoldSamplingBias/R-SemanticIdentityAdjudicator/R-LiveIdentityBacklog 부분진전(`_RISK`·종결 0·신규 0)·targeted acquisition 서브섹션+RealSourceLoop 표 #60(`2_ROADMAP/15`)·(11r)(`2_ROADMAP/00`)·agent readiness §6b(`RAG_KG`)·targeted acquisition 계약(`IU_CONTRACT`)·`_CANONICAL/02`(ADR#41~#60).

---
_as_of: 2026-06-25 · ADR#60 targeted same-event acquisition → near-match reviewer/gold operating readiness — ADR#59 커밋(`bf3ee6d`) 후, ADR#59 큐가 **captured fixture 수준에서 멈춰** 있던 한계를 넘어 **targeted same-event acquisition 과 연결하는 운영 경로**를 닫음(새 파일 1·기존 머신 전부 재사용·재구현 0): 신규 `targeted_same_event_acquisition.py` — `build_provider_capability_matrix`(오늘 query-capable key-free provider 사실상 0[GDELT 429·RSS topic 불가]→실 targeted near-match yield 낮음 정직)·`build_targeted_acquisition_plan`(매체쌍·주제·시간창)·`build_targeted_same_event_fixture`(중앙은행 금리결정·실 함수로 band 수치검증 near 2·hard 3·fingerprint 1·**synthetic_fixture 명시**)·`run_targeted_acquisition`(주입 records>governed fetch>fixture→`discover_overlap`→queue·실 0후보면 block_reason 정직·`_SYNTHETIC_FIXTURE_MARKER` 로 synthetic↔live 봉인 코드 강제)·`build_targeted_acquisition_report`(§4 필수 output)·`build_reviewer_operating_checklist`(§5·hidden_prediction/raw_body absent 구조적 verify·merge_policy prohibited until MERGE_GATE)·`simulate_gold_calibration`(§6·synthetic 5경로·**production_gold_count=0**)·`build_targeted_acquisition_linkage`(§8)·`build_targeted_agent_schema`(§9·agent_cannot·embedding No-Go). 옵션 **A+B+C 채택·D interface/eval plan 만(No-Go)·E 금지·신규 RISK 0**. **측정**: 신규 57·backend 비-live 716p/4s/0f·live-PG 94p(단독)·ingestion 1353p·frontend tsc0/test12/lint0·secret PASS(9)·docs_lifecycle 0·ruff 실게이트 0. 감사: adversarial PROCEED-WITH-FIXES(HIGH-1 docs→해소·MEDIUM-1/2→코드 강제+음성 테스트)·code-review 6 정정(host_gate→gdelt 우회 회귀 등). **정직 경계**: 실 targeted near-match 후보 0(GDELT 429·RSS topic 불가·fixture=경로 입증)·실 reviewer/gold 0(synthetic label=경로 검증·production_gold 0)·embedding/LLM adjudicator 미구현(No-Go)·운영 DB/production 백로그 0 불변(완전종결=OVERCLAIM·운영 readiness≠실 gold actuality). **R-SourceOverlapScarcity·R-IdentityHumanLabeling·R-ReviewerAgreement·R-GoldSamplingBias·R-SemanticIdentityAdjudicator·R-LiveIdentityBacklog 부분진전**·종결 0·신규 0(total 36). ADR#59 커밋 `bf3ee6d` 위 ADR#60 미커밋(신규 2+docs 7+PROJECT_STATUS=10)·커밋 지시 대기·push 안 함._
