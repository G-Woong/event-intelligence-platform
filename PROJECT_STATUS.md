# 📊 프로젝트 진행 현황 (PROJECT_STATUS)

> 매 턴 끝에 새로 쓰입니다. 항상 "가장 최근 1턴"만. 과거는 `git log -- PROJECT_STATUS.md`.
> 사실 원본(자동): `.harness/machine_status.json` · 완료 증거: `.harness/closeout_stamp.json` · 서술: 이 파일(에이전트).

## 🟢 한눈에 (비개발자용 3줄)
- **지금 무엇을 했나:** 지난 턴(ADR#79 — near-match 0의 원인을 가르는 **recall probe**를 만들어 *합성*으로만 검증)을 안정 기준점으로 **커밋**(`7f3ef11`)한 뒤, 그 probe를 **실제 Guardian×NYT 라이브 기사쌍에 처음으로 적용**했습니다. 그리고 사용자 승인을 받아 **실 네트워크 라이브 쿼리 1회**를 돌렸습니다(SCOTUS "Supreme Court ruling" 1일치). 결과는 **정직하게**: 라이브로 모은 **100개 cross-source 비교쌍** 중 recall probe가 reviewer 검토 후보로 끌어올린 쌍은 **0개**(정규화 후 최고 유사도 0.1765 < 0.2 문턱). 즉 합성에서 통하던 레버가 **이 라이브 seed에는 일반화되지 않았습니다**. 이건 코드 실패가 아니라 **데이터 결과**입니다(`RESULT_CLASS: LIVE_OK`) — 다음 레버는 "LLM"이 아니라 **provider 다양화 / 더 좁은 같은-사건 seed / 한국어 소스**입니다. 자동 병합·LLM/embedding 실호출·운영 DB·키 값 출력·실제 전송·입력/라벨 날조·same-event 단정 모두 **0**.
- **이번 턴에 실제로 끝낸 것:** ADR#79 커밋(`7f3ef11`·20파일) → **ADR#80**: (1) recall probe를 **ACTUAL live cross-source pair에 적용하는 경로**를 3개 레이어에 추가(`cross_source_live_overlap_smoke`에 additive `emit_recall_probe`→title이 사는 곳에서 `summarize_recall_probe` 적용·body-free; `r1_targeted_live_acquisition`이 seed별 집계; `r1_discrete_event_acquisition`이 소비·**3분류**[`live_recall_lift_found`/`live_no_recall_lift`/`live_blocked_by_rate_or_opt_in`]·root cause refine). (2) internal ops에 live recall 필드 4개(aggregate only·per-pair score 미노출)·**dict==pydantic==TS 26==26==26**·필수 문구("Newly routed does not mean same event"·"Production gold remains 0 until human labels are returned"). (3) **실 bounded LIVE run**(user opt 2·1 seed·1d·Guardian/NYT·host_gate 준수·raw body 0·secret 0). **변경 표면 = backend 4 + backend test 4 + frontend 3 + docs 10 = 21파일**(신규 0·전부 additive 수정).
- **정직한 한계:** **실 라이브에서 recall probe가 lift 0**(100쌍·max 0.1765). 이는 (a) probe가 live에 적용됨을 입증하고 (b) **synthetic 성공이 이 live seed에 일반화되지 않음을 실측**(→ R-LiveRecallProbeGeneralization 증거화)하지만, **(i)/(ii)를 단정하진 않습니다**(comparison_pair=100>0이므로 overlap 부재는 아님 → (ii) different-events 또는 정규화 사전 한계·reviewer 라벨 필요). **production_gold_count 0**·실 production candidate freeze 0(`blocked_no_publishable_pairs`)·실 reviewer contact·실 returned labels 여전히 0(gate exact passthrough·`current_r1_gap=200`). embedding/LLM 실호출·자동 병합·운영 DB·public IU·실 전송 모두 **0**. 다음 hard blocker=**provider breadth**(GDELT cooldown·RSS 다출처·Naver KO wiring) + **더 좁은 same-event seed** → 실 reviewer contact → actual returned labels(R1). **push 금지·커밋 지시 대기**.

## 📋 자동 수집 사실 (machine_status.json)
- **git HEAD `7f3ef11`**(ADR#79·본 턴 첫 커밋·20파일·+1832/−42) 위 **ADR#80 = 미커밋**: 신규 0·수정 21(backend code 4: `tools/cross_source_live_overlap_smoke.py`·`tools/r1_targeted_live_acquisition.py`·`tools/r1_discrete_event_acquisition.py`·`schemas/internal_ops.py`; backend test 4: `tests/test_cross_source_live_overlap_smoke.py`·`tests/test_r1_targeted_live_acquisition.py`·`tests/test_r1_discrete_event_acquisition.py`·`tests/test_internal_ops_api.py`; frontend 3: `lib/api/types.ts`·`lib/ops/opsPilotExecutionView.ts`·`lib/ops/__tests__/opsPilotExecutionView.test.mjs`; docs 10: `5_REFERENCE/RAG_KG_AGENT_READINESS·INTELLIGENCE_UNIT_CONTRACT·RAG_KG_ENTITY_GATE_CONTRACT`·`2_ROADMAP/00·15·19`·`_CANONICAL/02`·`_DECISIONS/2026-06`·`_RISK/RISK_REGISTER`·`PROJECT_STATUS`) = **추적 21항목**. **신규 파일 0·전부 additive(기본 off 플래그·신규 output 필드)·migration 없음·ADR#79 커밋분 frozen·단방향 import·재구현 0·EVENT_SCHEMA/main.py/api/internal_ops.py/config/.env/.env.example/package.json/LLM_EVIDENCE_PACKET_CONTRACT 무변경.**
- 열린 RISK: **종결 0·신규 4**(R-LiveRecallProbeGeneralization[MEDIUM·실측 증거화]·R-DiscreteEventSeedBias[LOW]·R-AcquisitionFrontierPersistence[LOW]·R-KOAnalyzerDependency[LOW]). 부분진전: R-DeterministicNearMatchRecall·R-CrossSourceNearMatchGap·R-ProviderPairNarrowness·R-ProductionCandidateScarcity·R-NormalizationRecallLeakage·R-RagKgPrematureBuild·R-KoreanR1SourceGap·R-LiveAcquisitionRateBudget. throwaway 는 `.harness/_TRASH/`·`frontend/.harness/`·`--paths/`·`--help/`(stray·커밋 제외)·`outputs/`·`ingestion/outputs/state/`(gitignore·host_gate state)·scratchpad(저장소 밖).

## ✅ 이번 턴에 달성한 것 (live discrete-event run + recall probe를 LIVE pair에 적용 + 3분류 + provider breadth — ADR#80)
- **① ADR#79 커밋**: 20파일 → `7f3ef11`(지정 subject·secret PASS·docs_lifecycle conflicts 0·closeout SIGNATURE_MATCH 25==25·unresolved 0·push 0·NO UPSTREAM). ADR#80 안정 기준점.
- **② 원자 분석(§2·10문항·구현 전 보고·코드 6개 정독)**: live pair의 `title_left/right`는 `cross_source_live_overlap_smoke` 내부 `disc["candidate_pairs"]`에만 존재(smoke 출력엔 shared_tokens만)·`summarize_recall_probe(pairs)`가 그 구조와 **정확히 동일** → 최소 변경=**title이 사는 smoke 레이어 1곳**에 probe 적용·나머지 passthrough. host_gate 막힘=정직 block→3분류 `live_blocked`. lift→production freeze=reviewer-routing 후보 freeze(gold/merge 아님). lift 0→breadth vs detector는 `comparison_pair_count`로 가름. score는 internal `recall_probe_diagnostic`에만·internal ops는 max aggregate만.
- **③ 옵션 결정(§3·§4)**: **A(actual labels)+B(discrete-event live·승인 시)+C(live pair recall probe·핵심)+D(provider/KO)+E(frontier)+F(contract guard) 채택·G/H 금지.** per-turn live 승인 질의→**user 'Option 2 — bounded 실 네트워크 live run'** 선택(1 seed·1d·Guardian/NYT·host_gate·raw body 0·secret 0·flaky 명시·코드/provider/data 실패 분리).
- **④ C — recall probe를 LIVE pair에 적용(핵심)**: `cross_source_live_overlap_smoke`에 additive `emit_recall_probe`+`_build_recall_probe_diagnostic`(cross-source candidate_pairs에 `summarize_recall_probe`·body-free)→ `recall_probe_diagnostic`. `r1_targeted_live_acquisition`이 seed별 집계(`_aggregate_recall_probe_diagnostic`→`live_recall_probe_diagnostic`·max/newly-routed/entity 합). `r1_discrete_event_acquisition`이 소비·**`_classify_live_recall_lift` 3분류**(코드 실패 vs provider/data 실패 분리)·`refine_root_cause_with_recall_probe`가 live verdict 갱신. **merge_allowed=False·recall_probe_applies_to_merge=False·same_event 단정 0·per-pair score reviewer/public 미노출** 전 경로 불변.
- **⑤ 🔴 실 LIVE RUN(user 승인·측정·flaky 명시)**: seed=SCOTUS "Supreme Court ruling"·**1d**·Guardian+NYT·**2 calls**·host_gate(HostRateGate·guardian 5s/nyt 12s min-spacing·no-bypass)·raw body 0·secret 0. 결과 `RESULT_CLASS: LIVE_OK`(코드 정상): **comparison_pair_count=100**(publishable cross-source 비교쌍=same-event 매치 아님)·max_title_jaccard 0.1765·`near_match_gap_status=all_below_hard_floor`·**`live_recall_probe_applied=True`**·**`live_recall_lift_status=live_no_recall_lift`**·`max_live_recall_probe_score=0.1765`(<0.2 routing floor)·**`live_pairs_newly_routed_by_probe=0`**·sharing_entity 0. **핵심 발견: synthetic에서 작동한 (i) recall-miss 레버가 100개 실 SCOTUS 1d pair에서는 lift 0** → synthetic 성공이 이 live seed에 **일반화되지 않음**(실측). comparison_pair=100>0 → overlap 부재 아님·(ii) different-events 또는 정규화 사전 한계(reviewer 라벨 필요·**same-event 단정 0**). `production_candidate_status=blocked_no_publishable_pairs`·freeze 0·gold 0·gap 200·merge/llm/embedding/db 전부 False·exact "score" 키 0.
- **⑥ E/F — internal ops frontier + contract**: `InternalOpsDiscreteAcquisitionFrontier`에 live recall 4 필드 추가(`max_live_recall_probe_score`·`live_pairs_newly_routed_by_probe`·`live_recall_lift_status`·`live_frontier_verdict`·**aggregate only·per-pair score 미노출**)·**dict==pydantic==TS 26==26==26**·필수 문구 4개("Recall probe is reviewer-routing only, not merge"·"Newly routed does not mean same event"·"Production gold remains 0 until human labels are returned"·"R2~R7 remain No-Go"). read API live 0 고정(dry=blocked) 유지·flag off 404·503 no path leak.
- **⑦ G/H 경계**: embedding/LLM scorer runtime·production DB·semantic merge·public IU·GraphRAG runtime·KG edge 생성 미착수(이번 턴 금지).

## 🧭 cross-batch identity 4단계 + 평가/gold gate (정직)
- ① anchor 병합(ADR#40) → ② 후보 LINK(ADR#41) → ③ shadow 판정(ADR#42·…·discrete-event acquisition + recall probe ADR#79·**recall probe를 LIVE pair에 적용 + 3분류 ADR#80**) → ④ 실 병합(**미구현**).
- **ADR#80 위치 / 핵심**: ② deterministic near-match recall 한계의 (i) 가설을 메우는 recall probe를 **실 live cross-source pair에 처음 적용**. 실측 결과 100개 SCOTUS 1d pair에서 **lift 0**(synthetic 0.333 ≠ live 0.1765) → **레버가 이 live seed에 일반화 안 됨**(R-LiveRecallProbeGeneralization 증거화). near 는 reviewer 라우팅이지 merge가 아니므로 lift가 났어도 false-merge 없음(merge=fingerprint+gold+MERGE_GATE 불변). **same-event 단정 0.**
- **⚠ 미해소(OPEN):** near-match 0 원인 (i)/(ii)의 **실 분리**(live recall lift 0 → (ii)/normalization 쪽 신호이나 reviewer 라벨 없이 미단정)·**provider breadth**(n=2 한계·lift 0이 breadth 부족인지 정규화 한계인지)·실 production candidate freeze(0)·실 reviewer contact·첫 returned labels·실 gold(production_gold 0)·R1 gold floor(live 200·KO 50·pos/neg 67·hard-neg 20)·MERGE_GATE 런타임·KO 캘리브레이션(KO source 미배선)·운영 DB 0009 배포·RAG/KG/Entity/public IU(R1~R7)·internal ops per-user auth·UI 화면 render. "live recall lift 0 ≠ same/different event 단정·newly_routed ≠ same-event·routing recall ≠ merge·contract guard ≠ runtime·synthetic lift ≠ live lift" — 완전종결=OVERCLAIM.

## 🔎 source orchestration / 제품 계약
- official/news=publishable evidence(merge anchor)·community=반응(anchor 불가)·market=signal·catalog=entity·search=URL candidate(truth 아님)·unknown=fail-closed. discrete seed 검증·production candidate 는 publishable×publishable(official/article/news)만 anchor(약화 0). **near=reviewer 라우팅 gate≠merge gate → live recall probe는 false-merge 위험 없이 적용**(probe가 `cluster_records`/fingerprint 미호출·AST 잠금·live 경로도 동일).
- **제품 계약(raw≠public)**: 출력·`internal_ops_discrete_acquisition_frontier`(sanitized) 노출 표면에 score/rationale/predicted_status/raw body/secret/raw reviewer 신원·same_event truth **0** — 출력 전 `_assert_pii_safe` 재귀 가드(정확 키 매칭: `score` 금지·`recall_probe_score`/`max_recall_probe_score`/`max_live_recall_probe_score` 허용)·API `response_model=InternalOpsDiscreteAcquisitionFrontier`(sanitized·26 field)·frontend `FORBIDDEN_OPS_FIELDS` 재귀 가드. recall probe per-pair score 는 smoke의 internal `recall_probe_diagnostic`에만·internal ops는 **max aggregate + 3분류 status + newly-routed count**만. production_gold_count 0·gate **exact passthrough**·`merge_allowed=False`·embedding/LLM 실호출 0·DB write 0·`actual_sending_performed=False`. live query=**1 seed**(user opt 2·2 calls·host_gate·raw body 0·secret 0). Agent 는 recall probe·discrete acquisition·contract readiness 를 *계획* 가능하나 live lift 로 same_event 단정·merge·anchor 약화·KG edge 무검증 생성·score reviewer/public 노출·PII/secret 출력·실 전송 불가.

## ⚠️ 이번 턴 종결/갱신 RISK
- **신규 4(실 present blocker만)**: **R-LiveRecallProbeGeneralization**(MEDIUM·synthetic lift[0.333]이 live[0.1765·lift 0]에 **일반화되지 않음을 실측**·완화=3분류 정직 보고·next lever=breadth/narrower seed/KO·**종결 금지**) · **R-DiscreteEventSeedBias**(LOW·cherry-pick seed가 (i) 신호를 부풀릴 위험·완화=seed_source `code_proposed_shape` 표기·다seed·live verdict 정직) · **R-AcquisitionFrontierPersistence**(LOW·frontier status persistence scope creep·완화=body-free read-only·DB row 아님) · **R-KOAnalyzerDependency**(LOW·KO 형태소 분절 부재로 KO Jaccard 더 어려움·완화=KO 문서화·Naver wiring 다음 레버).
- **부분진전(종결 0)**: **R-DeterministicNearMatchRecall**(recall probe live 적용·**live lift 0 측정**·reviewer-routing only·false-merge AST 분리·**종결 금지[live에서 미작동·(i)/(ii) 미분리]**) · **R-CrossSourceNearMatchGap**(live recall probe로 데이터 진전·100쌍 lift 0=(ii)/normalization 쪽 신호이나 미단정·**종결 금지**) · **R-ProviderPairNarrowness**(n=2 lift 0→breadth 필요 신호·**종결 금지**) · **R-ProductionCandidateScarcity**/**R-NormalizationRecallLeakage**/**R-RagKgPrematureBuild**/**R-KoreanR1SourceGap**/**R-LiveAcquisitionRateBudget**(ADR#80 참조·**종결 금지**).

## ❌ 달성하지 못한 것 & 왜 (이월)
- **실 (i)/(ii) 분리 / 실 production candidate freeze / 실 gold**: live recall probe lift 0(100쌍) → (ii) different-events 또는 정규화 한계 신호이나 **reviewer 라벨 없이 단정 0**·publishable near-match pair 0이라 freeze 0. 해소=**provider breadth**(GDELT cooldown·RSS 다출처·Naver KO) + **더 좁은 same-event seed**(특정 단일 판결/결정 1d)로 entity-공유 routing band pair 확보 → (i)면 reviewer 후보·(ii)면 breadth.
- **adjudicator runtime / RAG·KG·Entity·public IU**: Lane F 는 contract guard(docs)일 뿐 runtime 미구현(`product_bridge_runtime_built=False`·embedding/LLM 실호출·실 병합·운영 DB·GraphRAG·public IU·KG edge 이번 턴 No-Go·옵션 G/H 금지).
- **UI 화면 render**: frontier view/types/test(데이터층)는 배선됐으나 read API가 `live_query=False` 고정이라 internal ops UI는 `live_blocked_by_rate_or_opt_in`(비-live)만 표시·page.tsx 화면 render는 후순위.

## 🚧 닫을 수 없는 문제 (BLOCKED/UNKNOWN)
- BLOCKED: 실 (i)/(ii) 분리 = **provider breadth + 더 좁은 same-event seed**로 entity-공유 routing band pair 확보(R-CrossSourceNearMatchGap·R-DeterministicNearMatchRecall·R-ProviderPairNarrowness). 실 production candidate = near-match publishable pair 확보(현재 0). 실 gold = 실 후보→reviewer recruitment→실제 contact→첫 returned label(R-ProductionCandidateScarcity). 실 병합 = gold-calibrated adjudicator(R3)+MERGE_GATE(R2). KO floor = Naver adapter 배선(R-KoreanR1SourceGap·R-KOAnalyzerDependency).
- UNKNOWN: **실 live below-floor 쌍이 (i) normalization 인지 (ii) different-events 인지**(live lift 0은 (ii)/사전한계 쪽이나 reviewer 라벨 없이 미단정). **더 좁은 단일-판결 seed가 lift를 낼지**(미측정·이번 seed는 "Supreme Court ruling"이라 100개가 서로 다른 판결/측면일 개연). ⚠ **validator 가 이 category-lean seed 를 `discrete_event_shape=True` 로 통과시켰으나 이는 "(ii) 격리됨"의 증거 아님**(adversarial MEDIUM-2·Lane B 전제 이번 run 미달성·차기 seed=named single event 필요). provider breadth(RSS fleet·Naver·GDELT)가 near-match 수율을 올릴지(미테스트). 실 reviewer 회수율/SLA(plan·machinery만).

## 👉 다음 할 일
1. **[커밋 대기]** 본 턴 ADR#80: 신규 0·수정 21(backend code 4·backend test 4·frontend 3·docs 10) 미커밋. 커밋 여부 지시 대기. push 금지. (`--paths/`·`--help/` stray 정리 허가도 함께 받으면 처리.)
2. **[다음 단계·frontier]** (a) **provider breadth**(key-free RSS 다출처 fleet·GDELT cooldown-honored·미배선 Naver/NewsAPI adapter wiring[KO floor]) — live lift 0이 breadth 부족인지 정규화 한계인지 가름. (b) **더 좁은 same-event seed**(특정 단일 판결/결정 1d)에 recall probe 적용해 entity-공유 routing band pair가 나오는지 측정. (c) **reviewer recruitment + actual contact + 첫 returned labels**(R1) → (d) gold denominator(live 200·KO 50·**R1**) → (e) MERGE_GATE(precision≥0.98·**R2**) → (f) embedding scorer opt-in(**R3**) → (g) entity/KG/GraphRAG(**R4~R6**) → (h) Agent/public IU(**R7**). 별도 축: internal ops UI 화면 render·per-user auth.
3. **[권장·bounded]** live acquisition 실험은 **bounded·opt-in·rate governed·seed 분산**일 때만(무단 대량 호출 0·no-bypass): 다음은 더 좁은 same-event seed + provider breadth 로 lift 재측정(source role guard 유지).

## 📁 근거 (이번 턴 핵심)
- 커밋: `7f3ef11`(ADR#79·20파일·+1832/−42·push 0·NO UPSTREAM). ADR#80 변경: 신규 0·수정 21(backend code 4: smoke/targeted/discrete/schema·backend test 4·frontend 3·docs 10). **ADR#79 커밋분 frozen·재구현 0·EVENT_SCHEMA/main.py/api/internal_ops.py/config/.env/.env.example/package.json/LLM_EVIDENCE_PACKET_CONTRACT 무변경·전부 additive.**
- **실 LIVE run(측정·flaky·user 승인 opt 2)**: seed=SCOTUS "Supreme Court ruling"·1d·Guardian+NYT·**2 calls·host_gate 준수**·`targeted_live_query_executed=True`·**comparison_pair_count=100**·max_title_jaccard 0.1765·`all_below_hard_floor`·**`live_recall_probe_applied=True`**·**`live_recall_lift_status=live_no_recall_lift`**·`max_live_recall_probe_score=0.1765`(<0.2)·**`live_pairs_newly_routed_by_probe=0`**·sharing_entity 0·`production_candidate_status=blocked_no_publishable_pairs`·`candidate_provenance=none`·gold 0·gap 200·merge/llm/embedding/db_write/same_event_truth_exposed/raw_source_body_exposed=False·exact "score" 키 0·`RESULT_CLASS: LIVE_OK`(코드 정상·"lift 0"은 data 결과). **flaky·증거무게 주의**: 다른 날/seed면 결과 다를 수 있음·이 100쌍 제목은 미보존(body-free)·**이 수치(100·0.1765)는 단일 실행 자기보고이며 committed snapshot 부재로 repo 재현·감사 불가**(adversarial MEDIUM-1·차기 live run 은 body-free 집계 스냅샷 커밋 권장).
- 검증(정직): 신규 backend 테스트 **smoke +4**(emit_recall_probe off-default·below-floor live pair lift·merge/same_event 0·body-free)+**targeted +4**(live 집계·opt-in off None·additive·pii-safe)+**discrete +6**(3분류 lift_found/no_lift/blocked·merge·gold 0·frontier aggregate only·pii-safe)+**API 갱신**(기존 whitelist 22→26 field·기존 테스트 갱신)=**+14**·**backend 전체 1549 passed/101 skipped/0 failed**(975.89s·ADR#79 1535+14)·ingestion 무변경·frontend **tsc 0**·**lint 0**(`next lint`)·node:test **49 passed**(ops view +3: 46→49)·ruff 실게이트(E/F/I/B/W−E501) **0**·secret scan **PASS**(credential present/missing boolean만·값 미열람)·docs_lifecycle conflicts **0**. dict==pydantic==TS **26==26==26**.
- 감사(closeout subagents): architecture/code/test/evidence/risk_closure/adversarial/discrete_event_acquisition/live_recall_probe/source_role_guard/no_merge/no_llm_embedding/internal_ops_boundary/reviewer_pii/secret_boundary/rag_kg_gate/product_output_contract review. cardinal(synthetic 성공을 live 성공으로 둔갑 금지·recall score를 same_event/merge로 사용 금지·MERGE_GATE 전 merge 금지·LLM/embedding 실호출 금지·community/market/catalog anchor 금지·raw body 저장 금지·`.env` 값 출력 금지·score/rationale/predicted_status/same_event reviewer/public 노출 금지·production_gold 임의 증가 금지·public IU 생성 금지·push 금지) 차단 확인.
- 문서: ADR#80(`_DECISIONS`)·RISK(`_RISK`·종결 0·신규 4·부분진전 8)·ADR#80 블록(`2_ROADMAP/15` descending)·ADR#80 마커(`2_ROADMAP/00`)·ADR#80 §19(`2_ROADMAP/19` ascending)·**§6b-R1g live recall probe**(`RAG_KG_AGENT_READINESS`)·§4 ADR#80 bullet(`IU_CONTRACT`)·recall probe live guard(`RAG_KG_ENTITY_GATE_CONTRACT` §6)·`_CANONICAL/02`(ADR#41~#80). **EVENT_SCHEMA 무변경**(ADR#80 DB-free).

---
_as_of: 2026-06-28 · ADR#80 live discrete-event run + recall probe를 LIVE pair에 적용 + 3분류 + provider breadth — ADR#79 커밋(`7f3ef11`) 후, synthetic으로만 검증됐던 recall probe를 **실 Guardian×NYT live cross-source pair에 처음 적용**(재구현 0·전부 additive): smoke `emit_recall_probe`(title이 사는 레이어에서 `summarize_recall_probe`·body-free)→targeted seed별 집계→discrete **3분류**(`live_recall_lift_found`/`live_no_recall_lift`/`live_blocked_by_rate_or_opt_in`)+root cause refine. internal ops live recall 4필드(aggregate only·dict==pydantic==TS 26==26==26)+필수 문구. **user 승인 실 LIVE run**(opt 2·1 seed SCOTUS "Supreme Court ruling"·1d·Guardian+NYT·2 calls·host_gate·raw body 0·secret 0): **100 cross-source pair·recall probe LIVE 적용·lift 0**(max_live 0.1765<0.2 floor)=**synthetic 성공이 live seed에 일반화 안 됨 실측**(R-LiveRecallProbeGeneralization 증거화)·comparison_pair 100>0→(ii)/정규화 한계 신호이나 **same-event 단정 0**·`RESULT_CLASS: LIVE_OK`(코드 정상). **측정**: backend 전체 1549p/101skip/0fail(1535+14)·frontend tsc0/lint0/test49(ops +3)·ruff 0·secret PASS·docs_lifecycle 0·dict==pydantic==TS 26. **정직 경계**: live recall lift 0 ≠ same/different event 단정·newly_routed ≠ same-event·synthetic lift ≠ live lift·production_gold 0·freeze 0·gate exact passthrough·adjudicator runtime/public IU/merge/운영 DB/embedding·LLM 실호출/실 전송 0·완전종결=OVERCLAIM. 다음 hard blocker=provider breadth(GDELT/RSS/Naver) + 더 좁은 same-event seed → 실 reviewer contact → actual returned labels(R1). **신규 R-LiveRecallProbeGeneralization(MEDIUM)·R-DiscreteEventSeedBias/R-AcquisitionFrontierPersistence/R-KOAnalyzerDependency(LOW)**·종결 0. ADR#79 커밋 `7f3ef11` 위 ADR#80 미커밋(수정 21·신규 0)·커밋 지시 대기·push 안 함._
