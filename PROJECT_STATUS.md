# 📊 프로젝트 진행 현황 (PROJECT_STATUS)

> 매 턴 끝에 새로 쓰입니다. 항상 "가장 최근 1턴"만. 과거는 `git log -- PROJECT_STATUS.md`.
> 사실 원본(자동): `.harness/machine_status.json` · 완료 증거: `.harness/closeout_stamp.json` · 서술: 이 파일(에이전트).

## 🟢 한눈에 (비개발자용 3줄)
- **지금 무엇을 했나:** 지난 턴(ADR#61 provider 가용성 게이트)을 안정 기준점으로 **커밋**(`ade3133`)한 뒤, ADR#61 이 정직하게 남긴 병목 — "**검색 가능한 수집처 10종이 분류는 되지만 실제로 데이터를 가져오는 코드(fetcher)는 gdelt/rss 둘뿐(나머지는 '미배선')**" — 를 한 단계 줄였습니다. 새 기능을 짓지 않고, **수집처 fetch 가 따라야 할 공통 계약(contract)**을 정하고 그 계약에 맞는 **첫 실 수집처 어댑터(Guardian 뉴스 API)**를 배선했습니다. 키가 없으면 정직하게 "키 필요", 키가 있으면 선택적 실 수집→같은-사건 후보 큐까지 연결됩니다(병합·LLM·DB쓰기 **0**).
- **이번 턴에 실제로 끝낸 것:** ADR#61 커밋(`ade3133`·10파일) → **ADR#62**: 새 모듈 `provider_query_adapters.py`(ProviderQueryAdapter/ProviderQueryResult **공통 계약** + **Guardian Content API 실 어댑터**·공식 응답 shape 파싱·본문 미저장·secret-safe) + `provider_readiness.py` 보강(guardian 을 배선 목록에 추가·**어댑터 전용 분기**로 직접 fetch→ADR#60 운영경로에 records 주입·real_fetch 보존). 키 없으면 `missing_credentials`(네트워크 전 차단), fake transport→records 4·near 2·큐 5·`live_derived`. 감사 2종(adversarial **SOLID·HIGH 0·blocking 0** + code-review)에서 받은 지적(max_records=0 falsy·secret 로깅 취약성)을 **즉시 코드로 정정**.
- **정직한 한계:** **실 API 키 미주입·fake transport 로만 검증** → **실 near-match 후보·사람 검수자·gold 여전히 0**. 어댑터 계약+1 배선은 "**할 수 있다(능력)**"이지 실 데이터가 아닙니다. 미배선 수집처 9종(sec_edgar/federal_register/nyt/newsapi/…) 잔여·embedding/LLM 판정기 No-Go·**운영 DB 무변경·production 백로그 0·자동 병합 0·본문 미저장·LLM 0** 불변. push 금지.

## 📋 자동 수집 사실 (machine_status.json)
- **git HEAD `ade3133`**(ADR#61, 본 턴 첫 커밋) 위 **ADR#62 = 미커밋**: 신규 2(`backend/app/tools/provider_query_adapters.py`[어댑터 계약+Guardian] + `backend/tests/test_provider_query_adapters.py`[22 테스트]) + 수정 11(`backend/app/tools/provider_readiness.py`·`backend/tests/test_provider_readiness.py`[+7]·`ingestion/configs/rate_limit_policy.yaml`[guardian per-source] + docs 7[`2_ROADMAP/00·15`·`_RISK/RISK_REGISTER`·`5_REFERENCE/RAG_KG_AGENT_READINESS·INTELLIGENCE_UNIT_CONTRACT`·`_CANONICAL/02`·`_DECISIONS/2026-06`] + `PROJECT_STATUS`) = **추적 13항목**. **migration 없음·ADR#60(`targeted_same_event_acquisition.py`)·ADR#57(`source_overlap_discovery.py`) 미수정(단방향 import·frozen·재구현 0).**
- 열린 RISK: **total 36**(HIGH 3·MEDIUM 21·LOW 11). **신규 0·종결 0.** 부분진전 6: R-SourceOverlapScarcity·R-RealSourceLoopUnproven·R-LiveIdentityBacklog·R-IdentityHumanLabeling·R-SemanticIdentityAdjudicator·R-Gdelt429. throwaway 는 `.harness/_TRASH/`·`frontend/.harness/`(커밋 제외).

## ✅ 이번 턴에 달성한 것 (provider query adapter contract + 첫 wired adapter[Guardian] — ADR#62)
- **① ADR#61 커밋**: 10파일 → `ade3133`(secret PASS 10·docs_lifecycle conflicts 0·SIGNATURE MATCH·push 0).
- **② 원자 분석(§2·20문항·3-agent 병렬)**: fetch_implemented=False 10종 inventory(registry/auth/env·기존 collector 0)·governance 원시(host_rate_gate generic decide·rate_limit_policy per-source default fallback·retry_on 에 RATE_LIMITED 부재)·credential seam(env_status secret-safe)·**ADR#60 동결 seam=`records=` 주입 경로**(provider= 직접전달 시 fixture silent 낙하=rss_fleet seam). **1순위=guardian**(단일 env var·topic+time query·평탄 안정 JSON·본문없이 canonical/title/published·key 없으면 clean missing_credentials·news=제품 정합). Guardian API shape 공식 문서 확정(anti-skeleton). 분석 먼저 보고.
- **③ A — provider fetcher contract**: `provider_query_adapters.py` — `ProviderQueryAdapter`(provider_id/query_capable/auth_required/required_env_vars/fetch_implemented/supports_*/max_records/rate_limit_policy_id/host/host_gate_key)·`ProviderQueryResult`(status/records/raw_body_stored=False/secret_exposed=False/provenance/block_reason/next_action)·`provider_adapter_contract`(§5 표면·forbidden=raw_body/secret/db_write/merge/public_IU/llm).
- **④ B — Guardian adapter wiring**: **실 Guardian Content API parser**(공식 shape `response.results[].webTitle/webUrl/webPublicationDate`·title≤512+canonical+published·**본문 미저장**)·`run_provider_query`(gate: fetcher_not_wired→missing_credentials[**network 전**]→rate_limited[in_cooldown]→host_gate_blocked→fetch·status 분류). guardian→`_FETCH_WIRED_PROVIDERS`·`fetch_implemented=True`(실 parser+fake-transport test 통과 시에만·stub 등재 금지).
- **⑤ C — GDELT cooldown hygiene 유지 + governed**: adapter 가 `in_cooldown`(진행 중이면 fetch 미시도)·`record_rate_limited`(429 시 cooldown 영속·tight retry 없음)·shared `HostRateGate`(generic decide·상수 편집 0·no-bypass) 상속. `rate_limit_policy.yaml` guardian per-source(min 5s·cooldown 600s·max_retries 1) 명시.
- **⑥ D — adapter→near-match queue**: **adapter-wired 분기**(`run_optional_live_query`)가 guardian 을 직접 fetch→ADR#60 `run_targeted_same_event_operating_readiness(records=,real_fetch=True)` 주입(provider= 직접전달 회피·`_SYNTHETIC_FIXTURE_MARKER` 부재→live_derived 보존·**fixture 둔갑 0**). fake transport→records 4·near 2·hard 3·queue_pop 5·dataset_source live_derived·predicted_status 숨김·production_gold 0·merge_allowed False. readiness 필드(adapter_contract_version/adapter_module/tested_with_fake_transport/queue_integration_status/live_query_capable_if_credentials_present/missing_credentials_next_action)+agent schema(provider_adapter_contract·wired_providers·provider_adapter_readiness_review).
- **⑦ E/F 금지 + secret hygiene**: embedding/LLM adjudicator `EMBEDDING_LLM_ADJUDICATOR_INTERFACE` **No-Go**(호출 0)·운영 DB 미접촉(session 인자 부재·write 0). secret-safe: `env_status` present/missing(값 0)·API key 는 실 network 경로(transport=None)에서만 `os.getenv`·**httpx params 전용**(url 항상 keyless·로그/result 미기록).

## 🧭 cross-batch identity 4단계 + 평가/gold gate (정직)
- ① anchor 병합(ADR#40) → ② 후보 LINK(ADR#41) → ③ shadow 판정(ADR#42·운영 배선 ADR#48~#53·preflight/smoke ADR#54·live-db ADR#55·replay ADR#56·overlap discovery ADR#57·acquisition ADR#58·near-match 큐 ADR#59·targeted acquisition readiness ADR#60·provider readiness gate ADR#61·**provider query adapter contract+Guardian wired ADR#62**) → ④ 실 병합(**미구현**).
- **ADR#62 위치**: 단계 ①(source fetch)의 **provider fetcher 배선 계층**. ADR#61 이 표면화한 `fetcher_not_wired` 병목(10종 중 gdelt/rss 만 배선)을 공통 contract+첫 실 adapter(Guardian)로 한 단계 축소 — 어댑터 records 는 ADR#60 운영경로(`records=` 주입)로 near-match queue 까지만 연결(같은 사건 단정·병합 0).
- **⚠ 미해소(OPEN):** 실 key 미주입·fake transport 만 검증 → 실 targeted near-match 후보·reviewer/gold 0·미배선 9종(sec_edgar/federal_register/nyt/newsapi/gnews/naver/serper/tavily/exa) 잔여·embedding/LLM 실 병합(No-Go)·운영 DB 0009 배포·MERGE_GATE·한국어 캘리브레이션. "adapter(능력) ≠ 실 후보(actuality)" — 완전종결=OVERCLAIM.

## 🔎 source orchestration / 제품 계약
- official/news=publishable evidence(merge anchor)·community=반응 evidence(anchor 불가)·market=signal·catalog=entity·search=URL·unknown=fail-closed. provider adapter 는 **same-event news/official 텍스트 provider 만** 대상(Guardian=news). 어댑터 records 는 publishable×publishable 만 reviewer 후보.
- **제품 계약(raw≠public)**: provider adapter 는 Intelligence Unit 입력(실 near-match 후보)을 만들 수집처 fetch 의 **공통 계약 + 첫 배선** — 본문 미저장(title+canonical+published 만)·public IU 미생성·실 후보가 와도 same-event 확정은 reviewer/gold/MERGE_GATE 밖에서 금지(`merge_allowed=False`·`no_public_intelligence_unit`·`llm_invoked=False`). Guardian 무료 tier full-content 재배포 금지 ToS 경계 내(본문 미저장·재배포 아님). Agent 는 provider adapter readiness/source-pair/queue population 을 *계획* 가능하나 provider secret 추측·merge·public IU 불가. LLM/Agent 진입 9조건 여전히 No-Go(1·4·5·7).

## ⚠️ 이번 턴 종결/갱신 RISK
- **부분진전(종결 0·신규 0·total 36 불변)**: **R-SourceOverlapScarcity**(provider adapter contract+guardian wired·fetcher_not_wired 병목 축소[10종 중 1 wired]·adapter→queue 통합) · **R-RealSourceLoopUnproven**(live query path wired-or-honestly-blocked·DB/merge 0) · **R-LiveIdentityBacklog**(adapter-derived 후보 queue 충원 경로 입증) · **R-IdentityHumanLabeling**(adapter records→packet·predicted_status 숨김·production gold 0 유지) · **R-SemanticIdentityAdjudicator**(embedding No-Go 고정·candidate scoring interface 만) · **R-Gdelt429**(adapter no-tight-retry·in_cooldown 상속·GDELT 단일 의존도 완화).
- 디렉티브 신규 RISK 후보(R-ProviderAdapterCoverage/R-ProviderParserContract/R-ProviderSecretHygiene)는 **독립 blocker 아님** → **R-SourceOverlapScarcity**(provider acquisition closure)·**R-Secret**(secret hygiene 이미 mitigated·env_status·httpx params·url keyless)에 포섭(RISK 남발 금지). adversarial 가 subsumption 정당·과대종결 0 확인.

## ❌ 달성하지 못한 것 & 왜 (이월)
- **실 targeted near-match 후보**: 실 API 키 미주입(GUARDIAN_API_KEY 등)·fake transport 로만 검증 → 실 후보 0. 미배선 9종 잔여(어댑터 계약은 확장 가능하나 이번 턴 1개만 배선). adapter=능력 표면화.
- **실 human-reviewed gold**: 실 후보가 없어 큐가 실 데이터로 안 채워짐 → reviewer 충원·gold 0(production_gold 0). adapter→queue path 는 fake transport 로 **능력** 입증.
- **embedding/LLM adjudicator**: gold calibration+MERGE_GATE 후에만(No-Go 인터페이스·호출 0). 운영 DB·production 백로그 0(옵션 F 금지).

## 🚧 닫을 수 없는 문제 (BLOCKED/UNKNOWN)
- BLOCKED: 실 targeted near-match = **실 credential 주입**(GUARDIAN_API_KEY 등·코드 아닌 secret/ops 작업) 또는 추가 provider fetcher 배선 선결. 실 reviewer/gold = reviewer 충원·운영 절차 선결. production 백로그 = 운영 DB 0009 배포(승인) + 실 fetch 볼륨.
- UNKNOWN: paraphrase overlap 의 실 병합 허용 기준(production precision·실 gold·reviewer 합의·한국어 캘리브레이션·MERGE_GATE) → R-SemanticIdentityAdjudicator·R-IdentityHumanLabeling·R-ReviewerAgreement·R-IdentityEvalDataset 선결.

## 👉 다음 할 일
1. **[커밋 대기]** 본 턴 ADR#62: 신규 2(provider_query_adapters.py+test) + 수정 11(provider_readiness.py·test·rate_limit_policy.yaml·docs 7·PROJECT_STATUS) = 13파일 커밋 여부 지시 대기. push 금지.
2. **[다음 단계]** (a) **실 credential 주입**(GUARDIAN_API_KEY 등) → guardian live query 가 실 records 생산 → 실 near-match 후보 → 큐 충원, 또는 추가 provider(nyt/newsapi/federal_register) 어댑터 배선 → (b) reviewer 충원·합의 gold + 한국어 캘리브레이션 → (c) embedding/LLM/KG **실 병합** adjudicator(MERGE_GATE·`semantic_score` seam) → (d) (승인 하) 운영 DB 0009 배포 + scheduler 가동 → RAG/KG.

## 📁 근거 (이번 턴 핵심)
- 커밋: `ade3133`(ADR#61). ADR#62 변경: 신규 `provider_query_adapters.py`(어댑터 계약+Guardian·375줄)+`test_provider_query_adapters.py`(22)·`provider_readiness.py` 보강(adapter-wired 분기+필드)·`test_provider_readiness.py`(+7)·`rate_limit_policy.yaml`(guardian)·docs 7. **ADR#60/#57 파일 미수정(단방향 import·frozen·재구현 0).**
- 검증(정직): guardian fake transport(wire/para/diff)→status ok·records 4·**live_derived**·near 2·hard 3·queue_pop 5·raw_body_stored False·secret_exposed False·request URL api-key 미노출·hidden_prediction True·production_gold 0·merge_allowed False. missing creds→transport 미호출·dataset_source None. 테스트: 신규 **29**(test_provider_query_adapters 22[contract 1-8·key-required 9-16·governance 17-21·max_records=0 엣지 22]+test_provider_readiness 46-52[+7])·backend 비-live **788 passed/4 skipped/0 failed**(759+29 additive·ADR#60/#57 파일 미수정)·live-PG **94 passed**(write-free·session 인자 부재·무영향)·ingestion **1353 passed**·frontend tsc**0**/node:test **12**/lint **0**·secret scan **PASS(12)**·docs_lifecycle conflicts **0**·ruff 실게이트(E/F/I/B/W−E501) **0**. 운영 DB 무변경.
- 감사: adversarial-reality-critic **SOLID·HIGH 0·blocking 0**(8 claim 전부 CONFIRMED: synthetic→live 봉인·secret-safe·rss_fleet seam 미재발·anti-skeleton parser·no merge/LLM/DB·RISK 과대종결 0·ToS 경계·governed; LOW 3 비차단[host_gate 주입 조건부·로깅 부재 의존·실 network 날짜 비결정성]) → **LOW-2 즉시 하드닝**(api-key httpx params 전용·url keyless). code-review(10 angle·max effort: 4 finder→triage) → **max_records=0 falsy 정정·test_12 content 강화**(나머지 REFUTED[out["queue"] 항상 존재] 또는 contract 확장성=directive 명시 요구 또는 circular-import 회피). 양 감사 핵심 안전축(adapter-wired records 주입 fixture 둔갑 0·credential secret-safe·blocked 시 transport 미호출·live_derived 봉인·production_gold 0·no merge/LLM/DB) CONFIRMED.
- 문서: ADR#62(`_DECISIONS`)·6 RISK 부분진전(`_RISK`·종결 0·신규 0)·provider adapter runbook+ADR#62 블록(`2_ROADMAP/15`)·(11t)(`2_ROADMAP/00`)·agent readiness/Agent 역할(`RAG_KG`)·provider adapter contract 계약(`IU_CONTRACT`)·`_CANONICAL/02`(ADR#41~#62 + provider acquisition).

---
_as_of: 2026-06-26 · ADR#62 provider query adapter contract + 첫 wired adapter(Guardian) — ADR#61 커밋(`ade3133`) 후, ADR#61 이 표면화한 `fetcher_not_wired` 병목(query-capable 10종 중 실 fetcher=gdelt/rss 만)을 한 단계 축소(새 파일 1·ADR#60/#57 frozen·단방향 import·재구현 0): 신규 `provider_query_adapters.py` — `ProviderQueryAdapter`/`ProviderQueryResult` 공통 contract(옵션 A) + **Guardian Content API 실 adapter**(옵션 B·key-required·news·공식 shape parser·본문 미저장·secret-safe[env_status·key httpx params·url keyless])·`run_provider_query`(gate fetcher_not_wired→missing_credentials[network 전]→rate_limited→host_gate_blocked→fetch·status 분류). `provider_readiness.py` 보강: guardian→`_FETCH_WIRED_PROVIDERS`·**adapter-wired 분기**(직접 fetch→ADR#60 `records=` 주입·real_fetch=True·fixture 둔갑 0·rss_fleet seam 회피)·adapter readiness 필드+agent schema(provider_adapter_contract·wired_providers). `rate_limit_policy.yaml` guardian per-source(min 5s·cooldown 600s·no tight retry·옵션 C). adapter→ADR#60 near-match queue(옵션 D). 옵션 **A+B+C+D 채택·E/F 금지·신규 RISK 0**. **측정**: 신규 29·backend 비-live 788p/4s/0f·live-PG 94p·ingestion 1353p·frontend tsc0/test12/lint0·secret PASS(12)·docs_lifecycle 0·ruff 실게이트 0. 감사: adversarial SOLID(HIGH 0·blocking 0·LOW 3 비차단→LOW-2 하드닝)+code-review(max_records=0 falsy 정정·test_12 강화). **정직 경계**: 실 key 미주입·fake transport 만→실 targeted near-match 후보·reviewer/gold 0(adapter=능력 표면화·실 데이터 아님)·미배선 9종 잔여·embedding/LLM adjudicator No-Go·운영 DB/production 백로그 0 불변(완전종결=OVERCLAIM). **R-SourceOverlapScarcity·R-RealSourceLoopUnproven·R-LiveIdentityBacklog·R-IdentityHumanLabeling·R-SemanticIdentityAdjudicator·R-Gdelt429 부분진전**·종결 0·신규 0(total 36). ADR#61 커밋 `ade3133` 위 ADR#62 미커밋(신규 2+수정 11=13)·커밋 지시 대기·push 안 함._
