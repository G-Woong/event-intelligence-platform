# 📊 프로젝트 진행 현황 (PROJECT_STATUS)

> 매 턴 끝에 새로 쓰입니다. 항상 "가장 최근 1턴"만. 과거는 `git log -- PROJECT_STATUS.md`.
> 사실 원본(자동): `.harness/machine_status.json` · 완료 증거: `.harness/closeout_stamp.json` · 서술: 이 파일(에이전트).

## 🟢 한눈에 (비개발자용 3줄)
- **지금 무엇을 했나:** 지난 턴(ADR#60)을 안정 기준점으로 **커밋**(`9bdd7f4`)한 뒤, ADR#60 이 정직하게 남긴 한계 — "**표현만 다른 같은 사건(near-match) 후보가 0인 직접 원인은 큐가 아니라 '검색 가능한 수집처(provider) 확보 실패'**" — 를 닫는 **provider 가용성 점검 게이트**를 만들었습니다. 새 기능을 짓지 않고 기존 수집·검수 기계를 재사용해, **어떤 수집처가 무료-검색가능/키필요/차단(429)/미배선인지**를 분류하고, **키·쿨다운·실 fetcher 배선**을 동시에 보고하며, 막히면 **무엇을 준비해야 하는지(next_action)**를 정직하게 냅니다(병합·LLM·DB쓰기 **0**).
- **이번 턴에 실제로 끝낸 것:** ADR#60 커밋(`9bdd7f4`) → **ADR#61**: 새 모듈 `provider_readiness.py` — 수집처를 `key_free_query`(gdelt/sec_edgar/federal_register)·`key_required_query`(newsapi/gnews/guardian/nyt/naver/serper/tavily/exa)·`key_free_non_query`(rss 함대)·`blocked_rate_limited`(gdelt 429)·`unknown`(fail-closed)으로 분류 + 자격증명(값 비노출·존재만)·host gate 쿨다운·**`fetch_implemented` 게이트**(실 fetcher 는 gdelt/rss 만 → 미배선 수집처는 키가 있어도 **fixture 둔갑 코드 차단**)·선택적 bounded live query(opt-in·안전+자격+배선 통과 시에만 실 수집→후보를 near-match 큐로). 감사 2종(adversarial **SOLID·HIGH 0** + code-review) **동일 MEDIUM 1건**(rss_fleet 명칭 이음새→fixture 낙하)을 **코드로 정정**(rss_fleet→rss 정규화·회귀 테스트 2).
- **정직한 한계:** **실제로 검색되어 작동하는 무료 수집처는 gdelt(=429 차단) 하나뿐** → **실 near-match 후보·사람 검수자·gold 여전히 0**. provider readiness 는 "**무엇이 막는지**"를 표면화하는 **능력**이지 실 데이터가 아닙니다. embedding/LLM 판정기 미구현(No-Go)·**운영 DB 무변경·production 백로그 0·자동 병합 0·본문 미저장·LLM 0** 불변. push 금지.

## 📋 자동 수집 사실 (machine_status.json)
- **git HEAD `9bdd7f4`**(ADR#60, 본 턴 첫 커밋) 위 **ADR#61 = 미커밋**: 신규 2(`backend/app/tools/provider_readiness.py`[게이트] + `backend/tests/test_provider_readiness.py`[43 테스트]) + docs 7(`2_ROADMAP/00·15`·`_RISK/RISK_REGISTER`·`5_REFERENCE/RAG_KG_AGENT_READINESS·INTELLIGENCE_UNIT_CONTRACT`·`_CANONICAL/02`·`_DECISIONS/2026-06`) + `PROJECT_STATUS` = **추적 10항목**. **migration 없음·파이썬 source 1 신규(provider_readiness.py)·ADR#60 파일(`targeted_same_event_acquisition.py`) 미수정(단방향 import·frozen).**
- 열린 RISK: **total 36**(HIGH 3·MEDIUM 21·LOW 11). **신규 0·종결 0.** 부분진전 6: R-SourceOverlapScarcity·R-RealSourceLoopUnproven·R-LiveIdentityBacklog·R-IdentityHumanLabeling·R-SemanticIdentityAdjudicator·R-Gdelt429. throwaway 는 `.harness/_TRASH/`·`frontend/.harness/`(커밋 제외).

## ✅ 이번 턴에 달성한 것 (query-capable provider acquisition readiness gate — ADR#61)
- **① ADR#60 커밋**: 10파일 → `9bdd7f4`(secret PASS 10·docs_lifecycle conflicts 0·SIGNATURE 검증[stamp stale-by-4=ADR#59 이미 커밋된 near_match/overlap 잔존 규명]·push 0).
- **② 원자 분석(§2·20문항·3-agent 병렬)**: provider inventory/credential·host-gate/429·live-smoke/queue 재사용 매핑. **핵심 발견**: query-capable key-free=gdelt(429)/sec_edgar/federal_register **3개뿐**·credential 체크 secret-safe 단일 출처=`env_loader.env_status`(present/missing·값 0)·`.env.example` query provider placeholder **이미 완비**(누락=X/Twitter disabled→불요)·실 fetcher=`run_targeted_acquisition` 에 **gdelt/rss 만 배선**·provenance 봉인=real_fetch 단일 boolean. 분석 먼저 보고.
- **③ A — provider readiness gate**: `build_provider_readiness_report`(§5·5분류 + credential[env_status]·host_gate cooldown[gdelt_provider_status]·rate_limit_policy·next_action·buckets[query_capable/key_free_ready/key_required_missing/blocked/unknown]). **재구현 0**(무거운 일은 env_loader/host_rate_gate/rate_limit_policy/gdelt_provider_status 재사용).
- **④ honest `fetch_implemented` 정밀화**: 실 fetcher=gdelt/rss 만 → sec_edgar/federal_register/news API 는 credential-ready 여도 `fetcher_not_wired`(opt-in live query 시 **fixture 둔갑 코드 차단**·dataset_source None). newsapi+키 present 여도 silent fixture 낙하 0(음성 테스트 43 잠금).
- **⑤ B+D — optional live query + queue population**: `run_optional_live_query`(opt-in flag+safe+credential+fetcher gated→ADR#60 `run_targeted_same_event_operating_readiness` 재사용·live_derived 후보→near-match queue·실패 분류 missing_credentials/provider_rate_limited/host_gate_blocked/fetcher_not_wired/no_records/no_near_match). injected GDELT transport→candidate 5·queue_pop 5·**dataset_source live_derived**·production_gold 0.
- **⑥ C — runbook/env + §4/§9**: `.env.example` placeholder **이미 완비**(변경 0)·docs runbook(provider readiness 분류·next_action)·`run_provider_acquisition_readiness`(§4 통합 단일 dict)·`build_provider_readiness_agent_schema`(§9·agent_can_plan/agent_cannot[provider secret 추측·같은 사건 확정·merge·public IU·community/market anchor 금지]·no_secret_fabrication·embedding No-Go).
- **⑦ E/F 금지 + 감사 정정**: embedding/LLM adjudicator `EMBEDDING_LLM_ADJUDICATOR_INTERFACE` **No-Go**(호출 0)·운영 DB 미접촉(session 인자 부재·write 0). adversarial **SOLID·HIGH 0** + code-review **동일 MEDIUM 1**(rss_fleet 명칭 이음새: `run_optional_live_query` 가 'rss_fleet' 을 ADR#60['rss' 만 인식]에 미정규화→fixture silent 낙하; 봉인 자체는 유지[dataset_source 정직 synthetic]) → **정정**: `_ACQ_PROVIDER_ALIAS{rss_fleet→rss}`+source_ids 를 `_DEFAULT_TARGET_SOURCES` 단일 출처로+회귀 2(44·45)·cleanup 4(and/or→조건식·`readiness or`→`is not None`·blocked 세분류 enum 동치·query_capability 큐레이트 선언 주석).

## 🧭 cross-batch identity 4단계 + 평가/gold gate (정직)
- ① anchor 병합(ADR#40) → ② 후보 LINK(ADR#41) → ③ shadow 판정(ADR#42·운영 배선 ADR#48~#53·preflight/smoke ADR#54·live-db ADR#55·replay ADR#56·overlap discovery ADR#57·acquisition ADR#58·near-match 큐 ADR#59·targeted acquisition readiness ADR#60·**provider readiness gate ADR#61**) → ④ 실 병합(**미구현**).
- **ADR#61 위치**: 단계 ①(source fetch)의 **provider-측 게이트**. ADR#60 "다음 게이트=query-capable provider" 를 운영 readiness 로 물질화 — provider 가용성 분류 + 자격/쿨다운/fetcher 배선 동시 보고 + opt-in live query(준비 시 live_derived 후보→큐). 즉 실 후보 0 의 원인을 credential/cooldown/wiring 으로 정확 귀속.
- **⚠ 미해소(OPEN):** 실 wired+working key-free provider=**gdelt[429]만**(sec_edgar/federal_register=non-news·미배선·news API=key+미배선) → 실 targeted near-match 후보·reviewer/gold 0·embedding/LLM 실 병합(No-Go)·운영 DB 0009 배포·MERGE_GATE·한국어 캘리브레이션. "provider readiness(능력) ≠ 실 후보(actuality)" — 완전종결=OVERCLAIM.

## 🔎 source orchestration / 제품 계약
- official/news=publishable evidence(merge anchor)·community=반응 evidence(anchor 불가)·market=signal·catalog=entity·search=URL·unknown=fail-closed. provider readiness catalog 는 **same-event news/official 텍스트 provider 만** 포함(market/domain/catalog/community 의도적 제외=enrichment·anchor 아님). secret 값 0(env var 이름·present/missing boolean 만·`env_status` secret-safe·테스트로 잠금).
- **제품 계약(raw≠public)**: provider readiness 는 Intelligence Unit 입력(실 near-match 후보)을 채울 수집처가 **무엇이 막는지** 표면화하는 게이트 — 실 후보가 와도 same-event 확정은 reviewer/gold/MERGE_GATE 밖에서 금지(`merge_allowed=False`·`no_public_intelligence_unit`·`llm_invoked=False`). Agent 는 provider readiness/source-pair/queue population 을 *계획* 가능하나 provider secret 추측·merge·public IU 불가. LLM/Agent 진입 9조건 여전히 No-Go(1·4·5·7).

## ⚠️ 이번 턴 종결/갱신 RISK
- **부분진전(종결 0·신규 0·total 36 불변)**: **R-SourceOverlapScarcity**(coarse matrix→registry/env/host-gate 구동 readiness gate·fetch_implemented honest 차단) · **R-RealSourceLoopUnproven**(live query path ready-or-honestly-blocked·DB/merge 0) · **R-LiveIdentityBacklog**(backlog 충원 source 다음 게이트 정량화) · **R-IdentityHumanLabeling**(live_derived queue path 입증·predicted_status 숨김) · **R-SemanticIdentityAdjudicator**(agent schema embedding No-Go 고정) · **R-Gdelt429**(readiness/cooldown 표면화·acquisition 진입점 no-tight-retry).
- 디렉티브 신규 RISK 후보(R-ProviderCredentialReadiness/R-LiveQueryProviderCoverage/R-ProviderSecretHygiene)는 **독립 blocker 아님** → **R-SourceOverlapScarcity**(provider acquisition closure 가 "해당 provider 의 실 fetcher 배선"을 명시 흡수)·**R-Secret**(secret hygiene 이미 mitigated·env_status secret-safe 확인)에 포섭(RISK 남발 금지). adversarial 가 subsumption 정당·과대종결 0 확인.

## ❌ 달성하지 못한 것 & 왜 (이월)
- **실 targeted near-match 후보**: wired+working key-free query provider=**gdelt(429)만**·sec_edgar/federal_register=공식 register(non-news·동일사건 cross-source 약함)·미배선·news API=key+미배선 → 실 후보 0. readiness gate 는 무엇이 막는지(credential/cooldown/wiring) 정확히 표면화할 뿐.
- **실 human-reviewed gold**: provider 가 막혀 큐가 실 후보로 안 채워짐 → reviewer 충원·gold 0(production_gold 0). live_derived queue path 는 injected transport 로 **능력** 입증.
- **embedding/LLM adjudicator**: gold calibration+MERGE_GATE 후에만(No-Go 인터페이스·호출 0). 운영 DB·production 백로그 0(옵션 F 금지).

## 🚧 닫을 수 없는 문제 (BLOCKED/UNKNOWN)
- BLOCKED: 실 targeted near-match = **provider credential/cooldown/fetcher 배선**(gdelt 429 우회[cooldown 경과] 또는 key-required key 설정+fetcher 배선 또는 sec_edgar/federal_register fetcher 배선) 선결. 실 reviewer/gold = reviewer 충원·운영 절차 선결. production 백로그 = 운영 DB 0009 배포(승인) + 실 fetch 볼륨.
- UNKNOWN: paraphrase overlap 의 실 병합 허용 기준(production precision·실 gold·reviewer 합의·한국어 캘리브레이션·MERGE_GATE) → R-SemanticIdentityAdjudicator·R-IdentityHumanLabeling·R-ReviewerAgreement·R-IdentityEvalDataset 선결.

## 👉 다음 할 일
1. **[커밋 대기]** 본 턴 ADR#61: 신규 2(provider_readiness.py+test) + docs 7 + PROJECT_STATUS = 10파일 커밋 여부 지시 대기. push 금지.
2. **[다음 단계]** (a) **provider 확보**(gdelt 429 우회[cooldown] 또는 key-required key 설정 + 해당 provider fetcher 배선[ADR#61 fetch_implemented gate 가 미배선 표면화]) → 실 targeted near-match 후보 → 큐 충원 → (b) reviewer 충원·합의 gold + 한국어 캘리브레이션 → (c) embedding/LLM/KG **실 병합** adjudicator(MERGE_GATE·`semantic_score` seam) → (d) (승인 하) 운영 DB 0009 배포 + scheduler 가동 → RAG/KG.

## 📁 근거 (이번 턴 핵심)
- 커밋: `9bdd7f4`(ADR#60). ADR#61 변경: 신규 `provider_readiness.py`(게이트·639줄)+`test_provider_readiness.py`(43)·docs 7. **ADR#60 파일 미수정(단방향 import·frozen·재구현 0).**
- 검증(정직): readiness — query_capable 11·key_free_ready=[gdelt]만(sec_edgar/federal_register=fetcher_not_wired)·key_required_missing 8·gdelt cooldown→blocked. injected GDELT transport→candidate 5(near 2·hard 3)·fingerprint 1·queue_pop 5·**dataset_source live_derived**·packet_exportable True·predicted_status 숨김·production_gold 0·merge_allowed False. 테스트: 신규 **43**(§10 1-43+rss_fleet 회귀 44-45)·backend 비-live **759 passed/4 skipped/0 failed**(716+43 additive·ADR#60 파일 미수정)·live-PG **94 passed**(write-free·session 인자 부재·무영향)·ingestion **1353 passed**·frontend tsc**0**/node:test **12**/lint **0**·secret scan **PASS(9)**·docs_lifecycle conflicts **0**·ruff 실게이트(E/F/I/B/W−E501 config ignore) **0**(I001/F401 자동수정·UP045/UP035 noise=sibling 일관 Optional 스타일·비게이트). 운영 DB 무변경.
- 감사: adversarial-reality-critic **SOLID·HIGH 0**(synthetic→live 누출·secret 노출·미전달 진전 RISK 종결 셋 다 없음·6개 RISK 부분진전만·subsumption 정당) / code-review(10 angle·max effort: **두 감사 동일 MEDIUM 1=rss_fleet 명칭 이음새**[→fixture silent 낙하·봉인 유지이나 "정직 차단" 자기주장 거짓]→정정·cleanup 4 정정·CLAUDE.md 위반 0[secret 값 노출 경로 0 확인]). 양 감사 모두 핵심 안전축(fetch_implemented fixture 차단·credential secret-safe·blocked GDELT transport 미호출·live_derived 봉인·production_gold 0·no merge/LLM/DB) CONFIRMED.
- 문서: ADR#61(`_DECISIONS`)·R-SourceOverlapScarcity/R-RealSourceLoopUnproven/R-LiveIdentityBacklog/R-IdentityHumanLabeling/R-SemanticIdentityAdjudicator/R-Gdelt429 부분진전(`_RISK`·종결 0·신규 0)·provider readiness gate 서브섹션+RealSourceLoop 헤더(`2_ROADMAP/15`)·(11s)(`2_ROADMAP/00`)·agent readiness 조건 4/Agent 역할(`RAG_KG`)·provider readiness gate 계약(`IU_CONTRACT`)·`_CANONICAL/02`(ADR#41~#61).

---
_as_of: 2026-06-25 · ADR#61 query-capable provider acquisition readiness gate — ADR#60 커밋(`9bdd7f4`) 후, ADR#60 이 남긴 "실 near-match 후보 0 의 직접 원인=query-capable provider acquisition failure" 를 닫는 provider 가용성 게이트(새 파일 1·ADR#60 frozen·단방향 import·재구현 0): 신규 `provider_readiness.py` — `build_provider_readiness_report`(key_free_query[gdelt/sec_edgar/federal_register]·key_required_query[newsapi/gnews/guardian/nyt/naver/serper/tavily/exa]·key_free_non_query[rss_fleet]·blocked_rate_limited[gdelt 429]·unknown 분류+credential[env_status secret-safe present/missing]·host_gate cooldown[gdelt_provider_status]·rate_limit_policy·next_action)·**honest `fetch_implemented` gate**(실 fetcher=gdelt/rss 만→미배선 provider 는 credential-ready 여도 fixture 둔갑 코드 차단·dataset_source None)·`run_optional_live_query`(opt-in+safe+credential+fetcher gated→ADR#60 운영 경로 재사용·live_derived 후보→near-match queue·실패 분류)·`run_provider_acquisition_readiness`(§4 통합)·`build_provider_readiness_agent_schema`(no_secret_fabrication·embedding No-Go)·CLI(network 0·--live-query opt-in). 옵션 **A+B+C+D 채택·E/F 금지·신규 RISK 0**. **측정**: 신규 43·backend 비-live 759p/4s/0f·live-PG 94p·ingestion 1353p·frontend tsc0/test12/lint0·secret PASS(9)·docs_lifecycle 0·ruff 실게이트 0. 감사: adversarial SOLID(HIGH 0)+code-review 동일 MEDIUM 1(rss_fleet 명칭 이음새→fixture silent 낙하)→정정(rss_fleet→rss 정규화·source_ids 단일 출처·회귀 2). **정직 경계**: 실 wired+working key-free provider=gdelt[429]만→실 targeted near-match 후보·reviewer/gold 0(provider readiness=능력 표면화·실 데이터 아님)·embedding/LLM adjudicator No-Go·운영 DB/production 백로그 0 불변(완전종결=OVERCLAIM). **R-SourceOverlapScarcity·R-RealSourceLoopUnproven·R-LiveIdentityBacklog·R-IdentityHumanLabeling·R-SemanticIdentityAdjudicator·R-Gdelt429 부분진전**·종결 0·신규 0(total 36). ADR#60 커밋 `9bdd7f4` 위 ADR#61 미커밋(신규 2+docs 7+PROJECT_STATUS=10)·커밋 지시 대기·push 안 함._
