# 📊 프로젝트 진행 현황 (PROJECT_STATUS)

> 매 턴 끝에 새로 쓰입니다. 항상 "가장 최근 1턴"만. 과거는 `git log -- PROJECT_STATUS.md`.
> 사실 원본(자동): `.harness/machine_status.json` · 완료 증거: `.harness/closeout_stamp.json` · 서술: 이 파일(에이전트).

## 🟢 한눈에 (비개발자용 3줄)
- **지금 무엇을 했나:** 지난 턴(ADR#85)이 **"window-honoring source 가 spec 만 있고 미배선·official×news 정책 미설계"**로 남긴 숙제를, 이번 턴(ADR#86)에 **실제로 배선·검증**했습니다. (1) **Federal Register**(미국 연방관보·공식 출처)를 `run_provider_query` 에 **key-free official adapter 로 실배선**, (2) **bounded·key-free 라이브 호출로 date filter 가 실제 작동함을 실측** — operator window [06-25,06-26] 에서 "enforcement" 25건 **전부 in-window**(`live_verified`)·"asylum metering" 은 31건→필터 적용 0건 *(실 수치는 gitignored snapshot 에만·repo 비재현·R-LiveRunReproducibility)*. **이는 Guardian/NYT 와 정반대**(ADR#84/#85: 날짜 파라미터 무시·out-of-window 반환)이며, ADR#85 의 `date_filter_ignored`(news 측) 가설을 **보강**합니다(단 FR=규제문서 도메인 ≠ news 도메인이라 직접 증명 아닌 보강 신호). (3) **official×news role-bridge** 정책 모듈 구축 — 공식 문서와 news 기사를 같은 article 로 섞지 않고, date 근접+entity/action 토큰 공유 시에만 **reviewer-routing 후보**(같은 사건 단정·병합 아님). **production candidate freeze·gold·자동 병합·LLM/embedding 실호출·public IU·실 전송 모두 0.**
- **이번 턴에 실제로 끝낸 것:** (1) **FR adapter**(`provider_query_adapters.py`·key-free·official_record role·`_federal_register_url`/`parse_federal_register_items`·`ALL_ADAPTER_PROVIDERS`/`ADAPTER_WIRED_PROVIDERS` 2-set 분리로 guardian×nyt **byte-보존**). (2) **official×news role-bridge**(`official_news_role_bridge.py`·PURE·title-Jaccard 미사용·reviewer-routing only·same_event/merge/kg-edge/score 0·9 test). (3) **FR live smoke**(`federal_register_live_smoke.py`·§10 status·`live_verified`/`live_weak` 분류·7 test). (4) orchestrator/snapshot/frontier **+8 필드** **dict==pydantic==TS==mjs 46==46==46==46**·required_copy +2. (5) **실 FR live smoke 실행**(≤4 key-free governed 호출) → `live_verified`·**parser 버그 발견+수정**(FR 은 count=0 시 results 키 생략 → no_records 로 정정·test_47). **변경 표면 = 신규 4(tool 2+test 2)+수정 backend 9+frontend 3+docs 7 ≈ 23파일**(strays 제외·전부 additive·byte-보존).
- **정직한 한계:** **adapter 배선·date-honoring live_verified 는 확정이나 production candidate 확보는 아님** — official×news in-window **same-event pair 0**(FR=regulatory enforcement 문서 ≠ SCOTUS asylum-metering news subject·도메인 불일치·R-OfficialNewsDomainMismatch). **production candidate freeze 0**·reviewer_handoff_ready=False·actual returned labels 0·**production_gold_count 0**(`current_r1_gap=200`). **adapter wired ≠ same-event 후보 확보**·**live_verified ≠ production candidate**·**bridge candidate ≠ truth**. 자동 병합·운영 DB·public IU·실 전송·GraphRAG/KG runtime·embedding/LLM 실호출 모두 **0**. 다음 hard blocker = **official 문서+news 보도가 같은 subject·같은 window 인 regulatory-class event** → official×news live-derived freeze → 실 reviewer contact → **actual returned human labels**(R1). **push 금지·커밋 지시 대기**.

## 📋 자동 수집 사실 (machine_status.json)
- **git HEAD `049a716`**(ADR#85·직전 턴 커밋) 위 **ADR#86 = 미커밋**: 신규 4(backend tool 2: `tools/official_news_role_bridge.py`·`tools/federal_register_live_smoke.py`; backend test 2: `tests/test_official_news_role_bridge.py`·`tests/test_federal_register_live_smoke.py`)·수정 약 19(backend 9: `tools/provider_query_adapters.py`·`tools/sanitized_live_snapshot.py`·`tools/r1_bounded_live_breadth_run.py`·`tools/window_honoring_source_readiness.py`·`schemas/internal_ops.py`·`tests/test_provider_query_adapters.py`·`tests/test_r1_bounded_live_breadth_run.py`·`tests/test_internal_ops_api.py`·`tests/test_window_honoring_source_readiness.py`; frontend 3: `lib/api/types.ts`·`lib/ops/opsPilotExecutionView.ts`·`lib/ops/__tests__/opsPilotExecutionView.test.mjs`; docs 7: `PROJECT_STATUS`·`_DECISIONS/2026-06`·`_RISK/RISK_REGISTER`·`2_ROADMAP/00·15·19`·`_CANONICAL/02`). **전부 additive(FR adapter=신규 provider key·guardian×nyt URL byte-identical·2-set 분리로 news-pairing pool 무변경·신규 모듈 network 0/PURE·frontier 38필드 무변경+8 추가·migration 0)·EVENT_SCHEMA/main.py/config/.env/.env.example/package.json/cross_source_live_overlap_smoke.py/live_query_target.py/reviewer_handoff_bridge.py 무변경.**
- 열린 RISK: **종결 0·신규 1**(R-OfficialNewsDomainMismatch[MEDIUM]). 부분진전: R-WindowHonoringSourceScarcity(FR adapter wired·date filter live_verified→MEDIUM→부분완화)·R-ProviderDateWindowFidelity(window-honoring control 로 date_filter_ignored 보강)·R-ProductionCandidateScarcity·R-LiveRunReproducibility. throwaway/stray(`.harness/_TRASH/`·`frontend/.harness/`·`--paths/`·`--help/`)·`outputs/`(gitignore)·scratchpad(저장소 밖)는 커밋 제외.

## ✅ 이번 턴에 달성한 것 (Federal Register window-honoring adapter 실배선 + official×news role-bridge + FR live date-honoring 검증 — ADR#86)
- **① FR adapter 실배선(§7·§8)**: `provider_query_adapters.py` 에 `FEDERAL_REGISTER_ADAPTER`(key-free·auth_required=False·official_record role·authority 5·anchor-eligible)·`_federal_register_url`(conditions[term]+conditions[publication_date][gte/lte]+per_page+order+fields[])·`parse_federal_register_items`(results[].title/html_url/publication_date·abstract/본문 미저장). **byte-보존 핵심**: `ALL_ADAPTER_PROVIDERS`(dispatch 전체)/`ADAPTER_WIRED_PROVIDERS`(news-pairing·official 제외) **2-set 분리** → guardian×nyt URL·news pool·executor second_provider 선택 무변경(test_provider_query_adapters +8).
- **② official×news role-bridge(§9)**: 신규 `official_news_role_bridge.py`(PURE·deterministic) — official(FR) × news(guardian/nyt) 중 **date 근접 + entity/action token 공유**(title-Jaccard 미사용)면 `bridge_candidate`(reviewer-routing only). `same_event_asserted/merge_allowed/kg_edge_allowed/public_iu_allowed=False`·**단일 score 미생성**(feature 만)·freeze_eligible 은 양측 in-window 일 때만. official 단독 production candidate 금지. 9 test(role guard·low-overlap reject·in-window freeze gate).
- **③ FR live smoke 실행(§10·실 network)**: 신규 `federal_register_live_smoke.py`(key-free·governed·raw body 0)·≤4 governed 호출. **결과 `live_verified`**: operator window [06-25,06-26] "enforcement" **25/25 in-window·out-of-window 0**·"asylum metering" 31→0(필터 제약). **Guardian/NYT 와 정반대**→date_filter_ignored(news 측) 보강(FR=regulatory 도메인이라 보강 신호). **parser 버그 발견+수정**(FR count=0 시 results 키 생략→no_records·test_47).
- **④ window-honoring readiness→wired(§9)**: `window_honoring_source_readiness.py` `_wired_providers()` 를 `ALL_ADAPTER_PROVIDERS` 기준으로 전환 → FR `adapter_status`=**wired**·`adapter_wired_this_turn`=**True**(ADR#85 spec-only→배선). 단 `date_filter_confidence` 는 여전히 **documented_unverified**(wired ≠ live date-honoring 검증·live_verified 는 live smoke 가 별도 surface·readiness 는 PURE).
- **⑤ orchestrator/snapshot/frontier +8(§12·§13)**: `InternalOpsDatePinnedLiveRunFrontier` **+8 필드**(`federal_register_adapter_status`·`federal_register_live_status`·`federal_register_date_filter_capability`·`official_news_bridge_status`·`official_records_count`·`news_records_count`·`bridge_candidate_count`·`official_news_freeze_eligible_count`) **dict==pydantic==TS==mjs 46==46==46==46**·API allowed 46·required_copy +2("Federal Register is official evidence, not a news article"·"Official-news bridge is reviewer-routing only"). snapshot `window_honoring_source` block(count/status aggregate·official_records/title/url/score 제외).
- **⑥ freeze 재시도(§11·정직)**: official×news in-window **same-event pair 0**(FR=regulatory enforcement 문서 ≠ news subject·도메인 불일치·R-OfficialNewsDomainMismatch)→bridge_candidate 0→**production candidate freeze 0**·candidate_provenance=none·reviewer_handoff_ready=False·gold 0·gap 200. blocked_reason 세분화(no_official_news_bridge_candidate / bridge_candidates_not_in_window).
- **⑦ 게이트 검증 + 리뷰 fix**: backend **1732p**/101skip/0fail·frontend tsc0/lint0/node:test(ops view) 78·secret PASS(13)·ruff 0·docs_lifecycle 0·dict==pydantic==TS==mjs 46. adversarial **NO CARDINAL-VIOLATION**(9/9 cardinal VALID)·code-review **LGTM-WITH-NITS**. 비차단 finding 처리: H-1(이 status 잔존 블록 정정)·NIT-1(FR live smoke host_gate_blocked→rate_blocked·call_count 0·둔갑 금지)·NIT-2(max_records 전달)·NIT-3(bridge CLI window 길이 가드)·L-2(미저장 abstract 미요청)·L-5(parser count==0 정확화)·M-1/L-1(live_verified inline caveat·confidence high→documented_unverified).

## 🧭 cross-batch identity 4단계 + 평가/gold gate (정직)
- ① anchor 병합(ADR#40) → ② 후보 LINK(ADR#41) → ③ shadow 판정(ADR#42·…·첫 실 date-pinned live run+date-window fidelity 보정 ADR#84·date-window fidelity 통제실험 메커니즘 분해 ADR#85·**Federal Register window-honoring adapter 실배선+official×news role-bridge ADR#86**) → ④ 실 병합(**미구현**).
- **ADR#86 위치 / 핵심**: ADR#85 가 남긴 미배선 hedge(FR=spec only)·official×news 미설계를 실배선·검증한 턴 — FR adapter(key-free·official) 배선 + bounded live smoke 로 **date filter live_verified**(enforcement 25/25 in-window·Guardian/NYT date_filter_ignored 와 정반대)로 ADR#85 news-측 가설 보강 + official×news role-bridge(reviewer-routing only·title-Jaccard 미사용) 구축. bridge=같은 사건 단정이 아니라 reviewer worklist 후보(merge=fingerprint+gold+MERGE_GATE 불변). **same-event 단정 0·freeze 0·gold 0.**
- **⚠ 미해소(OPEN):** **official×news in-window same-event 동시 보도가 실재하는 regulatory-class event**(FR=regulatory 도메인 ≠ news subject 라 bridge 수율 0·R-OfficialNewsDomainMismatch)·date_filter_ignored vs zero_coverage news-측 **최종 분리**(FR=보강 신호이지 news 도메인 직접 증명 아님)·실 production candidate freeze(0)·실 reviewer contact·첫 returned labels·실 gold(production_gold 0)·R1 gold floor(live 200·KO 50·pos/neg 67·hard-neg 20)·MERGE_GATE 런타임·KO 캘리브레이션(KO gold 0)·운영 DB 0009 배포·RAG/KG/Entity/public IU(R1~R7)·internal ops per-user auth·UI 화면 render. "adapter wired ≠ same-event 후보 확보·live_verified ≠ production candidate·bridge candidate ≠ truth·date-pin ≠ 발생 증명·freeze ≠ truth·routing recall ≠ merge" — 완전종결=OVERCLAIM.

## 🔎 source orchestration / 제품 계약
- official/news=publishable evidence(merge anchor)·community=반응(anchor 불가)·market=signal·catalog=entity·search=URL candidate(truth 아님)·unknown=fail-closed. **ADR#86 FR adapter·bridge·readiness 는 모두 source role guard 보존**(FR=official_record→official authority 5·anchor-eligible·**그러나 news 와 자동 pairing 0**→official×news role-bridge 별도 lane·bridge 후보 official/news 만·community/market/catalog/search reject)·bridge 는 title-Jaccard 미사용(date 근접+entity/action token)·official 단독 production candidate 금지.
- **제품 계약(raw≠public)**: 출력·`internal_ops_date_pinned_live_run_frontier`(sanitized·**46필드**) 노출 표면에 score/rationale/predicted_status/raw body/secret/raw reviewer 신원·same_event truth·**named_entity/event_phrase 전문·FR title/url** **0** — 출력 전 `_assert_pii_safe` 재귀 가드·API `response_model`(sanitized·46필드)·frontend `FORBIDDEN_OPS_FIELDS` 재귀 가드. credential present/missing boolean만(값 0). **FR live smoke·bridge 출력 aggregate-only(official_records/title/url·per-pair·단일 score 0)·snapshot window_honoring_source 는 count/status 만**. production_gold_count 0·gate exact passthrough·`merge_allowed=False`·embedding/LLM 실호출 0·DB write 0. Agent 는 FR adapter·official×news bridge·window-honoring source·freeze readiness·reviewer handoff·KO lane 을 *계획* 가능하나 adapter wired 를 same-event 후보로·live_verified 를 production candidate 로·bridge candidate 를 truth 로·official 단독을 cross-source candidate 로·freeze 를 truth 로 사용 불가·merge·secret 생성·PII 출력·실 전송 불가.

## ⚠️ 이번 턴 종결/갱신 RISK
- **신규 1(실 present blocker)**: **R-OfficialNewsDomainMismatch**(MEDIUM·FR=regulatory 도메인(rules/notices/enforcement)이 news event class 와 자주 불일치 → official×news same-event bridge 후보 구조적 희소·이번 bounded run 0·완화=bridge 가 date 근접 AND entity/action token 둘 다 요구·reviewer-routing only·종결 금지).
- **부분진전(종결 0)**: **R-WindowHonoringSourceScarcity**(MEDIUM→**부분완화**·FR adapter **실배선**+date filter **live_verified** 실측→hedge 즉시 배선 가능·단 official×news 수율 미입증·GDELT/SEC EDGAR 미배선·종결 금지) · **R-ProviderDateWindowFidelity**(window-honoring control(FR live_verified)로 news-측 `date_filter_ignored` **보강**·단 FR=regulatory≠news 도메인이라 직접 증명 아닌 보강 신호·confidence medium≠확정·종결 금지) · **R-ProductionCandidateScarcity**(official×news in-window pair 0→freeze 0·gold 0·종결 금지) · **R-LiveRunReproducibility**(FR live 수치 gitignored snapshot only·repo 비재현·종결 금지).

## ❌ 달성하지 못한 것 & 왜 (이월)
- **메커니즘 완전 확정**: date_filter_ignored(medium·leading)와 zero_in_window_coverage(low)를 **완전 분리 못 함** — 단일 event·window-honoring control 부재. 해소=Federal Register adapter(date-honoring source 로 control) 또는 in-window 보도가 실재하는 event 로 재실행.
- **production candidate freeze / 실 gold**: enforce_window 가 out-of-window 전부 drop → comparison_pair_count 0 → cross-source same-event 후보 0 → freeze 0·`production_gold_count=0`. 해소=window-honoring source(ADR#86)→live-derived publishable freeze→reviewer contact→actual returned labels.
- **official×news in-window same-event 수율**: FR adapter **실배선 완료**(ADR#86·key-free·official·date filter live_verified) + official×news role-bridge **구축 완료**(reviewer-routing only). **그러나** FR=regulatory 도메인(rules/notices/enforcement)이 news event subject 와 자주 불일치 → 이번 bounded run official×news in-window same-event pair **0**(R-OfficialNewsDomainMismatch)·bridge 수율 미입증. GDELT 는 여전히 미배선(rate-fragile 60s·429 storm+aggregator attribution·ADR#87+).
- **adjudicator runtime / RAG·KG·Entity·public IU**: contract guard(docs)일 뿐 runtime 미구현(embedding/LLM 실호출·실 병합·운영 DB·GraphRAG·public IU·KG edge 이번 턴 No-Go·옵션 I 금지).

## 🚧 닫을 수 없는 문제 (BLOCKED/UNKNOWN)
- BLOCKED: 실 production candidate = live-derived **in-window** near-match publishable pair 확보(현재 0·date_filter_ignored→Guardian/NYT 로는 in-window 0·R-WindowHonoringSourceScarcity). 실 gold = 실 후보→reviewer recruitment→실제 contact→첫 returned label. 실 병합 = gold-calibrated adjudicator(R3)+MERGE_GATE(R2).
- UNKNOWN: **Guardian/NYT 가 date param 을 무시하는 근본 이유**(date_filter_ignored leading·medium·단 zero_coverage 와 미분리·R-ProviderDateWindowFidelity Closure 조건)·**Federal Register adapter 가 date-honoring in-window cross-source 수율을 낼지**(미테스트·official×news role-bridge 미설계)·**date_filter_ignored 가 API 측 어떤 원인인지**(param 명 오류 vs archive 한계 vs API 버그·미규명). 실 reviewer 회수율/SLA(plan·machinery만).

## 👉 다음 할 일
1. **[커밋 대기]** 본 턴 ADR#86: 신규 4·수정 약 19(backend 9·frontend 3·docs 7) 미커밋. 커밋 여부 지시 대기. push 금지. (`--paths/`·`--help/`·`.harness/_TRASH/`·`frontend/.harness/` stray 정리 허가도 함께 받으면 처리.)
2. **[다음 단계·ADR#86]** (a) **Federal Register window-honoring adapter 배선**(`run_provider_query` url+parser·key-free·official·explicit publication_date 범위·정밀 spec=`19_WEB_INTELLIGENCE_IMPLEMENTATION_SPEC`) + **official×news role-bridge**(guard 약화 금지) → date_filter_ignored vs zero_coverage **최종 분리** + same-event 수율 실측. (b) live-derived production candidate freeze → (c) **reviewer recruitment + actual contact + 첫 returned labels**(R1) → (d) gold denominator(live 200·KO 50·**R1**) → (e) MERGE_GATE(precision≥0.98·**R2**) → (f) embedding scorer opt-in(**R3**) → (g) entity/KG/GraphRAG(**R4~R6**) → (h) Agent/public IU(**R7**). 별도 축: internal ops UI 화면 render·per-user auth.
3. **[권장·bounded]** live acquisition 실험은 **bounded·opt-in·rate governed(pace 사전 대기·no-bypass)·operator 확인 date-pinned event·enforce_window** 일 때만(무단 대량 호출 0·source role guard 유지·out-of-window 기사 same-event 사용 금지·메커니즘 confidence high 단정 금지).

## 📁 근거 (이번 턴 핵심)
- 커밋: `049a716`(ADR#85·push 0·NO UPSTREAM). ADR#86 변경: 신규 4(backend tool 2+test 2)·수정 약 19(backend 8·frontend 3·docs 8). **ADR#85 커밋분 frozen·재구현 0·EVENT_SCHEMA/main.py/config/.env/.env.example/package.json/cross_source_live_overlap_smoke.py/live_query_target.py/reviewer_handoff_bridge.py 무변경·전부 additive(FR=신규 provider key·guardian×nyt URL byte-identical·2-set 분리로 news-pairing pool 무변경).**
- **실 FR live smoke(bounded·key-free·governed·실 live·raw body 0·aggregate only)**: ≤4 governed 호출. operator window [06-25,06-26]. 결과: "asylum metering" 31건(필터 미적용)→**0건(필터 적용·31→0)**·"asylum"/"immigration" 0건·**"enforcement" 25건·전부 in-window·out-of-window 0 → `fr_live_ok_in_window`·`date_filter_capability=live_verified`**. → **FR date filter 가 응답을 실제 제약**(Guardian/NYT date_filter_ignored 와 정반대)·news 측 date_filter_ignored 가설 **보강**(FR=regulatory 도메인≠news·보강 신호). **parser 버그**: FR count=0 시 results 키 생략({count,description}만)→초기 parse_error 오분류→count 키 존재 시 no_records 로 수정(test_47). official×news bridge: in-window same-event pair **0**(FR enforcement 문서 ≠ news subject·도메인 불일치)→freeze 0. **secret 0·raw body 0·title/url 미저장·host/rate governed·merge/llm/db_write/sending/score 0.**
- 검증(정직): 신규/수정 backend 테스트 **+27**(provider_query_adapters +8[FR adapter/parser/key-free/count=0]·official_news_role_bridge 9·federal_register_live_smoke 9[리뷰 fix +2: host_gate/max_records]·r1_bounded_live_breadth_run +1)·**backend 전체 1732 passed / 101 skipped / 0 failed**(ADR#85 1705→+27)·ingestion 무변경(1353 passed)·frontend **tsc 0**·**lint 0**·node:test **78 passed**(ADR#86 view +2: 76→78)·ruff 실게이트(E/F/I/B/W−E501) **0**·secret scan **PASS**(0 findings·credential present/missing boolean만)·docs_lifecycle conflicts **0**. dict==pydantic==TS==mjs **46==46==46==46**(date-pinned frontier·+8).
- 감사(closeout): fr_adapter/fr_url_parser/byte_identity_2set/federal_register_live_smoke/date_filter_capability/official_news_role_bridge/role_guard/bridge_reviewer_routing_only/freeze_in_window_gate/window_honoring_source/orchestrator_frontier/sanitized_snapshot/credential_secret_boundary/internal_ops_boundary/public_truth_boundary/no_merge/no_llm_embedding/source_role_guard/code/test/evidence/risk_closure/adversarial review. cardinal(adapter wired 를 same-event 후보 확보로 둔갑 금지·live_verified 를 production candidate 로 둔갑 금지·official 을 news 와 같은 role 로 섞기 금지·official 단독 cross-source candidate 금지·bridge candidate 를 truth/gold 로 둔갑 금지·official×news bridge 를 same-event 단정으로 사용 금지·out-of-window record freeze 금지·MERGE_GATE 전 merge 금지·LLM/embedding 실호출 금지·raw body/abstract 저장 금지·`.env` 값 출력 금지·score/rationale/predicted_status reviewer/public 노출 금지·실 전송 금지·public IU 생성 금지·RISK 과대 종결 금지·push 금지) 차단 확인.
- 문서: ADR#86(`_DECISIONS`)·RISK(`_RISK`·종결 0·신규 1[R-OfficialNewsDomainMismatch]·부분진전 4)·ADR#86 블록(`2_ROADMAP/15` descending·ADR#85 앞)·ADR#86 spec(`2_ROADMAP/19`)·`2_ROADMAP/00`(ADR 목록 +#86)·`_CANONICAL/02`(ADR#41~#86). **`_CANONICAL/03_SOURCE_STATUS.md` 무변경**(production_source_state.json 파생·FR 은 이미 OFFICIAL_RECORD_SOURCE 로 정확 분류·query-adapter 미추적 문서라 ADR#86 변경 불필요)·**3_SOURCE_STATUS 부재(ABSENT)·EVENT_SCHEMA/LLM_EVIDENCE_PACKET_CONTRACT 무변경**(ADR#86 DB-free·runtime 0·adapter/bridge 는 acquisition 진단이지 event schema 필드 아님).

---
_as_of: 2026-06-29 · ADR#86 Federal Register window-honoring adapter 실배선 + official×news role-bridge + FR live date-honoring 검증 — ADR#85 커밋(`049a716`) 후, ADR#85 가 남긴 미배선 hedge 를 **실배선·검증**. (1) FR adapter(`run_provider_query`·key-free·official_record·`_federal_register_url` conditions[term]+publication_date[gte/lte]·`parse_federal_register_items`·`ALL_ADAPTER_PROVIDERS`/`ADAPTER_WIRED_PROVIDERS` 2-set 분리로 guardian×nyt byte-보존). (2) official×news role-bridge(`official_news_role_bridge.py`·PURE·title-Jaccard 미사용·date 근접+entity/action token·reviewer-routing only·same_event/merge/kg-edge/score 0). (3) FR live smoke(`federal_register_live_smoke.py`·§10 status). **실 FR live**(≤4 key-free governed 호출·secret 0·raw body 0): operator window [06-25,06-26] — "enforcement" **25/25 in-window·out-of-window 0·`live_verified`**·"asylum metering" 31건→필터 0건. **Guardian/NYT 와 정반대**(date_filter_ignored)→news 측 date_filter_ignored 가설 보강(단 FR=regulatory 도메인≠news·보강 신호). **parser 버그 발견+수정**(FR count=0 시 results 키 생략→no_records·test_47). frontier +8 dict==pydantic==TS==mjs **46**·required_copy +2. **측정**: backend +27(1732p/101skip/0fail)·ingestion 1353p·frontend tsc0/lint0/test78·secret PASS·docs_lifecycle 0·ruff 0. **정직 경계**: adapter wired ≠ same-event 후보 확보·live_verified ≠ production candidate·bridge candidate ≠ truth·official×news in-window same-event pair 0(FR=regulatory≠news subject·R-OfficialNewsDomainMismatch)·freeze 0·production_gold 0·gap 200·reviewer_handoff_ready False·adjudicator runtime/public IU/merge/운영 DB/embedding·LLM 실호출/실 전송 0·완전종결=OVERCLAIM. 다음 hard blocker=official 문서+news 보도가 같은 subject·같은 window 인 regulatory-class event→official×news live-derived freeze→실 reviewer contact→actual returned labels(R1). **신규 R-OfficialNewsDomainMismatch(MEDIUM)**·R-WindowHonoringSourceScarcity 부분완화·종결 0. ADR#85 커밋 `049a716` 위 ADR#86 미커밋(신규 4·수정 약 19)·커밋 지시 대기·push 안 함._
