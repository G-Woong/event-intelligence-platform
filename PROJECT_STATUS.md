# 📊 프로젝트 진행 현황 (PROJECT_STATUS)

> 매 턴 끝에 새로 쓰입니다. 항상 "가장 최근 1턴"만. 과거는 `git log -- PROJECT_STATUS.md`.
> 사실 원본(자동): `.harness/machine_status.json` · 완료 증거: `.harness/closeout_stamp.json` · 서술: 이 파일(에이전트).

## 🟢 한눈에 (비개발자용 3줄)
- **지금 무엇을 했나:** 지난 턴(ADR#64 — Guardian+NYT cross-source 실측)을 안정 기준점으로 **커밋**(`0b6e2a9`)한 뒤, ADR#64 가 발견한 "**cross-source 쌍은 100개로 실제 생성됐는데 결정론적 제목-겹침 검출기가 0건 검출**" 이라는 병목(= 문제가 *소스 부족*에서 *검출 부족*으로 이동)을 풀기 위해, 그 100쌍을 **버리지도 병합하지도 않고 점수 매겨 사람 검수 큐의 우선순위로만 넘기는 offline semantic candidate scorer**를 만들었습니다. 자동 병합·LLM/embedding 실호출·키 값 출력은 **0**입니다.
- **이번 턴에 실제로 끝낸 것:** ADR#64 커밋(`0b6e2a9`·13파일) → **ADR#65**: 새 모듈 `semantic_candidate_scorer.py`(입력/출력 contract + scorer_mode 4종[결정론 scaffold·fake_semantic 배관용·embedding/LLM은 **주입 전용 No-Go·실호출 0**] + **top-k 순위**[임계값 아님]→검수 큐 prioritization·점수/근거는 labeler 에게 **숨김**) + 기존 2개 도구에 **기본 off 가산 옵션**(검출기 floor 와 무관하게 cross-source 쌍 전부를 scorer 입력으로 노출 / smoke 에 scorer 배선). 신규 **44 테스트**.
- **정직한 한계:** 이번 진전은 **검수 우선순위 substrate**이지 **검증된 판정기가 아닙니다**. 결정론 scaffold 모드는 ADR#64 에서 0을 낸 바로 그 신호라 **recall 개선이 0**이고, fake_semantic 은 배관(plumbing)을 테스트하는 결정론 proxy일 뿐 **실 embedding 이 아닙니다**. gold 없이는 어떤 점수도 잠정값이며(threshold 확정 금지), 실 reviewer/gold·MERGE_GATE·embedding 실호출·자동 병합·운영 DB는 여전히 **0**입니다. **push 금지·커밋 지시 대기**.

## 📋 자동 수집 사실 (machine_status.json)
- **git HEAD `0b6e2a9`**(ADR#64, 본 턴 첫 커밋) 위 **ADR#65 = 미커밋**: 신규 2(`backend/app/tools/semantic_candidate_scorer.py`[scorer] + `backend/tests/test_semantic_candidate_scorer.py`[41 테스트]) + 수정 11(`backend/app/tools/source_overlap_discovery.py`[`emit_candidate_pairs` 가산] + `backend/app/tools/cross_source_live_overlap_smoke.py`[`semantic_scoring` 가산] + `backend/tests/test_cross_source_live_overlap_smoke.py`[+3 통합 테스트] + docs 7[`2_ROADMAP/00·15`·`_RISK/RISK_REGISTER`·`5_REFERENCE/RAG_KG_AGENT_READINESS·INTELLIGENCE_UNIT_CONTRACT`·`_CANONICAL/02`·`_DECISIONS/2026-06`] + `PROJECT_STATUS`) = **추적 13항목**. **migration 없음·ADR#64 커밋분 frozen·단방향 import·재구현 0·additive 2개는 기본값 off → 커밋 ADR#64 동작 byte-identical.**
- 열린 RISK: **신규 0·종결 0(total 불변·HIGH 3·MEDIUM 21·LOW 11)**. 부분진전 8: R-SemanticIdentityAdjudicator·R-IdentityEvalDataset·R-ReviewerAgreement·R-GoldSamplingBias·R-SourceOverlapScarcity·R-RealSourceLoopUnproven·R-LiveIdentityBacklog·R-IdentityHumanLabeling. throwaway 는 `.harness/_TRASH/`·`frontend/.harness/`(커밋 제외).

## ✅ 이번 턴에 달성한 것 (offline/gated semantic candidate scorer — ADR#65)
- **① ADR#64 커밋**: 13파일 → `0b6e2a9`(secret PASS·docs_lifecycle conflicts 0·SIGNATURE MATCH 16-entry·unresolved 0·push 0).
- **② 원자 분석(§2·20문항·구현 전 보고)**: cross-source pair 100 은 **어디에도 영속 안 됨**(transient count)·기존 `_near_pair_record` 가 §5 `SemanticPairInput` 거의 1:1 보존·`EvalPair.ALLOWED_KEYS`/`validate_labeling_packet` 가 **score 누출을 fail-loud 로 구조 차단**(internal-only 강제)·env var=**`OPENAI_API_KEY`**(LLM·embedding 공용·`.env.example:11`)·`probe_env_var`(값 미열람)·**이번 턴 embedding/LLM 실호출 0**(fake+injection-only)·gold 없이 threshold 금지→**top-k rank**·다음 hard blocker=**real reviewer labels**.
- **③ A — scorer contract**: §5 `SemanticPairInput`/`SemanticPairScore`(provisional·`labeler_visible=False`·`merge_allowed=False`·`requires_gold/merge_gate=True`·`reasons_internal`/`model_metadata_internal` internal-only·`confidence="uncalibrated"`).
- **④ B — offline deterministic/fake scaffold**: `scorer_mode` = deterministic_scaffold(title-Jaccard×date_factor=**ADR#64 의 0 신호 그대로**·recall 개선 주장 0) / fake_semantic(char-bigram Dice+token containment 결정론 proxy·**실 embedding 아님·배관 scaffold**)·network/llm/embedding 실호출 **0**.
- **⑤ D — reviewer/eval packet export**: score→**top-k rank**(threshold 아님·zero-signal 제외)→top-k near + hard negative → `build_near_match_reviewer_queue`(score 없는 pair record 만 투입·`validate_labeling_packet` fail-loud·labeler_facing_view 가 score/rationale/predicted_status **숨김**)·`build_eval_gold_linkage`(MERGE_GATE precision 0.98·`production_gold_count=0`·current_status No-Go).
- **⑥ C — optional embedding/LLM scorer**: **injection-only interface**(실 client 미배선=No-Go·`create_embedding_client`/`create_llm_client` 미래 seam). gate: `opt_in=False`→scorer_disabled·credential 부재→missing_credentials/env_not_loaded(값 미열람·env var **이름**만 노출)·credential present+`scorer_fn` 미주입→provider_error. 주입 fake scorer_fn 만 실행(`scorer_fn_invoked` 분리 보고·`llm/embedding_invoked` 항상 False).
- **⑦ 가산 배선(기본 off=ADR#64 보존)**: `discover_overlap(emit_candidate_pairs)`(near floor 무관 전 publishable cross-URL same-date pair 를 band 태그로 노출·scorer 입력) + `cross_source_live_overlap_smoke(semantic_scoring)`(on 시 live combined_records 의 **cross-source pair**[guardian 10×nyt 10=cross_source_pair_count 100·same-source 90 제외·`cross_source_only` 기본]을 scorer 입력으로·`--semantic-scoring`).
- **⑩ 감사 정정(adversarial+code-review)**: **M-1** scorer 경로 cross-source 미강제→same-source pair 점수화 → `cross_source_only=True` 필터+`same_source_pair_excluded` 분해(테스트 47/48)·"100쌍"→cross_source_pair_count 정정. **M-2** deterministic_scaffold 가 sub-floor 데이터에 candidate>0→검출 오독 → `above_near_floor_count`/`candidate_band_distribution` 추가(테스트 49). **MED-1** hard_negative band high score→near positive 승격 → top-k 를 hard band 제외 space rank·hard lane 보존(테스트 50). **HIGH-1** fallback band 누락→hard lane 소실 → 명시 band 부여(테스트 51).
- **⑧ §9 Agent contract**: `SCORER_AGENT_CONTRACT`(cross-source pair scoring/semantic prioritization/reviewer packet priority/hard negative/expected gold value **계획 가능**·truth 사용·같은 사건 확정·merge·public IU·community/market/catalog anchor·secret 출력 **불가**·`no_merge_without_gate`·secret_boundary).
- **⑨ E/F 경계**: production semantic merge(E)·production DB(F) 미착수.

## 🧭 cross-batch identity 4단계 + 평가/gold gate (정직)
- ① anchor 병합(ADR#40) → ② 후보 LINK(ADR#41) → ③ shadow 판정(ADR#42·운영 배선 ADR#48~#53·preflight/smoke ADR#54·live-db ADR#55·replay ADR#56·overlap discovery ADR#57·acquisition ADR#58·near-match 큐 ADR#59·targeted readiness ADR#60·provider readiness gate ADR#61·provider query adapter+Guardian wired ADR#62·secret-safe live query smoke ADR#63·2nd provider(NYT)+cross-source live overlap smoke ADR#64·**offline/gated semantic candidate scorer ADR#65**) → ④ 실 병합(**미구현**).
- **ADR#65 위치**: 단계 ③(검출)~⑦(검수 큐) 사이의 **offline candidate-prioritization substrate**. ADR#64 가 드러낸 detection scarcity(cross-source pair 100·deterministic 0)를 scorer 로 점수화→top-k→검수 큐 우선순위로만 연결(병합·단정 0·score labeler 숨김). **검출 검증이 아님**(deterministic_scaffold=recall 개선 0·fake_semantic=배관 scaffold·embedding No-Go).
- **⚠ 미해소(OPEN):** gold-calibrated embedding/LLM **실 병합 판정기**(No-Go)·실 reviewer/gold(production_gold 0)·MERGE_GATE 런타임·한국어 캘리브레이션·운영 DB 0009 배포. "scorer prioritization ≠ 검출·후보·병합" — 완전종결=OVERCLAIM.

## 🔎 source orchestration / 제품 계약
- official/news=publishable evidence(merge anchor)·community=반응 evidence(anchor 불가)·market=signal·catalog=entity·search=URL·unknown=fail-closed. scorer 는 publishable×publishable(`source_role_compatible`)만 후보로 점수화(community/market/catalog anchor 차단).
- **제품 계약(raw≠public)**: scorer output(score/rationale/model metadata)은 **internal-only** — reviewer-facing packet 에는 `validate_labeling_packet` 가 누출을 fail-loud 로 차단. score 는 **truth 가 아니라 reviewer 주목 우선순위 신호**(threshold 로 같은 사건 단정 금지). 본문 미저장·public IU 미생성·`merge_allowed=False`·`no_public_intelligence_unit`·embedding/LLM 실호출 0. Agent 는 scoring/prioritization 을 *계획* 가능하나 score 를 truth 로 쓰거나 merge/검출 단정·secret 출력 불가. LLM/Agent 진입 9조건 여전히 No-Go(1·4·5·7).

## ⚠️ 이번 턴 종결/갱신 RISK
- **부분진전(종결 0·신규 0·total 불변)**: **R-SemanticIdentityAdjudicator**(scorer contract+offline scaffold+score→reviewer queue prioritization·No-Go 유지·**validated adjudicator 아님**) · **R-IdentityEvalDataset**(scored→reviewer/eval packet·top-k+hard negative·production_gold 0 정직) · **R-ReviewerAgreement**(top-k+hard negative packet·score labeler 숨김·실 합의 0) · **R-GoldSamplingBias**(top-k+hard negative 동시 표집·score 숨김 reviewer bias 차단·실 표집 0) · **R-SourceOverlapScarcity**(**cross-source pair scarcity→detection scarcity 재분해**·scorer 층 연결·검출 검증 아님) · **R-RealSourceLoopUnproven**(scorer 층·실 reviewer/gold/merge 0) · **R-LiveIdentityBacklog**(scorer→cross-source top-k queue 경로·실 backlog 0) · **R-IdentityHumanLabeling**(scored top-k→packet·score/predicted_status labeler 숨김·실 gold 0).
- 디렉티브 신규 RISK 후보(R-SemanticScorerCalibration/R-LLMAdjudicatorBias/R-EmbeddingProviderReadiness)는 **현 단계 독립 blocker 아님**(scorer 미배선·실호출 0이라 calibration/bias/provider readiness 가 아직 실재 X·R-LLMAdjudicatorBias 취지는 score labeler 숨김으로 선제 완화) → **미등록**(RISK 남발 금지·실 blocker 화 시 다음 턴).

## ❌ 달성하지 못한 것 & 왜 (이월)
- **validated detection / 실 병합**: scorer 는 **prioritization substrate**일 뿐 — deterministic_scaffold 는 ADR#64 의 0 신호와 동일(recall 개선 0)·fake_semantic 은 배관 scaffold(실 embedding 아님). 실 검출/병합은 gold-calibrated embedding/LLM adjudicator(No-Go·다음 턴) 선결.
- **실 human-reviewed gold**: scorer top-k 가 검수 큐를 채워도 **실 reviewer 라벨/gold 0**(packet≠gold·production_gold 0).
- **embedding/LLM 실호출**: `create_embedding_client` seam 배선·secret-safe·bounded·internal-only 후에만(이번 턴 injection-only·실호출 0). MERGE_GATE(precision 0.57<0.98)·운영 DB·production 백로그 0(옵션 E/F 금지).

## 🚧 닫을 수 없는 문제 (BLOCKED/UNKNOWN)
- BLOCKED: 검출 희소 = **gold-calibrated embedding/LLM adjudicator**(옵션 E·다음 턴) 선결 — scorer 는 점수만·검출/병합 단정 금지. 실 reviewer/gold = reviewer 충원·운영 절차 선결. production 백로그 = 운영 DB 0009 배포(승인)+실 fetch.
- UNKNOWN: semantic score 의 실 병합 허용 기준(production precision·실 gold·reviewer 합의·한국어 캘리브레이션·MERGE_GATE) → R-SemanticIdentityAdjudicator·R-IdentityEvalDataset·R-ReviewerAgreement·R-IdentityHumanLabeling 선결. ADR#64 의 deterministic 0 이 paraphrase recall 한계인지 real scarcity 인지도 여전히 n=2 미구분(gold 필요).

## 👉 다음 할 일
1. **[커밋 대기]** 본 턴 ADR#65: 신규 2(semantic_candidate_scorer.py+test) + 수정 11(source_overlap_discovery.py·cross_source_live_overlap_smoke.py·test_cross_source_live_overlap_smoke·docs 7·PROJECT_STATUS) = 13파일 커밋 여부 지시 대기. push 금지.
2. **[다음 단계]** (a) **real reviewer labels** 충원(gold 없이는 어떤 scorer threshold/embedding 도 provisional) → (b) **embedding scorer opt-in 실호출**(`create_embedding_client` seam 배선·secret-safe·bounded·internal-only) → (c) MERGE_GATE calibration(precision≥0.98) → (d) embedding/LLM/KG **실 병합** adjudicator runtime → (e) (승인 하) 운영 DB 0009 배포 + scheduler → RAG/KG/Agent/IU.

## 📁 근거 (이번 턴 핵심)
- 커밋: `0b6e2a9`(ADR#64·13파일·+1024/−47·push 0). ADR#65 변경: 신규 `semantic_candidate_scorer.py`(scorer)+`test_semantic_candidate_scorer.py`(41)·가산 `source_overlap_discovery.py`(`emit_candidate_pairs`)·`cross_source_live_overlap_smoke.py`(`semantic_scoring`)·`test_cross_source_live_overlap_smoke.py`(+3)·docs 7. **ADR#64 커밋분 frozen·단방향 import·재구현 0·additive 기본 off→ADR#64 동작 byte-identical.**
- 검증(정직): 신규 **49**(scorer 46[감사 fix-lock 47-51 포함]·smoke 통합 3)·backend 비-live **899 passed/101 skipped/0 failed**(DB 미기동→live-PG 101 skip)·**live-PG=미실행**(postgres:5432 CLOSED·docker 미기동; 단 변경이 DB/session/ORM 경로 무접촉=scorer write-free·`db_write:False`·session 인자 부재→무영향·baseline 94 불변)·ingestion **1353 passed**·frontend tsc**0**/node:test **12**/lint **0**·secret scan **PASS**·docs_lifecycle conflicts **0**·ruff 실게이트(E/F/I/B/W−E501) **0**. **실호출 0 입증**: opt-in 도 default scorer_disabled·credential present+scorer_fn 미주입→provider_error·주입 fake scorer_fn 만 실행(llm/embedding_invoked False·scorer_fn_invoked True 분리). 운영 DB 무변경.
- 감사: adversarial-reality-critic + code-review(stamp `audit_evidence` 단일 출처). 핵심 안전축(score internal-only·labeler 숨김·`validate_labeling_packet` fail-loud·top-k rank≠threshold·embedding/LLM injection-only 실호출 0·deterministic_scaffold recall 개선 0 정직·merge_allowed False·production_gold 0·db_write 0·source role guard).
- 문서: ADR#65(`_DECISIONS`)·8 RISK 부분진전(`_RISK`·종결 0·신규 0)·semantic candidate scorer runbook+ADR#65 블록(`2_ROADMAP/15`)·(11w)(`2_ROADMAP/00`)·condition 3/4/agent(`RAG_KG`)·scorer 계약(`IU_CONTRACT`)·`_CANONICAL/02`(ADR#41~#65 + semantic scorer 절).

---
_as_of: 2026-06-26 · ADR#65 offline/gated semantic candidate scorer + cross-source pair evaluation packet — ADR#64 커밋(`0b6e2a9`) 후, ADR#64 가 입증한 detection scarcity(cross-source pair 100·deterministic title-Jaccard 0 검출)를 다루는 첫 scorer 층 신설(새 파일 1·가산 2·ADR#64 커밋분 frozen·단방향 import·재구현 0): `semantic_candidate_scorer.py` — §5 `SemanticPairInput`/`SemanticPairScore` contract·scorer_mode(deterministic_scaffold=ADR#64 의 0 신호 그대로·recall 개선 0 / fake_semantic=char-bigram Dice+token containment 결정론 proxy·실 embedding 아님·배관 scaffold / embedding_opt_in·llm_opt_in=injection-only No-Go·실호출 0)·score→top-k rank(threshold 아님·zero-signal 제외)→top-k near+hard negative→`build_near_match_reviewer_queue`(score 없는 pair 만·labeler_facing_view 숨김·`validate_labeling_packet` fail-loud)·`build_eval_gold_linkage`(MERGE_GATE precision 0.98·production_gold 0·No-Go)·`SCORER_AGENT_CONTRACT`·CLI. 가산: `discover_overlap(emit_candidate_pairs)`·`cross_source_live_overlap_smoke(semantic_scoring)`(기본 off=ADR#64 byte-identical). 옵션 **A+B+D 채택·C=injection-only interface·E(production semantic merge)/F 금지·신규 RISK 0**. **측정**: 신규 49·backend 비-live 899p/101s/0f·live-PG 미실행(DB down·경로 무접촉·baseline 94 불변)·ingestion 1353p·frontend tsc0/test12/lint0·secret PASS·docs_lifecycle 0·ruff 실게이트 0. **실호출 0**(opt-in default scorer_disabled·scorer_fn 미주입→provider_error·주입 fake scorer_fn 만 실행). **감사 PROCEED-WITH-FIXES**(M-1 cross_source_only same-source 제외·M-2 above_near_floor 검출-floor 분해·MED-1 hard_negative 미승격·HIGH-1 fallback band→전부 코드/테스트 수정). **정직 경계**: 진전=candidate prioritization substrate·**validated adjudicator 아님**(deterministic_scaffold=recall 개선 0·fake_semantic=배관 scaffold·gold 없이 threshold 금지·`semantic_score` 미주입)·실 reviewer/gold 0·MERGE_GATE 미충족·embedding/LLM No-Go·운영 DB/production 백로그 0 불변(완전종결=OVERCLAIM). **R-SemanticIdentityAdjudicator·R-IdentityEvalDataset·R-ReviewerAgreement·R-GoldSamplingBias·R-SourceOverlapScarcity·R-RealSourceLoopUnproven·R-LiveIdentityBacklog·R-IdentityHumanLabeling 부분진전**·종결 0·신규 0. ADR#64 커밋 `0b6e2a9` 위 ADR#65 미커밋(신규 2+수정 11=13)·커밋 지시 대기·push 안 함._
