# 📊 프로젝트 진행 현황 (PROJECT_STATUS)

> 매 턴 끝에 새로 쓰입니다. 항상 "가장 최근 1턴"만. 과거는 `git log -- PROJECT_STATUS.md`.
> 사실 원본(자동): `.harness/machine_status.json` · 완료 증거: `.harness/closeout_stamp.json` · 서술: 이 파일(에이전트).

## 🟢 한눈에 (비개발자용 3줄)
- **지금 무엇을 했나:** 지난 턴(ADR#67 — reviewer batch launch pack)을 안정 기준점으로 **커밋**(`65065dd`)한 뒤, ADR#67 이 만든 batch pack 이 아직 *실제 운영 루프에 연결되지 않았다*는 병목을 풀기 위해, **실 reviewer 가 작성한 라벨 파일이 수거 디렉터리에 들어왔을 때** production label import→검증→합의→gold 승격→**캘리브레이션 delta** 까지 안전하게 통과/거부하는지 검증하는 **first production label intake pilot** 을 만들었습니다. 라벨 파일이 없으면 실패가 아니라 정직하게 **`awaiting_production_labels`** 로 보고하고 운영자가 무엇을 해야 하는지 actionable next_action 을 냅니다. 자동 병합·LLM/embedding 실호출·운영 DB·키 값 출력은 **0**입니다.
- **이번 턴에 실제로 끝낸 것:** ADR#67 커밋(`65065dd`·10파일) → **ADR#68**: 새 모듈 `production_label_intake.py`(first production label intake orchestrator·DB-free·ADR#67/#66 building block **조합**·재구현 0) — `_scan_intake_dir`(수거 디렉터리 `*.jsonl` **다중파일** 스캔·파일명만 노출[PII 차단]·JSON 오류→malformed **fail-loud**)+`build_no_labels_report`(라벨 없음=운영자 next_action checklist)+`build_calibration_delta`(import 전/후 gold·positive/negative/korean·precision/FPR/KO denominator readiness+`next_needed_for_merge_gate`)+`run_production_label_intake`(intake_status=awaiting_production_labels/invalid_labels/imported/conflict_pending/calibration_pending·production gold=production AND live_derived **decisive(same/different)** 만·duplicate/unknown/forbidden/model 거부 count·pair coverage). **가드**: `.gitignore` `outputs/reviewer_batch/`(실 reviewer 라벨 PII 커밋 방지). 신규 **57 테스트**. **기존 ADR#67/#66/#59 미변경**(최소 변경).
- **정직한 한계:** 이번 진전은 **first production label intake 준비 substrate**이지 **실 gold 가 아닙니다**. **실 production reviewer 라벨·실 gold 는 여전히 0**(synthetic/test 라벨은 simulated only·production_gold_count 0), calibration_ready/merge_gate_ready False(표본 floor live 200/KO 50 미충족), embedding/LLM 실호출·자동 병합·운영 DB·public IU 모두 **0**. no-labels 는 실패가 아니라 정직한 운영 상태(awaiting). 다음 hard blocker=**first production label file 회수**(실 reviewer 가 batch pack 으로 라벨 작성→intake_directory 배치·신규 **R-ReviewerFollowupOps**). **push 금지·커밋 지시 대기**.

## 📋 자동 수집 사실 (machine_status.json)
- **git HEAD `65065dd`**(ADR#67, 본 턴 첫 커밋) 위 **ADR#68 = 미커밋**: 신규 2(`backend/app/tools/production_label_intake.py`[orchestrator] + `backend/tests/test_production_label_intake.py`[57 테스트]) + 수정 9(`.gitignore`[PII 가드] + docs 7[`2_ROADMAP/00·15`·`_RISK/RISK_REGISTER`·`5_REFERENCE/RAG_KG_AGENT_READINESS·INTELLIGENCE_UNIT_CONTRACT`·`_CANONICAL/02`·`_DECISIONS/2026-06`] + `PROJECT_STATUS`) = **추적 11항목**. **migration 없음·ADR#67 커밋분 frozen·단방향 import·재구현 0·ADR#67/#66/#59 머신 조합·EVENT_SCHEMA(DB-free라 무변경) 무변경.**
- 열린 RISK: **신규 1(R-ReviewerFollowupOps)·종결 0**. 부분진전 8: R-IdentityHumanLabeling·R-ReviewerAgreement·R-GoldSamplingBias·R-IdentityEvalDataset·R-SemanticIdentityAdjudicator·R-LiveIdentityBacklog·R-SourceOverlapScarcity·R-RealSourceLoopUnproven. throwaway 는 `.harness/_TRASH/`·`frontend/.harness/`(커밋 제외)·`outputs/reviewer_batch/`(gitignore).

## ✅ 이번 턴에 달성한 것 (first production labels import pilot + intake/gold/calibration dry-run — ADR#68)
- **① ADR#67 커밋**: 10파일 → `65065dd`(제공 메시지 그대로·secret PASS·docs_lifecycle conflicts 0·SIGNATURE MATCH 13-entry·unresolved 0·push 0·NO UPSTREAM).
- **② 원자 분석(§2·20문항·구현 전 보고)**: launch pack 경로=`build_intake_plan`→`outputs/reviewer_batch/{batch_id}/intake`(문자열만·디렉터리 현재 **부재**)·**production label 파일 현재 0**→이번 턴 실제 상태=`awaiting_production_labels`·ADR#67 은 **단일파일/in-memory** intake 만(디렉터리 다중파일 스캔 없음)→ADR#68 이 보강. **추가 발견**: 최상위 `outputs/` 추적 파일 존재+`outputs/.gitignore` 부재→intake 라벨(reviewer_id/rationale=PII) **gitignore 안 됨**→`outputs/reviewer_batch/` 가드 추가. EVENT_SCHEMA=순수 Event(미변경)·docs/3_SOURCE_STATUS* 부재. **중복 방지**: ADR#67/#66 building block 조합·신규 파일 1. 다음 hard blocker=코드 아님·**실 reviewer recruitment/SLA+첫 label 회수**.
- **③ A — first production label intake pilot**: `run_production_label_intake`(§4·intake_status 5-state·라벨 시 `validate_label_intake`→`_rows_to_labels`→`resolve_label_operations`+`build_calibration_preflight` **직접 재사용**·**decisive gold=same/different 만**·`non_decisive_gold_count` 표면화·merge_allowed False·db_write False·llm/embedding_invoked False).
- **④ B — operator/reviewer follow-up status report**: `build_no_labels_report`(intake_directory/expected files/validation command/reviewer count/instruction summary[모델 점수 0]/operator next_action checklist) — no-labels 도 운영 상태로 actionable 관리(조용한 "라벨 없음" 금지).
- **⑤ C — production label audit manifest**: `_scan_intake_dir`(다중파일·basename only·malformed fail-loud) + intake dry-run(label_source/reviewer_kind=human/pair_id∈known/duplicate/forbidden field·production/synthetic/test 분리·model label 거부 count)·production gold=production AND live_derived decisive·`production_gold_provenance_verified`=False(선언 기반·B-1).
- **⑥ D — calibration readiness delta report**: `build_calibration_delta`(before/after production gold·positive/negative/korean·agreement/conflict delta·precision/FPR/KO denominator readiness·`next_needed_for_merge_gate`)·threshold 확정 금지·floor 미충족→merge_gate_ready False.
- **⑦ §9 Agent contract**: `PRODUCTION_INTAKE_AGENT_CONTRACT`(production label intake status/reviewer follow-up reminder/missing label next action/invalid label correction/agreement·adjudication workflow/gold calibration readiness **계획 가능**·label 조작·label file 임의 생성·score truth·같은 사건 확정·merge·public IU·anchor·secret·reviewer raw PII 출력 **불가**·`EMBEDDING_LLM_ADJUDICATOR_INTERFACE` No-Go).
- **⑧ E/F 경계**: embedding/LLM scorer runtime(E)·production DB activation/semantic merge(F) 미착수.

## 🧭 cross-batch identity 4단계 + 평가/gold gate (정직)
- ① anchor 병합(ADR#40) → ② 후보 LINK(ADR#41) → ③ shadow 판정(ADR#42·운영 배선 ADR#48~#53·preflight/smoke ADR#54·live-db ADR#55·replay ADR#56·overlap discovery ADR#57·acquisition ADR#58·near-match 큐 ADR#59·targeted readiness ADR#60·provider readiness ADR#61·provider adapter+Guardian ADR#62·live query smoke ADR#63·2nd provider+cross-source smoke ADR#64·semantic candidate scorer ADR#65·reviewer label operations ADR#66·reviewer batch launch pack ADR#67·**first production label intake pilot ADR#68**) → ④ 실 병합(**미구현**).
- **ADR#68 위치**: 단계 ⑦(검수 패킷)~⑩(MERGE_GATE) 사이의 **first production label intake readiness substrate**. ADR#67 batch pack 을 **실 운영 intake 루프**(filesystem 다중파일 스캔→validation→agreement→gold→calibration delta)로 연결(병합·단정 0·score/rationale/predicted labeler 숨김·reviewer_id 익명화·라벨 없음→awaiting_production_labels 정직). **실 gold 가 아님**(production_gold_count 0·synthetic/test=simulated only·merge_gate_ready False·decisive gold=same/different only·`production_gold_provenance_verified` False).
- **⚠ 미해소(OPEN):** 실 production reviewer 라벨·실 gold(production_gold 0)·reviewer 합의 실측·gold-calibrated embedding/LLM **실 병합 판정기**(No-Go)·MERGE_GATE 런타임·한국어 캘리브레이션·운영 DB 0009 배포. "first production label intake readiness ≠ 실 라벨·실 gold·실 병합" — 완전종결=OVERCLAIM.

## 🔎 source orchestration / 제품 계약
- official/news=publishable evidence(merge anchor)·community=반응 evidence(anchor 불가)·market=signal·catalog=entity·search=URL·unknown=fail-closed. production label intake 는 publishable×publishable 후보만(community/market/catalog anchor 차단·상속).
- **제품 계약(raw≠public)**: production label intake 의 노출 표면(report·no_labels_report·intake_report)에 score/rationale/predicted_status/raw body/secret/raw reviewer_id **0** — `_public_intake_report`(normalized_rows 제거) + allowlist(`REVIEWER_ALLOWED_KEYS`) + pseudonymization + 파일명 basename only 다중 방어. labeler 어휘(unsure/needs_review)는 frozen canonical(insufficient/ambiguous)로 **정규화**·둘 다 gold 비대상(decisive=same/different). production_gold_count 0·`merge_allowed=False`·`no_public_intelligence_unit`·embedding/LLM 실호출 0·DB write 0. Agent 는 production intake status/reviewer follow-up 을 *계획* 가능하나 label 조작·임의 생성·merge·검출 단정·PII/secret 출력 불가. LLM/Agent 진입 9조건 여전히 No-Go(1·4·5·7).

## ⚠️ 이번 턴 종결/갱신 RISK
- **부분진전(종결 0·신규 1·total +1)**: **R-IdentityHumanLabeling**(filesystem production intake+awaiting 정직 보고+production/synthetic/model 분리+calibration delta·실 production label/gold 0 불변) · **R-ReviewerAgreement**(intake→agreement/conflict report+conflict_pending 분리+capacity warning·실 합의 0) · **R-GoldSamplingBias**(label coverage+hard negative balance+positive/negative/korean delta·실 표집 0) · **R-IdentityEvalDataset**(first labels→eval/gold preflight 경로+denominator readiness **delta**·MERGE_GATE evidence 0) · **R-SemanticIdentityAdjudicator**(production labels→scorer calibration 준비·threshold uncalibrated·merge No-Go 유지) · **R-LiveIdentityBacklog**(batch launch→filesystem production intake 경로·실 backlog/gold 0) · **R-SourceOverlapScarcity**(detection scarcity 후속 production intake 운영층·검출 검증 아님) · **R-RealSourceLoopUnproven**(intake readiness substrate·실 병합 0).
- **신규 RISK 1**: **R-ReviewerFollowupOps**(batch pack 발행됐으나 **실 reviewer label 미회수가 독립 blocker**로 부상·실제 present blocker — 다음 hard blocker 는 코드 아닌 reviewer recruitment/SLA/label 회수). R-ProductionLabelIntake/R-LabelProvenanceIntegrity 는 **현 단계 독립 blocker 아님**(intake 경로 막 생김·provenance 는 `production_gold_provenance_verified:False`로 R-IdentityHumanLabeling 흡수)→미등록(남발 금지).

## ❌ 달성하지 못한 것 & 왜 (이월)
- **실 human-reviewed production gold**: production intake 경로(scan→validation→agreement→gold→calibration delta)는 닫혔으나 **실 production reviewer 라벨/gold 0**(synthetic/test=simulated only·production_gold_count 0). reviewer 충원·실 라벨 파일 **회수** 선결(R-ReviewerFollowupOps).
- **reviewer agreement 실측 / MERGE_GATE**: intake→agreement/conflict report+conflict_pending 코드 검증됐으나 **실 다중 reviewer 합의 0**·calibration_ready/merge_gate_ready False(표본 floor live 200/KO 50).
- **embedding/LLM 실호출 / 실 병합 / 운영 DB**: 이번 턴 No-Go(옵션 E/F 금지). gold-calibrated adjudicator runtime·운영 DB 0009 배포·production 백로그 0.

## 🚧 닫을 수 없는 문제 (BLOCKED/UNKNOWN)
- BLOCKED: 실 gold = **reviewer 충원·실 라벨 파일 회수** 선결(intake 는 readiness substrate·production_gold 0·R-ReviewerFollowupOps). 실 병합 = gold-calibrated embedding/LLM adjudicator(옵션 E·다음 턴)+MERGE_GATE. production 백로그 = 운영 DB 0009 배포(승인)+실 fetch.
- UNKNOWN: semantic score 의 실 병합 허용 기준(production precision·실 gold·reviewer 합의·한국어 캘리브레이션·MERGE_GATE) → R-IdentityHumanLabeling·R-ReviewerAgreement·R-IdentityEvalDataset·R-SemanticIdentityAdjudicator 선결. ADR#64 의 deterministic 0 이 paraphrase recall 한계인지 real scarcity 인지도 여전히 n=2 미구분(gold 필요).

## 👉 다음 할 일
1. **[커밋 대기]** 본 턴 ADR#68: 신규 2(production_label_intake.py+test) + 수정 9(.gitignore·docs 7·PROJECT_STATUS) = 11파일 커밋 여부 지시 대기. push 금지.
2. **[다음 단계]** (a) **first production label file 회수**(실 reviewer 가 batch pack 으로 라벨 작성→intake_directory 배치·R-ReviewerFollowupOps) → (b) reviewer recruitment(운영·SLA·roster) → (c) gold denominator 충족(live 200·KO 50) → (d) MERGE_GATE calibration(precision≥0.98·FPR≤0.01) → (e) **embedding scorer opt-in 실호출**(`create_embedding_client` seam·secret-safe·bounded·internal-only) → (f) embedding/LLM/KG **실 병합** adjudicator runtime → (g) (승인 하) 운영 DB 0009 배포 + scheduler → RAG/KG/Agent/IU.

## 📁 근거 (이번 턴 핵심)
- 커밋: `65065dd`(ADR#67·10파일·+1430/−50·push 0·NO UPSTREAM). ADR#68 변경: 신규 `production_label_intake.py`(orchestrator)+`test_production_label_intake.py`(52)·`.gitignore`(PII 가드)·docs 7·PROJECT_STATUS. **ADR#67 커밋분 frozen·단방향 import·재구현 0·ADR#67/#66/#59 머신 조합·EVENT_SCHEMA 무변경.**
- 검증(정직): 신규 **57**(production_label_intake 1-57: no-labels intake 1-6·labels-present validation 7-16·gold policy 17-25·calibration delta 26-32·no-merge/no-LLM/no-DB/secret 33-39·filesystem 다중파일 40-44·contract/drift lock 45-52·감사 fix-lock 53-57)·backend 비-live **1072 passed/101 skipped/0 failed**(1015+57·DB 미기동→live-PG skip)·**live-PG=미실행**(DB down; 단 변경이 DB/session/ORM 경로 무접촉=orchestrator write-free·`db_write:False`·session 인자 부재·DB 바운드 도구 미사용→무영향·baseline 불변)·ingestion **1353 passed**(불변)·frontend tsc**0**/node:test **12**/lint **0**(불변)·인접 회귀(ADR#65/#66/#67) **192p**·secret scan **PASS**·docs_lifecycle conflicts **0**·ruff 실게이트(E/F/I/B/W−E501) **0**·dead-code(신규 모듈) **0**(535 모듈 스캔·신규 미플래그)·gitignore 가드 IGNORED 확인. **honest 동작**: 파일 없음→`awaiting_production_labels`+no_labels_report·production_gold 0·malformed→`invalid_labels`·forbidden/api_key/raw_body/model label→거부·unknown pair_id→거부·2인 production+live same→positive gold 1·different→negative gold 1·conflict→`conflict_pending`·adjudicated→gold·unsure/needs_review→non_decisive·synthetic/test/production-tag-but-synthetic-dataset→production gold 0·calibration_ready False·order-invariant. 운영 DB 무변경.
- 감사: adversarial-reality-critic + code-review(high effort) → cardinal(no-labels 성공포장/synthetic→production/model→human/PII 노출/single→gold/conflict 자동 다수결/실 label 없이 gold 증가/MERGE_GATE 전 merge) 전부 VALID·차단 유지. 핵심 안전축(score/rationale/predicted_status labeler 숨김·forbidden field allowlist fail-loud·reviewer_id pseudonymization·파일명 basename only·single≠gold·conflict≠자동 다수결·model/self/LLM≠gold·unsure/needs_review≠gold·synthetic≠production gold·production gold=production AND live_derived decisive·`production_gold_provenance_verified` False·merge_allowed False·production_gold 0·db_write 0·source role guard). 결과·정정 단일 출처=closeout stamp `audit_evidence`.
- 문서: ADR#68(`_DECISIONS`)·8 RISK 부분진전+신규 R-ReviewerFollowupOps(`_RISK`·종결 0·신규 1)·ADR#68 블록+runbook+RealSourceLoop 표 7/8/9+정직 경계(`2_ROADMAP/15`)·(11z)(`2_ROADMAP/00`)·condition 4/agent(`RAG_KG`)·production label intake 계약(`IU_CONTRACT`)·`_CANONICAL/02`(ADR#41~#68 + production label intake 절). **EVENT_SCHEMA 무변경**(ADR#68 DB-free·event 필드/테이블 무변경·최소 변경).

---
_as_of: 2026-06-26 · ADR#68 first production labels import pilot + intake/gold/calibration dry-run — ADR#67 커밋(`65065dd`) 후, ADR#67 batch pack 을 실제 운영 루프에 연결(새 파일 1·재구현 0·ADR#67/#66 building block 조합·DB 0): `production_label_intake.py`(first production label intake orchestrator) — `_scan_intake_dir`(intake_directory `*.jsonl` 다중파일 스캔·basename only·malformed fail-loud)·`build_no_labels_report`(§5 operator next_action·label 없음=awaiting_production_labels 정직)·`build_calibration_delta`(§8 before/after gold·positive/negative/korean·precision/FPR/KO readiness·next_needed_for_merge_gate)·`run_production_label_intake`(§4·intake_status 5-state·라벨 시 `resolve_label_operations`+`build_calibration_preflight` 재사용·decisive gold=same/different 만·production gold=production AND live_derived·pair coverage·`production_gold_provenance_verified` False)·`PRODUCTION_INTAKE_AGENT_CONTRACT`·CLI·`.gitignore` outputs/reviewer_batch/ PII 가드. 옵션 **A+B+C+D 채택·E(embedding/LLM runtime)/F(production DB·merge) 금지·신규 RISK 1(R-ReviewerFollowupOps)**. **측정**: 신규 57·backend 비-live 1072p/101s/0f·live-PG 미실행(DB down·경로 무접촉·baseline 불변)·ingestion 1353p·frontend tsc0/test12/lint0·인접 회귀 192p·secret PASS·docs_lifecycle 0·ruff 실게이트 0·dead-code 0·gitignore 가드 IGNORED. **honest 동작**(파일 없음→awaiting_production_labels·production_gold 0·malformed→invalid_labels·model/forbidden/unknown pair_id→거부·2인 production+live same→positive gold 1·conflict→conflict_pending·synthetic→production gold 0·calibration_ready False). **정직 경계**: 진전=first production label intake readiness substrate·**실 gold 아님**(production_gold_count 0·synthetic/test=simulated only·calibration_ready/merge_gate_ready False·embedding/LLM 실호출 0·운영 DB/public IU/production 백로그 0 불변·완전종결=OVERCLAIM)·점수≠truth·다음 hard blocker=first production label file 회수(R-ReviewerFollowupOps). **R-IdentityHumanLabeling·R-ReviewerAgreement·R-GoldSamplingBias·R-IdentityEvalDataset·R-SemanticIdentityAdjudicator·R-LiveIdentityBacklog·R-SourceOverlapScarcity·R-RealSourceLoopUnproven 부분진전**·종결 0·신규 1. ADR#67 커밋 `65065dd` 위 ADR#68 미커밋(신규 2+수정 9=11)·커밋 지시 대기·push 안 함._
