# 📊 프로젝트 진행 현황 (PROJECT_STATUS)

> 매 턴 끝에 새로 쓰입니다. 항상 "가장 최근 1턴"만. 과거는 `git log -- PROJECT_STATUS.md`.
> 사실 원본(자동): `.harness/machine_status.json` · 완료 증거: `.harness/closeout_stamp.json` · 서술: 이 파일(에이전트).

## 🟢 한눈에 (비개발자용 3줄)
- **지금 무엇을 했나:** 지난 턴(ADR#84)이 **"요청 date window 가 provider 응답을 제약하지 못함은 확정·메커니즘은 미확정"**으로 남긴 숙제를, 이번 턴(ADR#85)에 **통제실험으로 직접 분해**했습니다. 같은 operator event(미국 대법원·asylum metering·2026-06-25·[06-25,06-26])를 5가지 variant(원본 / 날짜파라미터 제거 / relevance 정렬 / exact phrase / window 강제)로 바꿔가며 Guardian·NYT 에 **실제로 bounded·rate-governed 라이브 호출**했습니다(secret 0·raw body 0·host floor 사전 대기 준수). **결과**: 네 비교 variant 가 **모두 동일**(반환 기사 전부 당일 2026-06-29 = 요청 window 밖·주제 무관) → **order-by=newest 지배·느슨한 q 가설을 이 run 의 비차별 lever 로 약화(newest-dominance 미배제)**하고, **`date_filter_ignored`(요청 날짜 파라미터가 응답을 제약하지 못함)를 leading 가설(confidence medium·Guardian/NYT 양쪽 일관)**으로 좁혔습니다. 단 date_filter_ignored 와 "이 사건의 in-window 보도 0(zero coverage)"은 **완전히 분리하지 못했습니다**(잔존 low). 이를 위한 hedge 로 window-honoring source(Federal Register)를 **ADR#86 adapter 로 권고**(이 턴 미배선). **production candidate freeze·gold·자동 병합·LLM/embedding 실호출·public IU·실 전송 모두 0.**
- **이번 턴에 실제로 끝낸 것:** (1) **통제실험 harness**(`provider_date_window_fidelity.py`·5-variant·메커니즘 분류 confidence low/medium·**절대 high 금지**·`pace_seconds` host floor 사전 대기=gate **bypass 아님**·aggregate-only). (2) provider adapter **가산 knob**(`omit_date_window`·`order`·default 경로 byte-identical). (3) **window-honoring readiness**(`window_honoring_source_readiness.py`·GDELT/FR/SEC EDGAR 평가→**FR 권고**·source role guard 보존). (4) snapshot `control_experiment` block + frontier **+6 필드** **dict==pydantic==TS==mjs 38==38==38==38**. (5) **실 통제실험 실행**(Guardian 5+NYT 4+executor 2=11 governed 호출) → date_filter_ignored medium·enforce_window 전부 drop→comparison 0→freeze 0. **변경 표면 = 신규 4(tool 2+test 2)+수정 backend 8+frontend 3+docs 7 ≈ 22파일**(strays 제외·전부 additive·byte-보존).
- **정직한 한계:** **메커니즘은 분해됐으나 완전 확정은 아님** — date_filter_ignored(medium)이 leading 이나 zero_in_window_coverage(low)와 미분리(단일 event·window-honoring control 부재). **production candidate freeze 0**(enforce_window 가 out-of-window 전부 drop→comparison 0)·reviewer_handoff_ready=False·actual returned labels 0·**production_gold_count 0**(`current_r1_gap=200`). **통제실험 실행 ≠ 메커니즘 완전 확정**·**메커니즘 분해 ≠ production candidate 확보**·**adapter 미배선**(ADR#86). 자동 병합·운영 DB·public IU·실 전송·GraphRAG/KG runtime·embedding/LLM 실호출 모두 **0**. 다음 hard blocker = **Federal Register adapter(ADR#86) 또는 in-window 보도 실재 event 로 date_filter_ignored vs zero_coverage 최종 분리** → live-derived freeze → 실 reviewer contact → **actual returned human labels**(R1). **push 금지·커밋 지시 대기**.

## 📋 자동 수집 사실 (machine_status.json)
- **git HEAD `b8443b8`**(ADR#84·직전 턴 커밋) 위 **ADR#85 = 미커밋**: 신규 4(backend tool 2: `tools/provider_date_window_fidelity.py`·`tools/window_honoring_source_readiness.py`; backend test 2: `tests/test_provider_date_window_fidelity.py`·`tests/test_window_honoring_source_readiness.py`)·수정 약 18(backend 8: `tools/provider_query_adapters.py`·`tools/sanitized_live_snapshot.py`·`tools/r1_bounded_live_breadth_run.py`·`schemas/internal_ops.py`·`tests/test_provider_query_adapters.py`·`tests/test_sanitized_live_snapshot.py`·`tests/test_r1_bounded_live_breadth_run.py`·`tests/test_internal_ops_api.py`; frontend 3: `lib/api/types.ts`·`lib/ops/opsPilotExecutionView.ts`·`lib/ops/__tests__/opsPilotExecutionView.test.mjs`; docs 7: `PROJECT_STATUS`·`_DECISIONS/2026-06`·`_RISK/RISK_REGISTER`·`2_ROADMAP/00·15·19`·`_CANONICAL/02`). **전부 additive(가산 knob default 경로 param 순서·값 byte-identical=ADR#62~#84 동작 보존·신규 모듈은 network 0/PURE·frontier 32필드 무변경+6 추가·migration 0)·EVENT_SCHEMA/main.py/config/.env/.env.example/package.json/cross_source_live_overlap_smoke.py/live_query_target.py/reviewer_handoff_bridge.py 무변경.**
- 열린 RISK: **종결 0·신규 2**(R-ControlExperimentRateBudget[LOW]·R-WindowHonoringSourceScarcity[MEDIUM]). 부분진전: R-ProviderDateWindowFidelity(통제실험 실행·order/q 제거·date_filter_ignored medium)·R-ProductionCandidateScarcity·R-LiveRunReproducibility. throwaway/stray(`.harness/_TRASH/`·`frontend/.harness/`·`--paths/`·`--help/`)·`outputs/`(gitignore)·scratchpad(저장소 밖)는 커밋 제외.

## ✅ 이번 턴에 달성한 것 (provider date-window fidelity 통제실험 + window-honoring readiness — ADR#85)
- **① 분석(§4·구현 전 원자 분해)**: 호출 체인을 끝까지 추적 — `run_bounded_live_breadth_run → execute_date_pinned_bounded_live_run(enforce_window=True) → cross_source_live_overlap_smoke → run_provider_query → _guardian_url/_nyt_url`. **코드 발견**: 두 URL builder 가 `order-by=newest`(guardian)·`sort=newest`(nyt)를 **하드코딩** → newest-지배 가설의 1순위 구조 정황. GDELT/Federal Register endpoint 는 registry `_SERVICE_CONFIGS` 에 **이미 존재**(adapter 잔여=url+parser+등록). GDELT 는 `rate_limit_policy.yaml` min_interval 60s·실측 429 storm(rate-fragile).
- **② 가산 knob(§6·byte-보존)**: `provider_query_adapters.py` 에 `omit_date_window`(request URL 에서 date param 제외)·`order`(newest override) 가산. **default 경로 param 순서·값 byte-identical**(test_provider_query_adapters +5 잠금). 이 knob 으로 통제실험 variant 구성.
- **③ 통제실험 harness(§8)**: 신규 `provider_date_window_fidelity.py` — 5-variant(original/no_date/relevance_order/exact_phrase/enforce_window)·effect 분류(date_param/order_newest/query_relevance/coverage 각 weak/partial/strong/untested)·가설 목록(hypothesis+confidence+signal)·**메커니즘 분류 confidence low/medium·절대 high 금지**·result class 분리(in_window_related/unrelated/out_of_window_only/no_records/gated)·`pace_seconds` 사전 대기(host min_spacing 을 **bypass 아니라 정직 준수**)·aggregate-only(제목 전문·raw body·score 0)·transport 주입 시 결정론(test 8).
- **④ window-honoring readiness(§9)**: 신규 `window_honoring_source_readiness.py`(PURE·network 0) — GDELT/Federal Register/SEC EDGAR 를 date_filter 능력·의미론·confidence·rate/attribution risk·role·cost 로 평가 → **Federal Register 권고**(key-free·official·explicit `publication_date[gte/lte]`·confidence high)·GDELT 보류(rate-fragile+aggregator attribution)·source role guard 보존·`adapter_wired_this_turn=False`(test 7).
- **⑤ snapshot/frontier(§11·§12)**: snapshot 에 `control_experiment` block(메커니즘·effect·counts·variant→class·aggregate·제목 0)·`InternalOpsDatePinnedLiveRunFrontier` **+6 필드**(`provider_date_window_fidelity_status`·`control_experiment_status`·`date_filter_mechanism_primary`·`date_filter_mechanism_confidence`·`out_of_window_records_dropped`·`window_honoring_source_status`) **dict==pydantic==TS==mjs 38==38==38==38**·API allowed 38·required_copy +2(date param 신뢰 금지·out-of-window 후보 불가).
- **⑥ 실 통제실험 실행(§8·paced·gate-respected)**: Guardian 5-variant·NYT 4-variant **전부 실행**(secret 0·raw body 0·host floor 사전 대기 준수·11 governed 호출). **전 비교 variant 동일**(반환 전부 2026-06-29·요청 window 밖·overlap 0.107/0.044·in-window 0): date_param **weak**·order_newest **weak**·query_relevance **weak**·coverage zero_in_returned → **order-by=newest·loose-q 약화(이 run 비차별 lever·newest-dominance 미배제)**·**`date_filter_ignored` leading(medium·cross-provider)**·zero_in_window_coverage 잔존(low·미분리). enforce_window 전부 drop→comparison_pair_count 0→`blocked_no_live_overlap`·freeze 0·gold 0·gap 200.
- **⑦ 신규 blocker 발견+보정**: **R-ControlExperimentRateBudget** — shared host_rate_gate min_spacing(guardian 5s·nyt 12s)이 빠른 multi-variant probing 을 `host_min_spacing_not_elapsed` 로 차단해 첫 run 의 variant 2~5 가 not_run·분해 무산. 보정=`pace_seconds` 사전 대기(gate **bypass 아님**·no tight retry·sleep_fn 주입 test 결정론) → 전 variant 실행 가능.

## 🧭 cross-batch identity 4단계 + 평가/gold gate (정직)
- ① anchor 병합(ADR#40) → ② 후보 LINK(ADR#41) → ③ shadow 판정(ADR#42·…·date-pinned live query plumbing ADR#83·첫 실 date-pinned live run+date-window fidelity 보정 ADR#84·**date-window fidelity 통제실험 메커니즘 분해 ADR#85**) → ④ 실 병합(**미구현**).
- **ADR#85 위치 / 핵심**: ADR#84 가 남긴 미확정 메커니즘을 통제실험으로 분해한 턴 — 4 가설 중 order-by=newest·loose-q 를 **약화(이 run 비차별 lever·newest-dominance 미배제)**하고 `date_filter_ignored`(medium·cross-provider)로 좁힘(잔존 zero_coverage low·미분리). enforce_window 가 out-of-window 전부 drop → comparison 0 → freeze 0. near=reviewer 라우팅이지 merge 가 아니므로 통제실험도 false-merge 위험을 늘리지 않음(merge=fingerprint+gold+MERGE_GATE 불변). **same-event 단정 0·freeze 0·gold 0.**
- **⚠ 미해소(OPEN):** date_filter_ignored vs zero_coverage **최종 분리**(window-honoring control 부재)·**window-honoring source 배선**(Federal Register adapter ADR#86·official×news role-bridge)·실 production candidate freeze(0)·실 reviewer contact·첫 returned labels·실 gold(production_gold 0)·R1 gold floor(live 200·KO 50·pos/neg 67·hard-neg 20)·MERGE_GATE 런타임·KO 캘리브레이션(KO gold 0)·운영 DB 0009 배포·RAG/KG/Entity/public IU(R1~R7)·internal ops per-user auth·UI 화면 render. "통제실험 실행 ≠ 메커니즘 완전 확정·메커니즘 분해 ≠ production candidate 확보·date-pin ≠ 발생 증명·freeze ≠ truth·routing recall ≠ merge·confidence medium ≠ 확정" — 완전종결=OVERCLAIM.

## 🔎 source orchestration / 제품 계약
- official/news=publishable evidence(merge anchor)·community=반응(anchor 불가)·market=signal·catalog=entity·search=URL candidate(truth 아님)·unknown=fail-closed. **ADR#85 통제실험·readiness 는 모두 source role guard 보존**(window_honoring_source_readiness 후보=official/news role 만·community/market/catalog/search anchor 승격 0·FR=official 이라 news 와 자동 pairing 불가→role-bridge ADR#86)·통제실험은 publishable×publishable(guardian/nyt=article)만·`enforce_window` 로 window 밖 record drop.
- **제품 계약(raw≠public)**: 출력·`internal_ops_date_pinned_live_run_frontier`(sanitized·**38필드**) 노출 표면에 score/rationale/predicted_status/raw body/secret/raw reviewer 신원·same_event truth·**named_entity/event_phrase 전문** **0** — 출력 전 `_assert_pii_safe` 재귀 가드·API `response_model`(sanitized·38필드)·frontend `FORBIDDEN_OPS_FIELDS` 재귀 가드. credential present/missing boolean만(값 0). **통제실험 출력 aggregate-only(제목 전문·per-pair score 0)·snapshot control_experiment 는 variant→result_class 만(제목 0)·메커니즘은 confidence 와 함께(단정 0)**. production_gold_count 0·gate exact passthrough·`merge_allowed=False`·embedding/LLM 실호출 0·DB write 0. Agent 는 date-window fidelity·window-honoring source·freeze readiness·reviewer handoff·snapshot·KO lane 을 *계획* 가능하나 date param 을 검증 전 신뢰·통제실험 실행을 메커니즘 확정으로·메커니즘 분해를 production candidate 로·out-of-window 기사를 same-event 쌍으로·freeze 를 truth 로 사용 불가·merge·secret 생성·PII 출력·실 전송 불가.

## ⚠️ 이번 턴 종결/갱신 RISK
- **신규 2(실 present blocker·테스트 차단)**: **R-ControlExperimentRateBudget**(LOW·host gate min_spacing 이 multi-variant 통제실험을 차단·완화=`pace_seconds` 사전 대기·gate bypass 금지·종결 금지) · **R-WindowHonoringSourceScarcity**(MEDIUM·Guardian/NYT date_filter_ignored 인데 즉시 쓸 window-honoring source 부재·FR=official role-bridge 필요·GDELT rate-fragile/aggregator·완화=readiness 평가+FR ADR#86 권고·종결 금지).
- **부분진전(종결 0)**: **R-ProviderDateWindowFidelity**(통제실험 **실행**·order-by=newest·loose-q **약화(이 run 비차별 lever·newest-dominance 미배제)**·`date_filter_ignored` medium 으로 좁힘·단 date_filter_ignored vs zero_coverage 미분리·confidence medium≠확정·종결 금지) · **R-ProductionCandidateScarcity**(enforce_window 후 comparison 0→freeze 0·gold 0·종결 금지) · **R-LiveRunReproducibility**(paced snapshot 재작성·gitignored·종결 금지).

## ❌ 달성하지 못한 것 & 왜 (이월)
- **메커니즘 완전 확정**: date_filter_ignored(medium·leading)와 zero_in_window_coverage(low)를 **완전 분리 못 함** — 단일 event·window-honoring control 부재. 해소=Federal Register adapter(date-honoring source 로 control) 또는 in-window 보도가 실재하는 event 로 재실행.
- **production candidate freeze / 실 gold**: enforce_window 가 out-of-window 전부 drop → comparison_pair_count 0 → cross-source same-event 후보 0 → freeze 0·`production_gold_count=0`. 해소=window-honoring source(ADR#86)→live-derived publishable freeze→reviewer contact→actual returned labels.
- **provider 확장(GDELT/Federal Register) 실배선**: readiness 평가·**FR 권고+정밀 spec**(ADR#86)만·이 턴 미배선(`adapter_wired_this_turn=False`). FR=official role 이라 news 와 cross-source pairing 에 role-bridge 정책 필요·GDELT 는 rate-fragile(60s·429 storm)+aggregator attribution.
- **adjudicator runtime / RAG·KG·Entity·public IU**: contract guard(docs)일 뿐 runtime 미구현(embedding/LLM 실호출·실 병합·운영 DB·GraphRAG·public IU·KG edge 이번 턴 No-Go·옵션 I 금지).

## 🚧 닫을 수 없는 문제 (BLOCKED/UNKNOWN)
- BLOCKED: 실 production candidate = live-derived **in-window** near-match publishable pair 확보(현재 0·date_filter_ignored→Guardian/NYT 로는 in-window 0·R-WindowHonoringSourceScarcity). 실 gold = 실 후보→reviewer recruitment→실제 contact→첫 returned label. 실 병합 = gold-calibrated adjudicator(R3)+MERGE_GATE(R2).
- UNKNOWN: **Guardian/NYT 가 date param 을 무시하는 근본 이유**(date_filter_ignored leading·medium·단 zero_coverage 와 미분리·R-ProviderDateWindowFidelity Closure 조건)·**Federal Register adapter 가 date-honoring in-window cross-source 수율을 낼지**(미테스트·official×news role-bridge 미설계)·**date_filter_ignored 가 API 측 어떤 원인인지**(param 명 오류 vs archive 한계 vs API 버그·미규명). 실 reviewer 회수율/SLA(plan·machinery만).

## 👉 다음 할 일
1. **[커밋 대기]** 본 턴 ADR#85: 신규 4·수정 약 18(backend 8·frontend 3·docs 7) 미커밋. 커밋 여부 지시 대기. push 금지. (`--paths/`·`--help/` stray 정리 허가도 함께 받으면 처리.)
2. **[다음 단계·ADR#86]** (a) **Federal Register window-honoring adapter 배선**(`run_provider_query` url+parser·key-free·official·explicit publication_date 범위·정밀 spec=`19_WEB_INTELLIGENCE_IMPLEMENTATION_SPEC`) + **official×news role-bridge**(guard 약화 금지) → date_filter_ignored vs zero_coverage **최종 분리** + same-event 수율 실측. (b) live-derived production candidate freeze → (c) **reviewer recruitment + actual contact + 첫 returned labels**(R1) → (d) gold denominator(live 200·KO 50·**R1**) → (e) MERGE_GATE(precision≥0.98·**R2**) → (f) embedding scorer opt-in(**R3**) → (g) entity/KG/GraphRAG(**R4~R6**) → (h) Agent/public IU(**R7**). 별도 축: internal ops UI 화면 render·per-user auth.
3. **[권장·bounded]** live acquisition 실험은 **bounded·opt-in·rate governed(pace 사전 대기·no-bypass)·operator 확인 date-pinned event·enforce_window** 일 때만(무단 대량 호출 0·source role guard 유지·out-of-window 기사 same-event 사용 금지·메커니즘 confidence high 단정 금지).

## 📁 근거 (이번 턴 핵심)
- 커밋: `b8443b8`(ADR#84·push 0·NO UPSTREAM). ADR#85 변경: 신규 4(backend tool 2+test 2)·수정 약 18(backend 8·frontend 3·docs 7). **ADR#84 커밋분 frozen·재구현 0·EVENT_SCHEMA/main.py/config/.env/.env.example/package.json/cross_source_live_overlap_smoke.py/live_query_target.py 무변경·전부 additive(가산 knob default 경로 byte-identical).**
- **실 date-window fidelity 통제실험(paced·gate-respected·실 live)**: Guardian 5-variant(original/no_date/relevance_order/exact_phrase/enforce_window)·NYT 4-variant·executor 2 = **11 governed 호출**. 결과: 전 비교 variant 동일(반환 전부 `2026-06-29`·요청 window [06-25,06-26] 밖·`in_window=0`·overlap `0.107`(guardian)/`0.044`(nyt))·effect `date_param=weak`·`order_newest=weak`·`query_relevance=weak`·`coverage=zero_in_returned` → **mechanism_primary=`date_filter_ignored`·confidence=`medium`**(가설 목록: date_filter_ignored medium + zero_in_window_coverage low)·`out_of_window_records_dropped=10`·`no_in_window_records=True`. orchestrator frontier: `control_experiment_status=executed`·`comparison_pair_count=0`·`production_candidate_status=blocked_no_live_overlap`·`reviewer_handoff_ready=False`·`window_honoring_source_status=federal_register_recommended_adr86`·sanitized snapshot written(gitignored). **secret 0·raw body 0·host/rate 사전 대기 준수·merge/llm/db_write/sending/score 0.**
- 검증(정직): 신규 backend 테스트 **+22**(provider_query_adapters 가산 knob 5·provider_date_window_fidelity 8·window_honoring_source_readiness 7·sanitized_live_snapshot control_experiment 2)·**backend 전체 1705 passed / 101 skipped / 0 failed**(ADR#84 1683→+22)·ingestion 무변경(1353 passed)·frontend **tsc 0**·**lint 0**·node:test **76 passed**(date-pinned view +1: 75→76)·ruff 실게이트(E/F/I/B/W−E501) **0**·secret scan **PASS**(5 files·0 findings·credential present/missing boolean만)·docs_lifecycle conflicts **0**. dict==pydantic==TS==mjs **38==38==38==38**(date-pinned frontier·+6).
- 감사(closeout subagents): architecture/pipeline/actual_input_gate/provider_date_window_fidelity/control_experiment/enforce_window/window_honoring_source/provider_adapter/bounded_live_run/live_result_classification/sanitized_snapshot/production_candidate_freeze/reviewer_handoff_bridge/korean_source_lane/community_reaction_boundary/source_role_guard/acquisition_frontier/credential_secret_boundary/internal_ops_boundary/public_truth_boundary/no_merge/no_llm_embedding/rag_kg_entity_readiness/code/test/evidence/risk_closure/adversarial review. cardinal(통제실험 실행을 메커니즘 확정으로 둔갑 금지·메커니즘 분해를 production candidate 확보로 둔갑 금지·date param 을 검증 전 신뢰 금지·out-of-window 기사를 same-event 쌍으로 사용 금지·freeze 를 truth 로/gold 로 둔갑 금지·confidence high 과대주장 금지·synthetic 을 production candidate 로 사용 금지·MERGE_GATE 전 merge 금지·LLM/embedding 실호출 금지·raw body 저장 금지·`.env` 값 출력 금지·score/rationale/predicted_status reviewer/public 노출 금지·reviewer PII 노출 금지·실 전송 금지·public IU 생성 금지·RISK 과대 종결 금지·gate bypass(pace 단축) 금지·push 금지) 차단 확인.
- 문서: ADR#85(`_DECISIONS`)·RISK(`_RISK`·종결 0·신규 2·부분진전 3)·ADR#85 블록(`2_ROADMAP/15` descending·ADR#84 앞)·ADR#85+ADR#86 spec(`2_ROADMAP/19`)·`2_ROADMAP/00`(ADR 목록 +#85)·`_CANONICAL/02`(ADR#41~#85). **3_SOURCE_STATUS 부재(ABSENT)·active=`_CANONICAL/03_SOURCE_STATUS.md`·EVENT_SCHEMA/LLM_EVIDENCE_PACKET_CONTRACT 무변경**(ADR#85 DB-free·runtime 0·통제실험/readiness 는 acquisition 진단이지 event schema 필드 아님).

---
_as_of: 2026-06-29 · ADR#85 provider date-window fidelity 통제실험(메커니즘 분해) + window-honoring source readiness + live-derived freeze 재시도 — ADR#84 커밋(`b8443b8`) 후, ADR#84 가 남긴 미확정 메커니즘을 **통제실험으로 분해**. operator event 동일(U.S. Supreme Court asylum metering·2026-06-25·[06-25,06-26])·5-variant(original/no_date/relevance_order/exact_phrase/enforce_window)·`pace_seconds` host floor 사전 대기(gate bypass 아님). **실 live**: Guardian 5+NYT 4+executor 2=11 governed 호출·secret 0·raw body 0. **핵심 결과**: 전 비교 variant 동일(반환 전부 2026-06-29·요청 window 밖·주제 무관·in-window 0) → date_param/order_newest/query_relevance 모두 weak → **order-by=newest·loose-q 약화(이 run 비차별 lever·newest-dominance 미배제)**·**`date_filter_ignored` leading(medium·cross-provider Guardian+NYT)**·zero_in_window_coverage 잔존(low·미분리). enforce_window 전부 drop→comparison 0→freeze 0. 신규 `provider_date_window_fidelity.py`(통제실험·confidence high 금지)·`window_honoring_source_readiness.py`(Federal Register 권고·role guard 보존)·가산 knob(omit_date_window/order·byte-보존)·snapshot control_experiment block·frontier +6 dict==pydantic==TS==mjs 38. **측정**: backend +22(1705p/101skip/0fail)·ingestion 1353p·frontend tsc0/lint0/test76·secret PASS(5)·docs_lifecycle 0·ruff 0. **정직 경계**: 통제실험 실행 ≠ 메커니즘 완전 확정·메커니즘 분해 ≠ production candidate 확보·date_filter_ignored vs zero_coverage 미분리·confidence medium≠확정·freeze 0·production_gold 0·gap 200·adapter 미배선(ADR#86)·gate exact passthrough·adjudicator runtime/public IU/merge/운영 DB/embedding·LLM 실호출/실 전송 0·완전종결=OVERCLAIM. 다음 hard blocker=Federal Register adapter(ADR#86)/in-window event→date_filter_ignored vs zero_coverage 분리→live-derived freeze→실 reviewer contact→actual returned labels(R1). **신규 R-ControlExperimentRateBudget(LOW)·R-WindowHonoringSourceScarcity(MEDIUM)**·종결 0. ADR#84 커밋 `b8443b8` 위 ADR#85 미커밋(신규 4·수정 약 18)·커밋 지시 대기·push 안 함._
