# 📊 프로젝트 진행 현황 (PROJECT_STATUS)

> 매 턴 끝에 새로 쓰입니다. 항상 "가장 최근 1턴"만. 과거는 `git log -- PROJECT_STATUS.md`.
> 사실 원본(자동): `.harness/machine_status.json` · 완료 증거: `.harness/closeout_stamp.json` · 서술: 이 파일(에이전트).

## 🟢 한눈에 (비개발자용 3줄)
- **지금 무엇을 했나:** 지난 턴(ADR#63 — Guardian 실 키 live 경로 실증)을 안정 기준점으로 **커밋**(`e3cfba9`)한 뒤, ADR#63 이 발견한 "**단일 소스(Guardian)에선 같은 사건 중복 기사 overlap 이 0**" 라는 구조적 한계를 풀기 위해 **2번째 발행 소스(NYT)를 secret-safe 하게 배선**하고 **Guardian+NYT 를 같은 주제·기간으로 동시에 호출해 cross-source 겹침을 실측**했습니다. 키 값은 **어디에도 출력/기록하지 않습니다**.
- **이번 턴에 실제로 끝낸 것:** ADR#63 커밋(`e3cfba9`·11파일) → **ADR#64**: `provider_query_adapters.py` 에 **NYT 어댑터** 추가(Guardian 과 같은 `api-key` 방식 → 기존 호출 코드 무수정 재사용) + 새 모듈 `cross_source_live_overlap_smoke.py`(**두 소스가 모두 성공해야만 cross-source 로 인정**·한 소스만 성공하면 단일 소스 둔갑 금지). 신규 **39 테스트**. **실 Guardian+NYT 4회 호출**(Ukraine/earthquake 7d)→각 **Guardian 10건+NYT 10건=20건**·**cross-source 쌍 100개 생성**.
- **정직한 한계:** 단일 소스에선 구조적으로 0이던 cross-source 쌍이 **100개로 실제 생성**됐지만, 결정론적 제목-단어 겹침(title-Jaccard) 검출기는 그 100쌍에서 **0건 검출**(약한 임계 0.2까지도 0). 이 0은 (i) 검출기의 paraphrase 한계 **또는** (ii) 두 매체가 같은 토픽의 **다른 사건**을 보도(real scarcity)일 수 있어 **2개 토픽으론 단정 못 합니다**. 수집/소스/키 능력은 입증(단일 0→cross 100)됐고, 다음 방향은 의미 기반 판정기(embedding/LLM)이며 그 방향은 기존 결정론 정확도(precision 0.57<기준 0.98)라는 **별도 근거**로 정당화됩니다. 검수 큐 충원 0·reviewer/gold 0·자동 병합·LLM·운영 DB **0** 불변. **push 금지·커밋 지시 대기**.

## 📋 자동 수집 사실 (machine_status.json)
- **git HEAD `e3cfba9`**(ADR#63, 본 턴 첫 커밋) 위 **ADR#64 = 미커밋**: 신규 2(`backend/app/tools/cross_source_live_overlap_smoke.py`[cross-source smoke] + `backend/tests/test_cross_source_live_overlap_smoke.py`[30 테스트]) + 수정 11(`backend/app/tools/provider_query_adapters.py`[NYT adapter] + `backend/tests/test_provider_query_adapters.py`[NYT 9] + `backend/tests/test_provider_readiness.py`[test_49 정정] + docs 7[`2_ROADMAP/00·15`·`_RISK/RISK_REGISTER`·`5_REFERENCE/RAG_KG_AGENT_READINESS·INTELLIGENCE_UNIT_CONTRACT`·`_CANONICAL/02`·`_DECISIONS/2026-06`] + `PROJECT_STATUS`) = **추적 13항목**. **migration 없음·ADR#63 커밋분(`guardian_live_query_smoke.py`·`env_loader.py`) 미수정(frozen·단방향 import·재구현 0).**
- 열린 RISK: **total 36**(HIGH 3·MEDIUM 21·LOW 11). **신규 0·종결 0.** 부분진전 6: R-SourceOverlapScarcity·R-RealSourceLoopUnproven·R-LiveIdentityBacklog·R-IdentityHumanLabeling·R-SemanticIdentityAdjudicator·R-Gdelt429. throwaway 는 `.harness/_TRASH/`·`frontend/.harness/`(커밋 제외).

## ✅ 이번 턴에 달성한 것 (2nd publishable provider + cross-source live overlap smoke — ADR#64)
- **① ADR#63 커밋**: 11파일 → `e3cfba9`(secret PASS·docs_lifecycle conflicts 0·SIGNATURE MATCH 14-entry·unresolved 0·push 0).
- **② 원자 분석(§2·20문항·구현 전 보고)**: Guardian candidate 0=`no_title_overlap`(단일-source 구조)·2nd provider=**NYT**(영문 quality **단일 publisher**·aggregator(newsapi/gnews)는 Guardian 자기기사 재유입=same-source 오염→배제)·env=`NYT_API_KEY`(`.env.example:233`)·**auth=`api-key` param=Guardian 동일→`run_provider_query` 무수정 재사용**(최소 변경)·**다음 hard blocker=embedding/LLM adjudicator**(deterministic title-Jaccard paraphrase 약).
- **③ A — NYT adapter wire**: `NYT_ADAPTER`(key-required·news·topic+time_window)+`_nyt_url`(begin_date/end_date **YYYYMMDD**·대시 제거·**keyless**)+`parse_nyt_items`(headline.main/web_url/pub_date→`_rec`·본문 미저장). `api-key` param=Guardian 동일 → fetch/게이트/host gate/rate-limit 전부 generic 재사용.
- **④ B — cross-source live overlap smoke**: `cross_source_live_overlap_smoke.py`(얇은 composition) — §4 cross-source 계약(providers·credential_status_by_provider·**credential_value_exposed=False·env_file_read=False**·provider/host_gate/rate_limit_status_by_provider·records_count_by_provider·**cross_source_pair_count**·fingerprint/near/hard/queue_pop·dataset_source_by_provider·block_reasons·next_actions·production_gold 0·merge_allowed False·db_write False·llm 0).
- **⑤ 둘 다 성공만 cross-source 인정**: provider_a(guardian)만 성공·provider_b 실패→**single-source success 둔갑 금지**(`no_records_provider_b` 등·queue 0). 양 credential 부재→per-provider `env_not_loaded`(.env 부재)/`missing_credentials`(키 부재·network 전·값 미열람).
- **⑥ cross-source 전용 queue 충원**: near/hard pair 를 **source_id 상이(cross-source)만** 필터해 ADR#59 `build_near_match_reviewer_queue` 충원 — same-source(guardian↔guardian·nyt↔nyt) 오염을 **구조적으로 차단**. candidate 0 분해: no_cross_source_overlap/no_title_overlap/no_near_match.
- **⑦ 실 cross-source live overlap 실증(4회)**: opt-in·bounded·governed·secret-safe — topic Ukraine/earthquake 7d → 각 **Guardian 10+NYT 10=combined 20·`cross_source_pair_count` 100·fingerprint 0·near 0·hard 0·`no_title_overlap`·dataset_source live_derived·secret_exposed False·env_file_read False·host_gate passed**. → **cross-source pair 100 생성**(단일 소스 구조적 0 대비) but **deterministic 검출 0**.
- **⑧ D/E/F 경계**: 3rd provider(D)=docs/schema only·embedding/LLM adjudicator(E·No-Go)·production DB(F·session 인자 부재·write 0) 미착수.

## 🧭 cross-batch identity 4단계 + 평가/gold gate (정직)
- ① anchor 병합(ADR#40) → ② 후보 LINK(ADR#41) → ③ shadow 판정(ADR#42·운영 배선 ADR#48~#53·preflight/smoke ADR#54·live-db ADR#55·replay ADR#56·overlap discovery ADR#57·acquisition ADR#58·near-match 큐 ADR#59·targeted readiness ADR#60·provider readiness gate ADR#61·provider query adapter+Guardian wired ADR#62·secret-safe live query smoke 실증 ADR#63·**2nd provider(NYT)+cross-source live overlap smoke ADR#64**) → ④ 실 병합(**미구현**).
- **ADR#64 위치**: 단계 ①(source fetch)의 **cross-source 확장**. ADR#63 이 실증한 단일 소스 한계(overlap 0)를 NYT 배선+cross-source smoke 로 직접 검증 — cross-source pair 100 이 ADR#59 운영경로로 들어갈 수 있으나 deterministic title-Jaccard 0 검출이라 near-match queue 충원 0(병합·단정 0).
- **⚠ 미해소(OPEN):** **deterministic cross-source overlap 0**(cross-source pair 100 생성·실 fetch 성공이나 title-Jaccard 가 paraphrase 0 검출) → 실 near-match 후보·reviewer/gold 0·embedding/LLM 실 병합(No-Go)·운영 DB 0009 배포·MERGE_GATE·한국어 캘리브레이션. "cross-source 능력·pair 생성 ≠ deterministic 검출·후보" — 완전종결=OVERCLAIM.

## 🔎 source orchestration / 제품 계약
- official/news=publishable evidence(merge anchor)·community=반응 evidence(anchor 불가)·market=signal·catalog=entity·search=URL·unknown=fail-closed. Guardian·NYT=news(publishable). 어댑터 records 는 publishable×publishable 만 reviewer 후보.
- **제품 계약(raw≠public)**: cross-source smoke 는 Intelligence Unit 입력(실 near-match 후보)을 만들 **cross-source 수집 경계** — 본문 미저장(title+canonical+published 만)·public IU 미생성·실 records 가 와도 same-event 확정은 reviewer/gold/MERGE_GATE 밖에서 금지(`merge_allowed=False`·`no_public_intelligence_unit`·`llm_invoked=False`). Guardian/NYT 무료 tier 비상업·full-content 재배포 금지 ToS 경계 내(본문 미저장). Agent 는 cross-source provider readiness/source-pair/queue population 을 *계획* 가능하나 provider secret 추측/생성·`.env` 값 읽기·merge·public IU 불가. LLM/Agent 진입 9조건 여전히 No-Go(1·4·5·7).

## ⚠️ 이번 턴 종결/갱신 RISK
- **부분진전(종결 0·신규 0·total 36 불변)**: **R-SourceOverlapScarcity**(**실 cross-source pair 100 입증·단일 소스 0→cross-source 100**·단 deterministic 검출 0=embedding 필요 **실 데이터로 확정**) · **R-RealSourceLoopUnproven**(2nd publishable provider 실 fetch·DB/merge 0) · **R-LiveIdentityBacklog**(cross-source queue 경로 실 records 로 입증·단 deterministic 0 으로 queue_pop 0) · **R-IdentityHumanLabeling**(packet 경로 유지·gold 0) · **R-SemanticIdentityAdjudicator**(**다음 hard blocker 로 실데이터 확정**·No-Go 유지) · **R-Gdelt429**(Guardian+NYT 2 publishable 실 fetch·GDELT 단일 의존 완화).
- 디렉티브 신규 RISK 후보(R-CrossSourceProviderCoverage/R-SecondProviderCredentialReadiness/R-CrossSourceLiveSmokeReliability)는 **독립 blocker 아님** → **R-SourceOverlapScarcity**·**R-Secret**(HIGH·secret hygiene 이미 mitigated·probe_env_var 값 미열람·httpx params·url keyless)에 포섭(RISK 남발 금지).

## ❌ 달성하지 못한 것 & 왜 (이월)
- **deterministic cross-source overlap**: cross-source pair 는 **단일 소스 0→100 으로 실증 생성**됐으나 **deterministic title-Jaccard(near 0.5)가 100 쌍 중 0 검출**(Guardian/NYT 가 같은 사건도 각자 헤드라인 작성→token-set 불일치·`no_title_overlap`). → candidate 0·queue_pop 0. **다음 unblock=embedding/LLM semantic adjudicator(옵션 E·gated·다음 턴·이번 턴 금지)**.
- **실 human-reviewed gold**: cross-source near-match 후보가 deterministic 으로 안 잡혀 큐가 안 채워짐 → reviewer 충원·gold 0(production_gold 0).
- **embedding/LLM adjudicator**: gold calibration+MERGE_GATE 후에만(No-Go·호출 0·precision 0.57<0.98). 운영 DB·production 백로그 0(옵션 F 금지).

## 🚧 닫을 수 없는 문제 (BLOCKED/UNKNOWN)
- BLOCKED: deterministic cross-source overlap = **embedding/LLM semantic adjudicator**(옵션 E) 선결 — cross-source pair 는 생성되나(100×2 topic) deterministic title-Jaccard 가 paraphrase 0 검출. 실 reviewer/gold = reviewer 충원·운영 절차 선결. production 백로그 = 운영 DB 0009 배포(승인) + 실 fetch 볼륨.
- UNKNOWN: paraphrase overlap 의 실 병합 허용 기준(production precision·실 gold·reviewer 합의·한국어 캘리브레이션·MERGE_GATE) → R-SemanticIdentityAdjudicator·R-IdentityHumanLabeling·R-ReviewerAgreement·R-IdentityEvalDataset 선결.

## 👉 다음 할 일
1. **[커밋 대기]** 본 턴 ADR#64: 신규 2(cross_source_live_overlap_smoke.py+test) + 수정 11(provider_query_adapters.py·test_provider_query_adapters·test_provider_readiness·docs 7·PROJECT_STATUS) = 13파일 커밋 여부 지시 대기. push 금지.
2. **[다음 단계]** (a) **embedding/LLM/KG semantic adjudicator**(cross-source pair 는 생성됐으나 deterministic 검출 0 — paraphrase 를 묶을 의미 기반 판정·단 gold/MERGE_GATE·adversarial validation 필수) → (b) 실 reviewer 충원·합의 gold + 한국어 캘리브레이션 → (c) MERGE_GATE 런타임 배선 → (d) (승인 하) 운영 DB 0009 배포 + scheduler 가동 → RAG/KG. (3rd provider 는 coverage 확대용·옵션 D=docs only.)

## 📁 근거 (이번 턴 핵심)
- 커밋: `e3cfba9`(ADR#63·11파일). ADR#64 변경: 신규 `cross_source_live_overlap_smoke.py`(cross-source smoke)+`test_cross_source_live_overlap_smoke.py`(30)·`provider_query_adapters.py`(NYT adapter)·`test_provider_query_adapters.py`(NYT 9)·`test_provider_readiness.py`(test_49)·docs 7. **ADR#63 커밋분 미수정(frozen·단방향 import·재구현 0).**
- 검증(정직): **실 Guardian+NYT 호출 4회**(2 topic×2 provider·opt-in·bounded·governed·secret-safe·Ukraine/earthquake 7d)→각 Guardian 10+NYT 10=combined 20·`cross_source_pair_count` 100·fingerprint 0·near 0·hard 0·`no_title_overlap`·dataset_source live_derived·secret_exposed False·env_file_read False·host_gate passed = **cross-source pair 100 생성 + deterministic 검출 0**. fake transport: g=[WIRE,PARA]·n=[WIRE,DIFF]→cross fingerprint 1·near 1·hard 2·queue_pop 3·live_derived. single-source 둔갑 차단(no_records_provider_b→queue 0). 테스트: 신규 **39**(cross-source smoke 30·NYT adapter 9; test_49 정정 1)·backend 비-live **853 passed**(814+39 additive)·live-PG **94 passed**(write-free·무영향)·full `backend/tests` DB-up **947 passed/4 skipped/0 failed**·ingestion **1353 passed**·frontend tsc**0**/node:test **12**/lint **0**·secret scan **PASS**·docs_lifecycle conflicts **0**·ruff 실게이트(E/F/I/B/W−E501) **0**. 운영 DB 무변경.
- 감사: adversarial-reality-critic + code-review(stamp `audit_evidence` 단일 출처). 핵심 안전축(둘 다 성공만 cross-source·single-source 둔갑 차단·cross-source 전용 queue 필터·실 호출에서 credential_value_exposed/env_file_read False·실 records→live_derived 정직·deterministic 검출 0 정직 노출·no merge/LLM/DB).
- 문서: ADR#64(`_DECISIONS`)·6 RISK 부분진전(`_RISK`·종결 0·신규 0)·cross-source live overlap smoke runbook+ADR#64 블록(`2_ROADMAP/15`)·(11v)(`2_ROADMAP/00`)·agent readiness/Agent 역할(`RAG_KG`)·cross-source smoke 계약(`IU_CONTRACT`)·`_CANONICAL/02`(ADR#41~#64 + cross-source).

---
_as_of: 2026-06-26 · ADR#64 2nd publishable provider adapter(NYT) + cross-source live overlap smoke — ADR#63 커밋(`e3cfba9`) 후, ADR#63 이 실증한 단일 소스 same-event overlap 0 한계를 **2번째 publishable provider(NYT) 배선 + cross-source 실측**으로 해소(새 파일 1·adapter 1·ADR#63 커밋분 frozen·단방향 import·재구현 0): `provider_query_adapters.py` 에 `NYT_ADAPTER`+`_nyt_url`(begin/end_date YYYYMMDD·keyless)+`parse_nyt_items`(본문 미저장)·auth `api-key`=Guardian 동일→`run_provider_query` 무수정. 신규 `cross_source_live_overlap_smoke.py` — §4 cross-source 계약·**둘 다 성공만 cross-source 인정**(single-source 둔갑 금지)·near/hard pair 를 source_id 상이만 필터해 ADR#59 queue 충원·credential_value_exposed/env_file_read False. 옵션 **A+B+C 채택·D(3rd provider)=docs only·E/F 금지·신규 RISK 0**. **측정**: 신규 39·backend 비-live 853p(814+39)·live-PG 94p·full `backend/tests` DB-up 947p/4s/0f·ingestion 1353p·frontend tsc0/test12/lint0·secret PASS·docs_lifecycle 0·ruff 실게이트 0. **실 Guardian+NYT 호출 4회**(opt-in·governed·secret-safe·Ukraine/earthquake 7d→각 Guardian 10+NYT 10=combined 20·cross_source_pair_count 100·fingerprint 0·near 0·hard 0·no_title_overlap·dataset_source live_derived·secret_exposed False·env_file_read False·host_gate passed)→**cross-source pair 100 생성**(단일 소스 구조적 0 대비) but **deterministic title-Jaccard 0 검출**(hard band 0.2 까지 0)=recall 한계 또는 다른-사건(real scarcity)·n=2 미구분→다음 방향=embedding/LLM adjudicator(precision 0.57<0.98 별도 근거). **정직 경계**: cross-source 능력·pair 생성 ≠ deterministic 검출·후보(queue_pop 0·reviewer/gold 0)·embedding/LLM No-Go·운영 DB/production 백로그 0 불변(완전종결=OVERCLAIM). **R-SourceOverlapScarcity·R-RealSourceLoopUnproven·R-LiveIdentityBacklog·R-IdentityHumanLabeling·R-SemanticIdentityAdjudicator·R-Gdelt429 부분진전**·종결 0·신규 0(total 36). ADR#63 커밋 `e3cfba9` 위 ADR#64 미커밋(신규 2+수정 11=13)·커밋 지시 대기·push 안 함._
