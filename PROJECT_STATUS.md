# 📊 프로젝트 진행 현황 (PROJECT_STATUS)

> 매 턴 끝에 새로 쓰입니다. 항상 "가장 최근 1턴"만. 과거는 `git log -- PROJECT_STATUS.md`.
> 사실 원본(자동): `.harness/machine_status.json` · 완료 증거: `.harness/closeout_stamp.json` · 서술: 이 파일(에이전트).

## 🟢 한눈에 (비개발자용 3줄)
- **지금 무엇을 했나:** 지난 턴(ADR#76 — "실 live 후보를 확보하는 관문")을 안정 기준점으로 **커밋**(`00c70a9`)한 뒤, 그 관문을 **실제로 한 번 켰습니다**(ADR#77 — user 승인 live query). 실 뉴스 두 매체(Guardian·NYT)에 각 1회 안전하게 질의해 **같은 날짜의 교차 비교쌍 100쌍**을 형성했지만, **"같은 사건"으로 묶일 near-match 후보는 0**이었습니다(자격증명·접속·중첩 부족이 아님). 다만 **그 0의 원인은 이 1회로는 확정할 수 없습니다** — (i) 같은 사건을 다른 제목으로 보도(paraphrase)해 결정적 규칙이 못 잡았거나, (ii) 넓은 주제어("central bank rate decision") 아래 서로 다른 사건이 섞여 정당하게 안 겹쳤거나 — 둘 다 가능하고 이 신호만으로는 구분 못 합니다(직전 ADR#64도 "100쌍=비교대상이지 same-event match 아님·구분 불가"로 명시). 그래서 다음 관문은 "소스 부족"이 아니라 **near-match 단계의 0(원인 미확정)**이며, 좁은 주제로 재실행해 원인부터 가려내는 것이 먼저입니다. 그리고 이 상황에서 운영자에게 "소스 역할 가드를 약화하라"고 **잘못 안내하던 메시지를 교정**했습니다(단, 단정적 paraphrase로도 승격하지 않게). 자동 병합·LLM/embedding 실호출·운영 DB·키 값 출력·실제 전송·입력/라벨 날조는 모두 **0**, 외부 호출은 **딱 1회(bounded)**.
- **이번 턴에 실제로 끝낸 것:** ADR#76 커밋(`00c70a9`·20파일) → **ADR#77**: (1) `r1_production_candidate_acquisition --live-query` **1회 실행**(`live_candidate_count=100`[비교쌍]·`publishable_pair_count=0`[near-match]·`production_candidate_status=blocked_no_publishable_pairs`·root cause `no_title_overlap`·**원인 미확정**). (2) **blocked-reason 정직성 fix**(`_primary_next_action`+`_PCAND_NO_TITLE_OVERLAP_ACTION`: `live_candidate_count>0`+`no_title_overlap`이면 **원인 양가(paraphrase OR different-events·미확정)** 안내, 그 외에만 role-guard 안내)+테스트 **3종**. (3) **Lane C/D = 근거기반 최소증분**(신규 파일 0): 획득 전략·LLM/RAG/KG/IU 계약은 기존 docs(`§6b-R matrix`/`§6b-S table`/`§4`)에 이미 존재→**grounding만 추가**(§6b-R1d·§4 ADR#77 bullet). **변경 표면 = backend code 1 + test 1 + docs 9 = 11파일**(frontend·ingestion 미변경).
- **정직한 한계:** 이번 진전은 **live query 1회 실행 + cross-source near-match gap 실측(원인 미확정) + blocked-reason 정직성 fix + Lane D grounding**이지 **실 production candidate freeze가 아닙니다**(`live_candidate_count=100`은 **비교쌍**[≈10×10 Cartesian·≠same-event match]·publishable **near-match 0**·`candidate_provenance=none`·`production_candidate_batch_ready=False`). **production_gold_count 0**·실 reviewer contact·실 returned labels·실 gold 여전히 0(gate exact passthrough). **detectable same-event overlap은 여전히 0**(near/fingerprint)→overlap scarcity는 해소가 아니라 **재확인**(R-SourceOverlapScarcity closure ≥1 미충족). near-match 0의 원인이 (i) paraphrase면 해소 레버는 **gated R3 semantic adjudicator**(embedding/LLM/KG·MERGE_GATE/gold 전 No-Go), (ii) different-events면 targeted same-event 좁은 쿼리 — 어느 쪽인지 미해결. embedding/LLM 실호출·자동 병합·운영 DB·public IU 모두 **0**. internal ops UI는 read API가 `live_query=False` 고정이라 **마지막 비-live 상태**만 표시(Q10 gap·정직 표기·다음 증분). 다음 hard blocker=**near-match 0의 원인 (i)/(ii) 구분(좁은 topic/window 재실행) → 해당 lever → 실 reviewer contact → actual returned labels**. **push 금지·커밋 지시 대기**.

## 📋 자동 수집 사실 (machine_status.json)
- **git HEAD `00c70a9`**(ADR#76·본 턴 첫 커밋·20파일·+1449/−45) 위 **ADR#77 = 미커밋**: 수정 2(backend code 1: `tools/r1_production_candidate_acquisition.py`[`_primary_next_action` 분기]·backend test 1: `tests/test_r1_production_candidate_acquisition.py`[+3]) + docs 9(`2_ROADMAP/00·15·19`·`5_REFERENCE/RAG_KG_AGENT_READINESS·INTELLIGENCE_UNIT_CONTRACT`·`_CANONICAL/02`·`_DECISIONS/2026-06`·`_RISK/RISK_REGISTER`·`PROJECT_STATUS`) = **추적 11항목**. **신규 파일 0·migration 없음·ADR#76 커밋분 frozen·단방향 import·재구현 0·EVENT_SCHEMA(DB-free 무변경)·frontend/ingestion/main.py/config/.env/.env.example/package.json 무변경.**
- 열린 RISK: **종결 0·신규 2**(R-CrossSourceNearMatchGap[MEDIUM]·R-ProviderPairNarrowness[LOW]). 부분진전: R-ProductionCandidateScarcity·R-SourceOverlapScarcity·R-RealSourceLoopUnproven·R-RagKgPrematureBuild. throwaway 는 `.harness/_TRASH/`·`frontend/.harness/`·`--paths/`·`--help/`(stray·커밋 제외)·`outputs/reviewer_batch/`(gitignore).

## ✅ 이번 턴에 달성한 것 (R1 live query executed + cross-source near-match gap[원인 미확정] + blocked-reason honesty fix — ADR#77)
- **① ADR#76 커밋**: 20파일 → `00c70a9`(제공 메시지 그대로·secret PASS 0·docs_lifecycle conflicts 0·closeout SIGNATURE_MATCH 25==25·push 0·NO UPSTREAM). ADR#77 안정 기준점.
- **② 원자 분석(§2·20문항·구현 전 보고)**: `--live-query` 안전 실행 가능(opt-in+양 cred present+transport=None 시만 실 HTTP)·외부 호출=Guardian Content API+NYT Article Search 각 1회·단일 topic·1d·**governance**(`run_provider_query`가 host_gate=None이어도 rate cooldown[429 영속·no tight retry] 강제·host당 1회→min-spacing 위반 구조적 불가)·credential presence secret-safe(`build_provider_readiness_report`·이름 GUARDIAN_API_KEY/NYT_API_KEY·값 0)·UI Q10 gap(read API live_query=False 고정→마지막 비-live 상태)·**Lane D 계약 docs 기존 존재**(§6b-R/§6b-S/§4·신규 파일 불요)·docs/3_SOURCE_STATUS\* 부재 재확인.
- **③ A — actual returned labels re-check**: `run_actual_input_gate` re-check→`no_actual_input`·`external_input_required=True`·production_gold_count 0·returned/imported면 precedence(이번 턴 False). 입력 날조 0.
- **④ B — opt-in live query 실행(user 승인·bounded·secret-safe·raw body 0)**: `r1_production_candidate_acquisition --live-query` 1회 → **`live_call_performed=True`**·`providers_ready=[guardian,nyt]`·**`live_candidate_count=100`**(cross-source same-date **publishable 비교쌍**·≈10×10 Cartesian·≠same-event match)·**`publishable_pair_count=0`**(near-match 후보)·`production_candidate_status=blocked_no_publishable_pairs`·root cause **`no_title_overlap`**(near 0·fingerprint 0). **live→discover 경로 실데이터 1회 실행**(usable 산출 0)·detectable same-event overlap **0**(scarcity 재확인)·그 0의 **원인 미확정**((i) same-event paraphrase recall 한계 OR (ii) broad-topic different-events·단일 broad-topic 1회로 구분 불가·ADR#64 명시).
- **⑤ blocked-reason 정직성 fix(test-locked·adversarial 근거)**: `_primary_next_action`+`_PCAND_NO_TITLE_OVERLAP_ACTION` — `blocked_no_publishable_pairs`의 primary next_action(internal ops `next_manual_action`)을 `live_candidate_count>0`+`no_title_overlap`이면 **원인 양가(paraphrase OR different-events·미확정)** 안내(좁은 topic/window 재실행으로 구분), 그 외에만 role-guard 안내. live run이 운영자에게 "source role guard 약화" 오안내(publishable 비교쌍 100 존재)하던 결손 교정하되 **단정적 paraphrase로도 승격 안 함**. 테스트 3종(indeterminate action·role-guard fallback·helper 분기).
- **⑥ C — acquisition strategy(Lane C·docs·다음 턴 bounded·무단 대량 호출 0)**: **좁은 same-event-targeted topic/time-window 변주**(특정 breaking 사건·1d→3d/7d)로 (i)/(ii) 구분(일부 쌍이 hard band[0.2,0.5)에 드는지)·provider record depth·**(i) 확인 시에만 gated R3 adjudicator가 lever·(ii)면 targeted acquisition**·provider pair 확장은 publishable만·search=URL candidate only·community/market/catalog=anchor 0(source role guard 유지·약화 0).
- **⑦ D — LLM/RAG/Web product bridge gating(Lane D·기존 docs grounding·신규 파일 0)**: LLM evidence packet/RAG ingestion gate/KG edge eligibility/community reaction layer/public IU wireframe 계약은 **§6b-R matrix·§6b-S table·INTELLIGENCE_UNIT_CONTRACT §4에 이미 존재** → live run의 near-match 0(원인 미확정)이 §4 ④ semantic adjudicator의 **조건부((i)) 필요성을 시사**(grounding 추가·§6b-R1d·§4 ADR#77 bullet). runtime(embedding/LLM/GraphRAG/public IU) No-Go 유지.
- **⑧ E/F 경계**: embedding/LLM scorer runtime(E)·production DB·semantic merge·public IU·GraphRAG runtime(F) 미착수(이번 턴 금지).

## 🧭 cross-batch identity 4단계 + 평가/gold gate (정직)
- ① anchor 병합(ADR#40) → ② 후보 LINK(ADR#41) → ③ shadow 판정(ADR#42·…·R1 pilot batch freeze ADR#75·R1 live production candidate acquisition gate + dual-track ADR#76·**R1 live query executed + cross-source near-match gap[원인 미확정] ADR#77**) → ④ 실 병합(**미구현**).
- **ADR#77 위치 / 핵심 실측**: ②③ deterministic fingerprint·near-match title floor가 100 publishable 비교쌍에서 **near-match 0**(원인 미확정)이 실데이터로 드러남 → **(i) paraphrase**이면 **④ semantic adjudicator(embedding/LLM/KG)**가 메우도록 설계된 영역, **(ii) different-events**이면 targeted same-event 좁은 쿼리가 레버 — 어느 쪽인지 미해결. live run은 near-match 0을 **실측**할 뿐 원인 확정·④ 가동 아님(R3·MERGE_GATE/gold 전 No-Go·embedding/LLM 실호출 0·자동 same_event 0).
- **⚠ 미해소(OPEN):** near-match 0의 원인 (i)/(ii) 구분(좁은 topic/window 재실행)→해당 lever·실 production candidate freeze(near-match publishable pair·이번 턴 0)·실 reviewer contact·첫 returned labels·실 gold(production_gold 0)·R1 gold floor(live 200·KO 50·pos/neg 67·hard-neg 20)·MERGE_GATE 런타임·한국어 캘리브레이션·운영 DB 0009 배포·RAG/KG/Entity/public IU(R1~R7)·internal ops per-user auth·UI Q10(live 결과 UI 반영). "live query 실행 ≠ 실 후보·**cross 비교쌍 100 ≠ same-event overlap ≠ near-match**·production candidate ≠ event truth" — 완전종결=OVERCLAIM.

## 🔎 source orchestration / 제품 계약
- official/news=publishable evidence(merge anchor)·community=반응(anchor 불가)·market=signal·catalog=entity·search=URL·unknown=fail-closed. production candidate acquisition 은 publishable×publishable(official/article/news)만 anchor(`_is_publishable_production_pair`·약화 0). near-match 0의 원인이 (i) paraphrase면 해소는 **R-SemanticIdentityAdjudicator**(embedding/LLM/KG·R3·MERGE_GATE·gold) 영역.
- **제품 계약(raw≠public)**: 출력·`r1_production_candidate_contract`(23필드) 노출 표면에 score/rationale/predicted_status/raw body/secret/raw reviewer 신원·same_event truth **0** — 출력 전 `_assert_pii_safe` 재귀 가드·API `response_model=InternalOpsR1ProductionCandidateStatus`(sanitized 화이트리스트)·frontend `FORBIDDEN_OPS_FIELDS` 재귀 가드. production_gold_count 0·gate **exact passthrough**·`merge_allowed=False`·`no_public_intelligence_unit`·embedding/LLM 실호출 0·DB write 0·`actual_sending_performed=False`. credential presence는 **secret-safe**(present/missing·값 0·`.env` 값 미열람). internal ops dashboard 는 **4중 게이트+preflight posture**·workflow state(public truth 아님). live query는 **bounded·1회·rate cooldown 강제·raw body 0**·`live_candidate_count` 등은 카운트/sanitized status만(raw 응답 미영속). Agent는 acquisition status·acquisition strategy를 *계획* 가능하나 near-match 0을 LLM으로 same_event 단정·label 조작·merge·anchor 약화·PII/secret 출력·실 전송 불가.

## ⚠️ 이번 턴 종결/갱신 RISK
- **신규 2(실 present blocker만)**: **R-CrossSourceNearMatchGap**(MEDIUM·100 publishable 비교쌍/near-match 0·**원인 미확정**[paraphrase recall OR broad-topic different-events]·해소=먼저 좁은 topic/window 재실행으로 (i)/(ii) 구분 후 lever·완화=blocked 사유 정직 분해+`_primary_next_action` 양가 분기+acquisition strategy·완전종결=near-match 후보 확보 시) · **R-ProviderPairNarrowness**(LOW·watch·Guardian×NYT 단일 pair·**provider 확장이 near-match 올리는지 미증명**[원인과 직교]·완화=publishable provider 확장 strategy·완전종결=확장 수율 실측).
- **부분진전(종결 0)**: **R-ProductionCandidateScarcity**(live query 실행·100/0 실측·정확 사유[no_title_overlap]·**충분한 후보 0→종결 금지**) · **R-SourceOverlapScarcity**(100=비교쌍·**detectable same-event overlap 0 재확인**→희소 미해소[closure ≥1 미충족]·near-match가 frontier·종결 금지) · **R-RealSourceLoopUnproven**(live→discover **경로 실데이터 1회 실행**[usable 0]·reviewer→label loop 미완→종결 금지) · **R-RagKgPrematureBuild**(Lane D 계약 grounding·runtime No-Go 유지·gate 조건 명시·종결 금지).

## ❌ 달성하지 못한 것 & 왜 (이월)
- **실 production candidate freeze / 실 reviewer contact / 첫 returned labels / 실 gold**: live query는 실행됐으나 **near-match publishable pair 0**(`no_title_overlap`·원인 미확정)→freeze 대상 0(`candidate_provenance=none`). 해소=**먼저 좁은 topic/window 재실행으로 (i)/(ii) 구분**→(i)면 gated R3 semantic adjudicator(MERGE_GATE/gold 전 No-Go)·(ii)면 targeted acquisition(R-CrossSourceNearMatchGap)·이후 reviewer recruitment·실제 contact evidence·첫 returned labels 선결(R-RealSourceLoopUnproven·R-GoldAcquisitionPlanOnly).
- **adjudicator runtime / RAG·KG·Entity·public IU**: Lane D 계약은 docs(§6b-R/§6b-S/§4)일 뿐 runtime 미구현(embedding/LLM 실호출·실 병합·운영 DB·GraphRAG·public IU 이번 턴 No-Go·옵션 E/F 금지).
- **UI Q10(live 결과 UI 반영)**: read API가 `live_query=False` 고정이라 internal ops UI는 마지막 비-live 상태만 표시. live 결과(100 비교쌍/near-match 0)를 UI에 반영하려면 별도 영속/표면화 필요(다음 증분·정직 표기).

## 🚧 닫을 수 없는 문제 (BLOCKED/UNKNOWN)
- BLOCKED: 실 production candidate = **near-match 0의 원인 (i)/(ii) 구분**(좁은 topic/window 재실행)→(i)면 gated R3 adjudicator[R2 MERGE_GATE 후]·(ii)면 targeted acquisition→near-match publishable pair 확보 선결(R-CrossSourceNearMatchGap). 실 gold = 실 후보→reviewer recruitment→실제 contact(evidence)→첫 returned label 회수(R-ProductionCandidateScarcity·R-GoldAcquisitionPlanOnly·R-RealSourceLoopUnproven). 실 병합 = gold-calibrated embedding/LLM adjudicator(R3·E)+MERGE_GATE(R2).
- UNKNOWN: **near-match 0의 원인이 (i) paraphrase인지 (ii) broad-topic different-events인지**(단일 broad-topic 1회로 구분 불가·좁은 topic 재실행 미측정). topic/time-window 변주가 cross-source near-match 수율을 올릴지(이번 1회=100 비교쌍/0 near-match·변주 미측정). provider 확장이 near-match를 줄일지(R-ProviderPairNarrowness·미증명). (i) 확인 시 gated R3 adjudicator가 실 paraphrase pair를 calibration 가능하게 판정할지(R3 runtime 미가동). 실 reviewer 회수율/SLA(plan·machinery만·실 운영 0).

## 👉 다음 할 일
1. **[커밋 대기]** 본 턴 ADR#77: 수정 2(r1 production candidate code 1[`_primary_next_action`]+test 1[+3]) + docs 9(roadmap 3·reference 2·canonical 1·decisions 1·risk 1·PROJECT_STATUS) = **11파일** 미커밋. 커밋 여부 지시 대기. push 금지. (`--paths/`·`--help/` stray 정리 허가도 함께 받으면 처리.)
2. **[다음 단계·frontier]** (a) **near-match 0의 원인 (i)/(ii) 구분** — 좁은 same-event-targeted topic/time-window로 bounded·opt-in 재실행(일부 쌍이 hard band 진입하는지)→(i) paraphrase면 gated R3 semantic adjudicator(embedding/LLM·**R2 MERGE_GATE/gold 후**·No-Go)·(ii) different-events면 targeted acquisition(R-CrossSourceNearMatchGap) → near-match publishable pair 확보 → production-candidate batch freeze. (b) **reviewer recruitment + actual contact evidence + 첫 returned labels 회수**(actual input gate 자동 end-to-end·R1) → (c) gold denominator(live 200·KO 50·pos/neg 67·hard-neg 20·**R1**) → (d) MERGE_GATE calibration(precision≥0.98·FPR≤0.01·**R2**) → (e) **embedding scorer opt-in 실호출**(**R3**) → (f) entity/KG/GraphRAG(**R4~R6**) → (g) Agent/public IU(**R7**). 별도 축: internal ops UI Q10(live 결과 반영)·per-user auth(R-InternalOpsAuthBoundary).
3. **[권장·bounded]** acquisition strategy 실험은 **bounded·opt-in·rate governed**일 때만(무단 대량 호출 0): 좁은 same-event-targeted topic으로 (i)/(ii) 구분 측정(다음 턴·source role guard 유지).

## 📁 근거 (이번 턴 핵심)
- 커밋: `00c70a9`(ADR#76·20파일·+1449/−45·push 0·NO UPSTREAM). ADR#77 변경: 수정 2(r1 production candidate code[`_primary_next_action`+`_PCAND_NO_TITLE_OVERLAP_ACTION`]+test[+3])·docs 9. **신규 파일 0·ADR#76 커밋분 frozen·단방향 import·재구현 0·EVENT_SCHEMA 무변경·frontend/ingestion/main.py/config/.env/.env.example/package.json 무변경.**
- live query(실측·1회·bounded·secret-safe·raw body 0): `live_call_performed=True`·`providers_ready=[guardian,nyt]`·`live_candidate_count=100`(비교쌍)·`publishable_pair_count=0`(near-match)·`production_candidate_status=blocked_no_publishable_pairs`·`no_title_overlap`(near 0·fingerprint 0·**원인 미확정**)·`candidate_provenance=none`·`production_gold_count=0`·`current_r1_gap=200`·`merge_allowed/llm_invoked/embedding_invoked/db_write/actual_sending_performed=False`. detectable same-event overlap **0**(scarcity 재확인).
- 검증(정직): 신규 backend **3**(test_r1_production_candidate_acquisition: no_title_overlap→**indeterminate(양가)** action·role 거부→role-guard action·`_primary_next_action` 분기 직접)·r1 파일 **37**(34+3)·관련 3파일 **110 passed**(r1+internal_ops_api+smoke)·**backend 비-live 전체 1437 passed/101 skipped/0 failed**(1434+3·`.harness/_adr77_backend_full.log`)·ruff 실게이트(E/F/I/B/W−E501) **0**·secret scan **PASS**·docs_lifecycle conflicts **0**·frontend tsc **0**(미변경·test44/lint0 baseline 유지)·ingestion 미변경(1353 baseline). **live query=1회 실행**(bounded·rate cooldown 강제·raw body 0·DB/merge/LLM/embedding/전송 0).
- 감사(closeout subagents): architecture/pipeline/actual_input_gate/live_query_execution/provider_readiness/credential_secret_boundary/source_overlap/production_candidate_batch/synthetic_vs_production_separation/source_role_guard/acquisition_strategy/llm_evidence_packet/rag_ingestion_gate/kg_edge_eligibility/community_reaction_boundary/public_iu_gate/internal_ops_dual_track/r1_gold_acquisition/internal_ops_auth_boundary/ops_ui_contract/public_truth_boundary/reviewer_pii/secret_boundary/source_loop/code/test/evidence/risk_closure/**adversarial** review. **adversarial-reality-critic가 단정적 'paraphrase' 결론을 MEDIUM 정직성 결함으로 지적→원인 양가(미확정)로 교정·RISK 재명명(R-CrossSourceNearMatchGap)·"overlap 희소하지 않음" 철회**. code-review LGTM(37 passed·23-key contract 불변). cardinal(live evidence 없이 production_candidate_batch=True/community·market·catalog anchor/source role guard 약화/score·rationale·predicted_status·same_event 노출/실 전송/`.env` 값 읽기/uncalibrated merge) 차단 확인.
- 문서: ADR#77(`_DECISIONS`)·RISK(`_RISK`·종결 0·신규 2[R-CrossSourceNearMatchGap·R-ProviderPairNarrowness]·R-ProductionCandidateScarcity/R-SourceOverlapScarcity/R-RealSourceLoopUnproven/R-RagKgPrematureBuild 부분진전)·ADR#77 블록(`2_ROADMAP/15`)·ADR#77 마커(`2_ROADMAP/00`)·acquisition §19(`2_ROADMAP/19`)·**§6b-R1d live query executed + near-match gap[원인 미확정]**(`RAG_KG`)·§4 ADR#77 bullet(`IU_CONTRACT`)·`_CANONICAL/02`(ADR#41~#77). **EVENT_SCHEMA 무변경**(ADR#77 DB-free·최소 변경).

---
_as_of: 2026-06-28 · ADR#77 R1 live query executed + cross-source near-match gap[원인 미확정] + blocked-reason honesty fix + product bridge gating — ADR#76 커밋(`00c70a9`) 후, opt-in live acquisition gate를 user 승인으로 **실제 1회 실행**(근거기반 최소증분·신규 파일 0·재구현 0·live query 1회·DB 0·전송 0·secret 값 미열람): `r1_production_candidate_acquisition --live-query` → **`live_candidate_count=100`**(cross-source publishable **비교쌍**·≈10×10 Cartesian·≠same-event match)·**`publishable_pair_count=0`**(near-match)·`production_candidate_status=blocked_no_publishable_pairs`·root cause **`no_title_overlap`**(near 0·fingerprint 0). **live→discover 경로 실데이터 1회 실행**(usable 0)·detectable same-event overlap **0**(scarcity 재확인)·그 0의 **원인 미확정** — (i) same-event cross-source paraphrase recall 한계 OR (ii) broad-topic("central bank rate decision") 아래 different-events(단일 broad-topic 1회로 구분 불가·ADR#64가 "100 쌍=비교대상·n=2로도 구분 불가" 명시). (i)이면 INTELLIGENCE_UNIT_CONTRACT §4 ④ semantic adjudicator(embedding/LLM/KG·R3·MERGE_GATE/gold 전 No-Go), (ii)이면 targeted acquisition — 어느 쪽인지 미해결. **코드 fix(adversarial 근거)**: `_primary_next_action`+`_PCAND_NO_TITLE_OVERLAP_ACTION`(`blocked_no_publishable_pairs` primary action을 `no_title_overlap` 시 **원인 양가(미확정)** vs role 거부로 분기·운영자 "source role guard 약화" 오안내 교정하되 단정적 paraphrase도 회피·`next_manual_action` UI 정직)·테스트 3종. **Lane C/D**: 획득 전략·LLM/RAG/KG/IU 계약은 §6b-R matrix/§6b-S table/§4에 **기존 존재→grounding만**(§6b-R1d·§4 ADR#77 bullet·신규 sprawling 파일 0). 옵션 **A+B(user opt-in 승인)+C+D 채택·E(embedding/LLM runtime)/F(production DB·merge·public IU·GraphRAG) 금지·범위=근거기반 최소증분(user 결정)**. **측정**: 신규 backend 3·r1 파일 37·관련 3파일 110p·**backend 비-live 전체 1437p/101skip/0fail**(1434+3)·ruff 실게이트 0·secret PASS·docs_lifecycle 0·frontend tsc0(미변경). **정직 경계**: 진전=live query 1회 실행+cross-source near-match gap 실측(**원인 미확정**)+blocked-reason 정직성 fix+Lane D grounding·**실 production candidate freeze 아님**(live_candidate_count 100=비교쌍·publishable near-match 0·candidate_provenance none·production_gold_count 0·detectable same-event overlap 0·gate exact passthrough)·adjudicator runtime/public IU/merge/운영 DB/embedding·LLM 실호출/실 전송 0·internal ops UI read API live_query=False 고정→마지막 비-live 상태[Q10 gap·정직]·host_gate=None[host당 1회→min-spacing 위반 불가·반복 시 host gate 주입은 다음 증분]·완전종결=OVERCLAIM. 다음 hard blocker=near-match 0의 원인 (i)/(ii) 구분(좁은 topic/window 재실행)→해당 lever→실 reviewer contact→actual returned labels. **adversarial-reality-critic가 단정적 paraphrase 결론·"overlap 희소하지 않음"을 MEDIUM 정직성 결함으로 지적→원인 양가(미확정) 교정·RISK 재명명·scarcity 재확인으로 정정**. **신규 R-CrossSourceNearMatchGap(MEDIUM)·R-ProviderPairNarrowness(LOW)·R-ProductionCandidateScarcity/R-SourceOverlapScarcity/R-RealSourceLoopUnproven/R-RagKgPrematureBuild 부분진전**·종결 0. ADR#76 커밋 `00c70a9` 위 ADR#77 미커밋(수정 2+docs 9=11)·커밋 지시 대기·push 안 함._
