# 📊 프로젝트 진행 현황 (PROJECT_STATUS)

> 매 턴 끝에 새로 쓰입니다. 항상 "가장 최근 1턴"만. 과거는 `git log -- PROJECT_STATUS.md`.
> 사실 원본(자동): `.harness/machine_status.json` · 완료 증거: `.harness/closeout_stamp.json` · 서술: 이 파일(에이전트).

## 🟢 한눈에 (비개발자용 3줄)
- **지금 무엇을 했나:** 지난 턴(ADR#62 — Guardian 수집처 어댑터 배선)을 안정 기준점으로 **커밋**(`45046af`)한 뒤, ADR#62 가 **가짜 통신(fake transport)으로만** 검증했던 Guardian 어댑터를 **실제 API 키로 secret-safe 하게 실증**했습니다. 새 기능을 짓지 않고, 키가 준비됐을 때만 **선택적(opt-in)으로 실제 Guardian 호출을 1회씩 수행하는 smoke 경로**를 만들고, 키가 없으면 "**.env 파일 자체가 없음(env_not_loaded)**" 과 "**파일은 있는데 키 미설정(missing_credentials)**" 을 정직하게 구분합니다. 실제 키 값은 **어디에도 출력/기록하지 않습니다**(present/missing 만).
- **이번 턴에 실제로 끝낸 것:** ADR#62 커밋(`45046af`·13파일) → **ADR#63**: 새 모듈 `guardian_live_query_smoke.py`(§4 smoke 계약·`env_not_loaded`/`missing_credentials` 구분·기존 `run_optional_live_query` 재사용) + `env_loader.probe_env_var`(키 존재 여부·`.env` 파일 존재·`.env.example` 이름 선언만 — **값 미열람**). 신규 **26 테스트**(전부 가짜 통신/주입으로 네트워크·실 `.env` 0). **실 Guardian 2회 호출**(opt-in·Ukraine/earthquake 7d)→각 **실 기사 10건**·`live_derived`·`secret_exposed False`·`env_file_read False`·host gate 통과.
- **정직한 한계:** 실 fetch 는 **성공**(실 기사 10건×2)했으나 **단일 소스(Guardian) 안에서 같은 사건의 중복 기사 overlap 이 0**(이 토픽/윈도우에서 미관측 — 같은 날 기사들의 제목이 near 임계 미만 = 서로 다른 하위 사건·near-dup recall 한계 포함). 그래서 검수 큐 충원 0·reviewer/gold 0. **다음 진짜 해소책 = 2번째 발행 소스(cross-source)**. 운영 DB·자동 병합·LLM **0** 불변. **push 금지·커밋 지시 대기**.

## 📋 자동 수집 사실 (machine_status.json)
- **git HEAD `45046af`**(ADR#62, 본 턴 첫 커밋) 위 **ADR#63 = 미커밋**: 신규 2(`backend/app/tools/guardian_live_query_smoke.py`[smoke 오케스트레이터] + `backend/tests/test_guardian_live_query_smoke.py`[26 테스트]) + 수정 9(`ingestion/core/env_loader.py`[`probe_env_var`·`env_example_declares`] + docs 7[`2_ROADMAP/00·15`·`_RISK/RISK_REGISTER`·`5_REFERENCE/RAG_KG_AGENT_READINESS·INTELLIGENCE_UNIT_CONTRACT`·`_CANONICAL/02`·`_DECISIONS/2026-06`] + `PROJECT_STATUS`) = **추적 11항목**. **migration 없음·ADR#62 커밋분(`provider_query_adapters.py`·`provider_readiness.py`) 미수정(frozen·단방향 import·재구현 0).**
- 열린 RISK: **total 36**(HIGH 3·MEDIUM 21·LOW 11). **신규 0·종결 0.** 부분진전 6: R-SourceOverlapScarcity·R-RealSourceLoopUnproven·R-LiveIdentityBacklog·R-IdentityHumanLabeling·R-SemanticIdentityAdjudicator·R-Gdelt429. throwaway 는 `.harness/_TRASH/`·`frontend/.harness/`(커밋 제외).

## ✅ 이번 턴에 달성한 것 (secret-safe Guardian live query smoke — ADR#63)
- **① ADR#62 커밋**: 13파일 → `45046af`(secret PASS 13·docs_lifecycle conflicts 0·SIGNATURE MATCH 16-entry·unresolved 0·push 0).
- **② 원자 분석(§2·20문항·구현 전 보고)**: env var=`GUARDIAN_API_KEY`(`.env.example:229`)·readiness 함수=`env_status`(present/missing·값 0)·프로세스 자동 .env load 아님(env_status→load_env 호출 시)·**현재 missing_credentials 와 env_not_loaded 미구분**(ADR#63 에서 `.env` Path.exists 로 구분)·secret 은 adapter 내부 os.getenv real-network 분기만·httpx params 전용. **다음 hard blocker=2nd publishable provider(cross-source)** — 실 데이터로 확정.
- **③ A — secret-safe live query smoke**: `guardian_live_query_smoke.py` — §4 계약 단일 dict(smoke_name·live_query_requested/attempted·env_var_name·**credential_value_exposed=False·env_file_read=False** 불변·env_example_checked·provider/host_gate/rate_limit status·records/candidate/near/hard/queue_pop·dataset_source·provenance·block_reasons·next_actions·production_gold 0·merge_allowed False·db_write False·llm_invoked False). 기본 live_query=False→시도 0(opt-in only·CI 아님).
- **④ env_not_loaded vs missing_credentials 구분**: opt-in + 키 부재 시 `.env` 파일 부재→`env_not_loaded`(next_action: `.env` 생성·키 설정)·파일 존재·키 미설정→`missing_credentials`(둘 다 **network 전 차단**·값 미열람).
- **⑤ B — secret hygiene + probe**: `env_loader.probe_env_var`(present/missing[env_status] + `.env` Path.exists + `.env.example` 이름 선언[env_example_declares]·**전부 값 미열람**). smoke report 어디에도 실 값 0(이름만 노출)·credential present+opt-in 시 `run_optional_live_query` 위임(재구현 0).
- **⑥ C — no-key blocked report**: 키 없거나 `.env` 미로드 시 실패가 아니라 `env_not_loaded`/`missing_credentials` + next_action 으로 정직 산출(fixture 둔갑 0·dataset_source None).
- **⑦ 실 Guardian live query 실증(2회)**: opt-in·bounded·governed·secret-safe 실 호출 — topic Ukraine/earthquake 7d → 각 **records 10·candidate 0·`no_title_overlap`·dataset_source live_derived·secret_exposed False·env_file_read False·host_gate passed·rate_limit ok**. → live 경로(실 key·실 fetch·정규화·secret 경계) **닫힘**. 단 **단일 소스 same-event overlap 0**(구조적).
- **⑧ D/E/F 금지**: 2nd provider adapter(D)·embedding/LLM adjudicator(E·`EMBEDDING_LLM_ADJUDICATOR_INTERFACE` No-Go)·production DB(F·session 인자 부재·write 0) 미착수.

## 🧭 cross-batch identity 4단계 + 평가/gold gate (정직)
- ① anchor 병합(ADR#40) → ② 후보 LINK(ADR#41) → ③ shadow 판정(ADR#42·운영 배선 ADR#48~#53·preflight/smoke ADR#54·live-db ADR#55·replay ADR#56·overlap discovery ADR#57·acquisition ADR#58·near-match 큐 ADR#59·targeted readiness ADR#60·provider readiness gate ADR#61·provider query adapter+Guardian wired ADR#62·**secret-safe live query smoke 실증 ADR#63**) → ④ 실 병합(**미구현**).
- **ADR#63 위치**: 단계 ①(source fetch)의 **실 network 직전 마지막 경계**. ADR#62 가 fake transport 로 배선한 Guardian adapter 를 **실 key 로 실증** — 실 records 10×2 가 ADR#60 운영경로(`records=` 주입)로 들어가나 단일 소스 overlap 0 이라 near-match queue 충원 0(병합·단정 0).
- **⚠ 미해소(OPEN):** **단일 소스 same-event overlap 0**(live fetch 성공이나 구조적·cross-source 필요) → 실 targeted near-match 후보·reviewer/gold 0·미배선 9종 잔여·embedding/LLM 실 병합(No-Go)·운영 DB 0009 배포·MERGE_GATE·한국어 캘리브레이션. "live query 능력·실증 ≠ 실 near-match 후보" — 완전종결=OVERCLAIM.

## 🔎 source orchestration / 제품 계약
- official/news=publishable evidence(merge anchor)·community=반응 evidence(anchor 불가)·market=signal·catalog=entity·search=URL·unknown=fail-closed. Guardian=news(publishable). 어댑터 records 는 publishable×publishable 만 reviewer 후보.
- **제품 계약(raw≠public)**: live query smoke 는 Intelligence Unit 입력(실 near-match 후보)을 만들 수집처 fetch 의 **실 key 실증 경계** — 본문 미저장(title+canonical+published 만)·public IU 미생성·실 records 가 와도 same-event 확정은 reviewer/gold/MERGE_GATE 밖에서 금지(`merge_allowed=False`·`no_public_intelligence_unit`·`llm_invoked=False`). Guardian 무료 tier full-content 재배포 금지 ToS 경계 내(본문 미저장). Agent 는 provider live query readiness/source-pair/queue population 을 *계획* 가능하나 provider secret 추측/생성·`.env` 값 읽기·merge·public IU 불가. LLM/Agent 진입 9조건 여전히 No-Go(1·4·5·7).

## ⚠️ 이번 턴 종결/갱신 RISK
- **부분진전(종결 0·신규 0·total 36 불변)**: **R-SourceOverlapScarcity**(실 key live query 실증·records 10×2·단 overlap 0=cross-source 필요 **실 데이터로 확정**) · **R-RealSourceLoopUnproven**(live path 실 fetch 입증·DB/merge 0) · **R-LiveIdentityBacklog**(queue 충원 경로 실 records 로 입증·단 overlap 0 으로 queue_pop 0) · **R-IdentityHumanLabeling**(packet 경로 유지·gold 0) · **R-SemanticIdentityAdjudicator**(embedding No-Go 고정) · **R-Gdelt429**(Guardian 실 대체 fetch 입증·단일 의존 완화).
- 디렉티브 신규 RISK 후보(R-GuardianCredentialReadiness/R-LiveProviderSmokeReliability/R-SecretBoundary)는 **독립 blocker 아님** → **R-SourceOverlapScarcity**·**R-Secret**(HIGH·secret hygiene 이미 mitigated·env_status·probe_env_var 값 미열람·httpx params·url keyless)에 포섭(RISK 남발 금지).

## ❌ 달성하지 못한 것 & 왜 (이월)
- **실 same-event overlap**: 실 Guardian fetch 는 성공(records 10×2)했으나 **단일 소스 안 same-event overlap 이 이 토픽/윈도우에서 미관측**(같은 날 기사 제목 Jaccard<near 임계=서로 다른 하위 사건·near-dup recall 한계 포함·deterministic near-match=cross-source wire syndication 설계). → candidate 0·queue_pop 0. **다음 unblock=2번째 publishable provider(옵션 D·다음 턴·이번 턴 금지)**.
- **실 human-reviewed gold**: 실 near-match 후보가 없어 큐가 안 채워짐 → reviewer 충원·gold 0(production_gold 0).
- **embedding/LLM adjudicator**: gold calibration+MERGE_GATE 후에만(No-Go·호출 0). 운영 DB·production 백로그 0(옵션 F 금지).

## 🚧 닫을 수 없는 문제 (BLOCKED/UNKNOWN)
- BLOCKED: 실 same-event overlap = **2번째 publishable provider 배선**(cross-source·옵션 D) 선결 — 단일 소스 "더 fetch" 로 안 풀림(실 호출 2회 모두 overlap 0). 실 reviewer/gold = reviewer 충원·운영 절차 선결. production 백로그 = 운영 DB 0009 배포(승인) + cross-source 실 fetch 볼륨.
- UNKNOWN: paraphrase overlap 의 실 병합 허용 기준(production precision·실 gold·reviewer 합의·한국어 캘리브레이션·MERGE_GATE) → R-SemanticIdentityAdjudicator·R-IdentityHumanLabeling·R-ReviewerAgreement·R-IdentityEvalDataset 선결.

## 👉 다음 할 일
1. **[커밋 대기]** 본 턴 ADR#63: 신규 2(guardian_live_query_smoke.py+test) + 수정 9(env_loader.py·docs 7·PROJECT_STATUS) = 11파일 커밋 여부 지시 대기. push 금지.
2. **[다음 단계]** (a) **2번째 publishable provider 어댑터 배선**(nyt/newsapi/federal_register 등·cross-source same-event overlap 확보 — 실 데이터로 확정된 다음 unblock) → (b) 실 cross-source near-match 후보 → 큐 충원 → reviewer 충원·합의 gold + 한국어 캘리브레이션 → (c) embedding/LLM/KG **실 병합** adjudicator(MERGE_GATE·`semantic_score` seam) → (d) (승인 하) 운영 DB 0009 배포 + scheduler 가동 → RAG/KG.

## 📁 근거 (이번 턴 핵심)
- 커밋: `45046af`(ADR#62·13파일). ADR#63 변경: 신규 `guardian_live_query_smoke.py`(smoke 오케스트레이터)+`test_guardian_live_query_smoke.py`(25)·`env_loader.py`(probe_env_var·env_example_declares)·docs 7. **ADR#62 커밋분 미수정(frozen·단방향 import·재구현 0).**
- 검증(정직): **실 Guardian 호출 2회**(opt-in·bounded·governed·secret-safe·Ukraine/earthquake 7d)→각 records 10·candidate 0·`no_title_overlap`·dataset_source live_derived·secret_exposed False·env_file_read False·host_gate passed·rate_limit ok = **live 경로 실증 + 단일 소스 overlap 구조적 0**. fake transport: records 4·near 2·hard 3·queue_pop 5·live_derived. missing/env_not_loaded→transport 미호출. 테스트: 신규 **26**(secret boundary 1-8·live smoke 9-16·queue 17-19·probe 20-22·agent/CLI 23-25·real-network secret 26)·backend 비-live **814 passed**(788+26 additive)·live-PG **94 passed**(write-free·무영향)·full `backend/tests` DB-up 실측 **908 passed/4 skipped/0 failed**·ingestion **1353 passed**·frontend tsc**0**/node:test **12**/lint **0**·secret scan **PASS**·docs_lifecycle conflicts **0**·ruff 실게이트(E/F/I/B/W−E501) **0**. 운영 DB 무변경.
- 감사: adversarial-reality-critic + code-review(stamp `audit_evidence` 단일 출처). 핵심 안전축(실 호출에서 credential_value_exposed/env_file_read False 성립·fixture 둔갑 0·실 records→live_derived 정직·단일 소스 overlap 0 정직 노출·no merge/LLM/DB).
- 문서: ADR#63(`_DECISIONS`)·6 RISK 부분진전(`_RISK`·종결 0·신규 0)·Guardian live query smoke runbook+ADR#63 블록(`2_ROADMAP/15`)·(11u)(`2_ROADMAP/00`)·agent readiness/Agent 역할(`RAG_KG`)·live query smoke 계약(`IU_CONTRACT`)·`_CANONICAL/02`(ADR#41~#63 + secret-safe live query).

---
_as_of: 2026-06-26 · ADR#63 secret-safe Guardian live query smoke + live-derived queue population gate — ADR#62 커밋(`45046af`) 후, ADR#62 가 fake transport 로만 검증한 Guardian adapter 의 **실 network 직전 경계를 실 key 로 secret-safe 하게 실증**(새 파일 1·ADR#62 커밋분 frozen·단방향 import·재구현 0): 신규 `guardian_live_query_smoke.py` — §4 smoke 계약(credential_value_exposed=False·env_file_read=False 불변·records/candidate/near/hard/queue_pop·dataset_source·block_reasons·next_actions·production_gold 0·merge_allowed False·db_write False·llm_invoked False)·**env_not_loaded vs missing_credentials 구분**(`.env` Path.exists·둘 다 network 전·값 미열람)·credential present+opt-in 시 `run_optional_live_query` 위임. `env_loader.probe_env_var`(present/missing + `.env` Path.exists + `.env.example` 이름 선언·값 미열람)·`env_example_declares`. 옵션 **A+B+C 채택·D(2nd adapter)/E/F 금지·신규 RISK 0**. **측정**: 신규 26·backend 비-live 814p(788+26)·live-PG 94p·full `backend/tests` DB-up 실측 908p/4s/0f·ingestion 1353p·frontend tsc0/test12/lint0·secret PASS·docs_lifecycle 0·ruff 실게이트 0. **실 Guardian 호출 2회**(opt-in·governed·secret-safe·Ukraine/earthquake 7d→각 records 10·candidate 0·no_title_overlap·dataset_source live_derived·secret_exposed False·env_file_read False·host_gate passed)→**live 경로 실증** but **단일 소스 same-event overlap 0**(구조적·같은 날 제목 Jaccard<near·recall 한계 포함·deterministic near-match=cross-source 설계→**다음 unblock=2번째 publishable provider**). **정직 경계**: live query 능력·실증 ≠ 실 near-match 후보(queue_pop 0·reviewer/gold 0)·미배선 9종 잔여·embedding/LLM No-Go·운영 DB/production 백로그 0 불변(완전종결=OVERCLAIM). **R-SourceOverlapScarcity·R-RealSourceLoopUnproven·R-LiveIdentityBacklog·R-IdentityHumanLabeling·R-SemanticIdentityAdjudicator·R-Gdelt429 부분진전**·종결 0·신규 0(total 36). ADR#62 커밋 `45046af` 위 ADR#63 미커밋(신규 2+수정 9=11)·커밋 지시 대기·push 안 함._
