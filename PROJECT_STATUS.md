# 📊 프로젝트 진행 현황 (PROJECT_STATUS)

> 매 턴 끝에 새로 쓰입니다. 항상 "가장 최근 1턴"만. 과거는 `git log -- PROJECT_STATUS.md`.
> 사실 원본(자동): `.harness/machine_status.json` · 완료 증거: `.harness/closeout_stamp.json` · 서술: 이 파일(에이전트).

## 🟢 한눈에 (비개발자용 3줄)
- **지금 무엇을 했나:** 지난 턴(ADR#66 — reviewer label operations)을 안정 기준점으로 **커밋**(`8776be8`)한 뒤, ADR#66 이 만든 "검수 패킷 export→라벨 import→합의/gold/캘리브레이션" **함수**가 아직 *사람이 바로 라벨링을 시작할 운영 패키지*가 아니라는 병목을 풀기 위해, 그 함수를 **reviewer 지시문 + 라벨 템플릿 + 배정 manifest + intake(수거) 디렉터리/검증 명령 + no-labels 보고**로 묶은 **batch launch pack + intake 검증 루프**를 만들었습니다. 자동 병합·LLM/embedding 실호출·운영 DB·키 값 출력은 **0**입니다.
- **이번 턴에 실제로 끝낸 것:** ADR#66 커밋(`8776be8`·10파일) → **ADR#67**: 새 모듈 `reviewer_batch_launch.py`(launch-readiness orchestrator·DB-free·ADR#66/#60/#59 **조합**·재구현 0) — reviewer instruction(같은-사건 4-라벨 정의/금지/권장) + label template(점수/버킷 **0**·reviewer_id **익명화**·빈 worksheet) + assignment manifest(pair 당 **2명+** 배정·top-k+hard negative·인력<2면 `insufficient_reviewer_capacity`·raw 명단 **미commit**) + intake plan(디렉터리/예상 파일명/검증 명령) + intake validation(import 前 **dry-run**·금지필드/형식오류/모델라벨/미지 pair_id/중복 **거부**·라벨 어휘 unsure/needs_review→canonical insufficient/ambiguous **정규화**). 라벨 없음=실패 아닌 **`awaiting_labels`**(정직). 신규 **67 테스트**. **기존 ADR#66/#60/#59 미변경**(최소 변경). **자체 발견·수정**: 2명 만장일치 `needs_review`(→ambiguous)가 frozen resolve 로 gold 승격 가능→§6 "unsure/needs_review only→not gold" 위반→**gold 를 decisive(same/different)만** 카운트(test_49 잠금).
- **정직한 한계:** 이번 진전은 **reviewer batch launch 준비 substrate**이지 **실 gold 가 아닙니다**. **실 production reviewer 라벨·실 gold 는 여전히 0**(synthetic/test 라벨은 simulated only·production_gold_count 0), calibration_ready/merge_gate_ready False(표본 floor live 200/KO 50 미충족), embedding/LLM 실호출·자동 병합·운영 DB·public IU 모두 **0**. batch pack 은 사람이 라벨링을 *시작*할 패키지일 뿐. 다음 hard blocker=**first production labels import**(실 reviewer 가 batch pack 으로 라벨 작성→intake). **push 금지·커밋 지시 대기**.

## 📋 자동 수집 사실 (machine_status.json)
- **git HEAD `8776be8`**(ADR#66, 본 턴 첫 커밋) 위 **ADR#67 = 미커밋**: 신규 2(`backend/app/tools/reviewer_batch_launch.py`[orchestrator] + `backend/tests/test_reviewer_batch_launch.py`[67 테스트]) + 수정 8(docs 7[`2_ROADMAP/00·15`·`_RISK/RISK_REGISTER`·`5_REFERENCE/RAG_KG_AGENT_READINESS·INTELLIGENCE_UNIT_CONTRACT`·`_CANONICAL/02`·`_DECISIONS/2026-06`] + `PROJECT_STATUS`) = **추적 10항목**. **migration 없음·ADR#66 커밋분 frozen·단방향 import·재구현 0·ADR#66/#60/#59 머신 조합·EVENT_SCHEMA(DB-free라 무변경) 무변경.**
- 열린 RISK: **신규 0·종결 0(total 불변)**. 부분진전 8: R-IdentityHumanLabeling·R-ReviewerAgreement·R-GoldSamplingBias·R-IdentityEvalDataset·R-SemanticIdentityAdjudicator·R-LiveIdentityBacklog·R-SourceOverlapScarcity·R-RealSourceLoopUnproven. throwaway 는 `.harness/_TRASH/`·`frontend/.harness/`(커밋 제외).

## ✅ 이번 턴에 달성한 것 (reviewer batch launch pack + intake validation loop — ADR#67)
- **① ADR#66 커밋**: 10파일 → `8776be8`(제공 메시지 그대로·secret PASS·docs_lifecycle conflicts 0·SIGNATURE MATCH 13-entry·unresolved 0·push 0).
- **② 원자 분석(§2·20문항·구현 전 보고)**: 요구 머신 대부분 **이미 존재**(ADR#60 `build_reviewer_operating_checklist`·ADR#66 `export_reviewer_packet`/`run_reviewer_label_operations`·ADR#59 `build_near_match_reviewer_queue`)→이번 턴=**조합 + 결손 보강**(batch pack 컨테이너·assignment manifest·intake validation·labeler vocab 정규화). **핵심 긴장**: ④ 라벨 어휘 §5/§6 unsure/needs_review vs frozen `GOLD_LABELS`(ambiguous/insufficient)→정규화 매핑(`unsure≡insufficient·needs_review≡ambiguous`·둘 다 gold 비대상). **중복 방지**: ADR#60/#66/#59 조합·신규 파일 1. 다음 hard blocker=**first production labels import**.
- **③ A — reviewer batch launch pack**: `build_reviewer_instruction`(§6 4-라벨 정의/금지/권장·`model_score_shown`/`predicted_status_shown`/`hidden_candidate_rank_shown`=False)·`build_label_template`(packet→`REVIEWER_ALLOWED_KEYS` 빈 worksheet·reviewer_id pseudonym·score/bucket 0·allowlist 밖 키 fail-loud)·`build_intake_plan`(intake_directory/expected_label_files/validation_command).
- **④ B — assignment & duplication policy**: `build_assignment_manifest`(pair 당 ≥2 reviewer round-robin·`pseudonymize_reviewer_id` only·top-k positive+hard negative 동시 포함·capacity<2→`insufficient_reviewer_capacity`·`duplicate_assignment_coverage`·`raw_reviewer_pii_committed`=False).
- **⑤ C — intake validation loop**: `validate_label_intake`(§8 import 前 dry-run: forbidden field/malformed/label_source/reviewer_kind=human/pair_id∈known/duplicate·`REVIEWER_ALLOWED_KEYS`+frozen enum 재사용·통과 행 canonical 정규화)·`normalize_label`+`_LABELER_TO_GOLD`·intake_status[awaiting_labels/labels_present/invalid_labels/imported].
- **⑥ D — reviewer operations runbook**: ADR#67 runbook(batch export·intake validation·정직 경계) docs 기록·CLI(`--validate <dir>` dry-run·`--synthetic-labels` 데모).
- **⑦ §4 통합 entrypoint**: `run_reviewer_batch_launch`(40필드 output·intake_status·라벨 시 `resolve_label_operations`+`build_calibration_preflight` **직접 재사용**[packet 재export 0]·**decisive gold=same/different 만**·`non_decisive_gold_count` 표면화·merge_allowed False·db_write False·llm/embedding_invoked False·block_reasons/next_actions).
- **⑧ §10 Agent contract**: `BATCH_LAUNCH_AGENT_CONTRACT`(reviewer batch launch/assignment/label intake status/agreement·adjudication/gold calibration readiness/hard negative balancing/korean calibration/next reviewer action **계획 가능**·label 조작·score truth·같은 사건 확정·merge·public IU·anchor·secret·**reviewer raw PII 출력** **불가**·`EMBEDDING_LLM_ADJUDICATOR_INTERFACE` No-Go).
- **⑨ E/F 경계**: embedding/LLM scorer runtime(E)·production DB activation/semantic merge(F) 미착수.

## 🧭 cross-batch identity 4단계 + 평가/gold gate (정직)
- ① anchor 병합(ADR#40) → ② 후보 LINK(ADR#41) → ③ shadow 판정(ADR#42·운영 배선 ADR#48~#53·preflight/smoke ADR#54·live-db ADR#55·replay ADR#56·overlap discovery ADR#57·acquisition ADR#58·near-match 큐 ADR#59·targeted readiness ADR#60·provider readiness ADR#61·provider adapter+Guardian ADR#62·live query smoke ADR#63·2nd provider+cross-source smoke ADR#64·semantic candidate scorer ADR#65·reviewer label operations ADR#66·**reviewer batch launch pack + intake validation ADR#67**) → ④ 실 병합(**미구현**).
- **ADR#67 위치**: 단계 ⑦(검수 패킷)~⑨(gold) 사이의 **reviewer batch launch readiness substrate**. ADR#66 reviewer label 운영 함수를 사람이 바로 라벨링을 시작할 운영 패키지(batch pack→assignment manifest→intake validation→ADR#66 gold/calibration)로 전환(병합·단정 0·score/rationale/predicted labeler 숨김·reviewer_id 익명화·라벨 없음→awaiting_labels 정직). **실 gold 가 아님**(production_gold_count 0·synthetic/test=simulated only·merge_gate_ready False·decisive gold=same/different only).
- **⚠ 미해소(OPEN):** 실 production reviewer 라벨·실 gold(production_gold 0)·reviewer 합의 실측·gold-calibrated embedding/LLM **실 병합 판정기**(No-Go)·MERGE_GATE 런타임·한국어 캘리브레이션·운영 DB 0009 배포. "batch launch readiness ≠ 실 라벨·실 gold·실 병합" — 완전종결=OVERCLAIM.

## 🔎 source orchestration / 제품 계약
- official/news=publishable evidence(merge anchor)·community=반응 evidence(anchor 불가)·market=signal·catalog=entity·search=URL·unknown=fail-closed. reviewer batch pack 은 publishable×publishable 후보만(community/market/catalog anchor 차단·상속).
- **제품 계약(raw≠public)**: reviewer batch launch 의 노출 표면(instruction·template·assignment manifest·report)에 score/rationale/predicted_status/raw body/secret/raw reviewer_id **0** — `validate_labeling_packet` fail-loud + allowlist(`REVIEWER_ALLOWED_KEYS`) + pseudonymization 이중·삼중 방어. labeler 어휘(unsure/needs_review)는 frozen canonical(insufficient/ambiguous)로 **정규화**·둘 다 gold 비대상(decisive=same/different). production_gold_count 0·`merge_allowed=False`·`no_public_intelligence_unit`·embedding/LLM 실호출 0·DB write 0. Agent 는 batch launch/intake 를 *계획* 가능하나 label 조작·merge·검출 단정·PII/secret 출력 불가. LLM/Agent 진입 9조건 여전히 No-Go(1·4·5·7).

## ⚠️ 이번 턴 종결/갱신 RISK
- **부분진전(종결 0·신규 0·total 불변)**: **R-IdentityHumanLabeling**(batch launch pack+instruction/template/assignment manifest+intake validation+no-labels 정직·실 production label/gold 0 불변) · **R-ReviewerAgreement**(pair 당 2-reviewer assignment manifest+conflict/adjudication intake 분리+capacity warning·실 합의 0) · **R-GoldSamplingBias**(top-k+hard negative balanced manifest+top_k_bias_warning+korean floor+capacity warning·실 표집 0) · **R-IdentityEvalDataset**(batch label→eval/gold input 경로+calibration preflight decisive·MERGE_GATE evidence 0) · **R-SemanticIdentityAdjudicator**(scored queue→reviewer labeling launch·scorer threshold uncalibrated·merge No-Go 유지) · **R-LiveIdentityBacklog**(scorer top-k queue→batch launch→intake 경로·실 backlog/gold 0) · **R-SourceOverlapScarcity**(detection scarcity 후속 reviewer launch 운영층·검출 검증 아님) · **R-RealSourceLoopUnproven**(reviewer launch readiness substrate·실 병합 0).
- 디렉티브 신규 RISK 후보(R-ReviewerCapacity/R-LabelIntakeOps/R-ReviewerPIIExposure)는 **현 단계 독립 blocker 아님**(capacity warning=manifest 표면화·intake ops=validate dry-run 으로 선제·PII=pseudonymization 구조 봉인) → **미등록**(RISK 남발 금지·실 blocker 화 시 다음 턴).

## ❌ 달성하지 못한 것 & 왜 (이월)
- **실 human-reviewed production gold**: reviewer batch launch 경로(batch pack→assignment→intake validation→gold/calibration)는 닫혔으나 **실 production reviewer 라벨/gold 0**(synthetic/test=simulated only·production_gold_count 0). reviewer 충원·실 라벨 파일 선결.
- **reviewer agreement 실측 / MERGE_GATE**: 2-reviewer assignment manifest+conflict 분리 코드 검증됐으나 **실 다중 reviewer 합의 0**·calibration_ready/merge_gate_ready False(표본 floor live 200/KO 50).
- **embedding/LLM 실호출 / 실 병합 / 운영 DB**: 이번 턴 No-Go(옵션 E/F 금지). gold-calibrated adjudicator runtime·운영 DB 0009 배포·production 백로그 0.

## 🚧 닫을 수 없는 문제 (BLOCKED/UNKNOWN)
- BLOCKED: 실 gold = **reviewer 충원·실 라벨 파일** 선결(batch pack 은 launch substrate·production_gold 0). 실 병합 = gold-calibrated embedding/LLM adjudicator(옵션 E·다음 턴)+MERGE_GATE. production 백로그 = 운영 DB 0009 배포(승인)+실 fetch.
- UNKNOWN: semantic score 의 실 병합 허용 기준(production precision·실 gold·reviewer 합의·한국어 캘리브레이션·MERGE_GATE) → R-IdentityHumanLabeling·R-ReviewerAgreement·R-IdentityEvalDataset·R-SemanticIdentityAdjudicator 선결. ADR#64 의 deterministic 0 이 paraphrase recall 한계인지 real scarcity 인지도 여전히 n=2 미구분(gold 필요).

## 👉 다음 할 일
1. **[커밋 대기]** 본 턴 ADR#67: 신규 2(reviewer_batch_launch.py+test) + 수정 8(docs 7·PROJECT_STATUS) = 10파일 커밋 여부 지시 대기. push 금지.
2. **[다음 단계]** (a) **first production labels import**(실 reviewer 가 batch pack 으로 라벨 작성→intake→production gold) → (b) reviewer recruitment(운영·SLA·roster) → (c) **embedding scorer opt-in 실호출**(`create_embedding_client` seam·secret-safe·bounded·internal-only) → (d) MERGE_GATE calibration(precision≥0.98·표본 floor) → (e) embedding/LLM/KG **실 병합** adjudicator runtime → (f) (승인 하) 운영 DB 0009 배포 + scheduler → RAG/KG/Agent/IU.

## 📁 근거 (이번 턴 핵심)
- 커밋: `8776be8`(ADR#66·10파일·+1054/−54·push 0). ADR#67 변경: 신규 `reviewer_batch_launch.py`(orchestrator)+`test_reviewer_batch_launch.py`(67)·docs 7·PROJECT_STATUS. **ADR#66 커밋분 frozen·단방향 import·재구현 0·ADR#66/#60/#59 머신 조합·EVENT_SCHEMA 무변경.**
- 검증(정직): 신규 **67**(reviewer_batch_launch 1-67: batch launch 1-8·reviewer-facing secrecy 9-15·assignment policy 16-22·label template/intake 23-30·agreement/calibration 31-39·no-merge/no-LLM/no-DB/secret 40-46·정규화/production path/drift lock/회귀 47-57·**감사 fix-lock 58-67**)·backend 비-live **1015 passed/101 skipped/0 failed**(948+67·DB 미기동→live-PG skip)·**live-PG=미실행**(DB down; 단 변경이 DB/session/ORM 경로 무접촉=orchestrator write-free·`db_write:False`·session 인자 부재·DB 바운드 도구 미사용→무영향·baseline 불변)·ingestion **1353 passed**(불변)·frontend tsc**0**/node:test **12**/lint **0**(불변)·secret scan **PASS**(140 files)·docs_lifecycle conflicts **0**·ruff 실게이트(E/F/I/B/W−E501) **0**·dead-code(신규 모듈) **0**. **honest 동작**: 라벨 없음→`awaiting_labels`·production_gold 0·malformed→`invalid_labels`·model label→거부·unknown pair_id→거부·production+live unanimous human→production_gold 1·calibration_ready False·order-invariant·synthetic→production_gold 0. 운영 DB 무변경.
- 감사: adversarial-reality-critic **PROCEED-WITH-FIXES**(cardinal=false-merge/model→gold/labeler-leak/unsure→gold 전부 VALID) + code-review(2 HIGH 실질) → **MEDIUM/HIGH 8건 전부 수정**: CR-1(HIGH·crash) adjudication label 정규화 부재→`_normalize_adjudications`·CR-2(HIGH·latent) CLI `--validate` fixture pair_id 하드와이어→schema-only·B-2(MED) intake 가 frozen 검사 3개(reviewed_at/dataset_source/risk_tags) 누락→frozen `_validate_reviewed_at`+`DATASET_SOURCES` 재사용+동등성 테스트·B-3(MED) `normalized_rows` raw reviewer_id/rationale echo→`_public_intake_report` 제거+duplicate key pseudonymize·B-1(MED) production_gold 선언 기반→`production_gold_provenance_verified:False` 표면화·B-4(LOW) title len literal→frozen import·CR-3 vacuous test·CR-4 coverage·CR-5 미사용 param(테스트 58-67·stamp `audit_evidence` 단일 출처). **자체 발견·수정(decisive gold)**: 2명 만장일치 needs_review(→ambiguous)가 frozen `resolve_gold_from_reviewers` 로 gold 승격 가능→§6 "unsure/needs_review only→not gold" 위반→production/synthetic gold 를 **same/different 만** 카운트(calibration preflight 도 decisive only·`non_decisive_gold_count` 표면화·test_49 잠금). 핵심 안전축(score/rationale/predicted_status labeler 숨김·forbidden field allowlist fail-loud·reviewer_id pseudonymization·single≠gold·conflict≠자동 다수결·model/self/LLM≠gold·unsure/needs_review≠gold·synthetic≠production gold·merge_allowed False·production_gold 0·db_write 0·source role guard).
- 문서: ADR#67(`_DECISIONS`)·8 RISK 부분진전(`_RISK`·종결 0·신규 0)·ADR#67 블록+runbook+RealSourceLoop 표 7/8/9+정직 경계(`2_ROADMAP/15`)·(11y)(`2_ROADMAP/00`)·condition 4/agent(`RAG_KG`)·reviewer batch launch 계약(`IU_CONTRACT`)·`_CANONICAL/02`(ADR#41~#67 + reviewer batch launch 절). **EVENT_SCHEMA 무변경**(ADR#67 DB-free·event 필드/테이블 무변경·최소 변경).

---
_as_of: 2026-06-26 · ADR#67 reviewer batch launch pack + intake validation loop — ADR#66 커밋(`8776be8`) 후, ADR#66 reviewer label 운영 함수를 사람이 바로 라벨링을 시작할 운영 패키지로 전환(새 파일 1·재구현 0·ADR#66/#60/#59 조합·DB 0): `reviewer_batch_launch.py`(launch-readiness orchestrator) — `build_reviewer_instruction`(§6 4-라벨 정의/금지/권장)·`build_label_template`(`REVIEWER_ALLOWED_KEYS` 빈 worksheet·pseudonym·score/bucket 0)·`build_assignment_manifest`(pair 당 ≥2 reviewer·top-k+hard negative·capacity<2→insufficient·raw roster 미commit)·`build_intake_plan`(directory/expected files/validation command)·`validate_label_intake`(§8 import 前 dry-run·labeler vocab 정규화·forbidden/duplicate/unknown pair_id/model label 거부)·`run_reviewer_batch_launch`(§4·intake_status·decisive gold=same/different 만)·`BATCH_LAUNCH_AGENT_CONTRACT`·CLI. 옵션 **A+B+C+D 채택·E(embedding/LLM runtime)/F(production DB·merge) 금지·신규 RISK 0**. **측정**: 신규 67·backend 비-live 1015p/101s/0f·live-PG 미실행(DB down·경로 무접촉·baseline 불변)·ingestion 1353p·frontend tsc0/test12/lint0·secret PASS·docs_lifecycle 0·ruff 실게이트 0·dead-code 0. **honest 동작**(라벨 없음→awaiting_labels·production_gold 0·malformed→invalid_labels·model label→거부·production+live unanimous→production_gold 1·calibration_ready False). **자체 발견·수정**: 만장일치 ambiguous gold 승격 가능→decisive gold(same/different)만 카운트(test_49). **감사 PROCEED-WITH-FIXES**(cardinal 전부 VALID)·adversarial/code-review HIGH 2(adjudication 정규화 crash·CLI fixture pair_id)+MEDIUM 3(intake 3검사·raw PII echo·production_gold 선언 기반) 전부 수정(58-67 잠금). **정직 경계**: 진전=reviewer batch launch readiness substrate·**실 gold 아님**(production_gold_count 0·synthetic/test=simulated only·calibration_ready/merge_gate_ready False·embedding/LLM 실호출 0·운영 DB/public IU/production 백로그 0 불변·완전종결=OVERCLAIM)·점수≠truth·다음 hard blocker=first production labels import. **R-IdentityHumanLabeling·R-ReviewerAgreement·R-GoldSamplingBias·R-IdentityEvalDataset·R-SemanticIdentityAdjudicator·R-LiveIdentityBacklog·R-SourceOverlapScarcity·R-RealSourceLoopUnproven 부분진전**·종결 0·신규 0. ADR#66 커밋 `8776be8` 위 ADR#67 미커밋(신규 2+수정 8=10)·커밋 지시 대기·push 안 함._
