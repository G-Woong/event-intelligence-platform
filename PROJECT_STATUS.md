# 📊 프로젝트 진행 현황 (PROJECT_STATUS)

> 매 턴 끝에 새로 쓰입니다. 항상 "가장 최근 1턴"만. 과거는 `git log -- PROJECT_STATUS.md`.
> 사실 원본(자동): `.harness/machine_status.json` · 완료 증거: `.harness/closeout_stamp.json` · 서술: 이 파일(에이전트).

## 🟢 한눈에 (비개발자용 3줄)
- **지금 무엇을 했나:** 지난 턴(ADR#78 — "좁힌 seed로도 near-match 0[원인 양가·미확정] + 계약 문서")을 안정 기준점으로 **커밋**(`03829e4`)한 뒤, 그 0의 원인 (i)"같은 사건인데 규칙이 못 잡음[recall 한계]" vs (ii)"넓은 우산 아래 서로 다른 기사"를 **실제로 가려낼 레버**를 만들었습니다. 핵심은 **near-match recall probe** — 제목을 "Fed=Federal Reserve" 같은 **기관명 정규화·어간 정규화**로 다듬어 *같은 사건인데 표면이 달라 놓친 쌍*을 **reviewer 검토 후보로 끌어올리되**, 어느 정규화가 일치를 만들었는지까지 보고합니다(그래서 (i)/(ii)를 가름). **합성 검증(라이브 아님)**으로 결정론 측정: "Fed raises rates"↔"Federal Reserve lifts interest rates"가 규칙으론 0.125(놓침)였는데 probe로 0.333(검토 후보로 올림·공유 핵심어 federalreserve), 반대로 "Fed"↔"허리케인"은 0→0으로 **헛되이 올리지 않음**(다른 사건 판별). **결정적으로 이 probe는 자동 병합 경로를 절대 건드리지 않습니다**(병합은 여전히 정확 일치+gold+게이트). 이번 턴은 사용자가 "**합성만, 라이브 생략**"을 택해 외부 호출 0. 자동 병합·LLM/embedding 실호출·운영 DB·키 값 출력·실제 전송·입력/라벨 날조 모두 **0**.
- **이번 턴에 실제로 끝낸 것:** ADR#78 커밋(`03829e4`·20파일) → **ADR#79**: (1) 신규 `near_match_recall_probe.py`(feature-attributed 결정론 정규화=기관 phrase alias·acronym alias·light stemming·number normalize·**merge 경로 미호출**[AST 테스트로 물리 분리 잠금]·`merge_allowed=False`·score internal-only·body-free). (2) 신규 `r1_discrete_event_acquisition.py`(op `r1_discrete_event_acquisition_and_recall_probe`·discrete seed 엄격 검증[단일 이산사건·1d/2d·event phrase 요구·broad umbrella 거부]·ADR#78 gate 재사용·recall probe 통합·`refine_root_cause_with_recall_probe`[(i)/(ii) 구조 진전·단정 0]). (3) internal ops 배선(`InternalOpsDiscreteAcquisitionFrontier` 22 field + `GET /api/internal/ops/r1-discrete-acquisition` + TS + view·**dict==pydantic==TS 22==22==22**). **사용자 'synthetic only'→live 미실행.** **변경 표면 = backend 4 + frontend 3 + docs 10 + backend test(API) 1 + 신규 test 2 = 20파일**(신규 4).
- **정직한 한계:** 이번 진전은 **recall probe 구현 + synthetic known case 검증 + discrete-event acquisition 머신리(seed 검증) + (i)/(ii) 구조 진전**이지 **실 라이브 run이 아닙니다**(user opt synthetic-only). recall probe는 **synthetic에서만 검증**됐고 **live candidate pair엔 미적용** — 따라서 실 ADR#77/#78 below-floor 쌍이 (i)인지 (ii)인지는 **여전히 미분리**(probe는 (i) 레버가 실재·작동함을 증명할 뿐). **production_gold_count 0**·실 reviewer contact·실 returned labels·실 production candidate freeze 여전히 0(gate exact passthrough·`current_r1_gap=200`). embedding/LLM 실호출·자동 병합·운영 DB·public IU·실 전송 모두 **0**(Lane F는 계약 가드만·`product_bridge_runtime_built=False`). 다음 hard blocker=**단일 discrete event·1d·LIVE run + recall probe를 live pair에 적용**해 (i)/(ii) 경험적 분리 → provider breadth → 실 reviewer contact → actual returned labels. **push 금지·커밋 지시 대기**.

## 📋 자동 수집 사실 (machine_status.json)
- **git HEAD `03829e4`**(ADR#78·본 턴 첫 커밋·20파일·+1841/−49) 위 **ADR#79 = 미커밋**: 신규 4(`backend/app/tools/near_match_recall_probe.py`·`backend/app/tools/r1_discrete_event_acquisition.py`·`backend/tests/test_near_match_recall_probe.py`·`backend/tests/test_r1_discrete_event_acquisition.py`) + 수정 16(backend: `schemas/internal_ops.py`·`api/internal_ops.py`·`tests/test_internal_ops_api.py`; frontend: `lib/api/types.ts`·`lib/ops/opsPilotExecutionView.ts`·`lib/ops/__tests__/opsPilotExecutionView.test.mjs`; docs 10: `5_REFERENCE/RAG_KG_AGENT_READINESS·INTELLIGENCE_UNIT_CONTRACT·RAG_KG_ENTITY_GATE_CONTRACT`·`2_ROADMAP/00·15·19`·`_CANONICAL/02`·`_DECISIONS/2026-06`·`_RISK/RISK_REGISTER`·`PROJECT_STATUS`) = **추적 20항목**. **migration 없음·ADR#78 커밋분 frozen·단방향 import·재구현 0(ADR#78 gate 재사용)·EVENT_SCHEMA(DB-free 무변경)·main.py/config/.env/.env.example/package.json/LLM_EVIDENCE_PACKET_CONTRACT 무변경.**
- 열린 RISK: **종결 0·신규 1**(R-NormalizationRecallLeakage[LOW·테스트 차단]). 부분진전: R-DeterministicNearMatchRecall·R-CrossSourceNearMatchGap·R-ProviderPairNarrowness·R-ProductionCandidateScarcity·R-RagKgPrematureBuild·R-KoreanR1SourceGap. throwaway 는 `.harness/_TRASH/`·`frontend/.harness/`·`--paths/`·`--help/`(stray·커밋 제외)·`outputs/reviewer_batch/`(gitignore)·scratchpad(저장소 밖).

## ✅ 이번 턴에 달성한 것 (discrete-event acquisition + deterministic recall probe[reviewer-routing recall·merge 분리] + provider breadth — ADR#79)
- **① ADR#78 커밋**: 20파일 → `03829e4`(제공 메시지 그대로·secret PASS·docs_lifecycle conflicts 0·closeout SIGNATURE_MATCH 25==25·push 0·NO UPSTREAM). ADR#79 안정 기준점.
- **② 원자 분석(§2·20문항·구현 전 보고·코드 5개 정독)**: detector `_title_tokens`=소문자+21 stopword+len>1·**stemming/entity alias/number 정규화 전무**(한글 토큰은 잡으나 형태소 분해 없음)→paraphrase 가 표면 변형만으로 floor(0.2) 아래로. **near=reviewer 라우팅 gate≠merge gate**(merge=`cluster_records`/`semantic_identity_fingerprint`=exact token-set+date+gold+MERGE_GATE)→recall 완화 false-merge 위험 0. ADR#78 strategy 가 이미 recall probe 지목·기존 `fake_semantic`(char-bigram 블랙박스·"not validated recall")와 달리 probe 차별점=**feature-attributed 정규화**(어느 alias/stem 이 매치 생성)→(i)/(ii) 판별 레버. `naver_news_search`=registry 등재(type news·api_key_required)·**fetcher 미배선**(KO floor 레버).
- **③ 옵션 결정(§3)**: **A(actual input)+B(discrete-event live·승인 시)+C(recall probe·핵심)+D(provider/KO plan)+E(frontier)+F(contract guard) 채택·G/H(embedding/LLM/merge/public IU/DB runtime) 금지.** per-turn live 승인 질의→**user 'synthetic만, live 생략'** 선택(approval 미이월·정직).
- **④ C — deterministic recall probe(핵심·reviewer-routing ONLY)**: 신규 `near_match_recall_probe.py`(feature-attributed 정규화=organization phrase alias[`federal reserve`→`federalreserve`·다토큰 인접]·acronym alias[`fed`/`scotus`/`ecb`…·모호 단어 `who`/`us`/`un`/`eu` 제외]·light stemming[`rates`→`rate`·`operations`→`operation`·`shipping`→`ship`]·number normalize·routing stopword). **merge 경로 미호출**(AST 테스트로 `cluster_records`/`semantic_identity_fingerprint` import/참조 0 잠금)·`recall_probe_applies_to_merge=False`·`merge_allowed=False`·score `score_exposed_to_reviewer/public=False`·body-free 요약·`RECALL_PROBE_CONTRACT` forbidden 명문화.
- **⑤ recall probe 실측(synthetic known-paraphrase fixture·network 0·결정론)**: `fed_acronym`("Fed raises rates again"↔"Federal Reserve lifts interest rates") baseline Jaccard **0.125**(merge-path 놓침)→probe **0.333**(routing 올림)·lift +0.208·공유 `[federalreserve,rate]`·**entity 공유** federalreserve·**newly_routed True**. `scotus_phrase`(SCOTUS↔Supreme Court) **0.111→0.286**·entity [supremecourt]. `stem_generalization`("Officials probe banks"↔"Official probes bank") baseline 0→probe **1.0**·**alias 없이 stem 만으로 lift**(entity 0·curated table 밖 일반화). `diff_control`(Fed↔Hurricane) **0.0→0.0**·**newly_routed False**(무토큰중첩 음성 미lift). `pairs_newly_routed_by_probe=3`(entity 2 + non-entity stem 1)·sharing_entity 2 → **구성된 known case 에서 정규화가 below-floor 같은-사건을 routing 으로 올림을 결정론 시연**(=(i) recall-miss 레버가 작동·alias 테이블 밖 stem 일반화 포함)·**단 representative/held-out/live 일반화는 미측정**(deferred). **adversarial 검토 반영(이 턴 수정)**: 짧은 canonical 동음(raw 'sec'=seconds)이 거짓 entity 로 승격되던 결함을 **provenance 게이트**(alias origin 양쪽 + canonical 어휘 AND)로 차단·`_collapse_phrases` collapsed-flag 도입·tautology 테스트 교정·stem 일반화/same-entity-honesty/whitelist 테스트 추가.
- **⑥ B — discrete-event acquisition 머신리**: 신규 `r1_discrete_event_acquisition.py`(op `r1_discrete_event_acquisition_and_recall_probe`)·`validate_discrete_event_seed`(단일 이산사건·tight window 1d/2d·named entity+event phrase 요구·broad umbrella[`federal reserve`/`ukraine war`] 거부·community/market/catalog 거부)·ADR#78 gate/diagnostic/provider/Korean 재사용·`refine_root_cause_with_recall_probe`((i)/(ii) 구조 진전·단정 0·indeterminate 보존). dry: seed 3 valid(`code_proposed_shape`·특정 사건 날조 0)·live 0·`insufficient_debug_artifact`·`blocked_no_live_opt_in`·freeze 0·gold 0·gap 200.
- **⑦ D/E/F — provider/Korean/frontier/contract**: ADR#78 `build_provider_expansion_plan`/`build_korean_source_strategy` 재사용(provider breadth·KO floor ≥50 current 0 가시화·Naver adapter=lever). internal ops `InternalOpsDiscreteAcquisitionFrontier`(22 field·same_event truth/per-pair score/rationale/predicted_status/raw body/PII/secret 필드 부재)+`GET /api/internal/ops/r1-discrete-acquisition`(read API live 0·flag off 404·503 no path leak)+TS+view(§9 copy "Recall probe is reviewer-routing only, not merge"). RAG_KG_ENTITY_GATE_CONTRACT §6 에 recall probe guard 추가(recall lift ≠ same_event ≠ merge ≠ KG edge).
- **⑧ G/H 경계**: embedding/LLM scorer runtime·production DB·semantic merge·public IU·GraphRAG runtime 미착수(이번 턴 금지).

## 🧭 cross-batch identity 4단계 + 평가/gold gate (정직)
- ① anchor 병합(ADR#40) → ② 후보 LINK(ADR#41) → ③ shadow 판정(ADR#42·…·targeted live acquisition + near-match gap diagnostic ADR#78·**discrete-event acquisition + deterministic recall probe ADR#79**) → ④ 실 병합(**미구현**).
- **ADR#79 위치 / 핵심**: ② deterministic near-match 의 **recall 한계**가 (i) 원인일 경우를 메우는 레버 = recall probe(**reviewer 라우팅 recall 만**·merge 경로 물리 분리). synthetic known-paraphrase 에서 below-floor 같은-사건을 entity alias 로 routing 올림(measured)·different-events 미lift(measured). 단 **실 frontier 의 (i)/(ii)는 미분리**(probe 는 synthetic 에서만 검증·live pair 미적용). near 는 reviewer 라우팅이지 merge 가 아니므로 recall 개선이 false-merge 없이 가능(merge=fingerprint+gold+MERGE_GATE 불변).
- **⚠ 미해소(OPEN):** near-match 0 원인 (i)/(ii)의 **실 live 분리**(discrete-event 1d LIVE run + recall probe 적용)·provider breadth·실 production candidate freeze(0)·실 reviewer contact·첫 returned labels·실 gold(production_gold 0)·R1 gold floor(live 200·KO 50·pos/neg 67·hard-neg 20)·MERGE_GATE 런타임·KO 캘리브레이션(KO source 미배선)·운영 DB 0009 배포·RAG/KG/Entity/public IU(R1~R7)·internal ops per-user auth·UI 화면 render. "recall probe synthetic 검증 ≠ live frontier 분리·newly_routed ≠ same-event·routing recall ≠ merge·contract guard ≠ runtime" — 완전종결=OVERCLAIM.

## 🔎 source orchestration / 제품 계약
- official/news=publishable evidence(merge anchor)·community=반응(anchor 불가·`reaction_to`만)·market=signal·catalog=entity·search=URL candidate(truth 아님)·unknown=fail-closed. discrete seed 검증·production candidate 는 publishable×publishable(official/article/news)만 anchor(약화 0). **near=reviewer 라우팅 gate≠merge gate → recall probe 개선은 false-merge 위험 없이 가능**(probe 가 `cluster_records`/fingerprint 미호출·AST 잠금).
- **제품 계약(raw≠public)**: 출력·`internal_ops_discrete_acquisition_frontier`(sanitized) 노출 표면에 score/rationale/predicted_status/raw body/secret/raw reviewer 신원·same_event truth **0** — 출력 전 `_assert_pii_safe` 재귀 가드(정확 키 매칭: `score` 금지·`recall_probe_score`/`max_recall_probe_score` 허용)·API `response_model=InternalOpsDiscreteAcquisitionFrontier`(sanitized·22 field)·frontend `FORBIDDEN_OPS_FIELDS` 재귀 가드. recall probe per-pair score 는 reviewer/public 미노출(`max_recall_probe_score` 집계만). production_gold_count 0·gate **exact passthrough**·`merge_allowed=False`·`no_public_intelligence_unit`·embedding/LLM 실호출 0·DB write 0·`actual_sending_performed=False`. live query=**0**(user opt synthetic-only). Agent 는 recall probe·discrete acquisition·contract readiness 를 *계획* 가능하나 probe lift 로 same_event 단정·merge·anchor 약화·KG edge 무검증 생성·score reviewer/public 노출·PII/secret 출력·실 전송 불가.

## ⚠️ 이번 턴 종결/갱신 RISK
- **신규 1(실 present blocker만)**: **R-NormalizationRecallLeakage**(LOW·recall probe score/rationale 가 reviewer-facing 또는 public 으로 누출될 위험·완화=score internal-only[`score_exposed_to_reviewer/public=False`]·body-free·`validate_labeling_packet` fail-loud·forbidden-key 가드·잠금 test_near_match_recall_probe/test_r1_discrete_event_acquisition/test_internal_ops_api·완전종결=end-to-end 차폐 검증).
- **부분진전(종결 0)**: **R-DeterministicNearMatchRecall**(recall probe 구현·reviewer-routing only·false-merge AST 분리·**synthetic known case 에서 recall 개선 측정**[newly_routed 2]·**종결 금지[live 미측정]**) · **R-CrossSourceNearMatchGap**(recall probe 레버 시연·(i)/(ii) 구조 진전·overclaim 0·**종결 금지[live 미분리]**) · **R-ProviderPairNarrowness**/**R-ProductionCandidateScarcity**/**R-RagKgPrematureBuild**/**R-KoreanR1SourceGap**(ADR#79 참조·**종결 금지**).

## ❌ 달성하지 못한 것 & 왜 (이월)
- **실 live run / (i)/(ii) 실 분리 / 실 production candidate freeze / 실 gold**: user opt 'synthetic only'→live 미실행. recall probe 는 **synthetic 에서만** (i) 레버 작동 증명·**실 below-floor 쌍의 (i)/(ii)는 미분리**(live pair 미적용). 해소=**단일 discrete event·1d LIVE run + recall probe 를 live candidate pair 에 적용**(승인+host/rate governed)→일부 쌍이 routing band 진입하며 entity 공유하는지→(i)면 reviewer 후보·(ii)면 provider breadth.
- **adjudicator runtime / RAG·KG·Entity·public IU**: Lane F 는 contract guard(docs)일 뿐 runtime 미구현(`product_bridge_runtime_built=False`·embedding/LLM 실호출·실 병합·운영 DB·GraphRAG·public IU 이번 턴 No-Go·옵션 G/H 금지).
- **UI 화면 render**: frontier view/types/test(데이터층)는 배선됐으나 read API가 `live_query=False` 고정이라 internal ops UI는 `insufficient_debug_artifact`(비-live)만 표시·page.tsx 화면 render는 후순위.

## 🚧 닫을 수 없는 문제 (BLOCKED/UNKNOWN)
- BLOCKED: 실 (i)/(ii) 분리 = **단일 discrete event·1d LIVE run + recall probe 를 live pair 에 적용**(R-CrossSourceNearMatchGap·R-DeterministicNearMatchRecall). 실 production candidate = (i)면 routing 후보·(ii)면 provider breadth→near-match publishable pair 확보. 실 gold = 실 후보→reviewer recruitment→실제 contact(evidence)→첫 returned label 회수(R-ProductionCandidateScarcity·R-RealSourceLoopUnproven). 실 병합 = gold-calibrated embedding/LLM adjudicator(R3)+MERGE_GATE(R2). KO floor = KO live-query source 배선(Naver adapter·R-KoreanR1SourceGap).
- UNKNOWN: **실 live below-floor 쌍이 (i) recall/normalization 인지 (ii) different-events 인지**(synthetic 검증으로 레버는 확인했으나 live 미적용). discrete-event 1d LIVE 가 entity-공유 routing band 쌍을 만들지(미측정). recall probe 가 live 에서 same-event recall 을 올릴지(미측정·synthetic 만). provider breadth(RSS fleet·Naver)가 near-match 수율을 올릴지(미테스트). 실 reviewer 회수율/SLA(plan·machinery만·실 운영 0).

## 👉 다음 할 일
1. **[커밋 대기]** 본 턴 ADR#79: 신규 4(`near_match_recall_probe.py`+`r1_discrete_event_acquisition.py`+test 2) + 수정 16(schema/api/api-test·frontend types/view/test·docs 10) = **20파일** 미커밋. 커밋 여부 지시 대기. push 금지. (`--paths/`·`--help/` stray 정리 허가도 함께 받으면 처리.)
2. **[다음 단계·frontier]** (a) **단일 discrete breaking event·1d·LIVE run + recall probe 를 live candidate pair 에 적용**해 (i)/(ii) 경험적 분리(승인+host/rate governed·seed turn/scheduled 분산). (b) **provider breadth**(key-free RSS 다출처 fleet·GDELT cooldown-honored·미배선 Naver/NewsAPI adapter wiring[KO floor]). (c) **reviewer recruitment + actual contact evidence + 첫 returned labels 회수**(R1) → (d) gold denominator(live 200·KO 50·**R1**) → (e) MERGE_GATE(precision≥0.98·**R2**) → (f) embedding scorer opt-in(**R3**) → (g) entity/KG/GraphRAG(**R4~R6**) → (h) Agent/public IU(**R7**). 별도 축: internal ops UI 화면 render·per-user auth.
3. **[권장·bounded]** live acquisition 실험은 **bounded·opt-in·rate governed·seed 분산**일 때만(무단 대량 호출 0·no-bypass): 단일 discrete event 로 recall probe 의 live (i)/(ii) 분리 측정(다음 턴·source role guard 유지).

## 📁 근거 (이번 턴 핵심)
- 커밋: `03829e4`(ADR#78·20파일·+1841/−49·push 0·NO UPSTREAM). ADR#79 변경: 신규 4(`near_match_recall_probe.py`+`r1_discrete_event_acquisition.py`+test 2)·수정 16(internal_ops schema/api·API test·frontend types/view/test·docs 10). **ADR#78 커밋분 frozen·재구현 0(ADR#78 gate 재사용)·EVENT_SCHEMA/main.py/config/.env/.env.example/package.json/LLM_EVIDENCE_PACKET_CONTRACT 무변경.**
- recall probe(실측·synthetic known-paraphrase·network 0·결정론): `fed_acronym` baseline **0.125**→probe **0.333**(entity federalreserve·newly_routed True)·`scotus_phrase` 0.111→0.286(entity supremecourt)·`diff_control`(Fed↔Hurricane) 0→**0**(newly_routed False·false lift 차단)·`pairs_newly_routed_by_probe=2`·sharing_entity 2·`recall_probe_applies_to_merge=False`·`merge_allowed=False`·`score_exposed_to_reviewer/public=False`. discrete tool dry: seed 3 valid(code_proposed_shape)·`targeted_live_query_executed=False`·`live_call_count=0`·`production_candidate_status=blocked_no_live_opt_in`·`candidate_provenance=none`·`production_gold_count=0`·`current_r1_gap=200`·`merge_allowed/llm_invoked/embedding_invoked/db_write/actual_sending_performed/same_event_truth_exposed/raw_source_body_exposed=False`·`product_bridge_runtime_built=False`.
- 검증(정직): 신규 backend **31**(test_near_match_recall_probe: 정규화·lift·merge 분리 AST·score-hidden·결정론·**provenance entity 게이트[raw sec 동음 차단]**·stem 일반화·same-entity-honesty)+**29**(test_r1_discrete_event_acquisition: seed validation·recall probe 통합·(i)/(ii) 보존·6-state·안전)+**8**(test_internal_ops_api: r1-discrete-acquisition flag off 404/sanitized/405/no forbidden/whitelist/prod fail-closed)·**backend 전체 1535 passed/101 skipped/0 failed**(재집계·신규 6 추가)·ingestion **1353 passed**·frontend **tsc 0**·**lint 0**(`next lint`)·node:test **46 passed**(ops view +7: 39→46)·ruff 실게이트(E/F/I/B/W−E501) **0**·secret scan **PASS**·docs_lifecycle conflicts **0**. live=**0 seed**(user opt synthetic-only·network 0·DB/merge/LLM/embedding/전송 0).
- 감사(closeout subagents): architecture/pipeline/actual_input_gate/discrete_event_acquisition/near_match_gap_diagnostic/deterministic_recall_probe/normalization_recall/provider_readiness/provider_expansion/korean_source_strategy/credential_secret_boundary/source_overlap/production_candidate_batch/synthetic_vs_production_separation/source_role_guard/acquisition_frontier/llm_evidence_packet/rag_ingestion_gate/kg_edge_eligibility/community_reaction_boundary/public_iu_gate/internal_ops_dual_track/public_truth_boundary/reviewer_pii/secret_boundary/code/test/evidence/risk_closure/**adversarial** review. cardinal(recall probe 를 merge/same_event 로 사용 금지·synthetic 검증을 live 분리로 단정 금지·near-match 0→LLM 점프 금지·score reviewer/public 노출·community/market/catalog anchor·source role guard 약화·MERGE_GATE 전 merge·`.env` 값 읽기·테스트 없는 정책 변경) 차단 확인.
- 문서: ADR#79(`_DECISIONS`)·RISK(`_RISK`·종결 0·신규 1[R-NormalizationRecallLeakage]·R-DeterministicNearMatchRecall/R-CrossSourceNearMatchGap 부분진전)·ADR#79 블록(`2_ROADMAP/15` descending)·ADR#79 마커(`2_ROADMAP/00`)·ADR#79 §19(`2_ROADMAP/19` ascending)·**§6b-R1f discrete-event acquisition + recall probe**(`RAG_KG_AGENT_READINESS`)·§4 ADR#79 bullet(`IU_CONTRACT`)·recall probe guard(`RAG_KG_ENTITY_GATE_CONTRACT` §6)·`_CANONICAL/02`(ADR#41~#79). **EVENT_SCHEMA 무변경**(ADR#79 DB-free).

---
_as_of: 2026-06-28 · ADR#79 discrete-event acquisition + deterministic recall probe[reviewer-routing recall·merge 분리] + provider breadth — ADR#78 커밋(`03829e4`) 후, near-match 0(원인 미분리)을 가르는 **경험적 레버**를 구현(신규 2 tool·재구현 0·user opt synthetic-only→live 0·DB 0·전송 0·secret 값 미열람): 신규 `near_match_recall_probe`(feature-attributed 정규화=org phrase/acronym alias·light stemming·number normalize·**merge 경로 미호출**[AST 잠금]·merge_allowed False·score internal-only·body-free)+신규 `r1_discrete_event_acquisition`(discrete seed 엄격 검증·ADR#78 gate 재사용·recall probe 통합·`refine_root_cause_with_recall_probe`[(i)/(ii) 구조 진전·단정 0])+internal ops(`r1-discrete-acquisition` 22 field·dict==pydantic==TS·read API live 0). **recall probe 실측(synthetic known-paraphrase)**: fed_acronym 0.125→0.333(entity federalreserve)·scotus 0.111→0.286(entity supremecourt)·stem_generalization 0→1.0(alias 없이 stem만·entity 0)·diff_control 0→0(무토큰중첩 미lift)·newly_routed 3(entity 2 + non-entity stem 1)=**구성된 known case 에서 정규화가 below-floor 같은-사건을 routing 으로 올림을 결정론 시연**(=(i) recall-miss 레버 작동·alias 밖 stem 일반화 포함)·**representative/held-out/live 일반화 미측정(deferred)**. **adversarial 검토 반영**: raw 'sec'(seconds) 거짓 entity 승격 결함을 provenance 게이트로 차단·tautology/whitelist/stem 일반화/same-entity-honesty 테스트 추가. 옵션 **A+B(deferred)+C(recall probe)+D+E+F 채택·G/H 금지**. **측정**: 신규 backend 31+29+8·**backend 전체 1535p/101skip/0fail**·ingestion 1353p·frontend tsc0/lint0/test46(ops +7)·ruff 0·secret PASS·docs_lifecycle 0. **정직 경계**: 진전=recall probe 구현+**synthetic known case 검증**+discrete acquisition 머신리+(i)/(ii) 구조 진전·**실 live run 아님**(recall probe live pair 미적용→실 (i)/(ii) 미분리)·실 production candidate freeze 0·production_gold_count 0·gate exact passthrough·adjudicator runtime/public IU/merge/운영 DB/embedding·LLM 실호출/실 전송 0·`product_bridge_runtime_built=False`·완전종결=OVERCLAIM. 다음 hard blocker=단일 discrete event·1d LIVE run + recall probe 를 live pair 에 적용해 (i)/(ii) 경험 분리→provider breadth→실 reviewer contact→actual returned labels. **신규 R-NormalizationRecallLeakage(LOW)·R-DeterministicNearMatchRecall/R-CrossSourceNearMatchGap 부분진전**·종결 0. ADR#78 커밋 `03829e4` 위 ADR#79 미커밋(신규 4+수정 16=20)·커밋 지시 대기·push 안 함._
