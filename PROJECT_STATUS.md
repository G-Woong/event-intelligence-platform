# 📊 프로젝트 진행 현황 (PROJECT_STATUS)

> 매 턴 끝에 새로 쓰입니다. 항상 "가장 최근 1턴"만. 과거는 `git log -- PROJECT_STATUS.md`.
> 사실 원본(자동): `.harness/machine_status.json` · 완료 증거: `.harness/closeout_stamp.json` · 서술: 이 파일(에이전트).

## 🟢 한눈에 (비개발자용 3줄)
- **지금 무엇을 했나:** 지난 턴(ADR#86)이 **"Federal Register(공식 출처) adapter 는 배선했고 date-honoring 도 live 검증했으나, 공식 문서와 news 보도가 같은 사건을 같은 기간에 다루는 일이 드물어 official×news 후보가 0"**으로 남긴 숙제를, 이번 턴(ADR#87)에 **그 둘을 잇는 acquisition loop 를 실제로 구축**했습니다. (1) **regulatory-class event seed bank** — 공식 문서(FR)와 news 보도가 *같은 subject·같은 window 에 동시에 잡힐 가능성이 높은* 규제 이벤트 클래스(EPA 최종규칙·SEC 집행·FDA 안전조치·OFAC 제재)를 구조화하고, 단순 keyword 가 아니라 **official 검색어(FR 공식 어휘)와 news 검색어(보도 어휘)를 분리** 보유하며 named 기관/행위/날짜가 없으면 결정론으로 거부. (2) **official×news live acquisition** — FR(공식)과 guardian/nyt(news)를 *같은 window* 로 수집(news 측은 `enforce_window=True` 로 날짜 무시 방어)하고 bridge 를 적용해 reviewer-routing 후보를 산출, freeze-eligible 후보가 있으면 **production candidate freeze 까지** 연결. (3) freeze 경로를 **fake transport 로 end-to-end 결정론 검증**(공식+기사 둘 다 publishable → 기존 freeze 머신 무수정 재사용 → reviewer worklist·gold 0). **production gold·자동 병합·LLM/embedding 실호출·public IU·실 전송 모두 0.**
- **이번 턴에 실제로 끝낸 것:** (1) **regulatory_event_seed_bank**(`regulatory_event_seed_bank.py`·official_query≠news_query·named agency/action/ISO date 요구·broad/generic reject 자가검증 6/6·17 test). (2) **official_news_live_acquisition**(`official_news_live_acquisition.py`·FR official + news[enforce_window] → bridge → 결과 9-state 분류 → freeze 시도 → handoff·official×news 전용 reviewer instruction·14 test). (3) **bridge freeze-eligible record-pair iterator**(`iter_freeze_eligible_record_pairs`·freeze 술어 단일출처·title-free 출력 보존·기존 9 test byte-보존+4). (4) orchestrator/frontier **+5 필드** **dict==pydantic==TS==mjs 51==51==51==51**·required_copy +2. (5) **freeze 경로 결정론 검증**(fake transport 로 official×news in-window pair → `run_r1_production_candidate_acquisition` 무수정 freeze → production_candidate_batch_ready=True·gold 0). **변경 표면 = 신규 4(tool 2+test 2)+수정 backend 6+frontend 3+docs 7 = 20파일**(strays 제외·전부 additive·byte-보존).
- **정직한 한계:** **acquisition loop·freeze 경로는 결정론으로 완성·검증했으나 실 same-subject regulatory live run 은 미실행** — operator 가 *실제 FR 문서 + news 보도가 같은 subject·같은 window 에 동시 존재하는* regulatory event/date 를 제공하지 않았다(curated EPA seed 의 window 는 제안·operator 확인 필요·R-RegulatorySeedQuality). 따라서 **실 live official×news same-event pair 0·production candidate freeze 0(실 live 기준)**·reviewer_handoff_ready=False(실 live)·actual returned labels 0·**production_gold_count 0**(`current_r1_gap=200`). **seed bank/acquisition/freeze 머신 무장 ≠ 후보 확보**·**freeze 경로 결정론 검증 ≠ 실 live 수율**·**bridge candidate ≠ truth**. 자동 병합·운영 DB·public IU·실 전송·GraphRAG/KG runtime·embedding/LLM 실호출 모두 **0**. 다음 hard blocker = **operator 가 확인한 실 regulatory event(FR 문서 + news 보도 동시)** → official×news live-derived freeze → 실 reviewer contact → **actual returned human labels**(R1). **push 금지·커밋 지시 대기**.

## 📋 자동 수집 사실 (machine_status.json)
- **git HEAD `21c0bb5`**(ADR#86·직전 턴 커밋) 위 **ADR#87 = 미커밋**: 신규 4(backend tool 2: `tools/regulatory_event_seed_bank.py`·`tools/official_news_live_acquisition.py`; backend test 2: `tests/test_regulatory_event_seed_bank.py`·`tests/test_official_news_live_acquisition.py`)·수정 16(backend 6: `tools/official_news_role_bridge.py`·`tools/r1_bounded_live_breadth_run.py`·`schemas/internal_ops.py`·`tests/test_official_news_role_bridge.py`·`tests/test_r1_bounded_live_breadth_run.py`·`tests/test_internal_ops_api.py`; frontend 3: `lib/api/types.ts`·`lib/ops/opsPilotExecutionView.ts`·`lib/ops/__tests__/opsPilotExecutionView.test.mjs`; docs 7: `PROJECT_STATUS`·`_DECISIONS/2026-06`·`_RISK/RISK_REGISTER`·`2_ROADMAP/00·15·19`·`_CANONICAL/02`). **전부 additive(신규 모듈 network 0/opt-in·bridge 는 술어 추출 refactor 로 build_official_news_bridge 출력 byte-identical·frontier 46→51 추가·migration 0)·EVENT_SCHEMA/main.py/config/.env/.env.example/package.json/federal_register_live_smoke.py/provider_query_adapters.py/cross_source_live_overlap_smoke.py/r1_production_candidate_acquisition.py/reviewer_handoff_bridge.py 무변경.**
- 열린 RISK: **종결 0·신규 1**(R-RegulatorySeedQuality[MEDIUM]). 부분진전: R-OfficialNewsDomainMismatch(MEDIUM→부분진전·acquisition loop 구축·mismatch 분류·종결 금지)·R-WindowHonoringSourceScarcity(window-honoring FR 을 acquisition loop 에 연결)·R-ProductionCandidateScarcity(official×news freeze 경로 결정론 완성·실 yield 0). throwaway/stray(`.harness/_TRASH/`·`frontend/.harness/`·`--paths/`·`--help/`)·`outputs/`(gitignore)·scratchpad(저장소 밖)는 커밋 제외.

## ✅ 이번 턴에 달성한 것 (regulatory-class event seed bank + official×news live acquisition + production candidate freeze 경로 — ADR#87)
- **① ADR#86 커밋(§4)**: `21c0bb5`(23 files·+1271/−67·push 0·NO UPSTREAM). FR adapter + official×news role-bridge + FR live smoke.
- **② regulatory-class event seed bank(§8)**: 신규 `regulatory_event_seed_bank.py`(named_event_seed_bank 패턴) — `validate_regulatory_seed`(official_provider=federal_register·publishable news provider·named agency/entity[placeholder 거부]·action_phrase·ISO date_window·allowed regulatory domain·non-broad topic 을 **모두** 요구·결정론 reject)·`_curated_regulatory_seed_bank`(EPA final rule[live-selectable]·SEC/FDA/OFAC[respondent/product/target 미특정 template])·broad reject 자가검증 6/6·**official_query≠news_query 분리**·provenance=code_proposed_regulatory_shape·same_event 0. 17 test.
- **③ official×news live acquisition(§9·§11·§12)**: 신규 `official_news_live_acquisition.py` — seed → FR official(`federal_register_live_smoke`·key-free) + guardian/nyt news(`run_provider_query`·**enforce_window=True**·date_filter_ignored 방어) **같은 window 수집** → `build_official_news_bridge` → **9-state 분류**(invalid_seed/blocked_no_opt_in/provider_unavailable/host_gate/rate/official_no_records/news_no_records/no_in_window_news/no_official_news_overlap/bridge_candidates_found/production_batch_frozen) → freeze-eligible 후보면 freeze 시도 → handoff. **official×news 전용 reviewer instruction**(official=authoritative evidence·news=public reporting·같은 broad topic/agency 만으로 same_event 금지). 14 test(freeze 통합 포함).
- **④ freeze 경로(§11·결정론 검증)**: bridge `iter_freeze_eligible_record_pairs`(freeze 술어 단일출처·원본 record[title 포함] 반환·worklist 입력) → official×news smoke(packet_rows: source_type_left=official·source_type_right=article·둘 다 `_PUBLISHABLE_PRODUCTION_ROLES`) → `run_r1_production_candidate_acquisition(acquire_fn=)` **무수정 재사용** → publishable freeze·production_candidate_batch_ready=True·candidate_provenance=live_derived·**production_gold_count 0**·`build_reviewer_handoff_bridge` ready. **fake transport 로 freeze 성공 경로까지 잠금**(test_27/46/48/50).
- **⑤ orchestrator/frontier +5(§14)**: `r1_bounded_live_breadth_run` 에 `official_news_acquisition_result` 주입 param + `regulatory_event_seed_bank`(network 0·항상 산출) → `InternalOpsDatePinnedLiveRunFrontier` **+5 필드**(`regulatory_seed_bank_status`·`selected_regulatory_seed_id`·`official_news_live_status`·`official_news_production_candidate_status`·`official_news_reviewer_handoff_ready`) **dict==pydantic==TS==mjs 51==51==51==51**·API allowed 51·required_copy +2("Official record alone is not a production cross-source candidate"·"A regulatory-class seed needs an agency/entity, an action, and a confirmed date window"). 주입 시 acq 의 FR live + bridge sub-result 가 ADR#86 필드도 파생.
- **⑥ 실 live 미실행(§정직)**: operator 가 실 same-subject regulatory event(FR 문서 + news 보도 동시)를 미제공 → 실 official×news live acquisition 미실행 → **실 production candidate freeze 0**·reviewer_handoff_ready=False(실 live)·gold 0·gap 200. 경로는 결정론으로 검증됐으나 실 yield 는 미입증(R-RegulatorySeedQuality).
- **⑦ 게이트 검증 + 리뷰**: backend **1770p**/101skip/0fail(ADR#86 1732→+38)·frontend tsc0/lint0/node:test(ops view) 78→**81**·secret PASS·ruff 0·docs_lifecycle 0·dict==pydantic==TS==mjs 51. adversarial **NO CARDINAL-VIOLATION**(9/9 불변 clean)·code-review **LGTM-WITH-NITS**(blocking 0·finding 4건 FIXED 2/disposition 2).

## 🧭 cross-batch identity 4단계 + 평가/gold gate (정직)
- ① anchor 병합(ADR#40) → ② 후보 LINK(ADR#41) → ③ shadow 판정(ADR#42·…·date-window fidelity 통제실험 ADR#85·Federal Register window-honoring adapter+official×news role-bridge ADR#86·**regulatory-class seed bank+official×news live acquisition+freeze 경로 ADR#87**) → ④ 실 병합(**미구현**).
- **ADR#87 위치 / 핵심**: ADR#86 이 FR adapter·bridge·live_verified 를 *분리된 부품*으로 남긴 것을, regulatory-class seed bank + official×news live acquisition loop 로 **하나의 경로로 연결**한 턴 — seed(official_query≠news_query) → FR official + news(enforce_window) → bridge → freeze 시도 → handoff. freeze 머신은 official+article 둘 다 publishable 이라 **무수정 재사용**(결정론 검증). bridge=같은 사건 단정이 아니라 reviewer worklist 후보(merge=fingerprint+gold+MERGE_GATE 불변). **same-event 단정 0·실 live freeze 0·gold 0.**
- **⚠ 미해소(OPEN):** **operator 가 확인한 실 regulatory event**(FR 문서 + news 보도가 같은 subject·같은 window·R-RegulatorySeedQuality)·official×news in-window same-event **실 live 수율**(R-OfficialNewsDomainMismatch)·실 production candidate freeze(실 live 0)·실 reviewer contact·첫 returned labels·실 gold(production_gold 0)·R1 gold floor(live 200·KO 50·pos/neg 67·hard-neg 20)·MERGE_GATE 런타임·KO 캘리브레이션(KO gold 0)·운영 DB 0009 배포·RAG/KG/Entity/public IU(R1~R7)·internal ops per-user auth·UI 화면 render. "seed/acquisition/freeze 머신 무장 ≠ 후보 확보·freeze 경로 결정론 검증 ≠ 실 live 수율·bridge candidate ≠ truth·official 단독 ≠ cross-source candidate·freeze ≠ truth" — 완전종결=OVERCLAIM.

## 🔎 source orchestration / 제품 계약
- official/news=publishable evidence(merge anchor)·community=반응(anchor 불가)·market=signal·catalog=entity·search=URL candidate(truth 아님)·unknown=fail-closed. **ADR#87 seed bank·acquisition·freeze 는 모두 source role guard 보존**: regulatory seed 의 news provider 는 guardian/nyt 만(community/market/catalog reject)·official×news bridge 는 official(FR)×news(article) 만·freeze 는 `_PUBLISHABLE_PRODUCTION_ROLES`(official/article/news)만(community/market/catalog/search anchor 거부)·official 단독 production candidate 금지·bridge title-Jaccard 미사용(date 근접+entity/action token).
- **제품 계약(raw≠public)**: 출력·`internal_ops_date_pinned_live_run_frontier`(sanitized·**51필드**) 노출 표면에 score/rationale/predicted_status/raw body/secret/raw reviewer 신원·same_event truth·**FR title/url·official×news instruction 본문** **0** — 출력 전 `_assert_pii_safe` 재귀 가드(official_news_live_acquisition 출력 포함)·API `response_model`(sanitized·51필드)·frontend `FORBIDDEN_OPS_FIELDS` 재귀 가드. credential present/missing boolean만(값 0). **acquisition 출력 aggregate-only(`sanitized_official_news_acquisition`·official_records/title/url·per-pair·단일 score 0)**. production_gold_count 0·gate exact passthrough·`merge_allowed=False`·embedding/LLM 실호출 0·DB write 0. Agent 는 regulatory seed·official×news acquisition·freeze readiness·reviewer handoff·KO lane 을 *계획* 가능하나 seed 를 verified event 로·acquisition machinery 를 후보 확보로·freeze 경로 검증을 실 yield 로·bridge candidate 를 truth 로·official 단독을 cross-source candidate 로 사용 불가·merge·secret 생성·PII 출력·실 전송 불가.

## ⚠️ 이번 턴 종결/갱신 RISK
- **신규 1(실 present blocker)**: **R-RegulatorySeedQuality**(MEDIUM·curated regulatory seed 가 실제 FR+news 동시 보도 event 와 대응하는지 미검증 — 4 seed 중 live-selectable 1[EPA]·EPA window 는 제안·operator 확인 필요·완화=validator 가 named agency/action/ISO date·non-broad 모두 요구·provenance=code_proposed·operator_must_confirm·종결 금지).
- **부분진전(종결 0)**: **R-OfficialNewsDomainMismatch**(MEDIUM→**부분진전**·regulatory-class seed bank+official×news live acquisition loop 구축·mismatch 를 no_official_news_overlap 으로 분류·단 실 same-subject live 수율 미실측·종결 금지) · **R-WindowHonoringSourceScarcity**(window-honoring FR 을 acquisition loop 에 연결·news 측 enforce_window 유지·단 live-derived freeze 미실측·종결 금지) · **R-ProductionCandidateScarcity**(official×news freeze 경로 결정론 완성[freeze 머신 무수정 재사용·publishable guard·gold 0]·단 실 live yield 0·종결 금지) · **R-ProviderDateWindowFidelity**(news 측 enforce_window 유지·FR date-honoring 대비·종결 금지).

## ❌ 달성하지 못한 것 & 왜 (이월)
- **실 same-subject regulatory live run**: acquisition loop·freeze 경로는 결정론으로 완성·검증(fake transport)했으나, operator 가 *실제 FR 문서 + news 보도가 같은 subject·같은 window 에 동시 존재하는* regulatory event/date 를 미제공 → 실 live 미실행·실 official×news in-window same-event pair 0·실 freeze 0. 해소=operator 가 확인한 실 regulatory event(R-RegulatorySeedQuality).
- **official×news in-window same-event 실 수율**: seed bank(EPA/SEC/FDA/OFAC)·acquisition loop·freeze 머신 **구축·검증 완료**. **그러나** FR=regulatory 도메인이 news event subject 와 같은 window 에 동시 잡히는 빈도 미입증(R-OfficialNewsDomainMismatch·부분진전). GDELT 는 여전히 미배선(rate-fragile 60s·429 storm+aggregator attribution·ADR#88+).
- **실 production candidate freeze / 실 gold**: freeze 경로 결정론 검증(production_candidate_batch_ready=True·gold 0)·단 실 live records 부재 → 실 freeze 0·`production_gold_count=0`. 해소=실 regulatory event→official×news live-derived freeze→reviewer contact→actual returned labels.
- **adjudicator runtime / RAG·KG·Entity·public IU**: contract guard(docs)일 뿐 runtime 미구현(embedding/LLM 실호출·실 병합·운영 DB·GraphRAG·public IU·KG edge 이번 턴 No-Go·옵션 I 금지).

## 🚧 닫을 수 없는 문제 (BLOCKED/UNKNOWN)
- BLOCKED: 실 production candidate = operator 가 확인한 실 regulatory event(FR 문서 + news 보도 동시)로 official×news live-derived **in-window** bridge 후보 확보(현재 실 live 0·R-RegulatorySeedQuality/R-OfficialNewsDomainMismatch). 실 gold = 실 후보→reviewer recruitment→실제 contact→첫 returned label. 실 병합 = gold-calibrated adjudicator(R3)+MERGE_GATE(R2).
- UNKNOWN: **curated regulatory seed(EPA/SEC/FDA/OFAC)가 실제 FR 문서+news 보도 동시 보도 event 와 대응하는지**(미입증·operator 확인 필요)·**official×news in-window same-event 실 수율**(미실측·R-OfficialNewsDomainMismatch)·**Guardian/NYT date_filter_ignored 근본 원인**(news 측·미규명·R-ProviderDateWindowFidelity). 실 reviewer 회수율/SLA(plan·machinery만).

## 👉 다음 할 일
1. **[커밋 대기]** 본 턴 ADR#87: 신규 4·수정 16(backend 6·frontend 3·docs 7) 미커밋. 커밋 여부 지시 대기. push 금지. (`--paths/`·`--help/`·`.harness/_TRASH/`·`frontend/.harness/` stray 정리 허가도 함께 받으면 처리.)
2. **[다음 단계·ADR#88]** (a) **operator 가 확인한 실 regulatory event**(FR 문서 + news 보도가 같은 subject·같은 window·예: 특정 기관의 특정 named 최종규칙/집행/제재 + 그 보도) → official×news live acquisition **실 실행** → in-window bridge 후보 실측 → live-derived production candidate freeze. (b) **reviewer recruitment + actual contact + 첫 returned labels**(R1) → (c) gold denominator(live 200·KO 50·**R1**) → (d) MERGE_GATE(precision≥0.98·**R2**) → (e) embedding scorer opt-in(**R3**) → (f) entity/KG/GraphRAG(**R4~R6**) → (g) Agent/public IU(**R7**). 별도 축: GDELT adapter(rate/attribution 해소 후)·internal ops UI 화면 render·per-user auth.
3. **[권장·bounded]** official×news live acquisition 은 **bounded·opt-in·rate governed(host floor·no-bypass)·operator 확인 regulatory event·news 측 enforce_window·FR key-free** 일 때만(무단 대량 호출 0·source role guard 유지·out-of-window record freeze 금지·seed 를 verified event 로 둔갑 금지·수율 위해 token floor/validator 약화 금지·bridge candidate 를 truth 로 둔갑 금지).

## 📁 근거 (이번 턴 핵심)
- 커밋: `21c0bb5`(ADR#86·push 0·NO UPSTREAM). ADR#87 변경: 신규 4(backend tool 2+test 2)·수정 16(backend 6·frontend 3·docs 7). **ADR#86 커밋분 frozen·재구현 0·EVENT_SCHEMA/main.py/config/.env/.env.example/package.json/federal_register_live_smoke.py/provider_query_adapters.py/r1_production_candidate_acquisition.py/reviewer_handoff_bridge.py 무변경·전부 additive(신규 모듈 network 0/opt-in·bridge refactor 로 build 출력 byte-identical·frontier +5).**
- **official×news live acquisition(opt-in·기본 시도 0·network 0)**: 기본 live_approved=False → blocked_no_live_opt_in(network 0). live_approved=True 일 때만 bounded governed fetch(FR key-free·news enforce_window=True). **이번 턴 실 live 미실행**(operator 가 실 regulatory event 미제공) → 실 official×news in-window same-event pair 0 → 실 freeze 0. **그러나 freeze 경로는 fake transport 로 결정론 검증**: FR official(final rule) + news(emissions rule) in-window 공유 토큰 → bridge freeze-eligible 1 → official×news smoke → `run_r1_production_candidate_acquisition` 무수정 freeze → production_candidate_batch_ready=True·candidate_provenance=live_derived·**production_gold_count 0**·handoff ready. **secret 0·raw body 0·title/url aggregate 미노출·merge/llm/db_write/sending/score 0.**
- 검증(정직): 신규/수정 backend 테스트 **+38**(regulatory_event_seed_bank 17·official_news_role_bridge +5[iter_freeze_eligible+golden byte-identity]·official_news_live_acquisition 15[freeze 통합·blocked 분류·instruction·invariant·host-gate 분류]·r1_bounded_live_breadth_run +1[acq frontier])·**backend 전체 1770 passed / 101 skipped / 0 failed**(ADR#86 1732→+38)·ingestion 무변경(1353 passed)·frontend **tsc 0**·**lint 0**·node:test **81 passed**(ADR#87 view +3: 78→81)·ruff 실게이트(E/F/I/B/W−E501) **0**·secret scan **PASS**(0 findings·credential present/missing boolean만)·docs_lifecycle conflicts **0**. dict==pydantic==TS==mjs **51==51==51==51**(date-pinned frontier·+5).
- 감사(closeout): regulatory_seed_bank/seed_validator/official_news_live_acquisition/official_news_role_bridge(iter)/bridge_candidate_safety/production_candidate_freeze/reviewer_handoff_bridge/window_honoring_source/source_role_guard/credential_secret_boundary/internal_ops_boundary/public_truth_boundary/no_merge/no_llm_embedding/code/test/evidence/risk_closure/adversarial review. cardinal(seed 를 verified event 로 둔갑 금지·acquisition machinery 를 후보 확보로 둔갑 금지·freeze 경로 검증을 실 live 수율로 둔갑 금지·official 을 news 와 같은 role 로 섞기 금지·official 단독 cross-source candidate 금지·bridge candidate 를 truth/gold 로 둔갑 금지·out-of-window record freeze 금지·MERGE_GATE 전 merge 금지·LLM/embedding 실호출 금지·raw body 저장 금지·`.env` 값 출력 금지·score/rationale/predicted_status reviewer/public 노출 금지·실 전송 금지·public IU 생성 금지·RISK 과대 종결 금지·push 금지) 차단 확인.
- 문서: ADR#87(`_DECISIONS`)·RISK(`_RISK`·종결 0·신규 1[R-RegulatorySeedQuality]·부분진전 4)·ADR#87 블록(`2_ROADMAP/15` descending·ADR#86 앞)·ADR#87 spec(`2_ROADMAP/19`)·`2_ROADMAP/00`(ADR 목록 +#87)·`_CANONICAL/02`(ADR#41~#87). **`_CANONICAL/03_SOURCE_STATUS.md` 무변경**(production_source_state.json 파생·FR 은 이미 OFFICIAL_RECORD_SOURCE 로 정확 분류·query-adapter 미추적 문서라 ADR#87 변경 불필요)·**3_SOURCE_STATUS 부재(ABSENT)·EVENT_SCHEMA/LLM_EVIDENCE_PACKET_CONTRACT 무변경**(ADR#87 DB-free·runtime 0·seed/acquisition/freeze 는 acquisition 진단이지 event schema 필드 아님).

---
_as_of: 2026-06-30 · regulatory-class event seed bank + official×news live acquisition + production candidate freeze 경로 — ADR#86 커밋(`21c0bb5`) 후, ADR#86 이 남긴 분리된 부품(FR adapter·bridge·live_verified)을 **하나의 acquisition loop 로 연결**. (1) regulatory_event_seed_bank(`regulatory_event_seed_bank.py`·official_query≠news_query·named agency/action/ISO date 요구·broad reject 6/6·17 test). (2) official_news_live_acquisition(`official_news_live_acquisition.py`·FR official + guardian/nyt news[enforce_window=True] 같은 window → bridge → 9-state 분류 → freeze 시도 → handoff·official×news 전용 instruction·14 test). (3) bridge `iter_freeze_eligible_record_pairs`(freeze 술어 단일출처·title-free 출력 보존). (4) freeze 경로 **결정론 검증**(fake transport 로 official×news in-window pair → `run_r1_production_candidate_acquisition` 무수정 freeze → batch_ready·gold 0). (5) frontier +5 dict==pydantic==TS==mjs **51**·required_copy +2. **측정**: backend +38(1770p/101skip/0fail)·ingestion 1353p·frontend tsc0/lint0/test81·secret PASS·docs_lifecycle 0·ruff 0. **정직 경계**: seed/acquisition/freeze 머신 무장 ≠ 후보 확보·freeze 경로 결정론 검증 ≠ 실 live 수율·실 same-subject regulatory live run 미실행(operator 미제공)·실 official×news in-window same-event pair 0·실 freeze 0·production_gold 0·gap 200·reviewer_handoff_ready False(실 live)·adjudicator runtime/public IU/merge/운영 DB/embedding·LLM 실호출/실 전송 0·완전종결=OVERCLAIM. 다음 hard blocker=operator 가 확인한 실 regulatory event(FR 문서 + news 보도 동시)→official×news live-derived freeze→실 reviewer contact→actual returned labels(R1). **신규 R-RegulatorySeedQuality(MEDIUM)**·R-OfficialNewsDomainMismatch 부분진전·종결 0. ADR#86 커밋 `21c0bb5` 위 ADR#87 미커밋(신규 4·수정 16)·커밋 지시 대기·push 안 함._
