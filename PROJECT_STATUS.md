# 📊 프로젝트 진행 현황 (PROJECT_STATUS)

> 매 턴 끝에 새로 쓰입니다. 항상 "가장 최근 1턴"만. 과거는 `git log -- PROJECT_STATUS.md`.
> 사실 원본(자동): `.harness/machine_status.json` · 완료 증거: `.harness/closeout_stamp.json` · 서술: 이 파일(에이전트).

## 🟢 한눈에 (비개발자용 3줄)
- **지금 무엇을 했나:** 지난 턴(ADR#77 — "넓은 주제로 라이브 1회 질의 → 같은 사건 near-match 0[원인 미확정]")을 안정 기준점으로 **커밋**(`6ee7c31`)한 뒤, 그 0의 원인을 **가려내려고** ① 라이브 결과의 metadata를 본문 없이 들여다보는 **near-match 진단기**를 만들고 ② 넓은 주제 대신 **특정 사건(예: "Federal Reserve interest rate decision")으로 좁혀** 다시 한 번 안전하게 질의했습니다. 결과: 좁힌 seed(fed_rate·7d)에서 Guardian·NYT가 각 10건씩 와 **교차 비교쌍 30쌍**을 만들었지만 **near-match는 여전히 0**(최고 제목 일치도 Jaccard 0.0526·가장 비슷한 쌍조차 "it/over/day" 같은 일반 단어만 공유·사건 고유명 아님)이었고, 나머지 두 seed는 호출 간격 규칙(no-bypass)에 걸려 **정직하게 차단**됐습니다. **그래서 단정하지 않습니다** — 단일 기관·7일로 좁혀도 안 겹친 건 (ii)"넓은 우산 아래 서로 다른 기사"로 **약하게** 기울지만, (i)"같은 사건인데 짧은 제목+어간·고유명 정규화 부재로 규칙이 못 잡음"도 살아 있습니다(유효 seed 1개·n=1). 동시에 미래 LLM/RAG/KG가 무엇을 보고/추론/단정 못하는지를 못박는 **계약 문서 2개**를 새로 썼습니다(실 실행은 여전히 0). 자동 병합·LLM/embedding 실호출·운영 DB·키 값 출력·실제 전송·입력/라벨 날조 모두 **0**, 외부 호출은 **딱 1 seed(2 GET)**.
- **이번 턴에 실제로 끝낸 것:** ADR#77 커밋(`6ee7c31`·11파일) → **ADR#78**: (1) smoke에 `emit_band_diagnostic`(band 분포·max Jaccard·최고중첩 below-floor 샘플=**공유 정규화 토큰만**·raw body 0) 추가. (2) 신규 `r1_targeted_live_acquisition.py`(Lane B `classify_near_match_gap`[원인 **다수 가설+confidence=indeterminate**·같은/다른 사건 단정 0]·Lane C targeted live[seed 검증·ADR#76 gate 재사용 6-state·freeze-only-if-live-derived]·Lane D provider expansion plan + Korean source strategy·Lane E contract readiness). (3) 신규 contract 2(`LLM_EVIDENCE_PACKET_CONTRACT.md`·`RAG_KG_ENTITY_GATE_CONTRACT.md`·runtime No-Go). (4) internal ops 최소 배선(`InternalOpsAcquisitionFrontierStatus` + `GET /api/internal/ops/r1-acquisition-frontier`[read API live 0] + TS types + view[필수 §11 정직 copy] + 테스트). **승인된 bounded targeted live 1 seed 실행**. **변경 표면 = backend 4 + backend test 2 + frontend 3 + docs 11 = 20파일**(신규 4).
- **정직한 한계:** 이번 진전은 **targeted live 1 seed 실측 + near-match gap 진단(원인 양가·미확정) + Lane D/E 계약**이지 **실 production candidate freeze가 아닙니다**(fed_rate cross 30=비교쌍·publishable near-match **0**·`candidate_provenance=none`·freeze 0). **production_gold_count 0**·실 reviewer contact·실 returned labels·실 gold 여전히 0(gate exact passthrough·`current_r1_gap=200`). 좁힌 1 seed로도 원인 (i)/(ii)는 **분리 못 함**(유효 seed 1개·2개 host-gate 차단·7d 기관쿼리 한계). embedding/LLM 실호출·자동 병합·운영 DB·public IU·실 전송 모두 **0**(Lane E는 계약 문서만·`product_bridge_runtime_built=False`). internal ops read API는 `live_query=False` 고정이라 `near_match_gap_status=insufficient_debug_artifact`(정직)·page.tsx 화면 render는 **후순위**(근거기반 코어·Q10 gap). 다음 hard blocker=**단일 discrete event·1d·당일 동시보도로 (i)/(ii) 추가 분리 → normalization recall + provider breadth → 실 reviewer contact → actual returned labels**. **push 금지·커밋 지시 대기**.

## 📋 자동 수집 사실 (machine_status.json)
- **git HEAD `6ee7c31`**(ADR#77·본 턴 첫 커밋·11파일·+205/−44) 위 **ADR#78 = 미커밋**: 신규 4(`backend/app/tools/r1_targeted_live_acquisition.py`·`backend/tests/test_r1_targeted_live_acquisition.py`·`docs/5_REFERENCE/LLM_EVIDENCE_PACKET_CONTRACT.md`·`docs/5_REFERENCE/RAG_KG_ENTITY_GATE_CONTRACT.md`) + 수정 16(backend: `tools/cross_source_live_overlap_smoke.py`·`schemas/internal_ops.py`·`api/internal_ops.py`·`tests/test_cross_source_live_overlap_smoke.py`; frontend: `lib/api/types.ts`·`lib/ops/opsPilotExecutionView.ts`·`lib/ops/__tests__/opsPilotExecutionView.test.mjs`; docs 9: `5_REFERENCE/RAG_KG_AGENT_READINESS·INTELLIGENCE_UNIT_CONTRACT`·`2_ROADMAP/00·15·19`·`_CANONICAL/02`·`_DECISIONS/2026-06`·`_RISK/RISK_REGISTER`·`PROJECT_STATUS`) = **추적 20항목**. **migration 없음·ADR#77 커밋분 frozen·단방향 import·재구현 0(ADR#76 production gate 재사용)·EVENT_SCHEMA(DB-free 무변경)·main.py/config/.env/.env.example/package.json 무변경.**
- 열린 RISK: **종결 0·신규 3**(R-KoreanR1SourceGap[MEDIUM]·R-DeterministicNearMatchRecall[MEDIUM]·R-LiveAcquisitionRateBudget[LOW]). 부분진전: R-CrossSourceNearMatchGap·R-ProviderPairNarrowness·R-ProductionCandidateScarcity·R-RagKgPrematureBuild. throwaway 는 `.harness/_TRASH/`·`frontend/.harness/`·`--paths/`·`--help/`(stray·커밋 제외)·`outputs/reviewer_batch/`(gitignore)·scratchpad(저장소 밖).

## ✅ 이번 턴에 달성한 것 (targeted live acquisition + near-match gap diagnostic[양가·미확정] + LLM/RAG/KG product bridge contracts — ADR#78)
- **① ADR#77 커밋**: 11파일 → `6ee7c31`(제공 메시지 그대로·secret PASS·docs_lifecycle conflicts 0·closeout SIGNATURE_MATCH 16==16·push 0·NO UPSTREAM). ADR#78 안정 기준점.
- **② 원자 분석(§2·20문항·구현 전 보고)**: near-match 0 단일 실패 feature=**title-token Jaccard**(date/role/canonical PASS)·detector 의도적 **고정밀·저재현**(`_title_tokens`=소문자+21 stopword+len>1·**stemming/entity 정규화 없음**)·near=**reviewer 라우팅** gate≠merge gate(merge=fingerprint+gold+MERGE_GATE)→recall 개선 false-merge 위험 0·100쌍 제목 미보존→진단엔 bounded live 재실행 필요·provider landscape(Guardian/NYT 외 wired query-capable=GDELT[429]·SEC EDGAR·Federal Register·key-free RSS fleet[영문+KO·feed-only]·Naver/NewsAPI/search=미배선).
- **③ A — actual returned labels re-check**: gate re-check→`no_actual_input`·`external_input_required=True`·production_gold_count 0·returned/imported면 precedence(이번 턴 False). 입력 날조 0.
- **④ B — near-match gap 진단(양가 보존·단정 0)**: smoke `emit_band_diagnostic`(band 분포·max Jaccard·최고중첩 below-floor 샘플=공유 정규화 토큰만·raw body 0) → `classify_near_match_gap` 가 8-class root cause(same_event_possible_but_detector_missed/broad_topic_different_events/provider_pair_narrowness/time_window_mismatch/title_normalization_gap/source_role_metadata_gap/insufficient_debug_artifact/unknown)를 **다수 가설+confidence=indeterminate**로 산출(`same_event_truth_asserted=False`).
- **⑤ C — 승인된 targeted live 실행(user opt-in·bounded·governed·secret-safe·raw body 0)**: `r1_targeted_live_acquisition --live-query`(host_gate 주입·no-bypass) → seed **fed_rate**(7d)=guardian ok 10/nyt ok 10·**cross-source 비교쌍 30**·band 전부 below_floor·**max title Jaccard 0.0526**·최고 쌍이 generic 토큰("it"/"over"/"day")·entity 아님·**scotus_ruling·ukraine_war=host_gate_blocked**(반복 host min-spacing). live_call_count 2·진단 `all_below_hard_floor`·confidence **indeterminate**·production `blocked_no_publishable_pairs`·freeze 0·`candidate_provenance=none`·gold 0·r1_gap 200. **정직 해석**: 단일 기관·7d로 좁혀도 hard band 0·entity 미공유→(ii)약하게 기울되 **단정 불가**(n=1 유효 seed·(i)recall/normalization 생존). 미실행 실험=단일 discrete event·1d·당일 동시보도(host floor→seed turn/scheduled 분산).
- **⑥ D — acquisition strategy(provider/Korean·기계 산출)**: `build_provider_expansion_plan`(per-provider role/query capability/credential/rate/ToS/integration cost/next action·이미 배선 GDELT/SEC EDGAR/Federal Register·RSS fleet·미배선 Naver/NewsAPI=adapter wiring lever·search=URL-candidate-only·anchor 0)·`build_korean_source_strategy`(KO floor ≥50 current 0 가시화·KO official/news=anchor·community reaction-only·**KO live-query source 미배선**[RSS feed-only]·Naver adapter=lever·KO 형태소 미분절로 Jaccard 더 어려움).
- **⑦ E — LLM/RAG/Web product bridge **contracts**(runtime No-Go)**: 신규 `LLM_EVIDENCE_PACKET_CONTRACT.md`(LLM 가 볼/추론/인용/단정 못하는 것·uncertainty flags·source-role boundary·**same_event=MERGE_GATE dependency**)·`RAG_KG_ENTITY_GATE_CONTRACT.md`(RAG ingestion gate[storage_class/provenance/identity/merge_gate/uncertainty]·KG edge eligibility[same_event=MERGE_GATE·update_of=verified identity·**reaction_to=community 유일 attach**·market_signal_for=verified link·mentions=entity provenance·caused_by=high evidence+uncertainty]·public IU gate). `*_contract_ready=True`=계약 정의됨·**runtime 0**(`product_bridge_runtime_built=False`·llm/embedding/merge/public IU 실행 0).
- **⑧ internal ops 최소 배선**: `InternalOpsAcquisitionFrontierStatus`(sanitized·19필드·same_event truth/score/rationale/predicted_status/raw body/PII/secret 필드 부재)+`GET /api/internal/ops/r1-acquisition-frontier`(이중 게이트·read API live 0)+TS types(`AcquisitionRootCauseHypothesis`/`AcquisitionFrontierFlags`/`InternalOpsAcquisitionFrontierStatus`)+view(`toR1FrontierDisplayRows`/`r1FrontierWarnings`·필수 §11 copy "Near-match 0 does not prove no same event" 등)+테스트. page.tsx render 후순위(근거기반 코어·Q10 gap 정직).
- **⑨ F/G 경계**: embedding/LLM scorer runtime(F)·production DB·semantic merge·public IU·GraphRAG runtime(G) 미착수(이번 턴 금지).

## 🧭 cross-batch identity 4단계 + 평가/gold gate (정직)
- ① anchor 병합(ADR#40) → ② 후보 LINK(ADR#41) → ③ shadow 판정(ADR#42·…·R1 live production candidate acquisition gate ADR#76·R1 live query executed[near-match gap 원인 미확정] ADR#77·**targeted live acquisition + near-match gap diagnostic[양가·미확정] ADR#78**) → ④ 실 병합(**미구현**).
- **ADR#78 위치 / 핵심 실측**: ②③ deterministic near-match가 좁힌 targeted seed(fed_rate·7d)에서도 30 비교쌍 전부 below hard floor(max Jaccard 0.0526·entity 미공유) → **(i) paraphrase recall**이면 **④ semantic adjudicator(embedding/LLM/KG)**가 메우도록 설계된 영역(단, near는 reviewer 라우팅이지 merge가 아니라 deterministic recall 개선[KO/EN-aware normalization·scorer top-k]이 false-merge 없이 먼저), **(ii) different-events**이면 targeted+provider breadth가 레버 — **1 seed로 분리 못함**(단정 0). live run은 near-match 0을 **실측·진단**할 뿐 ④ 가동 아님(R3·MERGE_GATE/gold 전 No-Go·embedding/LLM 실호출 0·자동 same_event 0).
- **⚠ 미해소(OPEN):** near-match 0의 원인 (i)/(ii) 추가 구분(단일 discrete event·1d)·normalization recall·provider breadth·실 production candidate freeze(이번 턴 0)·실 reviewer contact·첫 returned labels·실 gold(production_gold 0)·R1 gold floor(live 200·KO 50·pos/neg 67·hard-neg 20)·MERGE_GATE 런타임·한국어 캘리브레이션(KO source 미배선)·운영 DB 0009 배포·RAG/KG/Entity/public IU(R1~R7)·internal ops per-user auth·UI Q10. "targeted live 실행 ≠ 실 후보·**cross 비교쌍 30 ≠ same-event overlap ≠ near-match**·진단 ≠ truth·contract ready ≠ runtime" — 완전종결=OVERCLAIM.

## 🔎 source orchestration / 제품 계약
- official/news=publishable evidence(merge anchor)·community=반응(anchor 불가·`reaction_to`만)·market=signal·catalog=entity·search=URL candidate(truth 아님)·unknown=fail-closed. targeted acquisition·production candidate 는 publishable×publishable(official/article/news)만 anchor(`_is_publishable_production_pair`·약화 0). near는 reviewer 라우팅 gate≠merge gate → recall 개선은 false-merge 위험 없이 가능.
- **제품 계약(raw≠public)**: 출력·`internal_ops_acquisition_frontier`(sanitized) 노출 표면에 score/rationale/predicted_status/raw body/secret/raw reviewer 신원·same_event truth **0** — 출력 전 `_assert_pii_safe` 재귀 가드·API `response_model=InternalOpsAcquisitionFrontierStatus`(sanitized 화이트리스트)·frontend `FORBIDDEN_OPS_FIELDS` 재귀 가드. band diagnostic 샘플은 **공유 정규화 토큰만**(제목 전문/본문 0). production_gold_count 0·gate **exact passthrough**·`merge_allowed=False`·`no_public_intelligence_unit`·embedding/LLM 실호출 0·DB write 0·`actual_sending_performed=False`. credential presence는 **secret-safe**(present/missing·값 0·`.env` 값 미열람·provider plan의 'api_key' 문자열은 env var **이름**·값 아님). live query는 **bounded·1 seed·rate/host gate 강제·raw body 0**. Agent는 near-match diagnostic·acquisition strategy·contract readiness를 *계획* 가능하나 near-match 0을 LLM으로 same_event 단정·label 조작·merge·anchor 약화·KG edge 무검증 생성·PII/secret 출력·실 전송 불가.

## ⚠️ 이번 턴 종결/갱신 RISK
- **신규 3(실 present blocker만)**: **R-KoreanR1SourceGap**(MEDIUM·KO floor ≥50용 topic-query 가능 KO source 미배선[RSS feed-only·Naver cataloged-not-wired]·영문 pair로 해결 불가·KO 형태소 미분절로 Jaccard 더 어려움·완화=korean strategy 가시화·완전종결=KO gold ≥50) · **R-DeterministicNearMatchRecall**(MEDIUM·고정밀·저재현 detector가 짧은 헤드라인+stemming/entity 정규화 부재로 same-event cross-source 쌍 누락 가능·near는 reviewer 라우팅≠merge→recall 개선 안전·완화=band diagnostic 가시화+scorer top-k+경량 normalization lever·완전종결=recall 개선 실측[merge 정밀도 불변]) · **R-LiveAcquisitionRateBudget**(LOW·multi-seed sweep가 host floor/cooldown에 막힘[ADR#78 scotus/ukraine host_gate_blocked]·완화=host gate 주입+seed 분산[multi-turn/scheduled]·완전종결=scheduled acquisition으로 충분 데이터·no-bypass 0).
- **부분진전(종결 0)**: **R-CrossSourceNearMatchGap**(targeted 1 seed·band diagnostic·여전히 below floor·**원인 미분리→종결 금지**) · **R-ProviderPairNarrowness**(expansion plan 산출·**대안 provider 미테스트→종결 금지**) · **R-ProductionCandidateScarcity**(targeted 시도·live_candidate 30·publishable 0·**충분 후보 0→종결 금지**) · **R-RagKgPrematureBuild**(contract 2 신규로 build discipline 강화·**runtime No-Go·종결 금지**).

## ❌ 달성하지 못한 것 & 왜 (이월)
- **실 production candidate freeze / 실 reviewer contact / 첫 returned labels / 실 gold**: targeted live는 실행됐으나 **near-match publishable pair 0**(below hard floor·원인 미확정)→freeze 대상 0(`candidate_provenance=none`). 해소=**단일 discrete event·1d로 (i)/(ii) 추가 분리**→(i)면 normalization recall/gated R3 adjudicator(MERGE_GATE/gold 전 No-Go)·(ii)면 provider breadth(RSS fleet·Naver adapter)→near-match 후보→reviewer recruitment·실제 contact evidence·첫 returned labels 선결(R-RealSourceLoopUnproven·R-GoldAcquisitionPlanOnly).
- **adjudicator runtime / RAG·KG·Entity·public IU**: Lane E 계약은 docs(2 contracts + §6b-R/§6b-S/§4)일 뿐 runtime 미구현(`product_bridge_runtime_built=False`·embedding/LLM 실호출·실 병합·운영 DB·GraphRAG·public IU 이번 턴 No-Go·옵션 F/G 금지).
- **UI Q10(live 결과 UI render)**: frontier view/types/test(데이터층)는 배선됐으나 read API가 `live_query=False` 고정이라 internal ops UI는 `insufficient_debug_artifact`(비-live)만 표시·page.tsx 화면 render는 후순위(근거기반 코어). live 결과(30 비교쌍/near-match 0)를 UI에 반영하려면 별도 영속/표면화 필요(다음 증분·정직 표기).

## 🚧 닫을 수 없는 문제 (BLOCKED/UNKNOWN)
- BLOCKED: 실 production candidate = **near-match 0의 원인 (i)/(ii) 추가 구분**(단일 discrete event·1d)→(i)면 normalization recall/gated R3 adjudicator[R2 MERGE_GATE 후]·(ii)면 provider breadth→near-match publishable pair 확보 선결(R-CrossSourceNearMatchGap·R-DeterministicNearMatchRecall). 실 gold = 실 후보→reviewer recruitment→실제 contact(evidence)→첫 returned label 회수(R-ProductionCandidateScarcity·R-RealSourceLoopUnproven). 실 병합 = gold-calibrated embedding/LLM adjudicator(R3·F)+MERGE_GATE(R2). KO floor = KO live-query source 배선(Naver adapter·R-KoreanR1SourceGap).
- UNKNOWN: **near-match 0의 원인이 (i) recall/normalization인지 (ii) different-events인지**(좁힌 1 seed로도 미분리·n=1·2 host-gate 차단). 단일 discrete event·1d가 hard band 쌍을 만들지(미측정). 경량 stemming/entity alias·scorer top-k가 same-event recall을 올릴지(미측정). provider breadth(RSS fleet·Naver)가 near-match 수율을 올릴지(미테스트). 실 reviewer 회수율/SLA(plan·machinery만·실 운영 0).

## 👉 다음 할 일
1. **[커밋 대기]** 본 턴 ADR#78: 신규 4(`r1_targeted_live_acquisition.py`+test+contract 2) + 수정 16(smoke·internal_ops schema/api·smoke test·frontend types/view/test·docs 9) = **20파일** 미커밋. 커밋 여부 지시 대기. push 금지. (`--paths/`·`--help/` stray 정리 허가도 함께 받으면 처리.)
2. **[다음 단계·frontier]** (a) **near-match 0의 원인 (i)/(ii) 추가 구분** — 단일 **discrete breaking event·1d·당일 동시보도**로 bounded·opt-in 재실행(반복 seed는 host floor 때문에 turn/scheduled 분산)→일부 쌍이 hard band 진입하는지. (b) **normalization recall**(KO/EN-aware 경량 stemming/entity alias·deterministic semantic_candidate_scorer top-k reviewer triage·LLM 0) + **provider breadth**(key-free RSS 다출처 fleet·GDELT cooldown-honored·미배선 Naver/NewsAPI adapter wiring[KO floor]). (c) **reviewer recruitment + actual contact evidence + 첫 returned labels 회수**(R1) → (d) gold denominator(live 200·KO 50·**R1**) → (e) MERGE_GATE(precision≥0.98·**R2**) → (f) embedding scorer opt-in(**R3**) → (g) entity/KG/GraphRAG(**R4~R6**) → (h) Agent/public IU(**R7**). 별도 축: internal ops UI Q10(live 결과 render)·per-user auth.
3. **[권장·bounded]** acquisition strategy 실험은 **bounded·opt-in·rate governed·seed 분산**일 때만(무단 대량 호출 0·no-bypass): 단일 discrete event로 (i)/(ii) 추가 구분 측정(다음 턴·source role guard 유지).

## 📁 근거 (이번 턴 핵심)
- 커밋: `6ee7c31`(ADR#77·11파일·+205/−44·push 0·NO UPSTREAM). ADR#78 변경: 신규 4(`r1_targeted_live_acquisition.py`+test+`LLM_EVIDENCE_PACKET_CONTRACT.md`+`RAG_KG_ENTITY_GATE_CONTRACT.md`)·수정 16(smoke band 확장·internal_ops schema/api·smoke test·frontend types/view/test·docs 9). **ADR#77 커밋분 frozen·재구현 0(ADR#76 gate 재사용)·EVENT_SCHEMA/main.py/config/.env/.env.example/package.json 무변경.**
- targeted live(실측·1 seed·bounded·secret-safe·raw body 0): `targeted_live_query_executed=True`·`live_call_count=2`·fed_rate(7d) cross 30·band 전부 below_floor·**max title Jaccard 0.0526**(최고 쌍 공유 토큰 "it"/"over"/"day"·entity 아님)·scotus/ukraine `host_gate_blocked`·`near_match_gap_status=all_below_hard_floor`·`root_cause_confidence=indeterminate`·`production_candidate_status=blocked_no_publishable_pairs`·`candidate_provenance=none`·`production_gold_count=0`·`current_r1_gap=200`·`merge_allowed/llm_invoked/embedding_invoked/db_write/actual_sending_performed/same_event_truth_exposed/raw_source_body_exposed=False`·`product_bridge_runtime_built=False`.
- 검증(정직): 신규 backend **27**(test_r1_targeted_live_acquisition: seed validation·near-match diagnostic 양가·6-state·freeze-only-live-derived·gold 0·provider expansion·Korean·contract·safety·frontier triple-consistency)+smoke **2**(band diagnostic additive·body-free)·**backend 비-live 전체 1466 passed/101 skipped/0 failed**(1437+29)·ingestion **1353 passed**·frontend **tsc 0**·**lint 0**(`next lint`)·node:test **51 passed**(ops view +7: 32→39)·ruff 실게이트(E/F/I/B/W−E501) **0**·secret scan **PASS**·docs_lifecycle conflicts **0**. live targeted=**1 seed 실행**(bounded·host gate 강제·raw body 0·DB/merge/LLM/embedding/전송 0).
- 감사(closeout subagents): architecture/pipeline/actual_input_gate/near_match_gap_diagnostic/targeted_live_acquisition/provider_readiness/provider_expansion/korean_source_strategy/credential_secret_boundary/source_overlap/production_candidate_batch/synthetic_vs_production_separation/source_role_guard/acquisition_strategy/llm_evidence_packet/rag_ingestion_gate/kg_edge_eligibility/community_reaction_boundary/public_iu_gate/internal_ops_dual_track/public_truth_boundary/reviewer_pii/secret_boundary/code/test/evidence/risk_closure/**adversarial** review. cardinal(near-match 0→LLM 점프 금지·진단을 truth로 사용 금지·single-seed로 paraphrase/different-events 단정 금지·community/market/catalog anchor·search를 truth·source role guard 약화·score/rationale/predicted_status/same_event 노출·실 전송·`.env` 값 읽기·KG edge 무검증·MERGE_GATE 전 merge·테스트 없는 정책 변경) 차단 확인.
- 문서: ADR#78(`_DECISIONS`)·RISK(`_RISK`·종결 0·신규 3[R-KoreanR1SourceGap·R-DeterministicNearMatchRecall·R-LiveAcquisitionRateBudget]·R-CrossSourceNearMatchGap/R-ProviderPairNarrowness/R-ProductionCandidateScarcity/R-RagKgPrematureBuild 부분진전)·ADR#78 블록(`2_ROADMAP/15`)·ADR#78 마커(`2_ROADMAP/00`)·acquisition §19(`2_ROADMAP/19`)·**§6b-R1e targeted live + near-match gap diagnostic[양가·미확정]**(`RAG_KG`)·§4 ADR#78 bullet(`IU_CONTRACT`)·신규 contract 2(`LLM_EVIDENCE_PACKET_CONTRACT`·`RAG_KG_ENTITY_GATE_CONTRACT`)·`_CANONICAL/02`(ADR#41~#78). **EVENT_SCHEMA 무변경**(ADR#78 DB-free).

---
_as_of: 2026-06-28 · ADR#78 targeted live acquisition + near-match gap diagnostic[양가·미확정] + LLM/RAG/KG product bridge contracts — ADR#77 커밋(`6ee7c31`) 후, broad-topic near-match 0(원인 미확정)을 가르려고 **event-specific named-entity seed로 좁힌 bounded targeted live 재실행 + metadata-level 진단**(근거기반 코어·user opt-in 승인·신규 4파일·재구현 0·live 1 seed·DB 0·전송 0·secret 값 미열람): smoke `emit_band_diagnostic`(band 분포·max Jaccard·공유 정규화 토큰·raw body 0)+신규 `r1_targeted_live_acquisition`(Lane B `classify_near_match_gap`[다수 가설+confidence indeterminate·같은/다른 사건 단정 0]·Lane C 6-state·freeze-only-live-derived·Lane D provider/Korean plan·Lane E contract readiness)+신규 contract 2(`LLM_EVIDENCE_PACKET_CONTRACT`·`RAG_KG_ENTITY_GATE_CONTRACT`·runtime No-Go)+internal ops(`r1-acquisition-frontier` read API live 0+frontier view 필수 §11 copy). **targeted live 실측(1 seed)**: fed_rate(7d)=guardian 10/nyt 10·cross 30·전부 below_floor·**max Jaccard 0.0526**·최고 쌍 generic 토큰만·scotus/ukraine host_gate_blocked·`all_below_hard_floor`·indeterminate·`blocked_no_publishable_pairs`·freeze 0·gold 0. **정직**: 단일 기관·7d로 좁혀도 hard band 0·entity 미공유→(ii)약하게 기울되 **단정 불가**(n=1 유효 seed·(i)recall/normalization 생존·near는 reviewer 라우팅≠merge→recall 개선 false-merge 위험 0). 옵션 **A+B+C(user opt-in)+D+E(contract) 채택·F/G 금지·범위=근거기반 코어(user 결정)**. **측정**: 신규 backend 27+smoke 2·**backend 비-live 1466p/101skip/0fail**·ingestion 1353p·frontend tsc0/lint0/test51(ops +7)·ruff 0·secret PASS·docs_lifecycle 0. **정직 경계**: 진전=targeted live 1 seed 실측+near-match gap 진단(**원인 양가·미확정**)+Lane D/E contract·**실 production candidate freeze 아님**(cross 30=비교쌍·publishable near-match 0·candidate_provenance none·production_gold_count 0·gate exact passthrough)·adjudicator runtime/public IU/merge/운영 DB/embedding·LLM 실호출/실 전송 0·`product_bridge_runtime_built=False`·internal ops read API live 0→insufficient_debug_artifact·page.tsx render 후순위[Q10 gap]·완전종결=OVERCLAIM. 다음 hard blocker=단일 discrete event·1d로 (i)/(ii) 추가 분리→normalization recall+provider breadth→실 reviewer contact→actual returned labels. **신규 R-KoreanR1SourceGap(MEDIUM)·R-DeterministicNearMatchRecall(MEDIUM)·R-LiveAcquisitionRateBudget(LOW)·R-CrossSourceNearMatchGap/R-ProviderPairNarrowness/R-ProductionCandidateScarcity/R-RagKgPrematureBuild 부분진전**·종결 0. ADR#77 커밋 `6ee7c31` 위 ADR#78 미커밋(신규 4+수정 16=20)·커밋 지시 대기·push 안 함._
