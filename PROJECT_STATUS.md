# 📊 프로젝트 진행 현황 (PROJECT_STATUS)

> 매 턴 끝에 새로 쓰입니다. 항상 "가장 최근 1턴"만. 과거는 `git log -- PROJECT_STATUS.md`.
> 사실 원본(자동): `.harness/machine_status.json` · 완료 증거: `.harness/closeout_stamp.json` · 서술: 이 파일(에이전트).

## 🟢 한눈에 (비개발자용 3줄)
- **지금 무엇을 했나:** 지난 턴 전략(ADR#58)을 안정 기준점으로 **커밋**한 뒤, "컴퓨터가 못 잡는 **표현만 다른 같은 사건**(paraphrase) 후보를 버리지 않고 **사람 검수·정답(gold) 대기열로 보내는 통로**"를 **실제로 작동하는 운영 큐**로 만들었습니다. 핵심: 새로 만들지 않고 **이미 있는 사람-라벨링·gold·합의 기계를 재사용** — near-match 후보 + (헷갈리는) 다른-사건 후보를 함께 **검증된 검수 패킷**으로 묶고, 모델 추정값은 **숨기고**, 병합·LLM 은 **0**.
- **이번 턴에 실제로 끝낸 것:** ADR#58 커밋(`e2f33fc`) → **ADR#59**: 새 오케스트레이터 `near_match_reviewer_queue.py` — near positive(표현만 다른 같은 사건 후보) + hard negative(헷갈리는 다른 사건 후보)→**검증된 reviewer packet**·gold 준비 리포트(**실 gold 있을 때만** gold_ready)·gold/충돌/합의 connector·"어느 매체쌍을 모아야 gold 가치가 높은가" **수집 연계**·embedding/LLM 판정기는 **No-Go 인터페이스만**. 감사 2종 통과(**HIGH 0·MEDIUM 0**).
- **정직한 한계:** **실제 사람 검수자·gold 는 여전히 0**(큐는 받침대일 뿐·패킷≠gold). 실 cross-source 같은-사건 겹침 미관측·embedding/LLM 판정기 미구현(No-Go)·**운영 DB 무변경·production 백로그 0·자동 병합 0·본문 미저장·LLM 0** 불변. push 금지.

## 📋 자동 수집 사실 (machine_status.json)
- **git HEAD `e2f33fc`**(ADR#58, 본 턴 첫 커밋) 위 **ADR#59 = 미커밋**: 신규 2(`backend/app/tools/near_match_reviewer_queue.py`[오케스트레이터] + `backend/tests/test_near_match_reviewer_queue.py`[30 테스트]) + 수정 2(`source_overlap_discovery.py`[hard-negative 밴드 가산] + `test_source_overlap_discovery.py`[+4]) + docs 6(`2_ROADMAP/00·15`·`_RISK/RISK_REGISTER`·`5_REFERENCE/RAG_KG_AGENT_READINESS·INTELLIGENCE_UNIT_CONTRACT`·`_CANONICAL/02`·`_DECISIONS/2026-06`) + `PROJECT_STATUS` = **추적 11항목**. **migration 없음·새 파일 2(near_match_reviewer_queue.py+test).**
- 열린 RISK: **total 36**(HIGH 3·MEDIUM 21·LOW 11). **신규 0·종결 0.** 부분진전 6: R-SemanticIdentityAdjudicator·R-IdentityHumanLabeling·R-ReviewerAgreement·R-GoldSamplingBias·R-LiveIdentityBacklog·R-SourceOverlapScarcity(+ R-RealSourceLoopUnproven 은 downstream substrate 참고). throwaway 는 `.harness/_TRASH/`·`frontend/.harness/`(gitignored·커밋 제외).

## ✅ 이번 턴에 달성한 것 (near-match reviewer/gold queue operationalization — ADR#59)
- **① ADR#58 커밋**: 12파일 → `e2f33fc`(secret PASS·docs_lifecycle conflicts 0·SIGNATURE MATCH 15·push 0).
- **② 원자 분석(§2·20문항)**: 3-agent 서브시스템 매핑(labeling/eval/gold·MERGE_GATE/adjudicator·source-role)+핵심 파일 정독(file:line). **핵심 발견**: `build_near_match_reviewer_candidates` 는 worksheet-row 모양이나 packet/gold 머신과 **코드 미연결**·predicted_status 부재; `build_labeling_packet`/`assign_candidate_bucket`(paraphrase·hard_negative bucket)/`validate_labeling_packet`(verdict 차단·enum)/`resolve_gold_from_reviewers`(2+합의=gold·conflict→human adjudication) 와 스키마 호환; `EvalPair`(label∈GOLD_LABELS)는 라벨 후에만 물질화. 다음 hard blocker 순서=① targeted acquisition ② gold volume ③ embedding/LLM(gated). 분석 먼저 보고.
- **③ A — near-match reviewer queue 운영화(핵심)**: 신규 `near_match_reviewer_queue.build_near_match_reviewer_queue` — near positive(paraphrase)+hard negative 정규화(language und→unknown·observed None→''·title str≤512)→**기존** `build_labeling_packet`→`validate_labeling_packet`(fail-loud)→검증된 packet+`labeler_facing_view`(bucket·model 판정 제거). **재구현 0**.
- **④ B — gold seed packet + gold 경로 connector**: `build_gold_seed_report`(queue/near/hard/fingerprint count·source_role·bucket·uncertainty·`reviewer_packet_exportable`·`labeler_prediction_hidden`·`gold_ready`[**실 gold 있을 때만**]·`merge_allowed=False`)·`resolve_queue_gold`(`resolve_gold_from_reviewers`→gold/conflict adjudication queue/agreement connector). hard negative=측정 밴드(discovery_derived)+trap-zone(`build_synthetic_hard_negative_candidates`·**synthetic_fixture 명시**·분리 카운트).
- **⑤ C — acquisition linkage(bounded)**: `build_reviewer_queue_acquisition_linkage`(source_pair별 `near_match_yield_potential`/`reviewer_value`/`gold_value`/`hard_negative_value`/`paraphrase_risk`/`source_pair_priority`·`next_fetch_plan`)→단순 수집량이 아니라 **gold/MERGE_GATE 가치 기반 수집으로 전환**.
- **⑥ D — embedding/LLM adjudicator No-Go**: `EMBEDDING_LLM_ADJUDICATOR_INTERFACE`(input=near-match candidate·output=provisional_score only·requires=gold+MERGE_GATE+adversarial·**status No-Go**·`semantic_score` seam) 인터페이스만 고정·이번 턴 **호출 0**. `augment_agent_schema_with_reviewer_queue`(near_match_queue_priority·hard_negative_sampling_plan·expected_gold/merge_gate_value·`no_merge_without_gate`).
- **⑦ discovery 가산(순수)**: `source_overlap_discovery.py` 에 `_HARD_NEG_FLOOR=0.2`+hard_negative_pairs 밴드(publishable·same-date·cross-URL·jaccard [0.2,near) different-event lean)+`build_hard_negative_reviewer_candidates` **순수 가산**(captured fixture fingerprint 1·near 5 불변=ADR#57/58 회귀 0).
- **⑧ 감사 정정(LOW 2)**: adversarial **PROCEED·HIGH 0·MEDIUM 0**→① worksheet `dataset_source` 명시(synthetic 이 `summarize_packet_sampling` 기본 live 에서 오집계되는 latent hazard 봉인·회귀 테스트 2) ② "음성 floor 채운다"→"calibration substrate(통계 floor 충족 아님)" 톤다운. code-review **0 결함**.

## 🧭 cross-batch identity 4단계 + 평가/gold gate (정직)
- ① anchor 병합(ADR#40) → ② 후보 LINK(ADR#41) → ③ shadow 판정(ADR#42·운영 배선 ADR#48~#53·preflight/smoke ADR#54·live-db ADR#55·replay ADR#56·overlap discovery ADR#57·acquisition ADR#58·**near-match reviewer/gold 큐 ADR#59**) → ④ 실 병합(**미구현**).
- **ADR#59 위치**: ③④ 사이의 **detection 레이어 입력 substrate**. deterministic 이 못 잡는 paraphrase 후보를 reviewer/gold/MERGE_GATE 로 보내는 통로(ADR#58)를 **운영 큐로 물질화** — near positive+hard negative 를 검증된 reviewer packet 으로·gold 준비 리포트·gold/충돌/합의 connector. 병합·LLM 은 detection 레이어(embedding/LLM adjudicator+gold·④)에서만, 그것도 MERGE_GATE 통과 후에만 열림(현재 No-Go).
- **⚠ 미해소(OPEN):** 실 reviewer/gold(packet≠gold·실 reviewer 충원 0)·targeted same-event acquisition(큐를 채울 실 near-match 후보)·embedding/LLM 실 병합 adjudicator(No-Go)·운영 DB 0009 배포·MERGE_GATE 충족·한국어 캘리브레이션. "큐 운영성(능력) ≠ 실 gold(actuality)" — 완전종결=OVERCLAIM.

## 🔎 source orchestration / 제품 계약
- official/news=publishable evidence(merge anchor)·community=반응 evidence(anchor 불가)·market=signal·catalog=entity·search=URL·unknown=fail-closed. near-match/hard-negative 후보는 **publishable(official/article)×publishable pair 만**·community/market/catalog 제외(`source_role_compatible` 이중 가드·both_pub 필터).
- **제품 계약(raw≠public)**: 큐는 Intelligence Unit **merge safety substrate** — near-match 는 같은 사건 단정/병합/public IU 가 아니라 **reviewer 에게 던지는 질문**(predicted_status 숨김·`labeler_facing_view` bias 0). hard negative 는 단정이 아닌 **sampling stratum**(gold label 있어야 음성·reviewer 가 라벨). Agent 는 reviewer queue/acquisition 을 *계획* 가능하나 `no_merge_without_gate`·`no_public_intelligence_unit`·`llm_invoked=False`. LLM/Agent 진입 9조건 여전히 No-Go(1·4·5·7).

## ⚠️ 이번 턴 종결/갱신 RISK
- **부분진전(종결 0·신규 0·total 36 불변)**: **R-SemanticIdentityAdjudicator**(paraphrase 사각지대→reviewer/gold 큐·embedding/LLM No-Go 인터페이스) · **R-IdentityHumanLabeling**(near-match→packet 운영 큐+hard negative+gold seed report) · **R-ReviewerAgreement**(hard negative calibration substrate+resolve_queue_gold connector) · **R-GoldSamplingBias**(near positive+hard negative 균형 표집+dataset_source 봉인) · **R-LiveIdentityBacklog**(통로→운영 큐 물질화) · **R-SourceOverlapScarcity**(acquisition linkage 목적 기반 수집). R-RealSourceLoopUnproven 은 소스 루프 무변경(downstream substrate 참고만).
- 디렉티브 신규 RISK 후보(R-NearMatchFalsePositive/R-NearMatchReviewerQueue/R-AgentGoldAcquisitionPlanning)는 **독립 blocker 아님**(near-match=자동행동 아닌 reviewer 질문·route 미배선·reviewer 충원 0→adversarial 도 등재 불요)→R-SourceOverlapScarcity·R-SemanticIdentityAdjudicator 에 포섭(RISK 남발 금지).

## ❌ 달성하지 못한 것 & 왜 (이월)
- **실 human-reviewed gold**: 큐는 검증된 reviewer packet 을 **export** 하나 packet≠gold·실 reviewer 충원 0 → gold_ready=False(정직). gold 경로는 synthetic human label 로 **입증**(2+합의→gold·conflict→adjudication queue·single→insufficient)했으나 production gold 0.
- **큐를 채울 실 near-match 후보**: untargeted RSS overlap 0(ADR#58)·captured fixture 로만 큐 입증. **targeted same-event acquisition**(query-capable provider) 선결.
- **embedding/LLM adjudicator**: paraphrase 검출 본경로는 gold calibration+MERGE_GATE 후에만(No-Go 인터페이스만·호출 0). 운영 DB·production 백로그 0(옵션 E 금지).

## 🚧 닫을 수 없는 문제 (BLOCKED/UNKNOWN)
- BLOCKED: 실 reviewer/gold = reviewer 충원·운영 절차 선결. 큐를 채울 실 near-match = targeted same-event acquisition(GDELT query=429·RSS=query 불가) 선결. production 백로그 = 운영 DB 0009 배포(승인) + 실 fetch 볼륨.
- UNKNOWN: paraphrase overlap 의 실 병합 허용 기준(production precision·실 gold·reviewer 합의·한국어 캘리브레이션·MERGE_GATE) → R-SemanticIdentityAdjudicator·R-IdentityHumanLabeling·R-ReviewerAgreement·R-IdentityEvalDataset 선결.

## 👉 다음 할 일
1. **[커밋 대기]** 본 턴 ADR#59: 신규 2(near_match_reviewer_queue.py+test) + 수정 2(source_overlap_discovery+test) + docs 6 + PROJECT_STATUS = 11파일 커밋 여부 지시 대기. push 금지.
2. **[다음 단계]** (a) **targeted same-event acquisition**(query-capable key-free provider·topic/time-windowed) → 실 near-match 후보 → 큐 충원 → (b) reviewer 충원·합의 gold + 한국어 캘리브레이션 → (c) embedding/LLM/KG **실 병합** adjudicator(paraphrase·MERGE_GATE·`semantic_score` seam) → (d) (승인 하) 운영 DB 0009 배포 + scheduler 가동 → RAG/KG.

## 📁 근거 (이번 턴 핵심)
- 커밋: `e2f33fc`(ADR#58). ADR#59 변경: 신규 `near_match_reviewer_queue.py`(오케스트레이터)+`test_near_match_reviewer_queue.py`(30)·수정 `source_overlap_discovery.py`(hard-negative 밴드)+`test_source_overlap_discovery.py`(+4)·docs 6.
- 검증(정직): captured fixture 큐 — near 5(paraphrase bucket·10 assignments)+synthetic hard 3(hard_negative bucket·6)·`labeler_facing_view` 에 sampling_bucket/predicted_status **부재**·gold_ready False·merge_allowed False·llm_invoked False·embedding No-Go. 테스트: 신규/확장 **34**(near-match queue 30+discovery hard-neg 4)·discovery+queue **88 passed**·backend 비-live **657 passed/4 skipped/0 failed(961s full·LOW 정정 이전)** + dataset_source 봉인 2(near_match_reviewer_queue.py 자기 테스트만 import→격리·targeted 재실행 30 green)=**effective 659**·live-PG **94 passed(215s·단독)**·ingestion **1353 passed(27.9s)**·frontend tsc**0**/node:test **12**/lint **0**·secret scan **PASS(10)**·docs_lifecycle conflicts **0**·ruff 실게이트(E/F/I/B/W) **0**(UP045 noise=기존 49건과 일관·codebase Optional 스타일). 운영 DB 무변경.
- 감사: adversarial-reality-critic **PROCEED·HIGH 0·MEDIUM 0**(claim A–H 8 중 7 코드 증거 VALID·honesty boundary 구조적[merge/persist grep ZERO·predicted_status 4중 차단·source role 이중 가드·hard-neg=stratum·synthetic 분리·gold_ready 정직·discover_overlap 순수 가산·RISK partial 정직])·**LOW 2 정정**(dataset_source 봉인·문구) / code-review **0 결함**(benign edge[near_jaccard<floor] noted·코드변경 불요).
- 문서: ADR#59(`_DECISIONS`)·R-SemanticIdentityAdjudicator/R-IdentityHumanLabeling/R-ReviewerAgreement/R-GoldSamplingBias/R-LiveIdentityBacklog/R-SourceOverlapScarcity 부분진전(`_RISK`·종결 0·신규 0)·near-match reviewer/gold queue 서브섹션+RealSourceLoop 표 #59(`2_ROADMAP/15`)·(11q)(`2_ROADMAP/00`)·agent readiness §6b(`RAG_KG`)·near-match queue 계약(`IU_CONTRACT`)·`_CANONICAL/02`.

---
_as_of: 2026-06-25 · ADR#59 near-match reviewer/gold queue operationalization — ADR#58 커밋(`e2f33fc`) 후, ADR#58 이 만든 near-match 통로(`build_near_match_reviewer_candidates`)를 **운영 reviewer/gold 큐로 물질화**(새 파일 1·기존 머신 재사용·재구현 0): 신규 `near_match_reviewer_queue.py` 가 near positive(paraphrase)+hard negative 를 정규화(language und→unknown·observed None→''·title str≤512)→**기존** `build_labeling_packet`→`validate_labeling_packet`(verdict 차단 fail-loud)→검증된 reviewer packet+`labeler_facing_view`(bucket·model 판정 제거·bias 0)·`build_gold_seed_report`(gold_ready 실 gold만·merge_allowed False·labeler_prediction_hidden)·`resolve_queue_gold`(`resolve_gold_from_reviewers`→gold/conflict adjudication queue/agreement connector)·`build_reviewer_queue_acquisition_linkage`(source_pair별 near_match_yield/reviewer/gold/hard_negative value→목적 기반 수집)·`EMBEDDING_LLM_ADJUDICATOR_INTERFACE`(**No-Go**·output=provisional_score only·requires=gold+MERGE_GATE+adversarial·`semantic_score` seam); `source_overlap_discovery` 에 hard_negative_pairs 밴드(`_HARD_NEG_FLOOR=0.2`·publishable·same-date·cross-URL·jaccard [0.2,near) different-event lean)+`build_hard_negative_reviewer_candidates` **순수 가산**(captured fixture fingerprint 1·near 5 불변); honesty 봉인=worksheet `dataset_source` 명시(synthetic 이 `summarize_packet_sampling` 기본 live 오집계 차단). 옵션 **A+B 채택·C bounded linkage·D No-Go 인터페이스·E 금지·신규 RISK 0**. **측정**: 신규/확장 34·discovery+queue 88p·backend 비-live 657p/4s/0f(961s full)+dataset_source 2(격리·targeted 30 green)=effective 659·live-PG 94p(단독)·ingestion 1353p·frontend tsc0/test12/lint0·secret PASS(10)·docs_lifecycle 0. 감사: adversarial PROCEED·HIGH 0·MEDIUM 0·LOW 2 정정(dataset_source 봉인·문구)·code-review 0 결함. **정직 경계**: 실 reviewer/gold 0(packet≠gold·synthetic human label 은 경로 입증용)·실 cross-source overlap 미관측(captured fixture)·embedding/LLM adjudicator 미구현(No-Go)·운영 DB/production 백로그 0 불변(완전종결=OVERCLAIM·큐 운영성≠실 gold actuality). **R-SemanticIdentityAdjudicator·R-IdentityHumanLabeling·R-ReviewerAgreement·R-GoldSamplingBias·R-LiveIdentityBacklog·R-SourceOverlapScarcity 부분진전**·종결 0·신규 0(total 36). ADR#58 커밋 `e2f33fc` 위 ADR#59 미커밋(신규 2+수정 2+docs 6+PROJECT_STATUS=11)·커밋 지시 대기·push 안 함._
