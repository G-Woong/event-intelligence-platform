# ml.txt — 무거운 ML 의존성 (임베딩 모델, 로컬 LLM, 데이터 처리)
# Linux 컨테이너에서 mkl_* 계열은 제외 (Windows-conda 전용).
-r base.txt

# --- 딥러닝 프레임워크 (CPU 기본; GPU는 별도 인덱스 사용 권장) ---
torch==2.6.0
torchvision==0.21.0
torchaudio==2.6.0
accelerate==0.34.2

# --- 트랜스포머 ---
transformers==4.56.1
tokenizers==0.22.0
safetensors==0.6.2
sentence-transformers==3.3.1
sentencepiece==0.2.1
huggingface-hub==0.34.4
datasets==2.21.0

# --- 수치 / 데이터 ---
numpy==1.26.4
pandas==2.3.1
pyarrow==21.0.0
scipy==1.16.0
scikit-learn==1.7.1
Bottleneck==1.4.2
numexpr==2.11.0
joblib==1.5.1
threadpoolctl==3.5.0

# --- 이미지 / 그래프 / 수학 ---
pillow==11.0.0
networkx==3.3
sympy==1.13.1
mpmath==1.3.0

# --- 캐시 / 병렬 ---
diskcache==5.6.3
dill==0.3.8
multiprocess==0.70.16

# --- 로컬 LLM 추론 ---
llama-cpp-python==0.3.6
