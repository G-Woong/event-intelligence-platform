# 00 — ROADMAP 인덱스 & 의존성 위상정렬 지도

> ┌─ 진행상황 식별 (STATUS STAMP) ──────────────────────────────────────────────
> │ **상태:** 🧭 META — 인덱스. 각 ROADMAP 파일의 상태는 **그 파일 헤더 스탬프가 권위**.
> │ **구현 사실의 단일 출처:** `docs/_CANONICAL/*` (이 폴더는 *미래 계획*이지 현재 사실이 아니다).
> │ **생성:** 2026-06-20 · 흡수 출처: `WEB_INTELLIGENCE_{MASTER_OVERVIEW,SESSION_CONCEPT_ANALYSIS,IMPLEMENTATION_SPEC,DIRECTION_UPDATE}.md` (4파일)
> └────────────────────────────────────────────────────────────────────────────

이 문서는 `docs/2_ROADMAP/`의 **모든 ROADMAP 파일을 "구현완료 → 미구현" 순으로 정렬**하고, 각 파일의 정직한 상태 스탬프·의존성 위상정렬·흡수 출처를 한 곳에 모은 진입 지도다. 사용자는 이 인덱스를 보고 **어디서·어떤 개념부터 상용화 구현을 시작할지** 판단한다(§4 임계경로 참조).

---

## 1. 상태 스탬프 규약 (모든 ROADMAP 파일 최상위에 부착)

각 번호 md 파일 최상위에는 아래 블록이 부착된다. **정직 원칙**: 구현 사실(`_CANONICAL`)에 어긋나는 허위 "완료"는 금지한다(`CLAUDE.md`·DIRECTION §5 "미구현을 구현된 것처럼 적지 않는다").

```
> ┌─ 진행상황 식별 (STATUS STAMP) ──────────────────────────
> │ **상태:** <아이콘 STATUS> — <한 줄 요약>
> │ **구현순위:** #N (00_ROADMAP_INDEX) · **그룹:** A/B/C/D
> │ **검증 근거:** <_CANONICAL/코드 file:line 근거 — DONE/PARTIAL 주장 시 필수>
> │ **잔여(미구현):** <무엇이 남았나 — risk와 함께>
> │ **완료정의(DoD):** <무엇이 충족되면 ✅ "risk 없이 완벽 구현완료"인가>
> │ **권위:** 구현 사실은 `_CANONICAL/*`. 본 문서는 ROADMAP(미래계획).
> └────────────────────────────────────────────────────────
```

### 상태 아이콘 범례

| 아이콘 | 의미 | 판정 기준 |
|---|---|---|
| ✅ **DONE** | 구현완료·검증됨·**risk 없음** | 코드 산출물 + 테스트 잠금 + 라이브 입증 + 열린 risk 0. **현재 이 스탬프를 받은 ROADMAP 파일은 없다**(토대조차 잔여 gap 보유 → 아래 PARTIAL). |
| 🟡 **PARTIAL** | 토대 일부 실구현 + 잔여 존재 | 실코드/라이브 입증된 토대가 있으나 DoD 미충족(잔여 gap·열린 risk). |
| 🔲 **NOT_DONE** | 미구현 (스텁/설계 청사진만) | grep 0건 또는 `NotImplementedError` 스텁. NET-NEW 포함. |
| 📘 **REFERENCE** | 설계·결정·개념 문서 (코드 산출물 아님) | 목표 아키텍처·판단·포지셔닝·법무분석 등. "구현" 개념 비적용. 인용 게이트 등 일부 실구현은 본문에 명시. |
| 🧭 **META / 📖 ONBOARDING** | 인덱스 / 비개발자 서사 레이어 | 사실 아님. 권위는 `_CANONICAL`·`_DECISIONS`. |

> **"risk 없이 완벽 구현완료"는 DoD가 충족될 때 부여한다.** 각 파일의 DoD 라인이 그 조건을 명문화한다. 현 시점 정직한 진실: **토대(57소스 수집·A→B bridge·Redis/DLQ·11노드 LangGraph baseline·3엔진 색인·1,517 테스트)는 라이브 입증됐으나, 어떤 ROADMAP 레이어도 "잔여 0·risk 0"에 도달하지 않았다.** 이 인덱스는 그 거리를 정직하게 보여준다.

---

## 2. 정렬: 구현완료(✅/🟡) → 미구현(🔲) (의존성·착수준비도순)

> 그룹 헤더로 정직하게 구분. 파일명 번호는 **레이어 아키텍처 의미**라 불변(재번호 안 함, 상호참조 비파괴). 정렬은 이 "구현순위"로만 표현한다.

### 그룹 A — 🟡 구현된 토대를 서술/감사하는 PARTIAL 문서 (코드 산출물 존재)

| 순위 | 파일 | 상태 | 한 줄 |
|---|---|---|---|
| 1 | **05_DISCOVERY_AND_SOURCE_EXPANSION** | 🟡 PARTIAL | registry 57·SourceCapability·정책게이트 인프라 실구현. P0 기본 sink mirror·Entity/Authority 발견엔진 미완 |
| 2 | **11_LLM_SOURCE_SUPERVISOR_AND_JUDGE** | 🟡 PARTIAL | `decide()`·allowlist·`_UNSAFE`·judge 추상화 실구현. `llm_propose` 실 provider 미배선(테스트 람다뿐) |
| 3 | **07_REDIS_QUEUE_CACHE_STREAM** | 🟡 PARTIAL | B Redis Stream·DLQ(`route_failure`/`reap_pending`) 실구현·배선·라이브. A→B EventQueue 배선·Celery·quota 미완 |
| 4 | **15_IMPLEMENTATION_ROADMAP** | 🟡 PARTIAL | Phase0 DONE / Phase1–3 PARTIAL / Phase4–10 NOT_DONE |
| 5 | **18_FINAL_EXECUTIVE_SUMMARY** | 📘 REFERENCE | 섹션 B P0배선 PARTIAL은 실사실 + 미래전략(수익화 단락 구독→광고 전면개정 대상) |

### 그룹 B — 📘 설계·결정·개념 REFERENCE (코드 0; 토대는 살아있음)

| 순위 | 파일 | 상태 | 한 줄 |
|---|---|---|---|
| 6 | **04_TARGET_ARCHITECTURE_LAYERS** | 📘 REFERENCE | L0~L14 목표 청사진(진입 지도) |
| 7 | **10_AGENT_ORCHESTRATION_LAYER** | 📘 REFERENCE | LangGraph/Deep Agents 프레임워크 결정서(현재상태 정합) |
| 8 | **16_LANGCHAIN_LANGGRAPH_DEEPAGENTS_RESEARCH** | 📘 REFERENCE | 도입판단 리서치(0.2.76/DeepAgents 0건 사실검증 정확) |
| 9 | **02_SEARCH_ENGINE_VS_EVENT_INTELLIGENCE_CONCEPT** | 📘 REFERENCE | 최상위 포지셔닝 선언(검색엔진 ≠ event intelligence) |
| 10 | **14_SECURITY_LEGAL_SAFETY_AND_NO_BYPASS** | 📘 REFERENCE | 정책/법무/안전 분석(EvidenceGate·`_UNSAFE`·SSRF·secret scan 인용 게이트는 실구현) |

### 그룹 C — 🔲 미구현 ROADMAP (착수준비도순; 의존 토대 일부 보유)

| 순위 | 파일 | 상태 | 한 줄 |
|---|---|---|---|
| 11 | **06_SEARCH_API_AND_WEB_EXPLORATION** | 🔲 NOT_DONE | `query_generator`/`search_enrichment_collector` = `NotImplementedError`, `expansion_router` 부재. S5 저렴·조기 착수 |
| 12 | **12_EVENT_CLUSTERING_RANKING_AND_DEDUP** | 🟡 PARTIAL | dedup+clique(S2b)+resolver(S2c)+CRUD 영속(S2d)+통합 파이프라인(S2e) 구현; live-PG E2E·heat/rank(S2.5~) 미구현. **임계경로(Event 라우팅)** |
| 13 | **08_RAG_VECTOR_DB_LAYER** | 📘 REFERENCE | 3엔진 색인·Milvus top-k 토대 살아있음; hybrid/RRF/rerank/nori 0% |
| 14 | **13_COMMERCIALIZATION_AND_PRODUCT_STRATEGY** | 📘 REFERENCE | 코드 0. 구독 전제 = 레거시 → 트래픽×광고×커뮤니티로 전면개정(ADR#15) |
| 15 | **09_KG_RAG_GRAPH_RAG_LAYER** | 📘 REFERENCE | **의도적 영구보류**(<1000 엔티티 도입 금지). risk 0 |

### 그룹 D — 🔲 신규 NET-NEW (전부 코드 부재, GroundTruth로 미존재 확정)

| 순위 | 파일 | 상태 | 한 줄 |
|---|---|---|---|
| 16 | **17_AUTHORITY_DISCOVERY_AND_SLM_BODY_FALLBACK** | 🔲 NOT_DONE (NET-NEW) | Entity Registry·Authority Source Graph·Sitemap·Change Detection·SLM Body Fallback |
| 17 | **19_WEB_INTELLIGENCE_IMPLEMENTATION_SPEC** | 🟡 PARTIAL (S1+S2 a~e) | **S1 토대 + S2 Event Resolution 전 단계 구현됨**(0004/0005; clique·event_resolver·event_timeline_service CRUD·event_resolution_pipeline 통합 + ADR#19/#20, 2026-06-22). live-PG E2E·Expansion·Evidence Graph·Agent Debate(S5~S11) 미구현 |

---

## 3. 4대 방향문서 → docs 흡수 매핑 (요약)

세션에서 작성된 4개 루트 문서의 고유 atom은 아래로 흡수됐다(상세 atom-단위 매핑은 흡수 매니페스트, §6 삭제판정 참조).

| 출처 문서 | 핵심 흡수처 |
|---|---|
| **DIRECTION_UPDATE** (방향 결정) | `_DECISIONS/2026-06.md` ADR#14/#15/#16(모순 M1~M6 해소·기각대안) + 본 인덱스 §4 + 13(광고)·11(P/G/F)·12(Event) |
| **SESSION_CONCEPT_ANALYSIS** (개념 원자분해) | `19`(5대 자산 스펙) + `17`(Authority/Entity/Change) + `_CANONICAL/07_ENHANCEMENT_BACKLOG` + 본 인덱스 |
| **IMPLEMENTATION_SPEC** (구현 상세) | `19`(통째 흡수: 스키마·의사코드·alembic·테스트) + `5_REFERENCE/EVENT_SCHEMA.md`(DDL) |
| **MASTER_OVERVIEW** (비개발자 개관) | `0_ONBOARDING/00_MASTER_OVERVIEW.md`(서사 4종) + `_CANONICAL/01·02`(현재 사실은 이미 보유) + 13(상용화) |

> **삭제 가능 여부:** 위 흡수가 100% 완료되면 4파일은 **조건부 삭제 가능**(CONDITIONAL DELETE). 단 ① 신규 4파일(`00_INDEX`/`17`/`19`/`0_ONBOARDING/00`) 생성 완료, ② `CLAUDE.md §4` 파괴적 명령 규율상 **사용자 명시 요청 시에만** 삭제(AI 임의 삭제 금지), ③ 안전 대안 = `_ARCHIVE_SUPERSEDED/`로 이동 후 1턴 관찰. 상세 = 본 세션 보고.

---

## 4. 의존성 위상정렬 — **착수 임계경로 (어디서부터 시작하는가)**

> 두 줄기[① Event Resolution(D)→Evidence Graph(I), ② Entity Registry(K)→Authority Graph(L)→Change Detection(N)]가 **Event 객체에서 합류**한다. **Event 토대(S1)를 먼저 고정하지 않으면 "걸음1(카드 AI 품질)"이 폐기될 1회성 카드 스키마 위에 쌓인다**(DIRECTION §0).

```
S1  Event/Update 타임라인 토대 ───────────────────────[최우선·임계경로 합류점]
    (events/event_updates + event_cards.event_id nullable FK — 최소 토대; cluster_event_map/event_links는 S2 이월)
    │   파일: 19(스펙)+EVENT_SCHEMA(DDL)+12(개정). alembic 0004. **토대 ✅ 구현 2026-06-22**(backend ORM/Pydantic+회귀 17, 1451 green); S2=Resolution.
    ├─ S1.5 Change Detection (read-only: ETag/Last-Modified/norm_hash→SKIP verdict)  ◄ 동시 착수 권장
    │       (비용 지렛대를 후속보다 먼저 켜야 S2~S6 폭주 방지 — orchestrator 인사이트 #4)
    ▼
S5  LLM Expansion Router (게이트 존재라 가장 저렴, S1 직후)  ── 파일 06+11+19
    ▼
S2  Event Resolution Engine [✓ S2-core a~c + S2d CRUD + S2e 통합 + ✅ live-PG 검증(0001~0006·2-세션 동시성·FK RESTRICT) + ✅ C live wiring 경로/seam(event_ingest_pipeline + orchestration sink, ADR#22) + ✅ D-1 운영 결선 composition root(backend-side sink 주입, NullPool 엔진 생명주기, ADR#23) 2026-06-22 + ✅ D-2a Event 타임라인 read API(`/api/events/timeline`, flag, held degenerate 제외, ADR#24) 2026-06-23 + ✅ D-2b frontend 렌더(`/events/timeline` 목록/상세 Next.js page+컴포넌트, 안전 evidence, ADR#25) 2026-06-23 + ✅ D-2b 하드닝(테스트 provider mock 격리·공개 read source_refs 제외·에러표현 통일, ADR#26) + ✅ D-2c 데모(합성 Event seed `seed_event_timeline`·DB target 2중 fail-closed 가드·**로컬**(uvicorn+next dev) 브라우저 E2E 실증=북극성 첫 가시화, ADR#27) 2026-06-23 + ✅ REAL_SOURCE_LOOP_AUDIT(ADR#28: 경로 A[수집→raw_events] 실데이터 PROVEN·경로 B[수집→Event 타임라인] **코드 배선 완료이나 실데이터 0회**·D-2c=synthetic 화면 검증≠실 파이프라인 검증→R-RealSourceLoopUnproven MEDIUM) 2026-06-23 + ✅ **실 소스 production-validation(ADR#29: keyless 뉴스 10소스·411 real records→2 클러스터→resolver CREATE 2+HOLD 3→`/api/events/timeline` 2 실 Event→브라우저 렌더[yna 코스피 서킷브레이커·매경 대우건설] → 경로 B 실 웹 데이터 Event 생성·화면 노출 최초 입증, R-RealSourceLoopUnproven MEDIUM→LOW 부분종결)** 2026-06-23; + ✅ **delta_summary deterministic 자연어화(ADR#30, 2026-06-23)** + ✅ **CREATE genesis update(ADR#31, 2026-06-23: `apply_routing` clean-win CREATE 에 genesis update 1행=생성 근거, "CREATE는 update 0" 불변식 의도적 개정·마이그레이션 0, 실 파이프라인 CREATE→`/events/timeline/{id}` 화면 genesis 자연어+evidence 렌더 1회 관측[Playwright] → R-EventTimelineRenderHardening 완전 종결①②③)**; **+ ✅ **비뉴스 라우팅·fidelity 검증(ADR#32, 2026-06-23: 결정론 5 시나리오+실 fetch; official→Event·structured→signal-only·community→HELD·pure-community→발행 gap → R-SourceTypeFidelityGate 신규, 코드 무변경)**; 다음 우선순위: (1) ✅ source-type publish gate(ADR#33: ACTION_WITHHELD — pure community/search/structured 단독 cross-source 발행 차단; R-SourceTypeFidelityGate MEDIUM→LOW 부분종결)→(2) **✅ primary-authority**(ADR#34: candidate 대표를 official>article>signal>search>community authority 순 — community/market 대표 차단; R-SourceTypeFidelityGate LOW 부분종결 유지·held 이중등장 잔여)→(3) ✅ held-dedup+fail-closed(ADR#35: apply_routing 대표 held 제외 + resolve_routing fail-closed; **R-SourceTypeFidelityGate LOW 부분종결 유지**·약신호-primary 시맨틱 잔여)→(4) ✅ 강신호 weak-primary core-policy(ADR#36)→(5) **✅ 약신호 정책+입력순서 불변(ADR#37: possible_duplicate 동질 publishable gate[혼합 WITHHELD·동일 type 저신뢰 발행] + primary_root=최대 강성분→동률 publishable 우선·members 키 정렬[fragility·cluster_id split 해소]; adversarial 2-pass JUSTIFIED → **R-SourceTypeFidelityGate 완전 종결(RISK_CLOSED 이관)**)→(6) ****✅ R-FalseMerge held 승격(ADR#38: held member 재등장 title-judge 승격 candidate B — same→parent APPEND·different→독립 CREATE; gap① 부분종결·인메모리 7 + gate-bypass 입증; **R-FalseMerge open 유지** — live-PG held 승격 미실증[Docker 대기]+cross-batch identity 잔여)→(7) **✅ R-FalseMerge 완전 종결(ADR#39, 2026-06-24: Docker 가동→live-PG 30/30 green[held 승격 3 실증]→CLOSED[OVER-merge 한정, RISK_CLOSED]; adversarial JUSTIFIED; cross-batch UNDER-merge=R-CrossBatchEventIdentity·catalog→official_record 누수=R-SourceCatalogFidelity 신규 분리·등록)**→(8) **✅ substrate hardening(ADR#40, 2026-06-24): R-SourceCatalogFidelity CLOSED**(catalog 6종→catalog_metadata 비-publishable override·live-PG 0 events) + **R-CrossBatchEventIdentity 부분종결**(deterministic Event Identity Layer=event_identity_map[alembic 0007]·shared-anchor cross-batch 수렴 live-PG 검증; semantic-only 독립보도 분열+fragment 오APPEND 잔여 open) — **RAG/KG 이전 필수 gate**→(9) **✅ deterministic semantic 후보 substrate(ADR#41, 2026-06-24): event_identity_candidate[alembic 0008]·token-set+date fingerprint→event_links(possible) LINK 만[병합 아님·중복 count 미감소·false-merge 0]·live-PG 4 검증; 실 병합=R-SemanticIdentityAdjudicator 신규 OPEN; Intelligence Unit 계약 명문화**→(10) **✅ semantic adjudicator shadow/eval(ADR#42, 2026-06-24): event_identity_adjudication[alembic 0009]·deterministic adjudicator 가 possible-link 소비→status[likely_same/ambiguous/likely_different/insufficient]·자동 병합 0·API 미노출·live-PG 5; R-SemanticIdentityAdjudicator 부분 진전·R-IdentityEvalDataset 신규**→(11) **✅ identity eval harness(ADR#43, 2026-06-24): identity_eval_dataset.py+fixtures(22 진단 pair)+evaluate_adjudicator(precision/FPR/recall/breakdown)·현 adjudicator precision 0.57 측정[merge gate 미달·auto-merge OFF]·export 소비처(human-labeling 워크시트); R-IdentityEvalDataset harness 부분 진전·R-IdentityHumanLabeling 신규**→(11b) **✅ human-labeled gold workflow(ADR#44, 2026-06-24): identity_human_labeling.py(GoldPair provenance·promote_worksheet_to_gold self-label 금지·gold-only metric·evaluate_gold_merge_readiness live gold만·auto-merge OFF)+sample gold(시연·precision 0.6·KO 0.5·merge gate 미달); R-IdentityHumanLabeling workflow 부분종결[실 production gold 0]·R-IdentityEvalDataset gold loader 부분진전**→(11c) **✅ reviewer agreement protocol(ADR#45, 2026-06-24): identity_human_labeling 확장(ReviewerLabel·resolve_gold_from_reviewers[다중 합의=gold·conflict 자동 gold 금지·single=insufficient·LLM adjudicator 차단]·sampling bucket 12·estimate_sample_floor_*[189/381])+reviewer 시연 샘플; R-IdentityHumanLabeling 부분진전·R-ReviewerAgreement·R-GoldSamplingBias 신규**→(11d) **✅ live-derived labeling packet(ADR#46, 2026-06-24): identity_human_labeling 확장(build_labeling_packet[live 워크시트→15 candidate bucket 샘플링·reviewer ≥2 distinct 배정·predicted_status 차폐=bias 0]·labeler_facing_view·summarize_packet_sampling[deficit/oversample·표본 floor 대조·selection=결정론 정렬 cut-off]·adjudication_queue_from_resolved[conflict→human-only])+export signal→market enum 정규화[adversarial HIGH 수정]+candidate 시연 샘플; R-ReviewerAgreement·R-GoldSamplingBias packet scaffold 부분진전[실 reviewer/충원/대표성 0·완전종결 금지]**→(11e) **✅ live-derived labeling packet pilot(ADR#47, 2026-06-24): `build_live_identity_labeling_packet.py`(read-only)가 live-PG adjudication 백로그→packet[collect/generate_live_packet_report·backlog/exclusion report·event count before/after·auto_merge_enabled False]·옵션 D bucket-hash 표집(sha256·정렬 편향 완화·재현·ADR#46 기본 하위호환); live-PG 실 파이프라인 유래 eligible 1·live_selected 1·운영 자동 백로그 0[단계 ③ 미배선·운영 DB 미마이그레이션→exclusion 표면화]; R-LiveIdentityBacklog 신규·R-ReviewerAgreement·R-GoldSamplingBias 부분진전[완전종결 금지]**→(11f) **✅ stage③ operational wiring + migration readiness(ADR#48, 2026-06-24): `ingest_records_to_events(adjudicate_semantic=)` 가 배치 뒤 flag(`EVENT_SEMANTIC_ADJUDICATION_ENABLED` off-by-default) on 이면 단계 ③ `adjudicate_semantic_links` 를 자동 실행→event_identity_adjudication 백로그 자동 누적[자동 병합 0·shadow write only·멱등]; live-PG ingest→③→packet eligible 1·live_selected 1[운영 loop 유래·수동 호출 0]; `identity_backlog_readiness`[read-only] 가 운영 DB 0003 vs head 0009 6 revision 뒤·non-destructive 정량화[운영 upgrade 미적용]; R-LiveIdentityBacklog 부분진전[배선≠production·운영 DB 배포·실 fetch·주기 job·incremental 잔여·완전종결 금지]**→(11g) **✅ incremental / no-cluster backfill + 배포 runbook(ADR#49, 2026-06-24): `adjudicate_semantic_links(only_unadjudicated=,limit=)` 로 비싼 per-link work 를 미판정 link 한정[O(N) 완화]·ambiguity 정확성 회귀 가드(cand_targets 필터 전 산출)·`ingest_records_to_events` 의 `if not clusters: return` 제거→no-cluster backfill·신규 `backfill_semantic_adjudications.py`[dry-run·bounded·read+adjudication upsert only]·운영 DB 0003→0009 배포 runbook(`15_ROADMAP §`); live-PG exclusion 1→0·Event 불변·멱등; R-LiveIdentityBacklog gap②③ 해소·운영 DB 배포/실 fetch/주기 스케줄러/cheap-scan keyset 잔여·완전종결 금지**→(11h) **✅ backfill operation hardening + deploy checklist(ADR#50, 2026-06-24): keyset(`after_link_id` cursor·`_semantic_links` 가 WHERE id>cursor·NOT IN·LIMIT 를 SQL 로 push→bounded run 의 O(전체) 적재 완화·ambiguity 는 page candidate 한정 GROUP BY 로 link별 status 동작 보존; **cursor=UUIDv4 byte 순서·시간순 아님·진행 보장은 only_unadjudicated**·`full_scan` 으로 전수 경로 표면화)·`backfill_semantic_adjudications` 운영 CLI(`--limit/--after-link-id/--dry-run`·safe-target 가드·next_cursor/full_scan/idempotent_persist report·lock 과대주장 0)·`build_operational_deploy_checklist`(0003→head 명령 문자열·backup_required·executed=False)·readiness read-only CLI·주기 가동은 기존 `run_recovery_scheduler --once` 관용구 **재사용 가능**[설계·미배선·운영 DB migration 후 게이트]; live-PG keyset cursor/교차 backfill 중복-persist 멱등/실 deploy checklist 검증; R-LiveIdentityBacklog gap③④ 추가 진전·운영 DB 배포/실 fetch/실제 주기 서비스 가동 잔여·완전종결 금지**→(11i) **✅ backfill scheduler operationalization(ADR#51, 2026-06-25): preflight gate(`backfill_preflight`/`run_backfill_with_preflight`·ready_for_stage3 **hard gate**[0003 dry-run 포함 차단·NOT IN 쿼리 크래시 방지]+flag persist gate[`EVENT_SEMANTIC_ADJUDICATION_ENABLED` off→persist만 차단·dry-run 허용·allow_flag_off 우회])·deterministic exit code(`decide_exit_code` 0=성공/1=blocked/2=runtime/3=dry-run pending)·`created_at` 시간순 cursor(`cursor_mode='created_at'`·`or_(created_at>cur, and_(created_at==cur, id>after_link_id))` 컬럼 비교[행값 tuple_ uuid>varchar 교체·동치]·`next_created_at`·**배치 간만 정확·intra-txn tie→id·인덱스 없음**)·scheduler 스크립트(`workers/tools/run_semantic_backfill_scheduler.py`·recovery-scheduler 관용구 복제·gated·**dry-run default·docker 미배선·미가동**); live-PG created_at 시간순/동일 created_at tie-break resume/**0003 테이블 DROP block readiness(크래시 0)**/preflight flag gate·Event 불변; adversarial **기술계약 VALID(정직)**·지적 4건 처리(HIGH 2=docs 동기화·MEDIUM 2=live-PG 테스트 보강·LOW=help)·복합 cursor uuid>varchar self-found+fixed; R-LiveIdentityBacklog gap④ scheduler-ready+cursor 부분진전·운영 DB 배포/실 fetch/실제 docker 가동 잔여·완전종결 금지**→(11j) **✅ production scheduler activation readiness(ADR#52, 2026-06-25): scheduler docker 배선 — compose `semantic-backfill-scheduler`[`profiles:["backfill"]` 기본 미기동·dry-run default(command --persist 부재)·preflight gated·backend 이미지 재사용(worker 이미지 services/tools/models 미COPY)·entrypoint override·단일 instance(replicas 미설정·restart "no")]·옵션 C(created_at index)/D(advisory lock) 근거 DEFER[backlog 0·prod 0003·test churn 회피·DDL/single-instance runbook 문서화]·**코드 변경 0**[compose+일관성 테스트 5]·scheduler --help 명령 검증; **실가동 0**[profile 비활성·운영 DB 0003·flag off]·docker build/up 미검증[정적 일관성 테스트만]; R-LiveIdentityBacklog gap② docker scaffold 부분진전·운영 DB 배포/실 fetch/profile 활성+--persist 가동 잔여·완전종결 금지**→(11k) **✅ production scheduler activation VALIDATION(ADR#53, 2026-06-25): ADR#52 의 "정적만" 한계를 실 docker 로 해소 — `docker compose --profile backfill build` 성공·`run --once` 3경로 실측[DB down→graceful exit 2·미마이그레이션→`BLOCKED readiness` exit 1·dev DB head→`dry_run=True event_count 0->0 auto_merge=False` exit 0]·profile 격리 config 실증; 🔴 정적 config/build 가 못 잡은 런타임 버그 발견·수정 — `backend/Dockerfile` ingestion/ 미COPY→adjudicator 전이 import `ModuleNotFoundError`→`COPY ingestion/`+회귀 테스트 2; live-PG `test_event_resolution_live_pg` 91 passed 재실행[ADR#52 미실행]; 정직: run#3 0->0=빈 dev DB(read-only 입증=코드+live-PG)·live-PG 91=DB orchestration(scheduler 주기 가동 아님)·dev event_intel head 마이그레이션[운영 DB 아님·additive]·C/D 계속 DEFER[backlog 0·test churn 실측]·adversarial CONDITIONAL VALID[코드·안전계약 VALID·HIGH-1 문서 정정 해소]; scheduler 실가동 0·production 백로그 0; R-LiveIdentityBacklog gap② docker 실행성 입증 부분진전·완전종결 금지**→(11l) **✅ production activation preflight + real-source smoke(ADR#54, 2026-06-25): `production_activation_preflight`[read-only·DDL/upgrade/persist 0]가 readiness+flag+safe_target+`classify_write_target`[named 분류 dev/test/staging/production/unknown+APP_ENV↔URL mismatch]를 17필드 report 로 묶어 can_persist[persist∧ready∧flag∧safe_target∧¬destructive∧consistent]·block_reasons·next_required_actions 산출·dev event_intel safe-target no-op(MEDIUM-1) warning 표면화·DATABASE_URL fingerprint 만·--allow-non-dev-db 는 readiness/flag 못 우회; `real_source_identity_smoke`[기본 offline fake·network 0·DB 0·결정론]가 fetch→cluster→candidate 단계별 실패 분류+role distribution 진단·DB 단계 offline None·--live-db(safe-target gated) opt-in·real_fetch 0·community anchor 금지·no_auto_merge 불변; C/D DEFER(persist warning)·E(LLM/Agent) docs 진입 9조건만(RAG_KG §6b·No-Go); 신규 테스트 2파일; 점검/진단≠가동·운영 DB 무변경·production 백로그 0; R-LiveIdentityBacklog·R-RealSourceLoopUnproven 부분진전·완전종결 금지**→(11m) **✅ real-source live-db smoke(ADR#55, 2026-06-25): `real_source_identity_smoke` 에 live_network(key-free official JSON API federal_register·bounded·opt-in·CI 아님·본문 미저장[headline/canonical/published_at 만]) + live_db(disposable test/dev DB·safe-target gated) 추가·신규 `real_source_smoke_report`(순수)가 §4 activation report+source quality matrix(§9·옵션 E)+agent readiness 9조건(§8·단일 출처) 조립; 실 federal_register fetch 5 official records→event_intel_test(head 0009) ingest: created 1+identity_link 1(held member)·adjudications 0·packet_eligible 0·no_auto_merge·event_count 4→6; 정직 분해: adjudications 0=held-member link reason `new_event_low_confidence`(≠semantic_cross_batch_candidate)·cross-batch adjudication 은 동일사건 다중소스/시계열 fingerprint 중첩 필요(source scarcity)·community/market/catalog guard_only(anchor 금지); live-PG 91p·신규/확장 테스트 36; R-RealSourceLoopUnproven(실 fetch+실 DB 도달)·R-LiveIdentityBacklog 부분진전·완전종결 금지[live_network=opt-in tool·live_db=disposable test DB≠production·실 cross-source 비뉴스/adjudication backlog/reviewer/gold/merge 잔여]**→(11n) **✅ time-series replay smoke(ADR#56, 2026-06-25): ADR#55 단일소스 adjudications 0 의 원인을 정밀 분해[distinct document=distinct URL=distinct fingerprint→같은-사건·다른-URL 후보 구조적 불가=source scarcity]하고 `run_time_series_replay_smoke`[safe-target gated·`--time-series-replay`·artificial 2배치(배치 A 같은 URL 2 record→CREATE E1+fingerprint F·배치 B 다른 URL 2 record[같은 제목·날짜=같은 F]→CREATE E2→`semantic_cross_batch_candidate` link→stage③ likely_same)]로 **cross-batch adjudication substrate 가 같은-사건·다른-URL·교차배치 데이터에서 닫힘을 실데이터 입증**[event_intel_test head 0009: created 2·semantic_cross_batch_candidate 1·adjudications 1(likely_same_event)·event_count 0→2·no_auto_merge·live-PG 잠금]·`classify_adjudication_block_reason`[§5 순수: db_not_reached/none/semantic_link_without_adjudication/non_publishable_role/no_fingerprint_overlap/no_cross_batch_overlap]가 단일소스 0 을 `no_cross_batch_overlap` 로 귀속; 옵션 A[multi-source 실 fetch]는 key-free 공식 단독 overlap 희소로 정직 분해만[스크레이퍼 미구현]·C[sanitized 계약]는 replay 템플릿에 내재·D/LLM 금지; 신규/확장 테스트 15[unit 12+live-PG 3]; **단 artificial_replay=True[실 source behavior 아님]·실 동일사건 다중소스/시계열 fetch·운영 DB 배포·reviewer/gold/merge 잔여**·production 백로그 0 불변; R-RealSourceLoopUnproven·R-LiveIdentityBacklog 부분진전·완전종결 금지**→(11o) **✅ source overlap discovery(ADR#57, 2026-06-25): 신규 `source_overlap_discovery.py`[write-free·no-DB·no-merge·LLM 호출 0]가 record 다입도 overlap 을 `fingerprint_overlap`[정확 token-set 일치→deterministic 검출→교차배치 시 semantic_cross_batch_candidate] vs `near_match_below_fingerprint`[paraphrase→deterministic 사각지대·adjudicator/embedding/LLM 영역·gated]로 분해·`possible_same_event_pairs`·`overlap_potential_matrix`·`block_reasons`·`build_agent_orchestration_schema`[no_merge_without_gate·no_public_intelligence_unit]; GDELT bounded fetch[key-free·opt-in·transport 결정론·실패 분류]; `real_source_smoke_report` §8 overlap_potential/utility 추가; **실 GDELT fetch 시도→429(R-Gdelt429)→captured fixture fallback**·fixture 6 possible overlap 중 deterministic 1(verbatim)만·5(paraphrase) adjudicator-zone; **R-SourceOverlapScarcity 신규**[key-free 실 cross-source overlap 구조적 희소]·R-RealSourceLoopUnproven·R-LiveIdentityBacklog 부분진전·실 overlap 미관측·합성≠실 source·완전종결 금지**→(11p) **✅ real overlap acquisition strategy(ADR#58, 2026-06-25): ADR#57 GDELT 429 근인=backend 도구의 기존 governance 우회 raw httpx 규명·시정 — (A) `gdelt_provider_status`[read-only preflight·network/write 0·HostRateGate(host floor·shared host_rate_gate.json)+rate_limit_policy(gdelt min 60s·cooldown 900s·retry 1)+in_cooldown honor]+`fetch_gdelt_overlap_records` short-circuit[provider_status≠ok→network 미시도·no tight retry]; (B) key-free RSS 함대 governed 실 fetch[`parse_rss_items`(stdlib ET·RSS/Atom·RFC822→ISO·본문 미저장)·`fetch_rss_overlap_records`(_SERVICE_CONFIGS endpoint(auth=none)·shared host gate 참여·bounded·failure 분류·transport 주입)]·**실측 bbc/aljazeera/the_verge/techcrunch 55 record(429 0)→cross-source overlap 0(no_title_overlap)·same-beat 30 record 도 0=governance 검증+실 overlap 구조적 희소 확증**; (C) `build_acquisition_plan`[source_pair/topic/time plan·expected_overlap_utility·LLM 0]; (D·핵심) `build_near_match_reviewer_candidates`[paraphrase 사각지대→reviewer/gold worksheet·EvalPair/build_labeling_packet 재사용·label=unlabeled·predicted_status 미포함·publishable만·no_merge_without_gold·병합 0]; §9 source quality matrix(overlap_acquisition_utility/title_paraphrase_risk/provider_accessibility/rate_limit_risk/robots_tos_status); 신규/확장 테스트 29(discovery+21·report §9+8); 다음 게이트=targeted same-event acquisition[RSS 고정 feed=query 불가·GDELT=429]→detection 레이어(embedding/LLM+gold); R-RealSourceLoopUnproven·R-LiveIdentityBacklog·R-SourceOverlapScarcity·R-Gdelt429 부분진전·실 cross-source overlap 미관측·완전종결 금지**→(11q) **✅ near-match reviewer/gold queue(ADR#59, 2026-06-25): 신규 `near_match_reviewer_queue.py`[오케스트레이터·재구현 0]가 ADR#58 통로를 운영 큐로 물질화 — `build_near_match_reviewer_queue`[near positive(paraphrase)+hard negative→`build_labeling_packet`→검증된 reviewer packet·predicted_status 미포함·`labeler_facing_view` bias 0·LLM 0]·`build_gold_seed_report`[queue/near/hard/fingerprint count·bucket·gold_ready(실 gold만)·merge_allowed False]·`resolve_queue_gold`[gold/conflict adjudication queue/agreement 재사용]·`build_reviewer_queue_acquisition_linkage`[source_pair별 near_match_yield/reviewer/gold/hard_negative value→목적 기반 수집 전환]·`EMBEDDING_LLM_ADJUDICATOR_INTERFACE` No-Go; `source_overlap_discovery` hard_negative_pairs 밴드 가산[publishable·same-date·jaccard [0.2,near) different-event lean·순수 가산·ADR#57/58 수치 불변]·dataset_source provenance 봉인[synthetic≠live]; adversarial PROCEED[HIGH 0·MEDIUM 0·LOW 2 정정]·code-review 0; **실 reviewer/gold 0**[큐는 substrate]; R-SemanticIdentityAdjudicator·R-IdentityHumanLabeling·R-ReviewerAgreement·R-GoldSamplingBias·R-LiveIdentityBacklog 부분진전·완전종결 금지)**→(11r) **✅ targeted same-event acquisition operating readiness(ADR#60, 2026-06-25: 신규 `targeted_same_event_acquisition.py`[재구현 0]가 ADR#59 큐[captured fixture 수준]를 targeted acquisition[source-pair·topic·time-window]+reviewer operating checklist+gold simulation 으로 연결·`build_provider_capability_matrix`[GDELT 429·RSS topic 불가→실 targeted near-match yield 낮음 정직]·§4 report[acquisition_run_id·candidate/near/hard/fingerprint·reviewer_packet_exportable·gold_ready·dataset_source·honesty_boundary]·`build_reviewer_operating_checklist`[hidden_prediction/raw_body absent 구조적 verify·allowed_labels·merge_policy prohibited until MERGE_GATE]·`simulate_gold_calibration`[synthetic 5경로·**production_gold 0**]·`_SYNTHETIC_FIXTURE_MARKER`[synthetic↔live 봉인 코드 강제]·adversarial **PROCEED-WITH-FIXES**+code-review 정정[host_gate→gdelt 우회 회귀·candidate_count 분리]; 신규 테스트 57·backend 비-live 716p·**실 targeted near-match 후보·reviewer/gold 0**·R-SourceOverlapScarcity·R-IdentityHumanLabeling·R-ReviewerAgreement·R-GoldSamplingBias·R-SemanticIdentityAdjudicator·R-LiveIdentityBacklog 부분진전·완전종결 금지)**→(11s) **✅ query-capable provider acquisition readiness gate(ADR#61, 2026-06-25: 신규 `provider_readiness.py`[ADR#60 frozen·단방향 import·재구현 0]가 ADR#60 "다음 게이트=query-capable provider" 를 운영 gate 로 물질화 — `build_provider_readiness_report`[query-capable provider 를 key_free_query(gdelt/sec_edgar/federal_register)·key_required_query(newsapi/gnews/guardian/nyt/naver_news_search/serper/tavily/exa)·key_free_non_query(rss_fleet)·blocked_rate_limited(gdelt 429)·unknown(fail-closed) 분류+credential(`env_status` secret-safe present/missing·값 0)·host_gate cooldown(`gdelt_provider_status`)·rate_limit_policy·next_action]·**honest `fetch_implemented` gate**[실 fetcher=gdelt/rss 만→미배선 provider 는 credential-ready 여도 fixture 둔갑 코드 차단·dataset_source None]·`run_optional_live_query`[opt-in+safe+credential+fetcher gated→ADR#60 운영 경로 재사용·live_derived 후보→near-match queue·실패 분류]·`run_provider_acquisition_readiness`[§4 통합]·agent schema[no_secret_fabrication·embedding No-Go]·CLI[network 0·--live-query opt-in]; injected GDELT transport→candidate 5·queue_pop 5·dataset_source live_derived·production_gold 0·merge_allowed False; 신규 테스트 43·backend 비-live 759p·**adversarial SOLID(HIGH 0)+code-review 동일 MEDIUM 1(rss_fleet 명칭 이음새→fixture silent 낙하)→정정(rss_fleet→rss 정규화·source_ids 단일 출처·회귀 2)**; **실 wired+working key-free provider=gdelt[429]만→실 near-match 후보·reviewer/gold 0**[readiness=능력 표면화·실 데이터 아님]; 신규 RISK 0[R-ProviderCredentialReadiness 등은 R-SourceOverlapScarcity·R-Secret 포섭]; R-SourceOverlapScarcity·R-RealSourceLoopUnproven·R-LiveIdentityBacklog·R-IdentityHumanLabeling·R-SemanticIdentityAdjudicator·R-Gdelt429 부분진전·완전종결 금지)**→(11t) **✅ provider query adapter contract + 첫 wired adapter(ADR#62, 2026-06-25: 신규 `provider_query_adapters.py`[ADR#60/#57 frozen·재구현 0]가 ADR#61 `fetcher_not_wired` 병목 축소 — ProviderQueryAdapter/ProviderQueryResult 공통 contract[옵션 A]+Guardian Content API 실 adapter[옵션 B·key-required·news·공식 shape response.results[].webTitle/webUrl/webPublicationDate·본문 미저장·secret-safe(env_status·key httpx params·url keyless)]·guardian→`_FETCH_WIRED_PROVIDERS`·**adapter-wired 분기**[직접 governed fetch→ADR#60 `records=` 주입·real_fetch=True·fixture 둔갑 0·provider= 직접전달 시 fixture silent 낙하(rss_fleet seam) 회피]·readiness adapter 필드(adapter_contract_version/queue_integration_status/live_query_capable_if_credentials_present)+agent schema(provider_adapter_contract·wired_providers)·`rate_limit_policy.yaml` guardian per-source(min 5s·cooldown 600s)·governed(HostRateGate·in_cooldown·no tight retry); fake transport→records 4·near 2·queue_pop 5·dataset_source live_derived·production_gold 0·merge_allowed False; 신규 테스트 29·backend 비-live 788p·adversarial **SOLID(HIGH 0·blocking 0·LOW 3 비차단)**+code-review[max_records=0 falsy 정정·test_12 content 강화]; **실 key 미주입·fake transport 만→실 near-match 후보 0·미배선 9종 잔여**[adapter=능력·실 데이터 아님]·embedding/LLM No-Go·운영 DB 0; R-SourceOverlapScarcity·R-RealSourceLoopUnproven·R-LiveIdentityBacklog·R-IdentityHumanLabeling·R-SemanticIdentityAdjudicator·R-Gdelt429 부분진전·완전종결 금지)**→(11u) **✅ secret-safe Guardian live query smoke + live-derived queue population gate(ADR#63, 2026-06-26: 신규 `guardian_live_query_smoke.py`[ADR#62 커밋분 frozen·재구현 0]가 ADR#62 adapter 의 **실 network 직전 경계 검증** — §4 smoke 계약[env_var_name·credential_present·**credential_value_exposed=False·env_file_read=False**·records/candidate/near/hard/queue_pop·dataset_source·block_reasons·next_actions·production_gold 0·merge_allowed False·db_write False]·**env_not_loaded vs missing_credentials 구분**(`.env` Path.exists)·credential present+opt-in 시 `run_optional_live_query` 위임·`env_loader.probe_env_var`[present/missing+.env Path.exists+.env.example 이름·**값 미열람**]·CLI(--live-query opt-in·값 미출력); 신규 테스트 26; **실 Guardian 호출 2회**[opt-in·bounded·governed·secret-safe·Ukraine/earthquake 7d→각 **records 10·candidate 0·no_title_overlap·dataset_source live_derived·secret_exposed False·env_file_read False·host_gate passed**]→**live 경로 실증**[실 key·실 fetch·정규화·secret 경계] but **단일 소스 same-event overlap 0**[구조적·같은 날 제목 Jaccard<near·recall 한계 포함·deterministic near-match=cross-source 설계→**2nd publishable provider 가 실 unblock**]; 옵션 A+B+C·D[2nd adapter]/E/F 금지·신규 RISK 0·R-SourceOverlapScarcity·R-RealSourceLoopUnproven·R-LiveIdentityBacklog·R-IdentityHumanLabeling·R-SemanticIdentityAdjudicator·R-Gdelt429 부분진전·완전종결 금지)**→(11v) **✅ 2nd publishable provider + cross-source live overlap smoke(ADR#64, 2026-06-26: `provider_query_adapters` 에 NYT adapter wire[key-required·news·`api-key` param=Guardian 동일→`run_provider_query` 무수정·begin/end_date YYYYMMDD keyless·본문 미저장]+신규 `cross_source_live_overlap_smoke.py`[**둘 다 성공만 cross-source 인정**(single-source 둔갑 금지·한쪽만 ok→no_records_provider_a/b)·near/hard pair 를 source_id 상이만 필터해 ADR#59 queue 충원·§4 계약·credential_value_exposed/env_file_read False]·CLI(--live-query·--provider-b·값 미출력); 신규 테스트 39; **실 Guardian+NYT 호출 4회**[Ukraine/earthquake 7d→각 **Guardian 10+NYT 10=combined 20·cross_source_pair_count 100·fingerprint 0·near 0·hard 0·no_title_overlap·dataset_source live_derived·secret_exposed False·host_gate passed**]→**cross-source pair 는 단일 소스 구조적 0→100 으로 실증 생성**[ADR#63 한계 직접 해소] but **deterministic title-Jaccard 0 검출**(hard band 0.2 까지 0)=deterministic paraphrase recall 한계 **또는** 다른-사건 보도(real scarcity) 둘 다 가능·n=2 미구분(100=비교 대상)·수집/source/key/fetch 능력은 입증→**다음 방향=embedding/LLM adjudicator(옵션 E·gated·precision 0.57<0.98 별도 근거)**; 옵션 A+B+C·D[3rd provider]=docs only·E/F 금지·신규 RISK 0·R-SourceOverlapScarcity·R-RealSourceLoopUnproven·R-LiveIdentityBacklog·R-IdentityHumanLabeling·R-SemanticIdentityAdjudicator·R-Gdelt429 부분진전·완전종결 금지)**→(11w) **✅ offline/gated semantic candidate scorer(ADR#65, 2026-06-26: 신규 `semantic_candidate_scorer.py`[ADR#64 frozen·재구현 0]가 ADR#64 "cross-source pair 100·deterministic 0 검출"=**detection scarcity** 를 다루는 첫 scorer 층 — §5 `SemanticPairInput`/`SemanticPairScore` contract·scorer_mode[deterministic_scaffold=ADR#64 의 title-Jaccard 0 신호 그대로(recall 개선 0)·fake_semantic=char-bigram Dice+token containment 결정론 proxy(실 embedding 아님·배관 scaffold)·embedding_opt_in/llm_opt_in=**injection-only**(실 client 미배선 No-Go·`create_embedding_client`/`create_llm_client` 미래 seam·secret-safe `probe_env_var` gate·실호출 0)]·additive `discover_overlap(emit_candidate_pairs)`[near floor 무관 전 publishable cross-URL same-date pair band 태그 노출·기본 off→ADR#57/#64 frozen]·score→**top-k rank**(threshold 아님·zero-signal 제외)→top-k near+hard negative→`build_near_match_reviewer_queue`[score 없는 pair record 만·`validate_labeling_packet` fail-loud·labeler_facing_view 숨김]·`build_eval_gold_linkage`[MERGE_GATE 단일 출처·precision 0.98·production_gold 0·No-Go]·`SCORER_AGENT_CONTRACT`[semantic prioritization 계획 가능·truth/merge/public IU/anchor/secret 출력 불가]·additive `cross_source_live_overlap_smoke(semantic_scoring)`[기본 off=ADR#64 출력 byte-identical·on 시 live combined_records 의 cross-source pair(guardian 10×nyt 10=cross_source_pair_count 100·same-source 90 제외) scorer 입력]·CLI; 신규 테스트 49[scorer 46+smoke 통합 3·감사 fix-lock 5]·backend 비-live 899p/101 skip/0 fail·ingestion 1353p·frontend tsc0/lint0/test12·ruff 0·**실호출 0**[opt-in 도 default scorer_disabled·credential present+scorer_fn 미주입→provider_error·주입 fake scorer_fn 만 실행]·**감사 PROCEED-WITH-FIXES**[M-1 cross_source_only·M-2 above_near_floor·MED-1 hard_negative 미승격·HIGH-1 fallback band→코드/테스트 수정]; **진전=candidate prioritization substrate·validated adjudicator 아님**[deterministic_scaffold=recall 개선 0·gold 없이 threshold 금지·`AdjudicationFeatures.semantic_score` 미주입]·옵션 A+B+D 채택·C=injection-only interface·E(production semantic merge)/F 금지·신규 RISK 0[R-SemanticScorerCalibration/R-LLMAdjudicatorBias/R-EmbeddingProviderReadiness=현 단계 독립 blocker 아님→미등록]·R-SemanticIdentityAdjudicator·R-IdentityEvalDataset·R-ReviewerAgreement·R-GoldSamplingBias·R-SourceOverlapScarcity·R-RealSourceLoopUnproven·R-LiveIdentityBacklog·R-IdentityHumanLabeling 부분진전·완전종결 금지)**→(11x) **✅ reviewer label operations + gold calibration preflight(ADR#66, 2026-06-26: 신규 `reviewer_label_operations.py`[얇은 orchestrator·DB-free·기존 머신 재사용·재구현 0]가 ADR#65 scorer top-k reviewer queue 를 **실 사람 라벨 운영**으로 연결 — `export_reviewer_packet`[labeler_view score/rationale/bucket 누출 재검증]·`import_reviewer_labels`[파일 없음→**no_labels graceful**·forbidden field(score/rationale/raw body/secret) allowlist fail-loud]·`pseudonymize_reviewer_id`[raw PII 미노출]·`resolve_label_operations`[single=insufficient·2+만장일치=agreed·conflict+human adjudication=adjudicated·LLM-as-judge 봉인+**production/synthetic gold 분리**(production=production AND live_derived)]·`build_calibration_preflight`[§7: positive/negative/korean gold·agreement_rate·conflict_rate·positive_negative_balance·hard_negative_coverage·**top_k_bias_warning**·precision/fpr_denominator_ready·merge_gate_ready]·`run_reviewer_label_operations`[§4 31필드]·CLI; 신규 테스트 49[감사 HIGH fix-lock 47-49=model/self label gold 불가 in-memory·file 양 경로 봉인]·**production_gold_count 0**[실 production reviewer label 0·synthetic/test=simulated only]·merge_allowed False·db_write False·llm/embedding 실호출 0·public IU 0·scorer/smoke 미변경[최소 변경]; **진전=reviewer/gold operational readiness substrate·실 gold 아님**·옵션 A+B+C+D 채택·E(embedding/LLM runtime)/F 금지·신규 RISK 0·R-IdentityHumanLabeling·R-ReviewerAgreement·R-GoldSamplingBias·R-IdentityEvalDataset·R-SemanticIdentityAdjudicator·R-LiveIdentityBacklog·R-SourceOverlapScarcity·R-RealSourceLoopUnproven 부분진전·완전종결 금지)**→(11y) **✅ reviewer batch launch pack + intake validation loop(ADR#67, 2026-06-26: 신규 `reviewer_batch_launch.py`[ADR#66/#60/#59 조합·재구현 0]가 ADR#66 reviewer label 운영 함수를 **사람이 바로 라벨링을 시작할 batch pack** 으로 전환 — `build_reviewer_instruction`[§6 4-라벨 정의/금지/권장·model score/rank 0]·`build_label_template`[`REVIEWER_ALLOWED_KEYS` 빈 worksheet·reviewer_id pseudonym·score/bucket 0]·`build_assignment_manifest`[pair 당 ≥2 reviewer·pseudonym only·top-k+hard negative·capacity<2→`insufficient_reviewer_capacity`·raw roster 미commit]·`build_intake_plan`[directory/expected files/validation command]·`validate_label_intake`[§8 import 前 dry-run·forbidden/malformed/reviewer_kind/pair_id/duplicate·labeler vocab(unsure/needs_review)→canonical(insufficient/ambiguous) 정규화]·`run_reviewer_batch_launch`[§4·intake_status awaiting/present/invalid/imported·**decisive gold=same/different 만**·`resolve_label_operations`+`build_calibration_preflight` 재사용]; 신규 테스트 67·backend 비-live 1015p/101skip/0fail·**production_gold_count 0**[실 production reviewer label 0·synthetic/test=simulated]·merge_allowed False·db_write False·llm/embedding 0·**자체발견·수정=만장일치 ambiguous gold 승격→decisive only**; **진전=batch launch readiness substrate·실 gold 아님**·옵션 A+B+C+D 채택·E/F 금지·신규 RISK 0·R-IdentityHumanLabeling·R-ReviewerAgreement·R-GoldSamplingBias·R-IdentityEvalDataset·R-SemanticIdentityAdjudicator·R-LiveIdentityBacklog·R-SourceOverlapScarcity·R-RealSourceLoopUnproven 부분진전·완전종결 금지)**→(12) **실** live-derived+human-reviewed gold set 누적·reviewer 합의 실측·sampling 대표성·한국어 캘리브레이션·**live 백로그 운영 가동(운영 DB 배포+실 fetch+주기 스케줄러)**→(13) embedding/LLM/KG **실 병합** adjudicator(MERGE_GATE 충족·런타임 배선)→(14) 실 cross-source 비뉴스/약신호 Event 관측→(8) 실 fetch APPEND→(9) full `docker compose up --build` E2E→(10) 주기 auto-trigger 이월**] + S3 domains/tags 2층  ── 파일 12+19
    ▼
S4  Entity Registry (NER 재사용+앵커 매칭+candidate 자동승격/병합 분리)  ── 파일 17+EVENT_SCHEMA
    ▼
S6  Source Routing 배선 (supervisor llm_propose + 사건유형→role + audit)  ── 파일 11+19
    ▼
S7  Change Detection 발견결합 → S8 Evidence Graph(09 경계 유지) → S9 Agent Debate(13 광고 연결)
    → S10 Authority Discovery → S11 SLM Body Fallback
```

**병행 가능(차단 아님):** DLQ 알림·RBAC·hybrid/nori(08)·L4 기본 sink 전환·46소스 전수 probe(05).
**선행 필수:** **상용화(13)·검색고도화(08)는 Event 토대(S1)+실데이터 이후** — 그 전엔 영업/검색이 모래성.

> **한 줄:** Event 객체부터. 그 위에 Resolution→(Change Detection 동시)→Expansion→domains→Entity→Routing→Evidence→Debate→Authority→SLM. 모든 단계 **비파괴·1517 green·우회 0·투자조언 0·전문저장 0**.

---

## 5. DIRECTION §5 docs 변경 매트릭스 (어디를 무엇으로 바꿨나)

| 문서 | 변경 | 연결 요구 |
|---|---|---|
| `11_LLM_SOURCE_SUPERVISOR…` | "관여 3층 경계(P/G/F)" 신설, audit trace 의무 | 1 |
| `06_SEARCH_API…` | tiered router(무료→유료) + per-event/월 budget guard + fallback chain | 1 |
| `05_DISCOVERY…` | Entity Registry·Authority Discovery 발견 목표(→17 위임) | 1·6 |
| `13_COMMERCIALIZATION…` | **전면 개정** — 구독 4티어 폐기, 트래픽×광고×커뮤니티 | 2 |
| `12_EVENT_CLUSTERING…` | "카드 dedup" → "Event append + timeline + heat" | 3 |
| `04_TARGET_ARCHITECTURE_LAYERS` | L8/L9/L13 새 방향 + Entity/Authority/Event 레이어 위치 | 1·2·3·6 |
| `5_REFERENCE/EVENT_SCHEMA.md` | `Event`·`EventUpdate`·`Entity`·`EvidenceNode` + Comment 확장 | 2·3 |
| `_DECISIONS/2026-06.md` | ADR#14(LLM 수집경계)·#15(구독→광고)·#16(Event 타임라인) | 1·2·3 |
| `_RISK/RISK_REGISTER.md` | 신규 RISK 7건 + 재평가 | 1·2·3 |
| `_CANONICAL/*` | **구현 후에만** 수치 갱신(미구현을 구현됨으로 적지 않음) | — |

---

## 6. UNKNOWN (구현 전 확정 불가 — 정직 표기)

1. SLM Body Fallback의 통신서버 인프라/모델 size·비용 (S11, 미정).
2. 광고 수요측 1차 인벤토리 실판매 형태(self-serve CPL vs 정액) — 콜드스타트 전 미검증(13).
3. Event 병합 해상도 임계 θ(merge_score 가중치) 도메인별 튜닝값(S2).
4. heat 가중치·half-life 실측 캘리브레이션(S2).
5. domains 통제어휘 최종 집합(현 20 제안, 거버넌스 ADR로 확장).
6. GraphRAG 진입 시점(<1000 엔티티 금지 → vector RAG 커버리지 실측 후, 09).

> 권위: 본 인덱스는 ROADMAP 정렬·진입의 단일 지도다. **구현 사실은 항상 `docs/_CANONICAL/*`**, 결정 논리는 `docs/_DECISIONS/2026-06.md`, 위험은 `docs/_RISK/RISK_REGISTER.md`가 권위다.
