# 02 — CURRENT ARCHITECTURE (현재 아키텍처 단일 출처)

> 수치·상태가 다른 문서와 어긋나면 **이 문서를 따른다**. 근거는 코드/산출물.

---

## 1. 두 서브시스템 (병행, 미통합)

| | A. ingestion 엔진 | B. 다운스트림 앱 |
|---|---|---|
| 위치 | `ingestion/` | `backend/` `workers/` `agents/` `frontend/` |
| 역할 | 57소스 수집·정규화·품질·정책 게이트 | raw_events→사건카드 생성·검색·표시 |
| 출력 | EventQueue JSONL → bridge(mirror **또는** backend) | event_cards PG + Milvus + OpenSearch |
| 입력 | 공개 RSS/API/정적HTML/Playwright | `workers/` RSS 3소스 + **A 통합경유** → raw_events PG |
| 패러다임 | deterministic, 신규 설치 0 | 컨테이너 10개, Redis Stream 기반 |
| 연결 | **A→B 배선 PARTIAL**: `ingestion/integration/`(BackendApiRawEventsWriter). **라이브 외부→backend sink→card E2E 관찰**(ap_news 100→event_cards 100 hold). 남은 blocker: 기본 sink mirror·46소스 전수 sweep·LLM급 카드(01/04/05) | — |

## 2. 컨테이너 (10개, docker-compose.dev.yml, project `event-intelligence-dev`)

`ei-backend`(FastAPI) · `ei-frontend`(Next.js) · `ei-postgres` · `ei-redis` ·
`ei-milvus` (+ `ei-milvus-etcd`, `ei-milvus-minio`) · `ei-opensearch` ·
`ei-worker` · `ei-agent-worker`. 인프라 서비스는 `127.0.0.1` 바인딩, backend/frontend만 `0.0.0.0`.
heartbeat 파일 healthcheck(worker/agent-worker, 60s 임계).

## 3. Backend API (FastAPI, `backend/app/api/`)

| Router | 상태 | 비고 |
|---|---|---|
| health | DONE | components.opensearch 포함 |
| events | DONE | list/search(OpenSearch multi_match)/detail; OpenSearch down 시 503 |
| admin | DONE | 10 엔드포인트, raw-events upsert/status/reconcile/requeue; **dev bypass 주의** |
| internal | DONE | /search-similar(Milvus) |
| themes / sectors | **PARTIAL** | 스켈레톤 자료 |
| comments / ai_replies | **PARTIAL** | 미완성 |

## 4. 데이터 스키마 (Postgres, alembic 0001~0006 · ✅ live-PG up/down 검증 2026-06-22)

- **raw_events**: source_type/name, external_id, url, title(≤1024), raw_text(요약만, 본문 저장 금지),
  published_at(UTC), feed metadata JSONB, status(collected→enqueued→processed|failed),
  content_hash UNIQUE, event_card_id FK, requeue_count. (`backend/app/models/raw_event.py`)
- **event_cards**: id/title/summary/theme/sectors(JSONB)/entities(JSONB)/impact_path/evidence/
  confidence_score/status/llm_provider/model_used/created_at + **event_id nullable FK→events**(S1, ADR#16: 카드=Event 스냅샷, NULL=degenerate). (`backend/app/models/event.py`)
- **events / event_updates** (S1 토대, alembic 0004, 2026-06-22): `events`(canonical_title/status/first_seen/last_update/heat/domains·tags·primary_entity_ids JSONB/snapshot_card_id FK→event_cards SET NULL) + `event_updates`(append-only: observed_at/delta_summary/evidence·source_refs·added_domains JSONB/heat_delta, event_id FK→events **RESTRICT**(0006, 감사 보호)). (`backend/app/models/event_timeline.py`)
- **cluster_event_map / event_links / event_identity_map / event_identity_candidate / event_identity_adjudication** (S2a alembic 0005 + 0007 + 0008 + 0009, 2026-06-22~24): `cluster_event_map`(cluster_id PK→event_id 라우팅 단일 진실원천, FK **RESTRICT** 0006) + `event_links`(event↔event, status possible/confirmed/rejected/merged CheckConstraint, FK **RESTRICT** 0006, 약신호 자동병합 금지) + **`event_identity_map`(identity_key PK→event_id, FK RESTRICT, alembic 0007, ADR#40)** — cluster_id 와 분리된 cross-batch Event 동일성 단일 출처(publishable strong anchor=canonical_url/official_id→event APPEND 병합) + **`event_identity_candidate`(candidate_key PK→event_id, FK RESTRICT, alembic 0008, ADR#41)** — 공유 anchor 없는 같은-사건 후보(normalized token-set+date fingerprint)를 `event_links(possible)` 로 **표면화만**(병합 아님·중복 count 미감소·false-merge 0) + **`event_identity_adjudication`(link_id PK→event_links FK RESTRICT, status/score, alembic 0009, ADR#42)** — possible-link 의 **deterministic shadow 판정**(likely_same/ambiguous/likely_different/insufficient; `semantic_identity_adjudicator.py`·자동 병합 0·API 미노출; 실 병합은 R-SemanticIdentityAdjudicator/R-IdentityEvalDataset 이월). (`backend/app/models/event_resolution.py`) — **entities 는 S4~ 미생성.**
- **alembic 0006 (FK RESTRICT, S2e live-PG, 2026-06-22)**: event_updates/cluster_event_map/event_links FK CASCADE→RESTRICT(ADR#20 DB 레벨 감사 보호 — 의존 행 있는 Event 삭제 차단). 실 Postgres up/down 검증. (`backend/alembic/versions/0006_fk_restrict_audit.py`)
- **Event Resolution 서비스 계층** (S2c/S2d/S2e, 2026-06-22): `event_resolver.resolve_routing`(순수 라우팅 APPEND/HOLD/CREATE + clique 게이트, ingestion 비의존) + `event_timeline_service`(CRUD 영속: create_event/append_update append-only/get_event/set_snapshot 쌍방향강제/map_cluster·get_cluster_event/hold_link possible/apply_routing 단일 원자 tx+동시 CREATE rollback — ADR#19) + **`event_resolution_pipeline`(S2e: 실 cross_source_dedup→resolver→apply_routing 배선, ingestion 비의존 duck-typed)**. 통합 E2E **✅ live-PG 검증**(`test_event_resolution_live_pg` 14, ADR#21): 실 Postgres 에서 CREATE/APPEND/HOLD·멱등·FSD(실 LEAST/GREATEST)·sanitize(실 JSONB)·**2-세션 동시 CREATE orphan 0**·**FK RESTRICT 삭제 차단**. (in-memory fake E2E 11 은 보조 유지.) **삭제 정책 ADR#20**(app no-delete + status + FK RESTRICT). **C live wiring(ADR#22, 2026-06-22):** `event_ingest_pipeline`(수집 후보 records→cross_source_dedup→candidate_for 매퍼→resolver→events/event_updates; flag `EVENT_RESOLUTION_ENABLED` 기본 off; 후보 단위 격리; 본문/PII 차단) + `run_production_orchestration(event_resolution_sink=)` 주입 seam(db_writer 패턴, cross_source_dedup 직후, 격리). event_cards **무변경 병행**. live-PG 입증(candidate→CREATE→APPEND·flag off 영속0·실DB 후보격리). **D-1 운영 결선(ADR#23, 2026-06-22):** `backend/app/tools/run_event_orchestration.py` = **backend-side composition root**(backend→ingestion 허용 방향으로 `run_production_orchestration.main` 재사용 + `make_orchestration_event_sink` 주입; **전용 NullPool async engine 생명주기를 backend 가 소유**; `--event-resolution`/`EVENT_RESOLUTION_ENABLED` 게이트, off=byte-identical). ingestion `main(argv, *, event_resolution_sink=None)` 으로 seam 을 main 레벨까지 연장(Callable 만 받음 — **ingestion→backend import 0, decoupling 불변식 보존**). live-PG 로 실 sink → Event CREATE→APPEND 입증 → 운영 runner 가 Event 영속 *능력* 확보. **D-2a Event 타임라인 read API(ADR#24, 2026-06-23):** `backend/app/api/events.py` 에 `/api/events/timeline`(list[Event], `id IN cluster_event_map` 매핑분만)·`/api/events/timeline/{id}`(event+updates) additive endpoint(`EVENT_TIMELINE_API_ENABLED` flag 기본 off→404, `/{event_id}` 보다 먼저 선언=라우트 우선순위, held degenerate 제외, read-only·결정론). `event_timeline_service.list_events` + `EventTimelineResponse` 신규. 레거시 `/api/events`(event_cards) 무변경. **D-2b frontend 렌더(ADR#25, 2026-06-23):** `frontend/src/app/events/timeline/{page,[eventId]/page}.tsx`(Next.js SSR) + `components/{EventTimelineCard,EventTimelineList,EventUpdateItem}.tsx` + `lib/api/{types,client}.ts`(Event/EventUpdate/EventTimelineResponse + listEventTimeline/getEventTimeline) + nav 1줄. 안전 렌더: evidence url `isSafeHttpUrl`(http/https)+`rel="noopener noreferrer nofollow"`, allowlist 6키만, source_refs/heat/entity_ids 미렌더; flag off(404)→목록 graceful 빈상태·상세 notFound. 기존 `/events`(event_cards) **무변경**(회귀 0; tsc 0·node:test 12·next lint 0). web read path 에 LLM/network 0. **렌더 *능력* 확보 — 실제 사용자 노출은 `EVENT_TIMELINE_API_ENABLED=true` + D-2c 데이터 결선(매핑된 실 Event) 전제(현재 빈 화면). delta_summary 가 현재 `"{confidence}:{reason}"` 디버그 라벨이라 자연어 서술화는 상류 잔여(R-EventTimelineRenderHardening).** **테스트 provider 격리 + 공개 read 스키마 분리(ADR#26, 2026-06-23):** `backend/tests/conftest.py` autouse fixture 가 전역 `settings.LLM_PROVIDER`/`EMBEDDING_PROVIDER` 를 mock 으로 pin(+ embedding 캐시 reset) → 기본/테스트 경로 network 0·`.env` 비의존(pre-existing embedding 싱글톤 결합 해소; dirty-env 시뮬 통과). `PublicEvent`(primary_entity_ids/snapshot_card_id=내부 FK 제외)·`PublicEventUpdate`(source_refs 제외)·`PublicEventTimelineResponse` 신규로 `/timeline`·`/timeline/{id}` 공개 응답에서 **내부 식별자 구조적 제외**(allowlist 별도 스키마, API 경계 변환=서비스 무변경; 비공개 `EventTimelineResponse` 제거; heat=제품신호 유지) + 비-404 에러를 페이지·전역 `error.tsx` 모두 raw 없이 일반 안내 → R-EventTimelineRenderHardening ①③ 종결. conftest 는 embedding+LLM 캐시 대칭 reset. **D-2c 데모(ADR#27, 2026-06-23):** `backend/app/tools/seed_event_timeline.py`(합성 Event 4건×update 3~4, 자연어 delta_summary·example.com evidence·멱등·오프라인, service 직접 영속 create_event→map_cluster→append_update) + `backend/app/tools/db_target.py`(`assert_safe_write_target` **2중 fail-closed**=APP_ENV allowlist(dev/test)+dbname prod-마커 교차검증 + `target_db_label` 자격증명 제외; seed/runner 단일 출처) + compose `EVENT_TIMELINE_API_ENABLED` 데모 on. **실증(로컬·비-컨테이너):** live-PG(event_intel_test) seed→list_events·get_public_event + uvicorn `/api/events/timeline`(내부ID/source_refs 미노출) + **Next.js `next dev`+Playwright 브라우저 스크린샷**(목록 4카드·상세 4 update+evidence 렌더) + `/events` event_cards graceful 회귀 → **제품 북극성("웹에서 Event 타임라인을 본다")의 첫 실거동 가시화(로컬; full `docker compose up --build` E2E 는 잔여)**. R-EventSinkDbTarget **종결**. **REAL_SOURCE_LOOP_AUDIT(ADR#28, 2026-06-23, 3-agent 전수):** 절대 혼동 금지 — **경로 A**(수집→raw_events; raw_events→event_cards)는 **실 웹 데이터 PROVEN**(live probe·`_prod_orch.log` 653 real records·ap_news 100→event_cards[전량 hold→공개 0]); **경로 B**(수집→cross_source_dedup→event_ingest→resolution→event_timeline→API→frontend)는 **코드 배선 완료이나 실데이터 0회**(전 입증=fake session/하드코딩 synthetic record/example.com seed; 최신 prod-validation 은 mirror sink+event_resolution 미주입; EVENT_RESOLUTION_ENABLED 실가동 0; delta_summary 실경로=디버그 라벨). **D-2c 합성 데모 = 화면 렌더 능력 입증이지 실 웹 인텔리전스 파이프라인 검증 아님** → R-RealSourceLoopUnproven 신규. **실 소스 production-validation(ADR#29, 2026-06-23):** keyless 뉴스 10소스 live fetch(411 real records)→cross_source_dedup 2 클러스터→resolver **CREATE 2 + HOLD 3**(약신호 corroborator 보류=R-FalseMerge 실데이터 작동)→`/api/events/timeline` 2 실 Event→`/events/timeline` 브라우저 렌더(yna `코스피 서킷브레이커`·매경 `대우건설 중동재건 TF`). **경로 B 가 실 웹 데이터로 Event 생성·화면 노출 최초 입증 → R-RealSourceLoopUnproven MEDIUM→LOW 부분종결.** 발견: Event 생성은 cross-source 겹침 필요(작은 fetch=클러스터 0), CREATE genesis=updates 0(evidence APPEND 에서만), delta_summary 실경로=디버그 라벨. **delta_summary 자연어화(ADR#30, 2026-06-23):** `build_delta_summary`(deterministic·LLM 0, member_count=distinct 정합)가 `event_ingest_pipeline.candidate_from_cluster` 에 적용 — 실경로 디버그 라벨 제거(live-PG 강신호 CREATE→APPEND 자연어 update 확인) → ② 코드 자연어화 done(①③ ADR#26 종결). **CREATE genesis update(ADR#31, 2026-06-23):** `apply_routing` clean-win CREATE 에 genesis update 1행(생성 근거) 추가 — "CREATE는 update 0" 불변식 의도적 개정(마이그레이션 0; event_updates="append-only 관측 이력") → CREATE-only Event 빈 상세 해소, 실 파이프라인 CREATE→`/events/timeline/{id}` 화면에 genesis 자연어+evidence 렌더 **1회 관측(Playwright)** → **R-EventTimelineRenderHardening 완전 종결(①②③). **비뉴스 타입 라우팅·fidelity 검증(ADR#32, 2026-06-23):** 결정론(실 파이프라인 5 시나리오)+실 fetch(probe). resolver 는 source_type 무관(signal 강도 라우팅) — official+news→CREATE(evidence official+article)·structured 단일/2종→0 Event(singleton=signal-only 정상)·community+news→CREATE 저신뢰+community HELD·**pure-community 강신호→발행 Event(gap)**. 실 fetch federal_register/sec_edgar/hacker_news LIVE(타입 분류 확인). community/search never_direct_publish 미강제 → **R-SourceTypeFidelityGate 신규(MEDIUM).** **source-type publish gate(ADR#33, 2026-06-23):** `resolve_routing` 에 `member_source_types`+신규 `ACTION_WITHHELD` — pure community/search/structured 단독 cross-source 직접 발행 차단(미매핑 CREATE 인데 publishable=official/article 0이면 WITHHELD·미영속·미노출·멱등; DB 미접근). S5/S6/S7 withheld 재현(투자조언 경계 S6 포함)·매핑 APPEND(community corroborator)는 미적용. 단위 12+ingest 3+live-PG 2. 마이그레이션/schema/UI 0. → **R-SourceTypeFidelityGate MEDIUM→LOW 부분종결**(핵심 발행 차단 done; **primary-authority 미구현 잔여** — held-publishable→community-primary). 코드 4(resolver/resolution_pipeline/timeline/ingest). **primary-authority(ADR#34, 2026-06-23):** `candidate_from_cluster` 가 mixed cluster 의 Event 대표(title/도메인/관측시각/delta_summary kind/primary evidence)를 최고 authority source(official5>article4>signal3>search2>community1>미지0)로 선정 — **community/market 이 Event 대표가 되는 것 차단**(단위 4+live-PG 1; tie=입력순 회귀 0). **R-SourceTypeFidelityGate LOW 부분종결 유지(완전 종결 아님 — held-publishable DB 이중등장·fail-open 잔여, adversarial). **held-dedup+fail-closed(ADR#35, 2026-06-23):** `apply_routing` 이 대표 멤버(`candidate.primary_member_key`)를 held 에서 제외(대표 record 의 held degenerate **DB 이중등장 제거**; 키 정확 일치·corroborator 유지) + `resolve_routing` **fail-closed**(source_type 빈/미지/누락→WITHHELD; `_cand` 합성 helper 도 명시 source_type). **R-SourceTypeFidelityGate LOW 부분종결 유지(완전 종결 아님 — 약신호-primary 시맨틱(authority>signal-strength) 정당화 미완, R-FalseMerge 교차, adversarial). **weak-primary core-policy(ADR#36, 2026-06-23):** 강신호(duplicate) cluster 는 **강신호 core**(distinct−weak_only)에서만 primary 선정 + gate(`core_source_types`)=core source_type → weak_only publishable 로 발행 안 함·weak_only 대표 불가(후보 A+C; B 기각). 약신호(possible_duplicate)는 전체 동등 저신뢰(ADR#29 흐름 보존). **R-SourceTypeFidelityGate LOW 부분종결 유지(완전 종결 OVERCLAIM — 약신호 weak-primary 미해소·members[0]→core→gate fragility(입력순서 의존) 잔여, R-FalseMerge 교차).** **약신호 정책·입력순서 불변(ADR#37, 2026-06-23):** possible_duplicate **동질 publishable gate**(`_homogeneous_publishable`: news+news/official+official 만 저신뢰 발행·혼합(official+news)/비-publishable 섞임 WITHHELD → authority 상향 weak-primary 차단; 동일 type 은 delta "추정됩니다" 저신뢰 표시) + **입력순서 불변 core**(`primary_root`=members[0] 대신 최대 강성분→동률 publishable 우선·`members` 키 정렬 → fragility·R-FalseMerge 약신호 cluster_id split 해소; `_PUBLISHABLE_RECORD_TYPES` drift 계약). adversarial **2-pass JUSTIFIED** → **R-SourceTypeFidelityGate 완전 종결(RISK_CLOSED 이관).** R-FalseMerge 는 held 승격 정책(gap①) 잔여로 open 유지. **held 승격 candidate B(ADR#38, 2026-06-23):** held member(degenerate+possible link)가 다른 cluster_id 로 강신호 재등장 시 — `find_held_parents`(degenerate canonical_title==member_key→possible→매핑 parent) + `titles_similar`(약신호 결합 동일 기준) 로 **재등장 cluster 제목 ↔ parent 제목 같은 사건일 때만** parent 로 라우팅(APPEND/HOLD)+cluster_id→parent 매핑(멱등), 불일치면 독립 CREATE(false-merge 방어). 인메모리 7(official/community/market match→parent 연결·mismatch→CREATE/WITHHELD). **R-FalseMerge open 유지(gap① 부분종결·완전 종결 보류 — live-PG held 승격 미실증[Docker 대기] + cross-batch event identity[신규 corroborator 분열·비-held 멤버 재등장] 잔여, adversarial OVERCLAIM). **R-FalseMerge 완전 종결(ADR#39, 2026-06-24):** Docker+`docker compose up -d postgres`→event_intel_test alembic head→**live-PG 30/30 green(held 승격 3 실증)**→**R-FalseMerge CLOSED**(OVER-merge 한정, RISK_CLOSED 이관, adversarial JUSTIFIED — 직전 2턴 OVERCLAIM 잡은 critic). cross-batch UNDER-merge(같은 사건 배치별 분열→결과적 중복)=**R-CrossBatchEventIdentity**(MEDIUM, RAG/KG 이전 필수 gate) 신규 분리·catalog(domain)→official_record 누수=**R-SourceCatalogFidelity**(MEDIUM, 미수정·ADR 필요) 신규. RAG/KG/Entity/LLM routing/agent=대부분 NOT BUILT/PARTIAL·mock-default(`docs/5_REFERENCE/RAG_KG_AGENT_READINESS.md`). **substrate hardening(ADR#40, 2026-06-24):** ① **deterministic Event Identity Layer** — 신규 `event_identity_map(identity_key→event_id, alembic 0007, FK RESTRICT)` 가 cluster_id 와 분리된 cross-batch 동일성 단일 출처. publishable 강신호 core 멤버의 canonical_url/official_id record_key 를 anchor 로 claim(CREATE/APPEND), 미매핑 cluster 가 같은 anchor 면 기존 Event APPEND(분열 방지). **R-CrossBatchEventIdentity 부분종결**(shared-anchor 닫힘·live-PG; semantic-only 독립보도 분열+fragment 오APPEND 잔여 open). ② **catalog source-type fidelity** — `_record_type_for` 가 `source_content_type` 단일 출처로 catalog 6종→catalog_metadata 비-publishable("catalog" source_type·fail-closed) → domain→official_record 누수 차단. **R-SourceCatalogFidelity CLOSED**(adversarial CLOSE-JUSTIFIED). 제품 계약: raw source≠public(intelligence unit substrate).**** **잔여:** cross-batch semantic event identity(R-CrossBatchEventIdentity 부분종결)·실 fetch APPEND·**실 cross-source 비뉴스 Event**·주기 auto-trigger(Phase 2)·운영 DB 0006 배포·full `docker compose up --build` E2E·event_cards.event_id 자동연결·약신호 cluster_id 안정키·3엔진 색인 정합·heat 4신호(S2.5)·merge_score entity/domain(S4)·LLM 보조 레이어(경계만 개방).
- **Semantic identity stack + provider acquisition (ADR#41~#86, 2026-06-25~29)** — cross-batch 동일성 4단계 + 평가/gold gate(전 경로 결정론·자동 병합 0): ① **anchor 병합**(`event_identity_map` 0007, ADR#40) ② **fingerprint 후보 LINK**(`event_identity_candidate` 0008 + `cross_source_dedup.semantic_identity_fingerprint` token-set+date→`event_resolution_pipeline` 가 미매핑 cluster 를 `event_links(possible)` 로 표면화, ADR#41·병합 아님) ③ **shadow 판정**(`event_identity_adjudication` 0009 + `semantic_identity_adjudicator`(feature→status likely_same/ambiguous/likely_different/insufficient·source role guard·API 미노출), ADR#42·병합 아님; **운영 배선 ADR#48**: `ingest_records_to_events(adjudicate_semantic=)` 가 배치 뒤 flag(`EVENT_SEMANTIC_ADJUDICATION_ENABLED` off-by-default) on 이면 ③를 자동 실행해 백로그 누적·자동 병합 0·멱등; **incremental ADR#49**: `adjudicate_semantic_links(only_unadjudicated=,limit=)`·`if not clusters` 제거→no-cluster backfill·신규 `backfill_semantic_adjudications.py`[dry-run·bounded]·ambiguity 정확성 불변[cand_targets 필터 전 산출]) ④ **실 병합**(embedding/LLM/KG, **미구현**). **④ 전 선결 = identity eval gate**(`identity_eval_dataset`(EvalPair·`evaluate_adjudicator` precision/FPR/recall·MERGE_GATE)+`fixtures/identity_eval_pairs.jsonl`(22 진단 pair)+`export_identity_eval_pairs`(adjudication→human-labeling 워크시트 소비처), ADR#43; 현 deterministic adjudicator **precision 0.57·merge gate 미달→자동 병합 OFF**) + **human-labeled gold workflow**(`identity_human_labeling`(GoldPair provenance·`promote_worksheet_to_gold` self-label 금지·gold-only metric·`evaluate_gold_merge_readiness` live gold만·표본 floor·auto-merge OFF)+`fixtures/identity_gold_pairs.sample.jsonl`(시연 샘플), ADR#44; sample gold **precision 0.6·KO 0.5·merge gate 미달**) + **reviewer agreement protocol**(`resolve_gold_from_reviewers`(다중 reviewer 합의=gold·**conflict 자동 gold 금지**·single=insufficient·`_validate_adjudication` LLM-as-judge 차단)·sampling bucket(12)·`estimate_sample_floor_*`(precision 0.98→189·FPR 0.01→381)+`fixtures/identity_reviewer_labels.sample.jsonl`, ADR#45; sample reviewer agreement 0.71·resolved gold precision 0.5·merge gate 미달) + **live-derived labeling packet**(`build_labeling_packet`(live 워크시트→15 candidate bucket 샘플링·동일 pair distinct ≥2명 배정·**predicted_status/score/reason 차폐**=bias 0)·`labeler_facing_view`(bucket+판정 제거)·`summarize_packet_sampling`(deficit/oversample·표본 floor 대조·selection=결정론 정렬 cut-off)·`adjudication_queue_from_resolved`(conflict→human-only)+`export_identity_eval_pairs._to_eval_source_type`(signal→market enum 정규화)+`fixtures/identity_labeling_candidates.sample.jsonl`, ADR#46; selected 16·packet item 32·gold_resolved 0[라벨 전]·adversarial HIGH 1 수정) + **live-derived packet pilot**(`build_live_identity_labeling_packet`(live-PG adjudication 백로그→packet·`collect_live_identity_candidates`[backlog/exclusion report]·`generate_live_packet_report`[event count before/after·auto_merge_enabled False]·read-only DB read·write 0) + 옵션 D `SELECTION_BUCKET_HASH`(sha256(pair_id) 정렬·정렬 편향 완화·재현·ADR#46 기본 하위호환), ADR#47; live-PG 실 파이프라인 유래 eligible 1·live_selected 1·운영 자동 백로그 0[단계 ③ 미배선·운영 DB 미마이그레이션 → exclusion `semantic_link_without_adjudication` 로 표면화]) + **stage③ operational wiring + migration readiness**(ADR#48: `ingest_records_to_events` 가 배치 뒤 flag-gated 로 ③ 자동 실행→adjudication 백로그 자동 누적; `identity_backlog_readiness`[read-only] 가 운영 DB 0003 vs head 0009 **6 revision 뒤·non-destructive** 정량화·운영 upgrade 미적용[배포 행위]) + **incremental / no-cluster backfill + 배포 runbook**(ADR#49: `adjudicate_semantic_links(only_unadjudicated=,limit=)` 로 비싼 per-link work 를 미판정 link 한정[O(N) 완화·ambiguity 정확성 회귀 가드]·no-cluster 배치도 backfill·`backfill_semantic_adjudications.py`[dry-run·bounded]·운영 DB 배포 runbook[`15_IMPLEMENTATION_ROADMAP §`]; live-PG ingest→③→packet eligible 1·live_selected 1·**수동 호출 0**·exclusion 1→0·Event 불변·멱등) + **backfill operation hardening + deploy checklist**(ADR#50: `_semantic_links`/`adjudicate_semantic_links(after_link_id=)` 가 keyset(WHERE id>cursor·NOT IN adjudication·LIMIT)을 SQL 로 push→bounded run 의 O(전체) 적재 완화[ambiguity 는 page candidate 한정 GROUP BY `_candidate_target_counts` 로 link별 status 동작 보존; **cursor=UUIDv4 byte 순서·시간순 아님·백로그 진행/완전성 보장은 only_unadjudicated**·`full_scan` 플래그로 전수 경로 표면화]·`backfill_semantic_adjudications` 운영 CLI(`python -m …`·`--limit/--after-link-id/--dry-run`·safe-target 가드·next_cursor/full_scan/idempotent_persist report·**lock 과대주장 0**[PK upsert 중복행 0=데이터 안전·lock 아님])·`build_operational_deploy_checklist`(0003→head 명령 문자열·backup_required·**executed=False**)·readiness read-only CLI; 주기 가동은 신규 scheduler 발명 불필요 — 기존 `run_recovery_scheduler --once` 관용구 **재사용 가능**[설계·**미배선**·운영 DB migration 후 게이트]·live-PG keyset cursor/교차 backfill 중복-persist 멱등/실 deploy checklist 검증) + **backfill scheduler operationalization**(ADR#51, 2026-06-25: `backfill_preflight`/`run_backfill_with_preflight`(ready_for_stage3 **hard gate**[0003 dry-run 포함 차단·NOT IN 쿼리 크래시 방지]+flag persist gate[off→persist만·dry-run 허용·allow_flag_off 우회])·`decide_exit_code`(0/1/2/3 scheduler/cron 결정론)·`cursor_mode='created_at'`(시간순 복합 cursor·`or_(created_at>cur, and_(created_at==cur, id>after_link_id))` 컬럼 비교[행값 tuple_ uuid>varchar 교체·동치]·`next_created_at`·**배치 간만 정확·intra-txn tie→id·created_at 인덱스 없음**)·`workers/tools/run_semantic_backfill_scheduler.py`(recovery-scheduler 관용구·gated·**dry-run default·docker 미배선·미가동**); live-PG created_at 시간순/동일 created_at tie-break/0003 테이블 DROP block/preflight gate·Event 불변·adversarial **기술계약 VALID**) + **production scheduler activation readiness**(ADR#52, 2026-06-25: scheduler 가 docker `semantic-backfill-scheduler` 서비스로 배선 — **`profiles:["backfill"]` 기본 미기동·dry-run default[command --persist 부재]·preflight gated·단일 instance[replicas 미설정·restart "no"]**·backend 이미지 재사용[worker 이미지는 services/tools/models 미COPY→import 불가]·entrypoint override[entrypoint.sh 의 alembic+uvicorn 대신 scheduler 모듈]; 옵션 C[created_at index]·D[advisory lock] 근거와 함께 **DEFER**[backlog 0·prod 0003·test churn 회피·DDL(`ix_event_links_created_at_id`)·single-instance 요건 runbook 문서화]·**코드 변경 0**[docker-compose.dev.yml+compose 일관성 테스트 5]; **실가동 0**[profile 비활성·운영 DB 0003·flag off]·docker build/up 미검증[정적 일관성 테스트만·scheduler --help 명령 검증]) + **production scheduler activation VALIDATION**(ADR#53, 2026-06-25: ADR#52 의 "정적만" 한계를 실 docker 로 해소 — `docker compose --profile backfill build` 성공·`run --once` **3경로 실측**[DB down→graceful **exit 2**·미마이그레이션→`BLOCKED readiness`→**exit 1**·dev DB head→`dry_run=True event_count 0->0 auto_merge=False`→**exit 0**]·profile 격리 config 실증; **🔴 정적 config/build 가 못 잡은 런타임 버그 발견·수정** — `backend/Dockerfile` 이 `ingestion/` 미COPY→adjudicator 의 `ingestion.orchestration.cross_source_dedup` 전이 import `ModuleNotFoundError`→`COPY ingestion/` 추가+회귀 테스트 2; **live-PG `test_event_resolution_live_pg` 91 passed**[ADR#52 미실행→`event_intel_test` head 재실행]; **정직**: run#3 `event_count 0->0` 은 빈 dev DB 라 read-only 미입증[입증=코드 `_persist_adjudication` adjudication-only+live-PG]·live-PG 91=DB orchestration 정확성[scheduler 주기 가동 아님]·dev event_intel head 마이그레이션[운영 DB 아님·additive]; C/D 계속 DEFER[backlog 0·test churn 실측]; adversarial **CONDITIONAL VALID**[코드·안전계약 VALID·HIGH-1 문서 미반영은 ledger #53·PROJECT_STATUS 정정 해소]; **scheduler 실가동 0**[one-shot dry-run 만]·production 백로그 0) + **production activation preflight + real-source smoke**(ADR#54, 2026-06-25: `production_activation_preflight`[read-only·DDL/upgrade/persist 0]가 readiness+flag+safe_target+`db_target.classify_write_target`[named 환경 분류 dev/test/staging/production/unknown + APP_ENV↔URL mismatch]를 **17필드 한 report** 로 묶어 `can_persist`[persist ∧ ready ∧ flag ∧ safe_target ∧ ¬destructive ∧ consistent]·block_reasons·next_required_actions 산출·**dev event_intel safe-target no-op[MEDIUM-1]을 warning 으로 표면화**·DATABASE_URL fingerprint 만·`--allow-non-dev-db` 는 readiness/flag 못 우회; `real_source_identity_smoke`[기본 offline fake·network 0·DB 0·결정론]가 fetch→cluster→candidate 단계별 실패 분류[body_missing/no_cluster_singleton/non_publishable_role/no_semantic_fingerprint]+source_role_distribution 진단·DB 단계 offline None·`--live-db`[safe-target gated·test/dev]는 opt-in·**real_fetch 0[기본 fake]**·community anchor 금지·no_auto_merge 불변; C/D 계속 DEFER[persist 시 warning]·E[LLM/Agent] docs 진입 9조건만[`RAG_KG_AGENT_READINESS §6b`·No-Go]; **점검/진단≠가동**·운영 DB 무변경·production 백로그 0 불변) + **real-source live-db smoke**(ADR#55, 2026-06-25: `real_source_identity_smoke` 에 **live_network**[key-free official JSON API `federal_register`·bounded·opt-in·CI 아님·본문 미저장] + **live_db**[disposable test/dev DB·safe-target gated]를 추가·`real_source_smoke_report`[순수]가 §4 activation report + **source quality matrix**[source별 body/canonical/published/identity readiness] + agent readiness 9조건[단일 출처]을 조립; 실 federal_register fetch **5 official records → event_intel_test(head 0009) ingest: created 1 + identity_link 1[held member]·adjudications 0·packet_eligible 0·no_auto_merge·event_count 4→6**; **정직 분해**: adjudications 0 = held-member link reason 이 `new_event_low_confidence`(≠`semantic_cross_batch_candidate`)·실 Event 형성되나 cross-batch adjudication substrate 는 동일사건 다중소스/시계열 fingerprint 중첩 필요[source scarcity]; community/market/catalog `guard_only`[anchor 금지]; live_network=opt-in tool/report·live_db=disposable test DB[production 아님]·실 cross-source 비뉴스 Event/실 adjudication backlog/reviewer·gold·merge 잔여) + **time-series replay smoke**(ADR#56, 2026-06-25: ADR#55 단일소스 adjudications 0 원인을 정밀 분해[distinct document=distinct URL=distinct fingerprint→같은-사건·다른-URL 후보 구조적 불가=source scarcity]하고 `run_time_series_replay_smoke`[safe-target gated·artificial 2배치·같은 사건·다른 URL·교차배치]로 **cross-batch adjudication substrate 가 닫힘을 실데이터 입증**[event_intel_test: semantic_cross_batch_candidate 1·adjudications 1(likely_same)·event_count 0→2·자동병합 0·live-PG 잠금]·`_link_reason_distribution`/`_adjudication_status_distribution`(read-only)+`classify_adjudication_block_reason`(§5: db_not_reached/none/semantic_link_without_adjudication/non_publishable_role/no_fingerprint_overlap/no_cross_batch_overlap)로 "왜 0/N 인가"를 source/data/fingerprint/cross-batch 단계 귀속; **단 artificial_replay=True[실 source 아님]·실 동일사건 다중소스/시계열 fetch 잔여**) + **source overlap discovery**(ADR#57, 2026-06-25: 신규 `source_overlap_discovery.py`[write-free·no-DB·no-merge]가 record 다입도 overlap 을 `fingerprint_overlap`[정확 token-set 일치→deterministic 검출] vs `near_match_below_fingerprint`[paraphrase→deterministic 사각지대·adjudicator/embedding/LLM 영역·gated]로 분해·`possible_same_event_pairs`·`overlap_potential_matrix`·`block_reasons`; GDELT bounded fetch[key-free·opt-in·transport 주입 결정론·실패 분류]; `build_agent_orchestration_schema`[§9·LLM 호출 0·`no_merge_without_gate`·`no_public_intelligence_unit`]; `real_source_smoke_report` §8 overlap_potential/utility 추가; **실 GDELT fetch 시도→429[R-Gdelt429]→captured fixture fallback**; fixture 실측 possible_same_event 6=fingerprint 1+near 5[deterministic 1/6 만 검출·5/6 adjudicator-zone]·**R-SourceOverlapScarcity 신규**[key-free 실 cross-source overlap 구조적 희소]; 실 overlap 미관측·합성≠실 source) + **real overlap acquisition strategy**(ADR#58, 2026-06-25: ADR#57 GDELT 429 근인=**backend 도구의 기존 governance 우회 raw httpx** 규명·시정 — **(A)** `gdelt_provider_status`[read-only preflight·network 0·write 0·`HostRateGate`(host floor·shared host_rate_gate.json)+`rate_limit_policy`(gdelt min 60s·cooldown 900s·retry 1)+`in_cooldown` honor]+`fetch_gdelt_overlap_records` short-circuit[provider_status≠ok→network 미시도·우회 재발 차단·no tight retry(RATE_LIMITED 는 retry_on 밖)]; **(B)** key-free RSS 함대 governed 실 fetch — `parse_rss_items`[stdlib ET·RSS/Atom·RFC822→ISO·본문 미저장]·`fetch_rss_overlap_records`[`_SERVICE_CONFIGS` endpoint(auth=none 만)·shared host gate 참여·bounded·failure 분류·transport 주입]; **실측: bbc/aljazeera/the_verge/techcrunch 55 record(429 0·canonical 55)→cross-source overlap 0(`no_title_overlap`)·same-beat 30 record 도 0** = governance 검증(RSS 429 안 남·GDELT 우회와 대조)+**실 overlap 구조적 희소 확증**; **(C)** `build_acquisition_plan`[source_pair/topic/time-window plan·expected_overlap_utility·LLM 0·no_merge_without_gate]; **(D·핵심)** `discover_overlap` near_match_pairs 캡처+`build_near_match_reviewer_candidates`[paraphrase 사각지대→reviewer/gold worksheet·`EvalPair`/`build_labeling_packet` 스키마 재사용·label=unlabeled·**predicted_status 미포함**·publishable×publishable 만·`no_merge_without_gold`·병합 0]; `assemble_acquisition_report`[§4 필드]·source quality matrix §9[overlap_acquisition_utility/title_paraphrase_risk/cross_source_pairability/provider_accessibility/rate_limit_risk/robots_tos_status·하드 근거만]; 다음 게이트=**targeted same-event acquisition**[RSS 고정 feed=query 불가·GDELT=query 가능하나 429]→detection 레이어[embedding/LLM adjudicator+gold]; 실 cross-source overlap 미관측·운영 DB·gold/merge 잔여). + **near-match reviewer/gold queue**(ADR#59, 2026-06-25: 신규 `near_match_reviewer_queue.py`[오케스트레이터·재구현 0]가 ADR#58 통로를 **운영 큐로 물질화** — paraphrase near-match[deterministic 사각지대]+hard negative 를 기존 `build_labeling_packet`/`validate_labeling_packet`/`resolve_gold_from_reviewers` 머신으로: `build_near_match_reviewer_queue`[near positive+hard negative→검증된 packet·predicted_status 미포함·`labeler_facing_view` bias 0·LLM 0]·`build_gold_seed_report`[queue/near/hard/fingerprint count·bucket·source_role·gold_ready(실 gold 있을 때만)·merge_allowed False]·`resolve_queue_gold`[gold/conflict adjudication queue/agreement connector]·`build_reviewer_queue_acquisition_linkage`[source_pair별 near_match_yield/reviewer/gold/hard_negative value→목적 기반 수집]·agent schema 보강[near_match_queue_priority·`EMBEDDING_LLM_ADJUDICATOR_INTERFACE` **No-Go**·`semantic_score` seam]; `source_overlap_discovery` 에 hard_negative_pairs 밴드[publishable·same-date·cross-URL·jaccard [0.2,near) different-event lean] 순수 가산[ADR#57/58 수치 불변]; 병합 0·LLM 0·dataset_source provenance 봉인[synthetic≠live 표본·adversarial LOW-1 정정]; adversarial PROCEED[HIGH 0·MEDIUM 0·LOW 2 정정]·code-review 0; **실 reviewer/gold 0**[큐는 substrate]·detection 레이어 미구현). + **targeted same-event acquisition → operating readiness**(ADR#60, 2026-06-25: 신규 `targeted_same_event_acquisition.py`[재구현 0]가 ADR#59 큐[captured fixture 수준]를 **targeted acquisition**[source-pair·topic·time-window]과 연결 — `build_provider_capability_matrix`[GDELT 429·RSS topic 불가→실 targeted near-match yield 낮음 정직]·`run_targeted_acquisition`[주입 records>governed fetch>fixture→`discover_overlap`→queue·실 0후보면 `block_reason` 정직·`_SYNTHETIC_FIXTURE_MARKER` 로 synthetic↔live 봉인 **코드 강제**]·`build_targeted_acquisition_report`[§4: acquisition_run_id·candidate/near/hard/fingerprint·reviewer_packet_exportable·gold_ready·dataset_source·honesty_boundary]·`build_reviewer_operating_checklist`[hidden_prediction/raw_body absent **구조적 verify**·allowed_labels·merge_policy=prohibited until MERGE_GATE]·`simulate_gold_calibration`[synthetic 5경로 unanimous/conflict/single/adjudicated·**production_gold_count=0**]·`build_targeted_acquisition_linkage`[expected near/hard/gold yield·reviewer_load·provider capability]·CLI; adversarial **PROCEED-WITH-FIXES**[production_gold=0·synthetic≠live·community anchor·near/hard/fp 산술 VALID·MEDIUM-1/2→코드 강제+음성 테스트 7]·code-review[**host_gate→gdelt 미전달 ADR#57 우회 회귀 정정**·candidate_count 분리·fixture topic 라벨·near_jaccard 死파라미터 제거]; **실 targeted near-match 후보·reviewer/gold 0**[fixture=경로 입증·운영 readiness≠실 gold·embedding/LLM No-Go 유지]). + **query-capable provider acquisition readiness gate**(ADR#61, 2026-06-25: 신규 `provider_readiness.py`[ADR#60 frozen·단방향 import·재구현 0]가 ADR#60 "다음 게이트=query-capable provider" 를 운영 readiness gate 로 물질화 — `build_provider_readiness_report` 가 query-capable provider 를 `key_free_query`[gdelt/sec_edgar/federal_register]·`key_required_query`[newsapi/gnews/guardian/nyt/naver_news_search/serper/tavily/exa]·`key_free_non_query`[rss_fleet]·`blocked_rate_limited`[gdelt 429]·`unknown`[fail-closed]로 분류 + credential[`env_loader.env_status` **secret-safe** present/missing·값 0]·host_gate cooldown[`gdelt_provider_status`]·rate_limit_policy·next_action 동시 산출·**honest `fetch_implemented` gate**[실 fetcher=gdelt/rss 만 배선→sec_edgar/federal_register/news API 는 credential-ready 여도 `fetcher_not_wired`·opt-in live query 시 fixture 둔갑 **코드 차단**·dataset_source None]·`run_optional_live_query`[opt-in+safe+credential+fetcher gated→ADR#60 운영 경로 재사용·live_derived 후보→near-match queue·실패 분류 missing_credentials/provider_rate_limited/host_gate_blocked/fetcher_not_wired/no_records/no_near_match]·`build_provider_readiness_agent_schema`[no_secret_fabrication·embedding No-Go]·CLI[기본 readiness·network 0·--live-query opt-in]; injected GDELT transport→candidate 5·queue_pop 5·dataset_source live_derived·production_gold 0·merge_allowed False; 신규 테스트 43·backend 비-live 759p·adversarial SOLID(HIGH 0)+code-review 동일 MEDIUM 1(rss_fleet 명칭 이음새→fixture silent 낙하)→정정(rss_fleet→rss 정규화·source_ids 단일 출처·회귀 2); **실 wired+working key-free provider=gdelt[429]만→실 near-match 후보·reviewer/gold 0**[provider readiness=능력 표면화·실 데이터 아님]; 신규 RISK 0[R-ProviderCredentialReadiness 등 후보는 R-SourceOverlapScarcity·R-Secret 에 포섭]). + **provider query adapter contract + 첫 wired adapter(Guardian)**(ADR#62, 2026-06-25: 신규 `provider_query_adapters.py`[ADR#60/#57 frozen·단방향 import·재구현 0]가 ADR#61 `fetcher_not_wired` 병목을 축소 — `ProviderQueryAdapter`/`ProviderQueryResult` 공통 contract(옵션 A) + **Guardian Content API 실 adapter**(옵션 B·key-required·news·공식 shape parser response.results[].webTitle/webUrl/webPublicationDate·title≤512+canonical+published·**본문 미저장**)·`run_provider_query`[gate: fetcher_not_wired→missing_credentials(**network 전**)→rate_limited(in_cooldown)→host_gate_blocked→fetch·status 분류·**secret-safe**(env_status present/missing·key 는 실 network 만 os.getenv·**httpx params 전용**·url 항상 keyless·로그/result 0)]; `provider_readiness` 가 guardian 을 `_FETCH_WIRED_PROVIDERS` 에 wire + **adapter-wired 분기**[직접 fetch→ADR#60 `records=` 주입·real_fetch=True·`_SYNTHETIC_FIXTURE_MARKER` 부재→live_derived 보존·fixture 둔갑 0·provider= 직접전달 시 fixture silent 낙하(rss_fleet seam) 회피] + readiness 필드[adapter_contract_version/adapter_module/tested_with_fake_transport/queue_integration_status/live_query_capable_if_credentials_present] + agent schema[provider_adapter_contract·wired_providers]; `rate_limit_policy.yaml` guardian per-source[min_interval 5s·cooldown 600s·no tight retry]·generic HostRateGate 참여(상수 편집 0); fake transport→records 4·near 2·queue_pop 5·dataset_source live_derived·production_gold 0·merge_allowed False; 신규 테스트 29·backend 비-live 788p·adversarial **SOLID**(HIGH 0·blocking 0·LOW 3[비차단])+code-review[max_records=0 falsy 정정·test_12 content 강화]; **실 key 미주입·fake transport 만→실 near-match 후보 0·미배선 9종 잔여**[adapter=능력·실 데이터 아님]·embedding/LLM No-Go·운영 DB 0). + **secret-safe Guardian live query smoke + live-derived queue population gate**(ADR#63, 2026-06-26: 신규 `guardian_live_query_smoke.py`[ADR#62 커밋분 frozen·단방향·재구현 0]가 ADR#62 adapter 의 **실 network 직전 경계를 검증** — §4 smoke 계약[smoke_name·env_var_name·credential_present·**credential_value_exposed=False·env_file_read=False**·records/candidate/near/hard/queue_pop·dataset_source·block_reasons·next_actions·production_gold 0·merge_allowed False·db_write False]·**env_not_loaded vs missing_credentials 구분**[`.env` Path.exists]·credential present+opt-in 시 `run_optional_live_query` 위임[재구현 0]·CLI[`--live-query` opt-in·값 미출력]; `env_loader.probe_env_var`[present/missing + `.env` Path.exists + `.env.example` 이름 선언·**전부 값 미열람**]; 신규 테스트 26; **실 Guardian 호출 2회**[opt-in·bounded·governed·secret-safe·topic Ukraine/earthquake 7d→각 **records 10·candidate 0·`no_title_overlap`·dataset_source live_derived·secret_exposed False·env_file_read False·host_gate passed**]→**live 경로 실증**[실 key·실 fetch·정규화·secret 경계] but **단일 소스(Guardian) 내 same-event overlap 0**[구조적·이 토픽/윈도우에서 같은 날 기사 제목 Jaccard<near 임계·near-dup recall 한계 포함·deterministic near-match=cross-source 설계→2nd publishable provider 가 실 unblock]; 옵션 A+B+C·D[2nd adapter]/E/F 금지·신규 RISK 0). + **2nd publishable provider(NYT) + cross-source live overlap smoke**(ADR#64, 2026-06-26: `provider_query_adapters` 에 **NYT adapter** wire[key-required·news·`api-key` param=Guardian 동일→`run_provider_query` 무수정·`_nyt_url` begin/end_date YYYYMMDD keyless·`parse_nyt_items` headline.main/web_url/pub_date·본문 미저장]+신규 `cross_source_live_overlap_smoke.py`[얇은 composition·§4 cross-source 계약·**둘 다 성공만 cross-source 인정**(single-source 둔갑 금지·한쪽만 ok→no_records_provider_a/b)·near/hard pair 를 **source_id 상이만** 필터해 ADR#59 `build_near_match_reviewer_queue` 충원(same-source 오염 구조 차단)·credential_value_exposed/env_file_read False]; 신규 테스트 39; **실 Guardian+NYT 호출 4회**[Ukraine/earthquake 7d→각 **Guardian 10+NYT 10=combined 20·cross_source_pair_count 100·fingerprint 0·near 0·hard 0·no_title_overlap·dataset_source live_derived·secret_exposed False·host_gate passed**]→**cross-source pair 는 단일 소스 구조적 0→100 으로 실증 생성**[ADR#63 단일 소스 한계 직접 해소] but **deterministic title-Jaccard(near 0.5)가 100 쌍 중 0 검출**(hard band 0.2 까지 0)=deterministic paraphrase recall 한계 **또는** 두 매체의 다른-사건 보도(real scarcity) 둘 다 가능·n=2 미구분(100=비교 대상·match 아님)·**수집/source/key/fetch 능력은 입증**(단일 0→cross 100)→다음 방향=embedding/LLM adjudicator[옵션 E·gated·deterministic precision 0.57<0.98 별도 근거]; 옵션 A+B+C·D[3rd provider]=docs only·E/F 금지·신규 RISK 0·R-SourceOverlapScarcity·R-RealSourceLoopUnproven·R-LiveIdentityBacklog·R-IdentityHumanLabeling·R-SemanticIdentityAdjudicator·R-Gdelt429 부분진전·완전종결 금지). + **offline/gated semantic candidate scorer**(ADR#65, 2026-06-26: 신규 `semantic_candidate_scorer.py`[ADR#64 커밋분 frozen·단방향·재구현 0]가 ADR#64 "cross-source pair 100·deterministic 0 검출"=**detection scarcity** 를 다루는 첫 scorer 층 — §5 `SemanticPairInput`/`SemanticPairScore` contract·`scorer_mode`[deterministic_scaffold=ADR#64 의 title-Jaccard 신호 그대로(recall 개선 0)·fake_semantic=char-bigram Dice+token containment 결정론 proxy(실 embedding 아님·배관 scaffold)·embedding_opt_in/llm_opt_in=**injection-only**(실 client 미배선 No-Go·`create_embedding_client`/`create_llm_client` 미래 seam·secret-safe `probe_env_var` gate·실호출 0)]·`discover_overlap(emit_candidate_pairs=True)`[additive·기본 off→ADR#57/#64 frozen 불변]가 near floor 무관 전 publishable cross-URL same-date pair 를 band 태그로 노출→scorer 입력·score→**top-k rank**(threshold 아님·zero-signal 제외)→top-k near + hard negative→`build_near_match_reviewer_queue`[score 없는 pair record 만 투입·`validate_labeling_packet` fail-loud·labeler_facing_view 숨김]·`build_eval_gold_linkage`[MERGE_GATE 단일 출처 재참조·precision 0.98·`production_gold_count=0`·current_status No-Go]·`SCORER_AGENT_CONTRACT`[semantic prioritization 계획 가능·truth/merge/public IU/anchor/secret 출력 불가]·CLI; `cross_source_live_overlap_smoke` 에 additive `semantic_scoring`[기본 off=커밋 ADR#64 동작 byte-identical·on 시 live combined_records 의 cross-source pair(guardian 10×nyt 10=cross_source_pair_count 100·same-source 90 제외·`cross_source_only` 기본) scorer 입력]; 신규 테스트 49[scorer 46 + smoke 통합 3·감사 fix-lock 5·M-1 cross_source_only/M-2 above_near_floor/MED-1 hard_negative 미승격/HIGH-1 fallback band 수정]; **score/rationale/model metadata internal-only·merge_allowed False·requires_gold/merge_gate True·llm/embedding 실호출 0·DB write 0·public IU 0**; 진전=**candidate prioritization substrate**·**validated adjudicator 아님**[deterministic_scaffold=recall 개선 0·gold 없이 threshold 확정 금지·`AdjudicationFeatures.semantic_score` 미주입]·옵션 A+B+D 채택·C=injection-only interface·E/F 금지·신규 RISK 0·R-SemanticIdentityAdjudicator·R-IdentityEvalDataset·R-ReviewerAgreement·R-GoldSamplingBias·R-SourceOverlapScarcity·R-RealSourceLoopUnproven·R-LiveIdentityBacklog·R-IdentityHumanLabeling 부분진전·완전종결 금지). + **reviewer label operations + gold calibration preflight**(ADR#66, 2026-06-26: 신규 `reviewer_label_operations.py`[얇은 orchestrator·DB-free·기존 머신 재사용·재구현 0]가 ADR#65 scorer top-k reviewer queue 를 **실 사람 라벨 운영**으로 연결 — `export_reviewer_packet`[queue→packet_rows+labeler_view score/rationale/bucket 누출 재검증]·`import_reviewer_labels`[`load_reviewer_labels` 래핑·파일 없음→**no_labels graceful**(실패 아님)·forbidden field(score/rationale/raw body/secret)→allowlist fail-loud·malformed 분류]·`pseudonymize_reviewer_id`[결정론 sha1·raw PII 미노출]·`resolve_label_operations`[`resolve_gold_from_reviewers`(single=insufficient·2+만장일치=agreed·conflict+human adjudication=adjudicated·`_validate_adjudication` LLM-as-judge 봉인)+agreement+conflict queue+**production/synthetic gold 분리**(production=label_source production AND dataset_source live_derived·synthetic/test=simulated only)]·`build_calibration_preflight`[§7: production/positive/negative/korean gold·language/source_pair 분포·agreement_rate·conflict_rate·positive_negative_balance·hard_negative_coverage·**top_k_bias_warning**(scorer top-k positive-lean)·korean_calibration_ready·precision/fpr_denominator_ready·`evaluate_gold_merge_readiness` 재사용·merge_gate_ready]·`run_reviewer_label_operations`[§4 통합 output 31필드]·`REVIEWER_OPS_AGENT_CONTRACT`[reviewer/gold 운영 계획 가능·merge/truth/anchor/secret 불가·`EMBEDDING_LLM_ADJUDICATOR_INTERFACE` No-Go 재참조]·CLI; **label enum 충돌**(§5 unsure/needs_review)→frozen GOLD_LABELS(ambiguous/insufficient) 유지[회귀 안전·테스트 없는 정책 변경 금지]·label_source 3-way=operation tag[frozen ReviewerLabel 미변경]·DB 바운드 export 도구 미사용; 신규 테스트 49[export/import 1-10·gold promotion 11-18·agreement/calibration 19-27·optional real label path 28-34·no-merge/no-LLM/no-DB/secret 35-41·회귀 cross-check 42-43·guard 44-46·감사 HIGH fix-lock 47-49(model/self label gold 불가 in-memory·file 양 경로)]; **production_gold_count 0**[실 production reviewer label 0·synthetic/test=simulated only]·`merge_allowed=False`·`db_write=False`·llm/embedding 실호출 0·public IU 0; 진전=**reviewer/gold operational readiness substrate**·실 gold 아님[scorer/smoke 미변경·최소 변경]·옵션 A+B+C+D 채택·E/F 금지·신규 RISK 0·R-IdentityHumanLabeling·R-ReviewerAgreement·R-GoldSamplingBias·R-IdentityEvalDataset·R-SemanticIdentityAdjudicator·R-LiveIdentityBacklog·R-SourceOverlapScarcity·R-RealSourceLoopUnproven 부분진전·완전종결 금지). + **reviewer batch launch pack + intake validation loop**(ADR#67, 2026-06-26: 신규 `reviewer_batch_launch.py`[ADR#66/#60/#59 조합·재구현 0]가 ADR#66 reviewer label 운영 함수를 **사람이 바로 라벨링을 시작할 batch pack** 으로 전환 — `build_reviewer_instruction`[§6 4-라벨 정의/금지/권장·model score/rationale/predicted/rank 0]·`build_label_template`[`REVIEWER_ALLOWED_KEYS` 빈 worksheet·reviewer_id pseudonym·score/bucket 0·allowlist 밖 키 fail-loud]·`build_assignment_manifest`[pair 당 ≥2 reviewer·pseudonym only·top-k+hard negative·capacity<2→`insufficient_reviewer_capacity`·`raw_reviewer_pii_committed`=False]·`build_intake_plan`[intake_directory/expected_label_files/validation_command]·`normalize_label`+`_LABELER_TO_GOLD`[labeler vocab unsure/needs_review→canonical insufficient/ambiguous]·`validate_label_intake`[§8 import 前 dry-run·forbidden/malformed/label_source/reviewer_kind/pair_id/duplicate·`REVIEWER_ALLOWED_KEYS`+enum frozen 재사용·통과 행 정규화]·`run_reviewer_batch_launch`[§4·intake_status awaiting_labels/labels_present/invalid_labels/imported·라벨 시 `resolve_label_operations`+`build_calibration_preflight` 재사용·**decisive gold=same/different 만**(unsure/needs_review 는 gold 아님·`non_decisive_gold_count` 표면화)]·`BATCH_LAUNCH_AGENT_CONTRACT`·CLI[--validate dry-run·--synthetic-labels]; 신규 테스트 67·**production_gold_count 0**[실 production reviewer label 0·synthetic/test=simulated only]·라벨 없음→awaiting_labels·`merge_allowed=False`·`db_write=False`·llm/embedding 실호출 0·public IU 0·**자체발견·수정=만장일치 ambiguous gold 승격→decisive only·adversarial/code-review HIGH 2+MEDIUM 3 수정**; 진전=**batch launch readiness substrate**·실 gold 아님[ADR#66/#60/#59 미변경·최소 변경]·옵션 A+B+C+D 채택·E/F 금지·신규 RISK 0·R-IdentityHumanLabeling·R-ReviewerAgreement·R-GoldSamplingBias·R-IdentityEvalDataset·R-SemanticIdentityAdjudicator·R-LiveIdentityBacklog·R-SourceOverlapScarcity·R-RealSourceLoopUnproven 부분진전·완전종결 금지); ADR#68, 2026-06-26: 신규 `production_label_intake.py`[ADR#67/#66 building block 조합·재구현 0]가 batch launch pack 을 **filesystem production label intake** 로 연결 — `_scan_intake_dir`[intake_directory `*.jsonl` 다중파일·basename only·malformed fail-loud]·`build_no_labels_report`[§5 operator next_action·label 없음=awaiting_production_labels 정직]·`build_calibration_delta`[§8 before/after gold·positive/negative/korean·precision/FPR/KO denominator readiness·next_needed_for_merge_gate]·`run_production_label_intake`[§4·intake_status awaiting_production_labels/invalid_labels/imported/conflict_pending/calibration_pending·production gold=production&live_derived decisive·duplicate/unknown/forbidden/model count·pair coverage·`production_gold_provenance_verified`=False]·`.gitignore` outputs/reviewer_batch/ 가드; 신규 테스트 57·**production_gold_count 0**[실 production reviewer label 0·synthetic/test=simulated only]·라벨 없음→awaiting_production_labels·`merge_allowed`=False·`db_write`=False·llm/embedding 실호출 0·public IU 0; 진전=**first production label intake readiness**·실 gold 아님·옵션 A+B+C+D 채택·E/F 금지·신규 RISK 1[R-ReviewerFollowupOps]·R-IdentityHumanLabeling·R-ReviewerAgreement·R-GoldSamplingBias·R-IdentityEvalDataset·R-SemanticIdentityAdjudicator·R-LiveIdentityBacklog·R-SourceOverlapScarcity·R-RealSourceLoopUnproven 부분진전·완전종결 금지); ADR#69, 2026-06-27: 신규 `reviewer_followup_ops.py`[ADR#68/#67 building block 조합·재구현 0]가 production label intake 를 **reviewer follow-up 운영층**으로 — `compute_coverage`[assignment(expected) vs 회수(actual)·pseudonym 공간·재pseudonymize 0·missing_by_reviewer/pair]·`run_reviewer_followup_ops`[§4·7-state followup_status(not_launchable/no_labels/partial_labels/invalid_labels/conflict_pending/calibration_pending/imported_ready_for_merge_gate_review·invalid>partial fail-loud)·intake **exact passthrough**(production_gold_count follow-up 만으로 미증가)]·`build_reminder_templates`[PII-safe·**전송 0**·pseudonym/basename/pair_id/validation command/allowed labels 만·forbidden key fail-loud]·`build_escalation_actions`[capacity/no_labels/partial/invalid/conflict/calibration→operator action·email/slack/webhook 0]·reviewer SLA(required/assigned·capacity_status·escalate_now)·`production_label_intake` 에 `conflict_count` additive 노출; 신규 테스트 61·merge_allowed False·db_write False·llm/embedding 0; 진전=**follow-up 운영 cockpit**·실 label 회수 아님·옵션 A+B+C+D 채택·E/F 금지·신규 RISK 0·R-ReviewerFollowupOps 부분진전(완전종결 금지); ADR#70, 2026-06-27: 신규 `reviewer_pilot_handoff.py`[ADR#69 `run_reviewer_followup_ops` 단일 호출 decorate·재구현 0]가 follow-up cockpit 을 **actual reviewer pilot handoff** 로 — `build_pilot_handoff_bundle`[operator 가 실 reviewer 에게 배포할 instruction·reviewer 별 assignment summary·label template schema·expected filename·intake dir·validation command·allowed labels·pseudonym only·**재귀 forbidden-key 가드**·전송 0]·`run_reviewer_pilot_handoff`[§4·**8-state pilot_status**(not_ready/ready_to_contact/awaiting_reviewer_return/partial_returned/invalid_returned/conflict_pending/calibration_pending/imported_ready·intake dir 존재로 접촉 전/회수 대기 분할)·**returned-label gate**(returned label 있으면 followup→intake end-to-end·없으면 정직 ready_to_contact/awaiting)·gold **exact passthrough**(handoff 만으로 production_gold_count 미증가)]·`build_correction_templates`/`build_adjudication_handoff`/`build_calibration_gap`[reason code only·human lead·자동 다수결 금지·gold/KO/negative gap·전송 0]·`build_ops_ui_contract`[§8 OpsReviewBatchStatus·internal dashboard·`flags{no_merge,no_public_iu,pii_safe,no_llm,no_db_write,gold_provenance_verified}`·public truth 아님]·`REVIEWER_PILOT_AGENT_CONTRACT`·CLI; 신규 테스트 57·`actual_sending_performed`=False·`pilot_executed`=False·merge_allowed False·db_write False·llm/embedding 0; 진전=**pilot handoff bundle + returned-label gate substrate**·실 reviewer contact·실 returned labels 아님·옵션 A+B+C+D 채택·E/F 금지·신규 RISK 2[R-ReviewerPilotExecution·R-OpsUIPrematureTruth]·R-ReviewerFollowupOps·R-IdentityHumanLabeling·R-ReviewerAgreement·R-GoldSamplingBias·R-IdentityEvalDataset 부분진전(완전종결 금지); ADR#71, 2026-06-27: 신규 `reviewer_pilot_execution.py`[ADR#70 `run_reviewer_pilot_handoff` 단일 호출 decorate·재구현 0]가 handoff readiness 를 **실 pilot 실행 추적**으로 — operator contact evidence ledger[`real_reviewers_contacted`=roster ∩ contacted 만·evidence 없으면 0·prepared/declined/unavailable 분리·둔갑 차단·PII-safe allowlist+값-레벨 가드]·8-state `execution_status`[not_started/awaiting_operator_contact/contacted_waiting_return + returned label 5종·contact 축 직교]·operator SLA/checklist[contact_status·returned_file_status·overdue(as_of 기준·wall-clock 미커플링)]·`InternalOpsPilotExecutionStatus`[internal_only/no_public_truth·same_event/verdict truth 미노출]·gold **exact passthrough**(execution 만으로 production_gold_count 미증가)·`actual_sending_performed`/`merge_allowed` False; 신규 테스트 66·진전=**pilot execution ledger + returned-labels monitor substrate**·실 reviewer contact·실 returned labels 아님·옵션 A+B+C+D 채택·E/F 금지·신규 RISK 2[R-ContactEvidenceIntegrity·R-ReviewerContactPII]·R-ReviewerPilotExecution·R-OpsUIPrematureTruth·R-ReviewerFollowupOps·R-IdentityHumanLabeling·R-ReviewerAgreement·R-GoldSamplingBias·R-IdentityEvalDataset 부분진전(완전종결 금지). **ADR#72** actual input gate(`reviewer_actual_input_gate.py`·ledger 단일 호출 dispatch·gitignored 입력 스캔만·날조 0·5-state actual_input_status·external_input_required 정직)+internal ops read-only API/seed(`/api/internal/ops/pilot-execution`·`/internal/ops-pilot`·**4중 게이트** admin-token prod fail-closed+flag-404+nav 미노출+server-env)로 reviewer pipeline workflow state 를 public truth 와 분리 노출(**internal ops≠public IU**·same_event/score/rationale/predicted_status 미노출·신규 RISK R-InternalOpsAuthBoundary LOW). **ADR#73** internal ops auth/deploy preflight(`internal_ops_preflight.py`·`run_actual_input_gate` 단일 호출·순수 5-state posture[disabled_safe/enabled_internal_safe/**unsafe_public_exposure**(dev+flag+무토큰=무인증 reachable)/misconfigured/unknown·admin token **존재 여부만**·값 미열람·`deployment_proven=False` 불변]+R1~R7 readiness matrix(7-stage 머신리더블·gold→MERGE_GATE→embedding→entity→KG→GraphRAG→IU·source role invariant)+`GET /api/internal/ops/preflight`(이중 게이트·read-only·sanitized·admin token 값 필드 부재)+frontend posture/warnings/R1~R7/next-action 표시·신규 RISK R-RagKgPrematureBuild LOW). **ADR#74** R1 production gold acquisition operating plan(`r1_gold_acquisition_plan.py`·`run_actual_input_gate` 단일 호출·재구현 0)=target floor[canonical live≥200/KO≥50/reviewer≥2 재사용+파생 positive≥67/negative≥67/hard-negative≥20·**operating floor≠production truth**]+`_r1_status` 4-state[blocked_no_labels(현재)/collecting/partially_satisfied/satisfied]+gap 산술[required−current·gold0→전 bucket 0 증명가능·>0→gate `calibration_delta`(baseline≠0 fail-loud)]+operator next_manual_actions[recruitment·handoff 배포·gitignored intake 적재·human-only adjudication·**전송 0·파일 생성 0**]+`GET /api/internal/ops/r1-gold-acquisition`(이중 게이트·read-only·sanitized `InternalOpsR1AcquisitionStatus`)+frontend R1 gap 패널+§6b-S source-role storage strategy(official/news=anchor·community=reaction·market=signal·catalog=enrichment·search=URL·unknown=fail-closed·KG edge=provenance 필수·docs only·runtime No-Go)·신규 RISK R-GoldAcquisitionPlanOnly LOW·production_gold_count 0 gate exact passthrough(plan 만으로 미증가). 전부 events/cluster_event_map 불변(read+shadow write only·human_labeling/packet 변환은 DB 미접근·live packet pilot/backfill 은 read+adjudication upsert only·preflight/smoke offline 은 write 0·**provider readiness/live query 는 session 인자 부재·DB write 0**). **OPEN**: 실 병합·live-derived+human-labeled production gold set·reviewer 합의 실측·sampling 대표성·한국어 캘리브레이션·MERGE_GATE 런타임 배선·**live 백로그 운영 가동**(운영 DB 배포·실 fetch·주기 스케줄러·cheap-scan keyset)(R-CrossBatchEventIdentity·R-SemanticIdentityAdjudicator·R-IdentityEvalDataset·R-IdentityHumanLabeling·R-ReviewerAgreement·R-GoldSamplingBias·R-LiveIdentityBacklog).
- **comments**: 스켈레톤. comment body_text/debate 확장 마이그레이션은 미생성(S9). (0004=event_timeline, 0005=event_resolution 에 할당됨)

## 5. 검색·RAG (3엔진 분리)

- **Milvus** `event_embeddings` dim=1536 IVF_FLAT/COSINE — 시맨틱. `retrieve_past_context` 노드가 top-k 실호출.
- **OpenSearch** `event_cards` standard analyzer + `text_all` 복합필드 — 키워드(bool/must multi_match, title^2).
- **Postgres** — 원천·정확필터.
- 인덱싱: `upsert_card → PG commit → Milvus insert(swallow) → OpenSearch index(swallow)`.

## 6. LLM / 임베딩 추상화

- `BaseLLMClient` Protocol: `complete()` + `complete_json(schema=...)`, 무상태, 예외 전파 안 함(실패 시 None→안전 기본값).
- `MockLLMClient`(schema.__name__ 디스패치) ↔ `OpenAIClient` — `LLM_PROVIDER` env로 전환.
- `MockEmbeddingClient`(sha256 결정론) ↔ `OpenAIEmbeddingClient`(text-embedding-3-small) — `EMBEDDING_PROVIDER`.
- 출력 스키마는 `agents/tools/llm.py`에 집중. 프롬프트는 `agents/prompts/*.md`(현재 코드 미연결, 08).

## 7. Frontend (`frontend/`, Next.js 15.5.18 / React 19 / TS5 / Tailwind / Node20-alpine)

11 페이지 라우트 + 4 API route handler(health/reindex/reconcile/requeue).
`server-only` 격리로 `X-Admin-Token`은 서버측만(`NEXT_PUBLIC_*` 금지). 8 UI 컴포넌트. 멀티스테이지 Dockerfile(non-root).

## 8. 버전 핀 (변경 금지 항목)

Python 3.11 / langgraph 0.2.76 / langchain 0.2.11(v1 업그레이드 보류) /
FastAPI 0.115.x / SQLAlchemy 2.0.x / asyncpg 0.30 / alembic 1.14 / pymilvus 2.4.4 / openai 1.108.x /
Next.js 15.5.18(CVE-2025-29927 대응). uv 전용, conda 금지.

## 9. 관측성·보안

- LangSmith: `setup_langsmith()`, `LANGSMITH_TRACING=true` opt-in, API 키 로깅 금지(길이만).
- Admin 토큰 bypass(빈 값=허용)는 dev 한정 — 운영 전 해제 필요(05 R-Auth).
- 비밀: `.env`만, `os.getenv`/pydantic-settings로만 읽음, 직렬화/로그 금지.
