# ARCHIVE INDEX (tombstone) — 적용 완료/미사용 메모리 문서 보관소

> `turn-closeout` 이 팀 감사 통과 후 `Move-Item`/`git mv` 로 여기 옮긴 메모리 md 의 **1줄 묘비**.
> 매니페스트 샤딩 없음 — 파일당 1줄. 보존기간(`config.archive_retention_turns`, 기본 30턴) 경과분은 `docs/_TRASH/`(gitignored, 로컬 복구만)로.
> 되돌리려면 이 표의 "원래 경로"로 `Move-Item` 역이동.

| 아카이브일(turn) | 원래 경로 | 사유(한 줄) | 코드 착지 근거(path/risk-id) | 보존만료(turn) |
|---|---|---|---|---|
| (아직 archive 된 문서 없음) | | | | |
